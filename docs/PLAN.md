# Taqinor OS — Build Plan & Progress

This file is the **single source of truth** for the Taqinor OS build backlog and the
**memory between Claude Code sessions**. Each run first works through EVERY unchecked task in `docs/PLAN2.md` (if that file exists) from top to bottom — not just one — ticking each off as it lands, then does the same for this file, and only stops when both queues are clear (or a usage limit pauses it, in which case re-running resumes from the next unchecked task). The next session reads this file and
continues. Nothing relies on the agent's own memory — the file on disk is the memory.
Each run partitions its unchecked tasks into independent **lanes** (grouped by which real
source files they write) and builds them with **up to 8 concurrent worktree subagents**, in
waves of 8 when there are more lanes — see HOW TO RUN.

---

## HOW TO RUN (read this every session)

1. **Read this whole file.**
2. **Drain the WHOLE queue, PLAN2 first — never just one task, with MAXIMUM SAFE PARALLELISM.**
   Check `docs/PLAN2.md` FIRST: work through EVERY pending `[ ]` task there that isn't
   gated/blocked — following PLAN2.md's own rules, which are the same as this file's — then
   drain this file's **BUILD QUEUE** the same way. Process EVERY unchecked `[ ]` task (not
   `[x]`, not `[SKIP]`, not `[BLOCKED]`); ignore the GATED and MANUAL sections entirely.
   **At the START, compute the file-ownership + dependency graph from the real code** (which
   source files each `[ ]` task must write) and **partition the queue into independent lanes**
   — a lane is a group that must run in sequence because it shares a file or has a dependency;
   different lanes never touch each other's files. **Fan the lanes out to up to 8 concurrent
   worktree subagents** (`isolation: worktree`, each in its own isolated git worktree so two
   never edit the same files at once); with more than 8 lanes, run them in **waves of up to 8**;
   with fewer, use fewer agents. **Tasks inside one lane run in sequence.** Each subagent
   commits its lane's work to its own worktree branch — ticking each task `[x]` + a DONE LOG
   line as it lands — and the orchestrator folds every worktree branch into one `dev` branch at
   the end. Derive lane ownership from the real code, not from guesses. **Default to running
   this as a dynamic workflow with review** — one worktree subagent per task plus a **separate
   adversarial review agent** that must pass each finished change (against the STANDING RULES
   and the task's acceptance criteria) before it is eligible to fold in — and **fall back to the
   same parallel worktree subagents (orchestrator reviews each lane) when no workflow engine is
   available; never a single serial one-task-at-a-time agent** (see STANDING RULES).
3. **Verify each task isn't already built — never trust these ticks or prior reports.**
   Inspect the actual repo and the deployed app. If a task already exists and works, mark it
   `[x] (already present)`, add a line to the DONE LOG, and move on to the next `[ ]` task.
4. **Build each task completely, with tests, and land it on its lane's worktree branch the moment
   it's done.** Obey every STANDING RULE below. As each task finishes: commit it to its **worktree
   branch** (folded into `dev` at the end of the run), flip it to `[x]`, and append one dated
   plain-language line to the DONE LOG — so an interrupted run never loses finished work and
   re-firing resumes from the first still-unchecked task. Then the lane **immediately continues to
   its next `[ ]` task. Do NOT merge after each task.**
5. **Fold every lane's worktree branch into one `dev`, then CI runs ONCE over the whole batch.**
   The four required checks must pass: backend-lint, backend-tests **with MinIO** (so PDF/storage
   tests actually run, including the PDF page-count guardrails), frontend-lint, and the stage-name
   check. When all four are green, **self-merge `dev` → `main` exactly once** (a single merge
   commit, history preserved, 0 approvals; no per-agent PR, no per-task merge). **Merging to `main` AUTO-DEPLOYS to api.taqinor.ma on its own** — the
   production server polls `main` about once a minute and runs the full deploy (rebuild +
   migrations + role sync + nginx/Caddy reload + the mandatory PDF pre-warm). **You do not run
   any deploy command.** `powershell -File scripts\deploy-prod.ps1` still works as a **manual
   fallback** from a PC if ever needed. **Make this one merge sync-safe:** right before merging,
   **integrate the latest `origin/main` into `dev`** (merge it in, never force-push), recompute
   the CODEMAP structure fingerprint if that changed the structural surface, **re-run CI once on
   the integrated tree, and merge only when green**; if the push is rejected because `main`
   advanced (e.g. a concurrent web-plan run landed first), **repeat the integrate → CI → push
   loop — never force, never overwrite the other run's commits** (see STANDING RULES).
6. **Skip-and-note blockers, never stall.** If a task hits a blocker (a destructive migration,
   a paid/external dependency that isn't pre-approved, an auth or cost change, a brand-new
   architectural component, a conflict with a non-negotiable rule, or a real decision): do
   **not** guess and do **not** stall. Mark it `[BLOCKED: <one-line reason>]`, move it to the
   GATED section, and continue with the remaining tasks. A single blocked task must never halt
   the run.
7. **STOP only when** the queue is drained (no buildable `[ ]` task remains in `docs/PLAN2.md`
   then this file), a usage/length cap pauses the run (fine — the plan is idempotent;
   re-firing resumes from the first still-unchecked task), or every remaining task is blocked.
   Then **report once**, in plain language only — no diffs, no commit hashes: every task that
   shipped, what was skipped and why, and exactly what Reda must click/type (with menu paths).

**Then the REFINEMENT QUEUE (run order).** Once the existing BUILD QUEUE is drained — **T1–T17
first**, then the N/F tasks — work the **REFINEMENT QUEUE — existing-feature polish (audit
2026-06-18)** section (below, after the BUILD QUEUE and before GATED) top-down: build the
**ROUTINE** tasks unattended exactly like a queue task, and **skip + flag** every gated task
(`SCHEMA` / `DEP` / `DECISION` / `GALLERY`) — same skip-and-report behaviour as a GATED item today.
Those gated refinements are mirrored into the GATED section so an unattended run never auto-builds
them. The refinement tasks use the format `[ ] [<MODULE>] [L<lens#>] [<GATE>] …` (not the `T#/N#`
ids), so they are deliberately outside the plan-status fingerprint — ticking one does not require a
CODEMAP §10 refresh.

**Run from anywhere — web or phone.** Because `main` auto-deploys itself, a task can be run
from Claude Code on the web or from the phone with no PC involved. **One-line starter** to
paste into a fresh cloud session:

> Read `docs/PLAN.md` top to bottom. Work through EVERY `[ ]` task — **first** `docs/PLAN2.md` (if it exists), **then** this file's BUILD QUEUE. First partition the unchecked tasks into independent lanes by the real files each writes, then build the lanes in parallel with **up to 8 concurrent worktree subagents** (each in its own git worktree, waves of 8 if there are more lanes), coupled tasks in sequence inside a lane (default: run this as a dynamic workflow with a separate adversarial review agent that must pass each change before it's merge-eligible; fall back to plain parallel worktree subagents — never a single serial one-task-at-a-time agent). For each task: verify it isn't already built, build it with tests, commit it to its worktree branch, tick it `[x]`, add a dated DONE LOG line, then continue to the next. Skip-and-note any blocker (`[BLOCKED: reason]` → GATED) and keep going. At the very end, fold every worktree branch into one `dev`, integrate the latest `origin/main` first (merge it in, never force-push) and recompute the CODEMAP structure fingerprint if that changed the structural surface, get the four required CI checks green over the whole batch (with MinIO) and self-merge `dev` → `main` exactly once (this auto-deploys — do not run any deploy command; no per-agent PR, no per-task merge). Report once, in plain language, including the lane plan. Do not stop after one task and do not merge per task.

---

## STANDING RULES (every task obeys these)

- **One run = the whole queue, not one task, fanned across up to 8 lanes.** At the start, partition the unchecked queue into independent **lanes** (grouped by the real files each task writes) and give each lane its own subagent in its own git worktree — **up to 8 worktree subagents at once, in waves of 8** when there are more lanes — so each subagent's context stays small and focused and two lanes never edit the same files at once; tasks that depend on or overlap each other run in sequence inside one lane. Never stop after a single task. The orchestrator folds every worktree branch into one `dev`, CI runs **once** over the whole batch, and the run self-merges `dev` → `main` **exactly once** — no per-agent PR, no per-task merge. (Human-review PRs are still not wanted — the run self-merges its own green work.)
- **Engine = workflow-with-review by default; parallel subagents as fallback; never single-serial.** Run the lanes as a **dynamic workflow with a fan-out-and-verify shape** — one worktree subagent per independent task **plus a separate adversarial review agent** that checks every finished change against these STANDING RULES and the task's acceptance criteria; nothing folds into `dev` or merges until its review passes. When no workflow engine is available (e.g. a phone or cloud session), **fall back to the same lane-planned worktree subagents** with the orchestrator reviewing each lane against these rules before folding it in. **Never drop to a single serial, one-task-at-a-time agent** — parallel lanes with review are the floor.
- **Sync-safe single merge.** Right before the one self-merge, **fetch and integrate the latest `origin/main` into `dev`** (merge it in — never rebase published history, never force-push); if that changed the code-structure surface, **recompute the CODEMAP structure fingerprint on the integrated tree** (the fingerprint the `stage-names` check verifies); **re-run CI once on the integrated state and merge only when green**; if the push is rejected because `main` advanced, **repeat (fetch, integrate, recompute if needed, re-run CI, push) — never force**. A run edits the shared files (`CLAUDE.md` / its own plan file / `docs/CODEMAP.md`) only for its own command and ships that change inside this same merge — so a concurrent OS run and web-plan run never fight over those files.
- **Verify against real code first. Never trust prior reports.** (Round 1 reported a preview
  fix that was never real, because that session's CI was silently broken.)
- **Additive only.** New tables / nullable columns / new defaults. **Never** a destructive
  migration (no dropping columns/tables, no deleting rows). If one is needed → `[BLOCKED]`.
- **Do not touch the DEBUG setting.** It is ON by Reda's explicit decision for the trial.
- **Do not redesign the PDFs.** Keep the existing WeasyPrint engine and the `PDF_ENGINE=legacy`
  fallback. (A full document redesign is a GATED item — see below.)
- **Never expose buy prices / prix revendeur / margins** in any client-facing output, link,
  message, or PDF. This is critical for the WhatsApp public links.
- **Do not change `STAGES.py` semantics.** Six canonical stages are a contract; "Perdu" is a
  boolean flag, never a stage.
- **Keep the contact form parked OFF.**
- **Multi-tenant:** every new model carries a `company` FK, filtered querysets, and a
  server-forced company. No client-supplied company is ever trusted.
- **All new user-facing text in French.** (Code/identifiers in English.)
- **New settings default to today's exact behavior** — nothing changes until Reda edits it.
- After merge, **deploy**, then **one plain-language report**.

**Pre-approved dependencies (do NOT treat as blockers):** `openpyxl` (real .xlsx) and
`vite-plugin-pwa` (build-time dev dependency for the PWA). Anything else new → `[BLOCKED]`.

**Status legend:** `[ ]` to do · `[x]` done · `[SKIP]` not needed / already present ·
`[BLOCKED: reason]` needs a decision (moved to GATED).

---

## ALREADY LIVE — do not rebuild (verify if unsure)

As reported in the build logs (treat as "very likely present, confirm before assuming"):
production on Hetzner at **api.taqinor.ma** (cx23, daily backups, deploy via
`scripts\deploy-prod.ps1`); multi-tenant ERP monorepo; CI now genuinely green **with MinIO**.

- **CRM:** Odoo-style multi-view (kanban default / liste / calendrier / graphique), full solar
  lead record, Historique chatter, lead-primary quoting, per-mode-gated "⚡ Devis auto",
  automatic stage movement, lead↔devis links, real `perdu` flag, reordered lead form,
  Activities + "Mes activités", Pièces jointes, employee avatars, lead routing/assignment,
  employee management, **Doublons workspace + N-way group merge**.
- **Quote generator:** three markets (résidentiel / industriel-commercial étude / agricole
  pompage), simulator-exact screen, prix/kWc, internal margin indicator, **reads saved
  settings** (validité, heures de pompage).
- **Per-line TVA** (panels 10% / else 20%), PDF suite (premium / one-page / étude), payment
  terms by mode, client ICE, seller legal IDs, unified warranties.
- **Invoicing:** Devis→Facture installment factures d'acompte, manual payments, running solde,
  per-line TVA on invoices, **Avoirs** (credit notes), **Relances/Impayés** + balance âgée +
  relevé client.
- **Chantier/Installation** module (lifecycle, interventions, mise en service, planning),
  **Equipment registry** + warranty clocks + "Expirant bientôt", **SAV tickets** (warranty-aware).
- **Stock:** catalogue + Pompage category, Category→Brand redesign, warranties populated.
- **Settings:** Société/Identité + Moroccan legal IDs, Devis (payment terms, validité,
  heures de pompage, prefixes), TVA, CRM tags & motifs de perte, niveaux de relance.
- **Sending:** **"Envoyer par WhatsApp"** on leads/factures/relances, with tokenized public
  PDF links and editable FR + Darija templates (Paramètres → Messages WhatsApp).
- Login is the front door (landing at `/landing`); website → CRM lead pipe live end-to-end.

---

## BUILD QUEUE (do top-down — highest value first)

### PRE-APPROVED FOR AUTONOMOUS BUILD (added 2026-06-18)

For the **specific ungated tasks below ONLY** — N14, N26, N59, N60, N64, N65, N67, N85,
N87, N88, G9, N103, N104, N105, N106, N107, and the additive-column / UI / comptable-export /
Odoo-importer refinement tasks flipped to `[ROUTINE]` in the REFINEMENT QUEUE — the following are
**already authorized**, and a future session must **NOT pause** on them despite the general rule to
stop-and-ask before a new dependency / schema / cost change:

- the **Leaflet** map library (N85 + the Parc « Carte » refinement);
- **Celery Beat** as the scheduler (G9);
- the **Brevo** email integration (N87/N88, SDK or SMTP, key from settings/env, no-op without key);
- the **specific additive columns** listed (Client `created_by` + `date_modification`; a
  `type_equipement` tag on `stock.Categorie`; per-company serial uniqueness on `Equipement`;
  ICE-inclusive client export);
- the **ONEE grid seed values** (the residential TTC scale + the cheaper force-motrice/agricole
  class in N64);
- the **stock-reserve-then-decrement rule** (N14 + the folded-in G6);
- the **silent DGI export/validator** capability (N105, master toggle default OFF);
- **converting the devis PDF literals to settings** (the rule-#4 literal→setting wiring of N26 /
  N59 / N60 / N67 — minimal premium-engine wiring with byte-identical defaults, visual layout
  unchanged).

The general stop-and-ask / pause rule **still applies to everything NOT on this list.**

### Active regressions — fix first (added 2026-06-18, top priority)

- [x] N103 — (SHIPPED 2026-06-19 — voir DONE LOG) RÉGRESSION (active, à corriger en priorité — bloque l'usage réel) : un
  Directeur / Administrateur ne peut plus changer le rôle assigné d'un utilisateur depuis
  l'interface. **Diagnostiquer d'abord la vraie cause** (inspecter le code réel, `git log` autour
  de PR #155) avant de supposer, **PUIS corriger, PUIS ajouter un test de régression** — pas un
  build aveugle. Vérifier CHAQUE couche : l'écran Utilisateurs (le contrôle de rôle est-il présent
  et activé, liste-t-il tous les rôles actuels, appelle-t-il le bon endpoint, gère-t-il les
  erreurs) ; le serializer + viewset utilisateur (le champ rôle, ou la récente validation
  superviseur/société, est-il devenu read-only ou échoue-t-il la validation) ; les classes de
  permission de gestion des utilisateurs (un Directeur/Admin est-il réellement autorisé à modifier
  un autre utilisateur, le nouveau gating a-t-il bloqué cela, et le Directeur/Admin connecté
  détient-il vraiment la permission de gestion utilisateur/rôle après l'auto-mapping de PR #155).
  Corriger pour qu'un Directeur ET un Administrateur puissent changer FACILEMENT le rôle de
  N'IMPORTE QUEL utilisateur, persistant et effectif immédiatement, chaque rôle sélectionnable,
  SANS affaiblir les contrôles de permission des autres rôles. Confirmer que l'attribution du
  superviseur d'un utilisateur fonctionne aussi et la corriger si la même cause l'a cassée.
  **Test de régression backend** : un Directeur ET un Administrateur peuvent changer le rôle d'un
  autre utilisateur via l'API et cela persiste, et la permission de gestion des rôles est accordée
  par défaut au Directeur/Admin. Règles permanentes : texte UI en français ; ne pas affaiblir les
  permissions des autres rôles ; FK company respectée ; additif ; STAGES.py/funnel intouchés ;
  DEBUG/déploiement/sécurité inchangés.
- [x] N104 — (SHIPPED 2026-06-19 — voir DONE LOG) RÉGRESSION (active, à corriger en priorité — bloque l'usage réel) : Reda ne peut plus
  joindre de fichiers nulle part dans l'ERP. **Diagnostiquer d'abord la vraie cause** (inspecter le
  code réel) avant de supposer, **PUIS reproduire et trouver POURQUOI** l'ajout d'une pièce jointe
  échoue aujourd'hui partout où les pièces jointes sont utilisées (le composant partagé
  `AttachmentsPanel` et l'`AttachmentViewSet` / le modèle `Attachment` générique sur MinIO —
  leads, clients, devis, factures, chantiers, tickets, et partout ailleurs), **PUIS corriger, PUIS
  ajouter un test de régression** — pas un build aveugle. Vérifier spécifiquement les suspects
  PR #155 : le scoping de visibilité (`_scoped`) restreint-il à tort le queryset des pièces
  jointes, et le gating d'écriture (`IsResponsableOrAdmin`) bloque-t-il des rôles censés pouvoir
  joindre — plus le flux d'upload lui-même (requête multipart, résolution de cible par modèle+id,
  stockage MinIO, validation type/taille) et l'hypothèse d'un build frontend caché périmé. Corriger
  PARTOUT où les pièces jointes sont utilisées pour que l'ajout (et l'ouverture/le téléchargement)
  d'un fichier refonctionne pour les rôles autorisés, sans affaiblir la sécurité. **Test de
  régression** exerçant RÉELLEMENT l'ajout d'une pièce jointe via l'API contre MinIO et vérifiant
  qu'elle persiste et est récupérable — pas un simple 200. Règles permanentes : texte UI en
  français ; ne pas affaiblir la sécurité ; FK company respectée ; toutes les pièces = données
  client réelles → object storage, jamais committées ; additif ; STAGES.py/funnel intouchés ;
  DEBUG/déploiement/sécurité inchangés.

---

## BUILD QUEUE — N1–N102 (post-sale, procurement, Moroccan billing, editability, platform)

Re-homed from the recovered `docs/PLAN2.md` (the overflow queue) on 2026-06-16. The ticks
below were **re-verified against the live `main` code**, task by task — the recovered file's
own ticks were written on a branch that never merged and were unreliable. `[x]` here means the
feature genuinely exists **and is usable** on `main` today; `[ ]` means missing, backend-only
with no usable screen, or only partially meeting the spec. Same STANDING RULES as the rest of
this file apply (additive only, multi-tenant, French UI, STAGES.py pipeline never edited, buy
prices never client-facing). **Reconciliation result: 30 of 102 done, 72 open.**

Two tasks have a working backend on `main` but no usable screen yet — flagged inline as
_Backend ready, frontend pending_: **N11** (supplier-PO management page) and **N29** (facture
conformity warning banner).

### Chantiers / projets & execution
### Parc installé (installed-systems asset base)
### Procurement & inventory
- [x] N14 — (SHIPPED 2026-06-19 — voir DONE LOG) Réservation de stock sur chantier puis consommation à « Installé » (regroupe N14 + G6). À la création d'un chantier, réserver auprès du stock les quantités requises (issues de la nomenclature gelée du devis lié) avec, par SKU, un indicateur réservé-vs-disponible ; les vues stock + alertes de stock bas tiennent compte des quantités engagées-mais-non-consommées. Quand le chantier atteint « Installé », la consommation décrémente le stock réservé (un seul mouvement de sortie par SKU, idempotent — jamais en double si l'on repasse par « Installé »). Annuler/clôturer un chantier libère la réservation restante. Réutiliser le mécanisme de réservation/consommation de stock existant. La règle comptable réserver-puis-décrémenter est PRÉ-APPROUVÉE (voir le bloc PRÉ-APPROUVÉ en tête de file) — ne pas se mettre en pause dessus. Règles permanentes : texte UI en français ; FK company forcée serveur sur tout nouveau modèle/mouvement ; migrations additives uniquement ; aucun prix d'achat/marge sur une sortie client ; STAGES.py et le funnel pipeline jamais touchés (le statut chantier « Installé » est sa propre machine à états, distincte de STAGES.py) ; DEBUG et les réglages de déploiement/sécurité inchangés.
- [x] N20 — (SHIPPED 2026-06-19 — voir DONE LOG) Optional QR/barcode labels for stock SKUs and installed systems; printable labels; chatbot or search field resolves a scanned code to the SKU or Système installé.
### Post-sale / client-facing documents
- [x] N106 — (SHIPPED 2026-06-19 — voir DONE LOG) Deux livrables additifs uniquement (ungate partiel de G3), dans le langage visuel du gabarit premium EXISTANT (look actuel conservé, AUCUN re-stylage) : (1) **trois lettres de relance à ton croissant** (courtois, ferme, mise en demeure) et (2) une **fiche de remise / garantie après-vente d'une page**, générées à la demande. La refonte visuelle complète des documents reste GATÉE comme non-objectif délibéré (voir G3). Règles permanentes : texte client en français ; aucun prix d'achat/marge sur ces documents ; FK company respectée ; additif ; le moteur de mise en page premium n'est pas redessiné ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
### Devis acceptance trigger
- [x] N26 — (SHIPPED 2026-06-19 — voir DONE LOG) Capture d'acceptation client légère sur un devis (nom saisi + date + confirmation « Bon pour accord »), enregistrée sur le devis — pas de e-signature cryptographique, aucun prestataire externe — produisant une copie d'acceptation régénérée du devis tamponnée « accepté le <date> par <nom> ». La capture nom+date existe déjà via N25 (accepte_par_nom / date_acceptation + chatter DevisActivity) ; il reste à imprimer le tampon. Partie du chantier « littéraux PDF règle #4 » : le libellé du tampon d'acceptation devient un réglage éditable versionné dans Paramètres dont la valeur PAR DÉFAUT reproduit EXACTEMENT le texte actuel, pour que le PDF rendu soit identique au byte près tant que rien n'est édité. PRÉ-APPROUVÉ (voir bloc PRÉ-APPROUVÉ) : câbler ces littéraux du moteur premium (`apps/ventes/quote_engine`) vers des champs de réglage versionnés — la disposition visuelle NE CHANGE PAS ; exception ciblée à « ne jamais éditer les pages premium », limitée à ce seul câblage littéral→réglage. Règles permanentes : texte UI en français ; FK company forcée serveur ; chaque réglage versionné par défaut = texte actuel exact (PDF byte-identique) ; migrations additives ; aucun prix d'achat sur le PDF client ; STAGES.py/funnel jamais touchés ; DEBUG/déploiement/sécurité inchangés.
### Moroccan legal billing & compliance
- [x] N36 — RIB + payment-instructions block on facture and devis PDFs, sourced from Paramètres. (Facture side SHIPPED 2026-06-18 — Feature B: editable RIB + Instructions de paiement + Conditions générales in Paramètres → Société, rendered on the facture PDF only when non-empty, byte-identical when empty. Devis side intentionally NOT added: the premium engine has no slot and rule #4 forbids editing it.)
- [x] N105 — (SHIPPED 2026-06-19 — voir DONE LOG) Capacité DGI LOCALE, BACKEND UNIQUEMENT, SILENCIEUSE (ungate partiel de G14). AUCUNE sortie visible dans l'interface : pas de pastille par facture, pas de statut, pas de colonne de liste, aucune modif du modèle Facture. Construire (1) un export XML **UBL 2.1 / CII** des factures portant l'**ICE du destinataire sur chaque ligne**, généré à la demande, et (2) un **validateur de conformité** vérifiant qu'une facture porte les champs obligatoires DGI. Les deux atteignables uniquement par programme / à la demande, gardés derrière un **seul interrupteur maître dans Paramètres qui par DÉFAUT est OFF** ; tant qu'il est OFF, la capacité est totalement invisible et ne change RIEN au comportement actuel. NE PAS ajouter de sceau d'archivage, de piste d'audit ni de champ de statut de facture — hors périmètre. PRÉ-APPROUVÉ (voir bloc PRÉ-APPROUVÉ) : l'export/validateur DGI silencieux. RESTENT GATÉS (voir G14, bloqués sur le décret d'application DGI non publié — aucune spec d'API n'existe, ce n'est pas une décision de Reda) : la transmission au portail Simpl-TVA et la signature électronique certifiée. Règles permanentes : texte UI en français ; FK company forcée serveur ; interrupteur par défaut OFF = comportement actuel exact ; migrations additives ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
### Loi 82-21 / Article 33 regulatory
### SAV / maintenance / warranty / monitoring
- [x] N50 — (SHIPPED 2026-06-19 — voir DONE LOG) Monitoring-integration framework with a swappable provider interface, starting with a Huawei FusionSolar connector that (given per-system credentials in config) pulls recent production data; admin enables it per system; no-ops safely when no provider is configured.
- [x] N51 — (SHIPPED 2026-06-19 — voir DONE LOG) Per-installed-system production view showing recent yield pulled by the monitoring framework when configured, with a manual-entry fallback.
- [x] N52 — (SHIPPED 2026-06-19 — voir DONE LOG) Configurable under-performance rule that (when monitoring data exists) flags a system producing below an expected threshold and optionally auto-creates a SAV ticket; threshold + auto-ticket behaviour editable in Paramètres.
- [BLOCKED: needs production data from the monitoring framework (N50, gated — external service/credentials).] N53 — Client energy-yield report PDF (French): a system's production over a period, estimated bill savings, CO2 avoided; client-facing, no buy prices.
### Editability layer (Paramètres hub)
- [x] N58 — Make chantier statuses, SAV statuses, and bon-de-commande statuses configurable in label & order from Paramètres while keeping their underlying state-machine semantics intact; never touch the protected lead pipeline. (SHIPPED 2026-06-18 — display-only overlay: a per-company `StatutConfig` (parametres) surcharge le libellé/ordre/visibilité des statuts chantier, SAV et bon de commande ; les clés canoniques et la machine à états restent intactes dans leurs modèles sources, défauts lus à la source donc sortie byte-identique tant que rien n'est édité. Onglet Paramètres → Statuts ; endpoints `/parametres/statuts/`, `/effective/`, `/bulk/` ; STAGES.py jamais touché.)
- [x] N59 — (SHIPPED 2026-06-19 — littéraux DEVIS câblés byte-identique + éditeur « Modèles de documents » ; facture/acompte/avoir/PV/BL/dossier/attestation à câbler, voir DONE LOG) Éditeur de gabarits de documents dans Paramètres pour les portions de texte éditables des documents client (devis/facture/acompte/avoir/PV de réception/bon de livraison/dossier de remise/attestation) : en-têtes, pieds de page, mentions légales, CGV, texte de validité, texte de conditions de paiement, avec des espaces réservés sûrs pour les champs société/client/système ; le moteur de mise en page reste intact ; impossible d'y insérer un prix d'achat. Partie du chantier « littéraux PDF règle #4 » : chaque littéral devient un réglage éditable versionné dont le défaut reproduit EXACTEMENT le texte actuel → PDF identique au byte près tant que non édité. PRÉ-APPROUVÉ (voir bloc PRÉ-APPROUVÉ) : câbler ces littéraux du moteur premium vers des champs de réglage versionnés ; la disposition visuelle NE CHANGE PAS ; exception ciblée règle #4, limitée au câblage littéral→réglage. Règles permanentes : texte UI en français ; FK company forcée serveur ; défauts = texte actuel exact ; migrations additives ; aucun prix d'achat client-facing ; STAGES.py/funnel jamais touchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N60 — (SHIPPED 2026-06-19 — voir DONE LOG) Conditions générales éditables + durée de validité configurable appliquées aux nouveaux devis, avec la date de validité imprimée sur le PDF du devis. Les CGV éditables existent déjà dans Paramètres et s'impriment sur la FACTURE (Feature B 2026-06-18, voir N36) ; la durée de validité est déjà éditable. Reste à câbler l'impression CÔTÉ DEVIS. Partie du chantier « littéraux PDF règle #4 » : le texte CGV + la validité deviennent des réglages versionnés dont le défaut reproduit EXACTEMENT le texte premium actuel (« 30 jours », bloc CGV) → PDF devis identique au byte près tant que non édité. PRÉ-APPROUVÉ (voir bloc PRÉ-APPROUVÉ) : câbler ces littéraux du moteur premium vers des champs de réglage ; disposition visuelle inchangée ; exception ciblée règle #4. Règles permanentes : texte UI en français ; FK company forcée serveur ; défauts = texte actuel ; migrations additives ; aucun prix d'achat sur le PDF ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N64 — (SHIPPED 2026-06-19 — voir DONE LOG) Moteur tarifaire ONEE + ROI (TOUTES les valeurs en réglages éditables versionnés, JAMAIS en dur), utilisé par l'estimateur de facture saisonnière et le calcul de ROI ; valeurs actuelles semées comme défauts. **Modèle de facturation** : PROGRESSIF à ≤ 150 kWh/mois (chaque tranche facturée à son propre tarif) ; SÉLECTIF au-dessus de 150 kWh/mois (le mois ENTIER facturé au tarif unique de la tranche où tombe le total — NON progressif), avec une tolérance de 10 kWh décalant les bornes opératoires à 210 / 310 / 510. **Grille résidentielle semée en défauts TTC (NE JAMAIS rajouter de TVA par-dessus)** : 0–100 kWh = 0,9010 ; 101–150 et 151–210 = 1,0732 ; 211–310 = 1,1676 ; 311–510 = 1,3817 ; > 510 = 1,5958. **Classe « force motrice / agricole » SÉPARÉE et moins chère** (≈ 0,90–0,95), jamais le tarif résidentiel haut. **Réglage « surplus injecté compensé » par défaut FAUX** (pas de net-metering : surplus valorisé à zéro, dimensionner sur l'autoconsommation). **Si la production est calculée** : PVGIS aux coordonnées GPS exactes, convention azimut Sud 0 / Est −90 / Ouest +90 / Nord +180. Par défaut, chiffres CONSERVATEURS qui SOUS-estiment les économies. PRÉ-APPROUVÉ (voir bloc PRÉ-APPROUVÉ) : le barème/seed ONEE ci-dessus — ne pas se mettre en pause sur le barème. Règles permanentes : texte UI en français ; FK company forcée serveur sur les modèles de réglage ; chaque réglage par défaut = chiffre actuel ; migrations additives ; aucun prix d'achat/marge client-facing ; STAGES.py/funnel jamais touchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N65 — (SHIPPED 2026-06-19 — voir DONE LOG) Hypothèses d'irradiation / productible par localisation pour estimer la production annuelle, semées de défauts marocains conservateurs. **RÉALISÉ PAR L'APPROCHE PVGIS** : au lieu d'une table régionale figée, utiliser PVGIS aux coordonnées GPS EXACTES du site (convention azimut Sud 0 / Est −90 / Ouest +90 / Nord +180), ce qui satisfait l'intention « irradiation régionale » de N65 par une irradiance précise à la localisation — donc N65 est rempli par l'approche PVGIS de N64 plutôt que par une table régionale séparée. Les hypothèses de productible restent des réglages éditables versionnés ; repli manuel de productible si PVGIS indisponible. Règles permanentes : texte UI en français ; FK company forcée serveur ; défauts = chiffres actuels conservateurs ; migrations additives ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N67 — (SHIPPED 2026-06-19 — voir DONE LOG) Textes de garantie éditables par produit & par catégorie dans Paramètres (imprimés sur les devis & dossiers de remise), textes de garantie actuels semés comme défauts et utilisés partout où la garantie apparaît. Le texte garantie par produit existe déjà sur le modèle Produit. Partie du chantier « littéraux PDF règle #4 » : les textes de garantie imprimés par le moteur premium deviennent des réglages versionnés dont le défaut reproduit EXACTEMENT le texte actuel → PDF identique au byte près tant que non édité. PRÉ-APPROUVÉ (voir bloc PRÉ-APPROUVÉ) : câbler ces littéraux du moteur premium vers des champs de réglage ; disposition visuelle inchangée ; exception ciblée règle #4, limitée au câblage littéral→réglage. Règles permanentes : texte UI en français ; FK company forcée serveur ; défauts = texte actuel ; migrations additives ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N68 — Roles-and-permissions RBAC editor in Paramètres: define roles, grant/restrict per module & per action (view/create/edit/delete/export), restrict sensitive fields (buy prices, margins) to specific roles, safe default role set (owner/commerciale/technicien/viewer) so current access is unchanged, record-level rules limiting a user to their own assigned leads/chantiers when desired. (SHIPPED 2026-06-18 — seven roles seeded & editable as a module×action grid; record-visibility scoping by supervisor/team across all main objects; existing users mapped so nobody lost access.)
- [x] N69 — Buy prices & internal margins governed by an explicit permission, visible only to roles Reda authorises, default owner-only. (SHIPPED 2026-06-18 — `prix_achat_voir` permission gates buy prices on the Produit serializer; granted to Directeur & Administrateur by default; legacy accounts unchanged.)
- [x] N72 — (SHIPPED 2026-06-19 — voir DONE LOG) No-code automation-rules engine in Paramètres (if-this-then-that over the app's own events): triggers (lead stage change, devis accepted, chantier reaching a status, facture overdue, warranty nearing expiry, maintenance visit due, stock below threshold); actions (send WhatsApp/email/SMS from a template, create activity/task, assign a record, set a field, create a SAV ticket); rules editable/enabled/disabled, all runs logged; complements not duplicates n8n.
- [x] N73 — (SHIPPED 2026-06-19 — voir DONE LOG) Simple approval-step capability in the automation engine so selected actions (e.g. a discount above a configurable threshold) require owner approval before proceeding.
- [x] N74 — Chantier/onboarding checklists fully configurable as named workflow templates in Paramètres, selected automatically by type d'installation. (SHIPPED 2026-06-18 — nouveau modèle `ChecklistTemplate` (installations) : modèles nommés, chacun une liste ordonnée d'étapes, auto-sélectionnés par `type_installation` à la création du chantier, repli sur un modèle « Défaut » protégé qui porte exactement les 7 étapes d'aujourd'hui (comportement préservé, migration additive rattachant les étapes historiques). Onglet Paramètres → Checklists ; endpoint `/installations/checklist-templates/`.)
### Notifications / dashboards / analytics
- [x] N75 — (SHIPPED 2026-06-19 — voir DONE LOG) Unified notification engine: in-app + (where configured) WhatsApp/email/SMS for key events (new lead assigned, devis accepted, chantier due to install, facture overdue, warranty expiring, maintenance visit due, stock low, SAV ticket opened/breaching target); per-user & per-event preferences in settings; in-app notification centre; reuse planned templates/channels.
- [BLOCKED: scheduled daily/weekly digest delivery needs a scheduler (G9, gated). On-demand summary data is now available via the N49/N80 insights endpoints.] N76 — Daily & weekly digest notification for Reda & Meryem (jobs to plan, quotes awaiting acceptance, overdue payments, due maintenance, open SAV), in-app and optionally WhatsApp/email.
- [BLOCKED: the "schedule a periodic export by email" half needs a scheduler + email provider (G9/G1, gated). Saved-view persistence deferred with it.] N79 — Saved-reports & custom-views capability: save filtered/grouped views of any major object, pin to dashboard, schedule a periodic export of a saved report by email when email is configured.
- [x] G9 — (SHIPPED 2026-06-19 — voir DONE LOG) Planificateur **Celery Beat** + deux jobs planifiés en heure **Africa/Casablanca** (ungate de G9) : (1) rappels de relance planifiés, et (2) un contrôle quotidien des factures en retard. Règle de retard : une facture dont l'échéance est passée et qui n'est pas entièrement payée bascule en « En retard » ; si aucune échéance n'est posée, la défaut = 30 jours après émission ; le job quotidien effectue le basculement. Celery Beat est PRÉ-APPROUVÉ comme planificateur (voir bloc PRÉ-APPROUVÉ) — ne pas se mettre en pause dessus. Règles permanentes : texte UI en français ; TOUTE la logique horaire en Africa/Casablanca ; FK company respectée/forcée serveur ; chaque nouveau réglage par défaut = comportement actuel ; migrations additives ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
### Import/export / search / calendar / map
- [x] N107 — (SHIPPED 2026-06-19 — voir DONE LOG) Importateur de leads Odoo : une **commande de gestion** (`manage.py`) prenant en argument le chemin d'un fichier de sauvegarde, important les leads de façon **idempotente**, et ne faisant RIEN tant qu'elle n'est pas lancée avec un fichier. Réutiliser le framework d'import existant (T9 / N81) ; création/mise à jour idempotente (jamais de doublon — rapprochement email/téléphone). L'extraction one-shot réelle des 619 leads reste manuelle/gatée sur la vraie sauvegarde Odoo (voir G13 — le fichier contient des PII, jamais committé). Règles permanentes : texte UI en français ; FK company forcée serveur sur les leads importés ; idempotent (aucun écrasement silencieux) ; additif ; aucune donnée client réelle dans le dépôt ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N85 — (SHIPPED 2026-06-19 — voir DONE LOG) Vue carte traçant leads/chantiers/systèmes installés/visites planifiées par GPS ou adresse, filtrable par type & statut, pour planifier les visites sans routage lourd. **Intégrer Leaflet** (pré-approuvé — voir bloc PRÉ-APPROUVÉ) pour cette carte du Parc/équipement ET partout où une carte est nécessaire (réutiliser le même composant carte). Marqueurs cliquables ouvrant le détail de l'enregistrement ; requêtes bornées société. Règles permanentes : texte UI en français ; aucune sortie client exposant un prix d'achat ; FK company respectée dans les requêtes ; additif ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
### Chatbot / integrations / API
- [x] N86 — (SHIPPED 2026-06-19 — voir DONE LOG) Extend the unified chatbot to read & act across all new objects (e.g. which chantiers à planifier, which garanties expire this quarter, which factures overdue, what production a named client's system did last month; open a SAV ticket, draft a BC for a chantier shortfall, schedule a maintenance visit), reusing the existing chatbot interface, respecting role permissions.
- [x] N87 — (SHIPPED 2026-06-19 — voir DONE LOG) Intégration email pour envoyer les documents client & relances via un compte d'envoi configurable (templates FR, pièce jointe = le PDF pertinent, journaliser ce qui a été envoyé sur le chatter du client/document) ; complète WhatsApp. **Intégration BREVO (SDK ou SMTP)** lisant la clé API depuis les réglages/variables d'environnement (pré-approuvé — voir bloc PRÉ-APPROUVÉ) ; si la clé est absente, l'envoi est un no-op et les tests ne doivent PAS exiger de clé live. **Acheminer les emails de rappel de relance par cette intégration.** Règles permanentes : texte UI/templates en français ; toute logique horaire en Africa/Casablanca ; FK company forcée serveur ; clé absente ⇒ no-op (comportement actuel préservé) ; additif ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N88 — (SHIPPED 2026-06-19 — voir DONE LOG) Capture d'email entrant rattachant les réponses au fil client/chantier pertinent lorsqu'une référence reconnaissable est présente. S'appuie sur la même intégration **BREVO** que N87 (pré-approuvé) ; no-op sans clé configurée (les tests n'exigent pas de clé live). Règles permanentes : texte UI en français ; Africa/Casablanca ; FK company forcée serveur ; clé absente ⇒ no-op ; additif ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
- [x] N89 — (SHIPPED 2026-06-19 — voir DONE LOG) Public REST API exposing core objects with token-based API keys managed in settings, scoped permissions, rate limiting, and webhooks on key events (new lead, devis accepted, chantier completed, facture paid).
### PWA / mobile / offline
- [BLOCKED: offline-sync — nouvelle architecture de file d’attente hors-ligne (décision/architecture), couplée à F21] N91 — Offline-tolerant field capture for the chantier checklist, photos, and PV de réception signature, syncing when back online.
- [BLOCKED: nouvelle dépendance pywebpush + clés VAPID (push web)] N92 — Push notifications to the PWA for high-priority events from the notification engine.
### Localisation / audit / security / data
- [BLOCKED: i18n + RTL pan-application — nouvelle dépendance i18n et composant architectural couvrant tout le frontend] N93 — Full Arabic & Darija localisation as a selectable interface language with RTL layout support across the app, French default, English in code; client-facing document language selectable per client (facture/devis in French or Arabic).
- [BLOCKED: dépend du cadre i18n de N93 (gaté)] N94 — Translation-management surface in settings so interface strings can be reviewed/adjusted per language without a code change.
- [BLOCKED: changement d’authentification (2FA) + nouvelle dépendance pyotp] N96 — Account security: optional 2FA, visible active sessions with revoke, forced credential-rotation flow; production DEBUG setting left unchanged.
- [x] N97 — (SHIPPED 2026-06-19 — voir DONE LOG) Configurable data export & backup action for the tenant's data (reversibility/retention), real customer-data exports kept out of the repo.
### Growth / multi-tenant platform
- [BLOCKED: plateforme multi-tenant + facturation par tenant (coût) + nouvelle architecture] N100 — Build out multi-tenant operation on the existing tenant_id foundation (strict per-tenant isolation verification, tenant onboarding flow, per-tenant branding/white-label of client-facing documents, configurable per-plan feature limits, tenant-level billing).
- [BLOCKED: console d’administration tenant + auto-inscription (changement d’auth) + nouvelle architecture] N101 — Tenant administration console (manage tenants/plans/usage/support) + self-serve signup for design-partner installers.
- [BLOCKED: dépend de N100/N101 (gatés)] N102 — After the modules above are built, update the master project document + PLAN + DONE log in plain language to reflect the new post-sale, procurement/inventory, Moroccan billing/compliance, full-editability, and platform additions, noting which shipped and which were skipped.

---

## BUILD QUEUE — F1–F24 (field-execution & outillage module — added 2026-06-17)

Reda's "intervention" / field-execution module. **Build order = the order below** (outillage and
the intervention spine first, then the departure gate, then on-site capture, reconciliation, voice,
completion, and the advanced layer last). It is a big queue and **paces against the usage window** —
re-firing "work on the plan" after a cap resets resumes from the next unchecked task. Honour every
STANDING RULE plus the **module-specific constraints** below.

**MODULE CONSTRAINTS (in addition to the STANDING RULES):**
- The lead pipeline **`STAGES.py` stays a fixed CI contract** and is **never** made runtime-editable.
- The new Intervention **`statut` is its own separate state machine** that **never reads from or
  writes to `STAGES.py`** or to the **chantier status field**.
- **Buy prices and margins never appear on any client-facing document**, including the
  **compte-rendu d'intervention**.
- **Voice-memo capture, OCR, and AI photo-QA each add NO external credential and NO per-use cost by
  default**, and **no-op safely** until a provider is explicitly configured in Paramètres. The
  **transcription provider is left unconfigured** — a separate operator decision, added by **no**
  part of these tasks.
- **All photos, voice memos, GPS, and site data are real customer data** → object storage, **never
  committed to the repo**.
- **The production DEBUG setting is left exactly as configured — unchanged by these tasks.**
- **Reuse existing patterns**: file attachments / object storage, audit + chatter, kanban visual
  language + drag-to-change-status, the existing PDF engine, stock reservation + consumption,
  race-safe gapless numbering, Paramètres editability.
- **One session = the whole queue across up to 8 concurrent worktree lanes, every branch folded
  into one `dev` → self-merged to `main` exactly once after tests pass — never one PR per agent,
  never a merge per task, never split into review PRs.**

- [x] F1 — **Outillage (équipement durable) catalogue**, kept completely separate from the consumable Stock SKUs. Each tool carries: nom, catégorie, asset tag, optional numéro de série, a current location chosen from the existing stock locations (dépôt + camionnette) plus an **En intervention** state, a statut (Disponible / En intervention / En réparation / Perdu), a purchase date, and an optional photo via the existing file-attachment feature. List view + filter by location and statut. Durable tools are tracked across dépôt, camionnette, and job sites **without ever being treated as sellable stock, consumed, or shown on any client-facing document**. (SHIPPED 2026-06-18 — nouvelle app `apps.outillage` company-scopée : modèle `Outillage` (nom/catégorie/asset_tag/n° série/emplacement FK `stock.EmplacementStock`/statut/date d'achat), endpoints `/outillage/outils/` (lecture tout rôle, écriture responsable+admin) filtrables par statut/emplacement + recherche ; page `/outillage` (liste + filtres + création/édition en panneau + photo via la pièce jointe générique `outillage.outillage`) sous le menu CHANTIERS. Jamais vendable, jamais client-facing. 12 tests backend.)
- [x] F2 — **Reusable kits d'outillage** as named templates editable in Paramètres (seeded defaults: Kit pose structure, Kit raccordement électrique, Kit mise en service), each an ordered list of required tools drawn from the Outillage catalogue, selectable per type d'intervention. Seeded defaults support add / rename / reorder / deactivate like every other référentiel; deactivation preserves the value on historical records. (SHIPPED 2026-06-18 — modèles `KitOutillage` + `KitOutillageItem` (ordre, FK outil) ; les 3 kits par défaut semés à la première liste (idempotent), pleinement éditables ; `type_intervention` (clé TypeIntervention) pré-sélectionne un kit ; endpoints `/outillage/kits/` + `/outillage/kit-items/` (écriture admin, company de l'item suivant le kit) ; onglet Paramètres → « Kits d'outillage » (renommer/réordonner/activer + liste ordonnée d'outils). Couvert par les tests outillage.)
- [x] F3 — **Intervention (sortie chantier) object** belonging to a Chantier (one chantier → several interventions). Carries: type (Pose / Mise en service / SAV / Visite), planned date, an assigned équipe of one+ employees (default = the chantier installer), an assigned camionnette, and **its own ordered `statut`** (À préparer / Prête / En route / Sur site / Terminée / Validée) that is **completely separate from and never modifies the chantier status field or the `STAGES.py` contract**. Chatter + audit log reusing the existing activity-log pattern; linked to its chantier, devis, client, and the site GPS pulled from the chantier. (SHIPPED 2026-06-18 — le modèle `installations.Intervention` existant (« sortie chantier ») est ÉTENDU de façon additive : nouveau champ `statut` (machine à états PROPRE `a_preparer/prete/en_route/sur_site/terminee/validee` + `STATUT_ORDER`, défaut `a_preparer`), `equipe` M2M (défaut = installateur du chantier, posé serveur), `camionnette` FK `stock.EmplacementStock`. Nouveau modèle chatter `InterventionActivity` + helper `intervention_activity.py` (création + changements de champs suivis + notes) ; endpoints `/installations/interventions/<id>/historique/` + `/noter/` ; filtres `?statut=`/`?type_intervention=`. La référence/client/devis/ville/GPS sont tirés du chantier (lecture seule). CHANGER LE STATUT INTERVENTION NE TOUCHE JAMAIS le statut chantier ni STAGES.py (garanti par test). Camionnette scopée société. Migration additive `0007`. 10 tests F3.)
- [x] F4 — **Intervention list view + kanban grouped by intervention statut**, reusing the same visual language, drag-to-change-status, and reassignment behaviour as the existing lead and chantier kanbans. Each card shows client, ville, type, planned date, équipe, statut. (SHIPPED 2026-06-18 — nouvelle page `/interventions` (menu CHANTIERS) : vue Liste + vue Kanban en colonnes par statut intervention, glisser-déposer pour changer de statut (même `kb-*`/`kc-*` que le kanban chantiers via @dnd-kit, aucune nouvelle dépendance), réassignation du technicien par carte, filtres statut + type, et un panneau (Sheet) de détail surfaçant l'Historique + ajout de note + changement de statut/technicien. Cartes : chantier, client, ville, type, date prévue, équipe, statut. eslint 0 erreur, build vite+PWA OK.)
- [x] F5 — (SHIPPED 2026-06-19 — voir DONE LOG) **Liste de préparation** generated per intervention: pulls matériel from the chantier bill of materials (copied from its devis) and outils from the selected kit d'outillage. Every matériel line shows required quantity + a **chargé** checkbox; every outil shows a checked checkbox; computes a **préparation completion %**; exposes a **« Tout est chargé »** confirmation that must be set before the intervention can move from À préparer to Prête/En route. Per-line **manquant** flag links into the existing **Besoin matériel** + one-click draft bon de commande flow. Reuses the **existing stock reservation** so preparing an intervention reserves the required quantities and surfaces a manquant as a shortfall.
- [x] F6 — (SHIPPED 2026-06-19 — voir DONE LOG) **GPS check-in on arrival**: stamps an arrival timestamp + coordinates on the intervention using the browser geolocation already used for site GPS (**no new external service**); shows a distance-to-site indicator against the chantier GPS as a presence record; plus a **départ-dépôt** stamp and a **retour** stamp for travel timing.
- [x] F7 — (SHIPPED 2026-06-19 — voir DONE LOG) **Guided photo capture** per intervention driven by a configurable **shot list** (defaults seeded to the solar field-documentation standard, fully editable in Paramètres), grouped **Avant** (état de la toiture et pénétrations existantes, coffret et compteur, tracé du câble, vue d'ensemble du site), **Pendant** (structure et rails avant pose, câblage avant dissimulation, mises à la terre, numéro de série de chaque panneau), **Après** (champ posé, onduleur, compteur de production avec relevé, vue d'ensemble). Every photo stored via the existing file-attachment feature in object storage, tagged to its shot-list slot, stamped with capture timestamp + GPS coordinates as a **visible overlay**, presented in an Avant/Pendant/Après gallery per intervention. **Real site data, never committed to the repo.**
- [x] F8 — (SHIPPED 2026-06-19 — voir DONE LOG) **Required-photo enforcement**: an intervention cannot move to Terminée until every shot-list slot marked **obligatoire** has at least one photo; shows a clear checklist of which obligatoire shots are still missing; the obligatoire flag per slot is editable in Paramètres.
- [x] F9 — (SHIPPED 2026-06-19 — voir DONE LOG) **Per-component serial-number capture** on the relevant Pendant/Après steps: a photo of the plaque signalétique + an editable serial field, with **optional automatic serial extraction behind a swappable OCR interface** that adds **no external credential by default** and safely **no-ops** (returns the manual field) when no provider is configured — exactly like the existing Bill OCR swappable interface. Captured serials flow into the **Système installé** record per the existing parc-installé design and **never block** step or intervention completion when a serial is left empty.
- [x] F10 — (SHIPPED 2026-06-19 — voir DONE LOG) **Photo annotation**: a captured photo can be marked up with simple drawing + a caption to flag an issue, stored as part of the photo record.
- [x] F11 — (SHIPPED 2026-06-19 — voir DONE LOG) **Matériel consommé reconciliation** per intervention: lists every bill-of-materials line with its **prévu** quantity against a **réellement utilisé** quantity, allows extra unplanned lines (e.g. mètres de câble, vis et boulons, connecteurs MC4, presse-étoupes), requires a short justification on any line where utilisé ≠ prévu (justification accepted as typed text **or a voice memo**), and on validation reconciles against the **existing stock reservation + consumption** so real field consumption (not the quote estimate) drives stock movements and feeds the per-chantier job-costing margin. **Buy prices stay internal.**
- [x] F12 — (SHIPPED 2026-06-19 — voir DONE LOG) **Overage review surface**: interventions whose consumption exceeds the quoted quantities **beyond a configurable percentage** are flagged to Reda for review with the justifications attached, feeding restock accuracy + a per-chantier margin-impact alert.
- [x] F13 — (SHIPPED 2026-06-19 — voir DONE LOG) **Voice-memo capture** available anywhere on an intervention (a general note, a note attached to a photo, the justification on a Matériel consommé variance line, a note on a réserve): records audio in the field, stores it via the existing file-attachment feature in object storage, with in-app playback. **No external service for capture/storage; audio is real site data, never committed to the repo.**
- [x] F14 — (SHIPPED 2026-06-19 — voir DONE LOG) **Swappable Transcription interface** with a default **no-op** implementation that labels every voice memo **« Non transcrit — service non configuré »** until a provider is set; the provider + any credential is configurable **only in Paramètres** and **added by no part of this task**; designed for Darija + French code-switching; always keeps the **original audio as source of truth** and the produced transcript editable by Reda or Meryem; mirrors the existing swappable-interface no-op pattern (field captures + stores voice memos fully today, transcription lights up later once a provider is explicitly chosen). **No external credential introduced by default.**
- [x] F15 — (SHIPPED 2026-06-19 — voir DONE LOG) **Crew time tracking** per intervention: on-site duration from the GPS arrival stamp + the Terminée time, travel time from the départ-dépôt + retour stamps, feeding the existing chantier **actual labour-days** field.
- [x] F16 — (SHIPPED 2026-06-19 — voir DONE LOG) **Réserves punch-list** per intervention for items needing a return (e.g. câble manquant, réglage onduleur): each with a description, optional photo, optional voice memo, an assignee, and a resolution; optionally spawning a follow-up intervention or a SAV ticket per the existing SAV design.
- [x] F17 — (SHIPPED 2026-06-19 — voir DONE LOG) **Tool-return reconciliation** on closing an intervention: confirms each outil from the kit is returned to its dépôt or camionnette location, flags any tool not returned, and updates the Outillage statut + location accordingly.
- [x] F18 — (SHIPPED 2026-06-19 — voir DONE LOG) **Consignes de sécurité sign-off** step on an intervention with a configurable checklist (seeded with EPI portés + consignation électrique, editable in Paramètres), recorded with who + when reusing the existing audit pattern.
- [x] F19 — (SHIPPED 2026-06-19 — voir DONE LOG) **Compte-rendu d'intervention PDF** per intervention (French, existing PDF engine + company identity from Paramètres): client + site, the équipe, arrival + departure times, the Avant/Pendant/Après photo set, the serials captured, the matériel réellement consommé with its variance justifications, the réserves, and a client + installer signature block with the manuscrit « Bon pour accord » line. **Client-facing, no buy prices.** Complements the existing PV de réception.
- [x] F20 — (SHIPPED 2026-06-19 — voir DONE LOG) **Optional AI photo-QA** behind a swappable vision interface reusing the **Claude API already in the stack**: when enabled, flags likely-missing or low-quality obligatoire shots (blurred / wrong-subject) so the équipe can fix them before leaving the site; **adds no external credential by default**, safely no-ops when disabled, and **never blocks completion** on its own.
- [BLOCKED: capture hors-ligne — même architecture de synchronisation hors-ligne gatée que N91] F21 — **Offline-tolerant field capture** covering the whole intervention flow — préparation checklist, GPS check-in, photos, serial capture, voice memos, Matériel consommé, réserves, and the signature — queuing locally on a poor connection and syncing when back online (extends the planned offline field capture to the full intervention workflow).
- [x] F22 — (SHIPPED 2026-06-19 — voir DONE LOG) **« Ma journée » crew view** for the logged-in technician showing today's assigned interventions in order, each opening straight into its préparation checklist → on-site capture → completion flow, thumb-reachable and in French, as the single screen a field technicien uses all day; **seed the existing technicien RBAC role** so a technicien sees only their own assigned interventions + chantiers and can capture everything but **never sees buy prices or margins**.
- [x] F23 — (SHIPPED 2026-06-19 — voir DONE LOG) **Per-intervention QR or short code** so scanning a chantier or matériel label resolves straight to its intervention, reusing the existing label + scan-resolution feature.
- [ ] F24 — After F1–F23 are built, **update the master project document + the PLAN + DONE log** in plain language to record the new field-execution + outillage module, the Matériel consommé reconciliation, the voice-memo capture with its swappable transcription interface left unconfigured, the crew-facing « Ma journée » experience, and which items shipped and which were skipped.

---

## REFINEMENT QUEUE — existing-feature polish (audit 2026-06-18)

Atomic refinements of **already-shipped** features, found by reading the real frontend + backend
source surface by surface (not the docs). Each line is one focused session in the format
`[ ] [<MODULE>] [L<lens#>] [<GATE>] <imperative>. Fait = <observable acceptance>`. **Build order:**
drain the existing BUILD QUEUE first (T1–T17, then the N/F tasks), **then** work this queue
top-down — build the **ROUTINE** tasks unattended and **skip + report** every gated task
(`SCHEMA`/`DEP`/`DECISION`/`GALLERY`); the gated ones are mirrored into the GATED section below so an
unattended run never auto-builds them. Every task obeys the STANDING RULES (French UI, additive only,
company-scoped, `STAGES.py` contract untouched, buy prices never client-facing, DEBUG untouched,
client-facing PDFs are GALLERY-gated).

### CRM / Leads

#### Fiche & formulaire
[ ] [CRM/Leads] [L7] [ROUTINE] Normaliser le téléphone/WhatsApp à la saisie dans LeadForm.jsx (réutiliser apps/ventes/utils/phone.normalize_ma_phone côté serveur via validate_telephone) au lieu de stocker la chaîne brute. Fait = saisir "06 12-34 56 78" ou "+212612..." enregistre une forme canonique cohérente, et le doublon par téléphone (services.normalize_phone) matche les variantes.
[ ] [CRM/Leads] [L15] [ROUTINE] Avertir en ligne dans la section Contact de LeadForm.jsx quand le numéro saisi correspond déjà à un lead existant (le check /crm/leads/<id>/duplicates n'existe qu'en édition) — faire un check "téléphone/email déjà utilisé" dès la création. Fait = en créant un nouveau lead, taper un téléphone déjà présent affiche un avertissement FR "Un lead avec ce numéro existe déjà" avec lien, sans bloquer.
[ ] [CRM/Leads] [L7] [ROUTINE] Valider le format email côté LeadForm.jsx avant submit (le champ est type="email" mais le form est noValidate donc rien n'est vérifié). Fait = saisir "abc@" et enregistrer affiche un message FR "Email invalide" sous le champ au lieu d'envoyer une valeur cassée au serveur.
[ ] [CRM/Leads] [L3] [ROUTINE] Pré-remplir le canal d'un nouveau lead créé manuellement à "walk_in" ou "telephone" (aujourd'hui canal reste vide par défaut dans LeadForm fields.canal). Fait = ouvrir "Nouveau lead" propose un canal par défaut configurable, et un lead créé à la main n'a plus canal=null dans la vue graphique "Non renseigné".
[ ] [CRM/Leads] [L6+L7] [ROUTINE] Empêcher de placer manuellement l'étape à "Signé" via le select Étape de LeadForm.jsx (la liste/kanban ouvrent SigneDialog, mais le formulaire laisse choisir SIGNED librement sans devis accepté). Fait = sélectionner "Signé" dans le formulaire de fiche ouvre le même SigneDialog (choix devis+option) au lieu d'enregistrer stage=SIGNED directement.
[ ] [CRM/Leads] [L7] [ROUTINE] Exiger un motif de perte quand "Perdu ?" est coché dans LeadForm.jsx (motif_perte reste facultatif aujourd'hui). Fait = cocher Perdu et enregistrer sans motif affiche "Indiquez le motif de perte" et n'enregistre pas tant que le champ est vide.
[ ] [CRM/Leads] [L4] [ROUTINE] Afficher une mini-carte / lien Google Maps cliquable quand gps_lat/gps_lng sont renseignés dans la section Contact de LeadForm.jsx (les coordonnées ne sont aujourd'hui que deux champs numériques). Fait = un lead avec GPS montre un lien "📍 Voir sur la carte" ouvrant maps.google.com aux coordonnées du lead.
[ ] [CRM/Leads] [L8] [ROUTINE] Afficher la tranche de facture du site (bill_range_bucket) et roi_band sur la fiche d'un lead venu du site web dans LeadForm.jsx (ces champs reçus par le webhook ne sont visibles nulle part dans le formulaire). Fait = un lead canal "Site web" affiche en lecture sa tranche de facture déclarée et la bande ROI préliminaire dans la section Profil énergétique.
[ ] [CRM/Leads] [L8] [ROUTINE] Afficher l'attribution publicitaire (utm_source/medium/campaign, fbclid) en lecture sur la fiche d'un lead web dans LeadForm.jsx (capturée par webhooks.py mais jamais montrée). Fait = un lead avec UTM montre une petite section "Origine de la campagne" listant source/campagne, masquée si tous les UTM sont vides.
[ ] [CRM/Leads] [L11] [ROUTINE] Donner un retour d'erreur visible quand l'enregistrement inline de la facture échoue dans LeadForm.saveBill (catch silencieux aujourd'hui). Fait = couper le réseau et enregistrer la facture inline affiche "Échec de l'enregistrement — réessayez" au lieu de rester silencieux.
[ ] [CRM/Leads] [L11] [ROUTINE] Remplacer les catch silencieux de postNote/doMerge dans LeadForm.jsx par un message FR (note non envoyée / fusion échouée). Fait = poster une note hors-ligne affiche "La note n'a pas pu être enregistrée — réessayez", la saisie restant intacte.
[ ] [CRM/Leads] [L16] [ROUTINE] Tracer regularisation_8221, visite_notes, tranche_onee, ombrage_notes, structure_pref, nb_etages, inclinaison_deg dans l'Historique (activity.TRACKED_FIELDS) — plusieurs champs de la fiche ne génèrent aucune entrée de chatter quand on les modifie. Fait = modifier "Régularisation 82-21" sur un lead écrit une ligne Historique "Régularisation 82-21 : Non → Oui".
[ ] [CRM/Leads] [L16] [ROUTINE] Inclure les champs GPS et societe dans le journal des modifications (activity.TRACKED_FIELDS n'a pas gps_lat/gps_lng/societe). Fait = changer la société ou les coordonnées GPS d'un lead produit une entrée Historique correspondante.

#### Funnel / étapes & cycle de vie
[x] [CRM/Leads] [L6] [ROUTINE] Afficher une pastille "Devis expiré" sur la carte kanban (LeadCard.jsx) et la ligne liste quand le dernier devis du lead est expiré (le serializer get_devis expose statut mais l'expiry on-the-fly de T7 n'est pas reflété sur le lead). Fait = un lead dont le dernier devis est "Expiré" montre un badge orange "Devis expiré" sur sa carte.
[x] [CRM/Leads] [L6] [ROUTINE] Ajouter un badge "Sans activité depuis N jours" / lead stagnant sur LeadCard.jsx et ListView (date_modification est exposée mais non utilisée visuellement). Fait = un lead non touché depuis un seuil (ex. 14 j) affiche un indicateur "Inactif 14 j" sur sa carte kanban.
[x] [CRM/Leads] [L6] [ROUTINE] Suggérer la prochaine action selon l'étape sur LeadCard.jsx (ex. NEW → "À contacter", QUOTE_SENT sans relance → "Planifier une relance"). Fait = un lead en "Devis envoyé" sans relance_date affiche une suggestion cliquable "Planifier une relance".
[x] [CRM/Leads] [L7] [ROUTINE] Bloquer le glisser-déposer kanban qui RECULE une étape côté UI (KanbanView.handleDragEnd appelle onChangeStage sans vérifier le sens ; le backend updateLead ne refuse pas un recul manuel d'étape). Fait = glisser une carte de "Relance" vers "Nouveau" est refusé avec un message FR "On ne recule pas une étape", l'étape restant inchangée.
[ ] [CRM/Leads] [L7] [ROUTINE] Empêcher côté serveur (LeadSerializer.validate) qu'un PATCH d'étape déplace un lead Perdu ou recule dans le funnel (la règle existe pour bulk via _bulk_stage_allowed mais pas pour un update simple). Fait = un PATCH stage sur un lead perdu=true est rejeté 400 "Lead perdu — étape non modifiable", aligné sur la règle bulk.
[x] [CRM/Leads] [L6] [ROUTINE] Indiquer le total pondéré (prévision) par colonne dans le header kanban (groupLeadsByStage calcule totalDevis brut, pas de pondération par étape). Fait = chaque colonne kanban affiche, sous le total devis, une prévision pondérée par la probabilité de l'étape.

#### Vues (kanban / liste / calendrier / graphique)
[x] [CRM/Leads] [L14] [ROUTINE] Afficher dans ChartsView.jsx un graphique "Leads par responsable" (owner_nom est déjà sur chaque lead, seul stage+canal sont graphés). Fait = la vue graphique montre une barre par responsable avec le nombre de leads assignés.
[x] [CRM/Leads] [L14] [ROUTINE] Ajouter à ChartsView.jsx un taux de conversion par étape (funnel) au-delà des simples comptes (groupLeadsByStage donne les comptes mais aucun % de passage). Fait = la vue graphique affiche le % de leads qui atteignent chaque étape suivante.
[ ] [CRM/Leads] [L11] [ROUTINE] Distinguer "aucun lead du tout" de "aucun lead avec ces filtres" dans ChartsView.jsx et les vues (le message actuel "Aucun lead à représenter avec ces filtres" s'affiche même sans filtre). Fait = sans aucun lead, le message est "Aucun lead — créez votre premier lead"; avec filtres actifs, "Aucun résultat pour ces filtres — effacer".
[ ] [CRM/Leads] [L9] [ROUTINE] Permettre de colorer le CalendarView.jsx par étape ou par responsable plutôt que seulement par date de relance (aujourd'hui une seule dimension : relance_date). Fait = un sélecteur "Couleur par : étape / responsable" recolore les pastilles du calendrier.
[ ] [CRM/Leads] [L6] [ROUTINE] Poser aussi les visites prévues (visite_prevue_le) sur le CalendarView.jsx, pas seulement relance_date (le champ existe sur le modèle mais n'apparaît pas au calendrier). Fait = un lead avec une visite prévue apparaît sur le calendrier le jour de la visite avec une icône distincte de la relance.
[ ] [CRM/Leads] [L9] [ROUTINE] Ajouter à ListView.jsx des colonnes triables Ville et Téléphone (m-hide sur mobile) — la liste n'expose ni l'un ni l'autre alors que ce sont des champs clés de contact. Fait = la vue liste montre une colonne Téléphone cliquable (tel:) et Ville triable.
[ ] [CRM/Leads] [L5] [ROUTINE] Rendre la facture mensuelle éditable en ligne dans ListView.jsx (InlineEdit existe pour stage/priorité/relance/tags mais pas pour facture_hiver, le déclencheur le plus fréquent du "⚡ Devis auto"). Fait = double-clic sur une cellule "Facture" de la liste enregistre facture_hiver via PATCH et débloque le bouton Devis auto de la ligne.

#### Filtres / recherche / findability
[ ] [CRM/Leads] [L9] [ROUTINE] Ajouter un filtre par étape et un filtre "type d'installation" à FilterBar.jsx (on filtre par canal/responsable/priorité/tag mais pas par stage ni par marché résidentiel/agricole/etc.). Fait = la barre de filtres a un sélecteur "Type d'installation" qui réduit les quatre vues aux leads du marché choisi.
[ ] [CRM/Leads] [L9] [ROUTINE] Ajouter un filtre "En retard de relance" / "Relance cette semaine" à FilterBar.jsx (relance_date est affichée en retard sur les cartes mais non filtrable). Fait = une pastille "Relances en retard" ne montre que les leads dont relance_date < aujourd'hui dans toutes les vues.
[ ] [CRM/Leads] [L2] [ROUTINE] Étendre la recherche FilterBar/filterLeads au WhatsApp et à la société (filterLeads cherche nom/prénom/société/email/téléphone/ville mais le numéro WhatsApp distinct n'est pas couvert). Fait = taper un numéro WhatsApp dans la recherche trouve le lead même si telephone diffère du whatsapp.
[ ] [CRM/Leads] [L9] [ROUTINE] Persister les filtres FilterBar dans localStorage comme la vue active (VIEW_KEY existe, mais filters repart à EMPTY_FILTERS à chaque rechargement). Fait = appliquer un filtre canal puis recharger la page conserve le filtre.
[ ] [CRM/Leads] [L9] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé) Ajouter des "vues enregistrées" nommées (combinaison filtres+vue) réutilisables, enregistrées LOCALEMENT (sans email) — distinct de N79 qui reste BLOCKED car couplé à l'export planifié. Fait = l'utilisateur enregistre "Mes relances chaudes" et la rappelle d'un clic. Règles permanentes : texte UI FR ; additif ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés. [cf. N79]

#### Actions en masse (bulk)
[ ] [CRM/Leads] [L10] [ROUTINE] Ajouter une action en masse "Définir le canal" dans BulkActionBar.jsx + services.apply_bulk_action (les BULK_ACTIONS couvrent reassign/tag/stage/relance/perdu/archive mais pas le canal, souvent vide sur les imports). Fait = sélectionner des leads et choisir "Canal → Référence" met à jour canal en masse avec entrée Historique "en masse".
[ ] [CRM/Leads] [L10] [ROUTINE] Ajouter une action en masse "Définir la priorité" dans BulkActionBar.jsx + apply_bulk_action (priorité n'est éditable qu'en ligne, pas en masse). Fait = "Priorité → Haute" sur une sélection met à jour tous les leads avec journal "en masse".
[ ] [CRM/Leads] [L13] [ROUTINE] Ajouter une action en masse "Planifier une activité" (records.Activity) dans BulkActionBar.jsx pour une sélection de leads. Fait = sélectionner 5 leads et "Planifier un appel demain" crée une activité ouverte sur chacun.
[ ] [CRM/Leads] [L11] [ROUTINE] Désactiver le bouton "Restaurer" en masse de BulkActionBar.jsx quand le filtre courant ne montre aucun lead archivé (action toujours visible mais sans effet → bilan "0 mis à jour" confus). Fait = en vue "Actifs", le bouton Restaurer est grisé avec un title "Aucun lead archivé sélectionné".

#### Doublons
[ ] [CRM/Doublons] [L15] [ROUTINE] Afficher un compteur de doublons probables dans l'en-tête du pipeline (LeadsPage) à partir de /crm/leads/doublons (aujourd'hui il faut ouvrir l'atelier pour savoir s'il y en a). Fait = le bouton "🔀 Doublons" affiche un badge avec le nombre de groupes détectés.
[ ] [CRM/Doublons] [L4] [ROUTINE] Montrer un aperçu du résultat de fusion avant de confirmer dans DoublonsPanel.ClusterCard (la fusion se lance sur confirm() sans montrer quels champs vides du survivant seront comblés ni combien de devis migrent). Fait = avant "Fusionner le groupe", un récapitulatif liste "X devis, Y activités déplacés vers le survivant; champs comblés : …".
[ ] [CRM/Doublons] [L15] [ROUTINE] Inclure le nom normalisé comme critère affiché dans DoublonsPanel (find_duplicate_clusters fusionne sur téléphone/email/nom mais l'UI ne montre que tél/email/ville). Fait = chaque membre de cluster indique la clé de rapprochement (même téléphone / même email / même nom) qui l'a regroupé.
[ ] [CRM/Doublons] [L7] [ROUTINE] Empêcher de choisir un lead archivé comme survivant dans DoublonsPanel.ClusterCard quand des actifs existent (merge_leads accepte n'importe quel survivant, on pourrait absorber des actifs dans un archivé). Fait = le radio "à garder" d'un membre archivé est désactivé tant qu'un membre actif est présent dans le groupe.
[ ] [CRM/Leads] [L16] [ROUTINE] Journaliser le client résolu/lié sur le lead dans l'Historique (services.resolve_client_for_lead pose lead.client sans écrire d'entrée LeadActivity). Fait = la résolution/création du client au 1er devis écrit "Client lié : <nom>" dans le chatter du lead.
[ ] [CRM/Leads] [L16] [ROUTINE] Tracer dans l'Historique la création d'un lead via le site web côté webhook (webhooks.py écrit "Lead créé via le site web" mais pas les mises à jour idempotentes <1 min qui écrasent les champs). Fait = une mise à jour idempotente du webhook ajoute une entrée "Mis à jour via le site web (doublon < 1 min)".

#### Activités / Mes activités / rappels
[ ] [CRM/Activités] [L13] [ROUTINE] Afficher la prochaine activité (next_activity déjà exposée par le serializer) dans ListView.jsx et la fiche, pas seulement la pastille ⏰ du kanban. Fait = la vue liste montre une colonne "Prochaine activité" avec l'échéance et un style "en retard".
[ ] [CRM/Activités] [L11] [ROUTINE] Donner un retour d'erreur dans MesActivitesPage.markDone et load (catch vides : un échec laisse la liste inchangée sans message). Fait = marquer "Fait" hors-ligne affiche "Action impossible — réessayez" au lieu de ne rien faire.
[ ] [CRM/Activités] [L5] [ROUTINE] Permettre de reprogrammer (changer l'échéance) une activité depuis MesActivitesPage et ActivitiesPanel sans la supprimer/recréer (aujourd'hui on ne peut que "Fait" ou supprimer). Fait = un bouton "Reporter" ouvre un date-picker et PATCH la due_date de l'activité.
[ ] [CRM/Activités] [L14] [ROUTINE] Afficher un compteur d'activités en retard par responsable dans MesActivitesPage (la page ne montre que les activités de l'utilisateur courant). Fait = un encart "Charge de l'équipe" liste, pour un admin, le nombre d'activités en retard par employé.
[ ] [CRM/Activités] [L7] [ROUTINE] Empêcher de planifier une activité avec une échéance dans le passé dans ActivitiesPanel.create (le champ date accepte n'importe quelle valeur). Fait = choisir une date passée affiche "L'échéance ne peut pas être dans le passé" et bloque la création.

#### Gagné (Won)
[ ] [CRM/Won] [L4] [ROUTINE] In the lead→Gagné flow, let the user preview each candidate devis inline (reuse the existing PDF.js canvas renderer) before choosing the devis + option, instead of choosing blind by reference. Fait = the won dialog shows an openable preview of each quote so the user reads it and picks the right one without leaving the dialog.
[ ] [CRM/Won] [L1] [ROUTINE] Pré-sélectionner dans SigneDialog.jsx le devis le plus pertinent (le plus récent envoyé, sinon le total le plus élevé) au lieu du premier de la liste retournée. Fait = ouvrir le dialogue "Signé" sur un lead à plusieurs devis présélectionne le devis envoyé le plus récent, pas devisList[0].
[ ] [CRM/Won] [L11] [ROUTINE] Distinguer dans SigneDialog.jsx "aucun devis du tout" de "aucun devis sélectionnable" (selectableDevis filtre les refusés/inactifs → un lead n'ayant que des devis refusés affiche "n'a aucun devis", trompeur). Fait = un lead avec uniquement des devis refusés affiche "Tous les devis sont refusés — créez ou rouvrez un devis" au lieu de "aucun devis".
[ ] [CRM/Won] [L4] [ROUTINE] Afficher dans SigneDialog.jsx le détail de l'option (kWc / total par option) quand un devis a 2 options, pour aider le choix (aujourd'hui seuls les libellés "Sans/Avec batterie" sont montrés). Fait = choisir entre les deux options affiche le total TTC de chacune à côté du radio.
[ ] [CRM/Won] [L7] [ROUTINE] Confirmer la date d'acceptation dans SigneDialog.jsx quand elle est dans le futur (le date-picker accepte une date future, qui se propage en date de signature du chantier). Fait = saisir une date d'acceptation future demande confirmation "Date dans le futur — confirmer ?" avant d'enregistrer.

#### Mobile / ergonomie
[x] [CRM/Leads] [L12] [ROUTINE] Rendre le numéro de téléphone du lead cliquable (tel:) et le WhatsApp cliquable (wa.me) sur LeadCard.jsx et la fiche, pour appeler/écrire d'un tap sur iPhone. Fait = taper le téléphone d'un lead sur mobile lance l'appel; taper le WhatsApp ouvre la conversation.
[ ] [CRM/Leads] [L12] [ROUTINE] Rendre la barre de filtres FilterBar.jsx repliable sur mobile (5 sélecteurs + 6 pastilles débordent sur un écran iPhone). Fait = sur largeur ≤768px la barre se réduit à un bouton "Filtres" qui déplie un panneau.
[ ] [CRM/Leads] [L12] [ROUTINE] Donner aux actions de ligne de ListView (Éditer / Devis auto / Archiver / Supprimer) un repli en menu "⋯" sur mobile (4 boutons par ligne débordent de la cellule Actions). Fait = sur iPhone chaque ligne montre un bouton "⋯" ouvrant les actions au lieu de 4 boutons côte à côte.

#### Cohérence cross-module / export / localisation
[ ] [CRM/Leads] [L17] [ROUTINE] Formater les montants de facture du lead de façon cohérente (LeadForm utilise toLocaleString('fr-MA') pour la facture mais 'fr-FR'+"DH" pour les totaux devis, et SigneDialog "MAD"). Fait = tous les montants de la fiche lead utilisent le même format/locale et le même symbole (MAD).
[ ] [CRM/Leads] [L17] [DECISION] Capturer/afficher la langue de communication préférée du lead (FR/Darija) pour préparer l'envoi WhatsApp bilingue (whatsapp_devis prend déjà un paramètre langue, mais rien ne le mémorise sur le lead). Fait = un champ "Langue préférée" sur la fiche pré-sélectionne la langue du message WhatsApp. [cf. N93]
[ ] [CRM/Leads] [L8] [ROUTINE] Alimenter le sélecteur de canal de LeadForm.jsx depuis le référentiel Canal géré (Paramètres → CRM, éditable via T6) au lieu de la constante en dur CANAUX. Fait = ajouter un canal dans Paramètres le fait apparaître dans le sélecteur Canal de la fiche lead sans redéploiement.
[ ] [CRM/Leads] [L8] [ROUTINE] Alimenter FilterBar.jsx et ChartsView.jsx en libellés de canaux depuis le référentiel Canal géré plutôt que CANAL_LABELS figé dans stages.js (un canal ajouté en Paramètres apparaît comme "Non renseigné" dans le graphique). Fait = un canal personnalisé s'affiche avec son libellé dans le filtre et le graphique "Leads par canal".
[ ] [CRM/Leads] [L7] [ROUTINE] Valider côté LeadSerializer que canal appartient aux canaux gérés de la société (aujourd'hui canal est un CharField libre validé seulement contre les choices figés du modèle). Fait = un PATCH canal avec une clé inexistante est rejeté 400 "Canal inconnu".
[x] [CRM/Leads] [L1] [ROUTINE] Afficher sur LeadCard.jsx un indicateur "Devis prêt / Facture manquante" basé sur devis_auto.pret déjà exposé (le bouton ⚡ est grisé mais rien n'explique POURQUOI sur la carte). Fait = une carte dont le devis auto est bloqué montre une petite étiquette "Facture manquante" reprenant devis_auto.message.
[ ] [CRM/Leads] [L14] [ROUTINE] Exposer le montant du dernier devis (latestDevisTotal, déjà calculé côté front) dans l'export Excel des leads (exports.LEAD_EXPORT_HEADERS n'a aucune colonne devis/valeur). Fait = le fichier leads.xlsx contient une colonne "Dernier devis (TTC)" et "Statut devis".
[ ] [CRM/Leads] [L14] [ROUTINE] Ajouter au lead_row de l'export (exports.py) la colonne date_modification ("Dernière activité") absente. Fait = leads.xlsx inclut une colonne "Modifié le" reflétant date_modification.

### Clients
[ ] [Clients] [L17] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — export client ICE-inclusif) Ajouter les identifiants légaux marocains (ICE/IF/RC/CIN/RIB) à l'export Excel des clients. Fait = export_clients_xlsx dans apps/crm/exports.py sort des colonnes Type, ICE, IF, RC, CIN, RIB à côté de nom/email/téléphone/adresse, alimentées depuis les champs Client correspondants. Règles permanentes : FR ; FK company respectée ; additif ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [Clients] [L1] [ROUTINE] Rendre le compteur de devis liés cliquable vers la liste des devis filtrée. Fait = la pastille Devis dans ClientList.jsx (c.devis_count) devient un lien qui ouvre /ventes/devis pré-filtré sur les devis de ce client.
[ ] [Clients] [L9] [ROUTINE] Étendre la recherche client à ICE/IF/RC et CIN. Fait = le memo `filtered` de ClientList.jsx teste c.ice/c.if_fiscal/c.rc/c.cin et le search_fields du ClientViewSet ajoute ice/if_fiscal/rc/cin.
[x] [Clients] [L8] [ROUTINE] Afficher le type de client (Particulier/Entreprise) en badge dans la liste. Fait = ClientList.jsx affiche un badge coloré depuis c.type_client dans la colonne Client.
[x] [Clients] [L7] [ROUTINE] Ne plus exiger l'email à la création d'un client (le modèle l'autorise vide). Fait = ClientForm.jsx validate() ne rejette plus un email vide (optionnel, format vérifié seulement s'il est rempli), conforme à Client.email blank/null.
[x] [Clients] [L15] [ROUTINE] Valider doucement le format ICE/IF/RC/CIN avec un indice FR non bloquant. Fait = ClientForm.jsx affiche un avertissement (jamais bloquant) quand l'ICE n'a pas 15 chiffres ou que CIN/IF/RC sont manifestement malformés.
[ ] [Clients] [L14] [ROUTINE] Afficher une colonne valeur-client cumulée (total facturé/payé) dans la liste. Fait = ClientSerializer expose total_facture_ttc/total_paye agrégés par client et ClientList.jsx l'affiche en colonne.
[ ] [Clients] [L4] [ROUTINE] Ajouter un panneau détail client en lecture listant ses devis, factures et chantiers. Fait = cliquer une ligne client ouvre un panneau montrant ses devis/factures/installations liés (référence, statut, total), via une méthode serializer/endpoint scopée société.
[x] [Clients] [L11] [ROUTINE] Remplacer l'erreur JSON.stringify de la liste clients par un message FR clair. Fait = la branche error de ClientList.jsx rend une phrase française au lieu de "Erreur : {JSON.stringify(error)}".
[ ] [Clients] [L11] [ROUTINE] Donner à l'état vide clients un bouton d'action principal. Fait = le bloc empty-state de ClientList.jsx inclut un bouton "+ Nouveau client" câblé à openNew.
[x] [Clients] [L5] [ROUTINE] Ajouter un raccourci "Nouveau devis" par ligne client. Fait = chaque ligne de ClientList.jsx ouvre /ventes/devis/nouveau avec le client présélectionné.
[x] [Clients] [L3] [ROUTINE] Passer type_client à Entreprise dès qu'un ICE/IF/RC est saisi à la création. Fait = ClientForm.jsx bascule type_client à entreprise la première fois qu'une valeur ICE/IF/RC est saisie (parité avec le défaut d'édition), sans écraser un choix manuel.
[ ] [Clients] [L8] [ROUTINE] Surfacer l'ICE client dans l'indice de résolution client du générateur de devis. Fait = DevisGenerator.jsx resolvedClientLabel ajoute l'ICE quand présent pour un devis B2B.
[ ] [Clients] [L10] [ROUTINE] Ajouter une suppression en masse des clients sans devis liés (admin). Fait = ClientList.jsx permet à un admin de multi-sélectionner et supprimer ceux dont devis_count===0, avec message FR de protection pour les lignes ignorées.
[x] [Clients] [L2] [ROUTINE] Filtrer la liste clients par type (Particulier/Entreprise). Fait = ClientList.jsx ajoute un filtre segmenté de type qui réduit `filtered`.
[x] [Clients] [L13] [ROUTINE] Signaler les clients B2B sans ICE dans la liste. Fait = ClientList.jsx affiche un badge non bloquant "ICE manquant" sur les lignes Entreprise (ou avec IF/RC) dont ice est vide, miroir de Facture.mentions_manquantes.
[ ] [Clients] [L16] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — colonnes additives) Enregistrer qui a créé/modifié un client. Fait = Client reçoit created_by (nullable) et date_modification (auto_now), perform_create force created_by côté serveur, et ClientSerializer les expose en lecture. Règles permanentes : FR ; FK company forcée serveur ; migration additive uniquement ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [Clients] [L12] [ROUTINE] Rendre la table clients mobile-ergonomique en lignes empilées. Fait = ClientList.jsx porte des attributs data-label (comme DevisList.jsx) pour s'empiler en cartes lisibles sur téléphone.
[ ] [Clients] [L9] [ROUTINE] Ajouter le tri (nom / date de création) à la liste clients. Fait = les en-têtes Client et "Depuis" de ClientList.jsx sont cliquables, via ordering_fields ['nom','date_creation'] déjà déclaré sur ClientViewSet.
[x] [Clients] [L15] [ROUTINE] Normaliser le téléphone client pour l'affichage et la cohérence dedup. Fait = ClientForm.jsx montre la forme marocaine normalisée (réutilisant normalize_phone de crm/services.py) en indice et stocke la valeur saisie.

### Devis (génération & PDF)
[x] [Devis/Générateur] [L7] [ROUTINE] Avertir avant de créer un devis depuis un lead perdu/archivé avec un motif FR clair. Fait = DevisGenerator.jsx validate() avertit (message non bloquant) quand le lead choisi est perdu ou archivé, en le nommant, avant l'enregistrement.
[x] [Devis/Générateur] [L3] [ROUTINE] Pré-remplir le type de structure technique depuis lead.structure_pref. Fait = applyLead dans DevisGenerator.jsx fixe structureType depuis lead.structure_pref (acier/aluminium) quand présent et non touché.
[x] [Devis/Générateur] [L3] [ROUTINE] Pré-remplir CV/HMT/débit de pompe depuis un lead agricole. Fait = applyLead copie lead.pompe_cv/pompe_hmt_m/pompe_debit_m3h dans pompeCv/pompeHmt/pompeDebit pour un lead agricole.
[x] [Devis/Générateur] [L3] [ROUTINE] Pré-remplir l'alimentation agricole par défaut depuis lead.raccordement. Fait = applyLead mappe lead.raccordement (monophase→mono / triphase→tri) sur pompeAlim pour les leads agricoles non touchés.
[x] [Devis/Générateur] [L11] [ROUTINE] Afficher un état de chargement/vide pendant le chargement des leads, clients et produits. Fait = DevisGenerator.jsx affiche "Chargement…" / "Aucun lead — créez-en un d'abord" au lieu de selects vides.
[x] [Devis/Générateur] [L11] [ROUTINE] Surfacer les échecs de fetch leads/clients/produits au lieu de les avaler. Fait = les trois .catch(() => {}) de DevisGenerator.jsx affichent une bannière d'erreur FR.
[ ] [Devis/Générateur] [L8] [ROUTINE] Faire utiliser le modèle TVA par ligne + TTC partagé au modal DevisForm autonome, ou le retirer. Fait = DevisForm.jsx gagne la colonne taux_tva par ligne (parité per-line TVA) ou est retiré du flux d'édition, pour que les deux éditeurs ne divergent plus sur la TVA.
[ ] [Devis/Générateur] [L5] [ROUTINE] Utiliser ProduitPicker dans le select produit du DevisForm hérité. Fait = DevisForm.jsx remplace son <select> produit par le composant ProduitPicker (recherche, prix) utilisé par DevisGenerator.
[ ] [Devis/Générateur] [L7] [ROUTINE] Avertir quand le taux TVA d'une ligne contredit la classification du produit. Fait = DevisGenerator.jsx affiche un indice non bloquant quand une ligne panneau porte 20% ou une ligne non-panneau 10%, via isPanel de solar.js, sans jamais snapper la valeur saisie.
[x] [Devis/Générateur] [L6] [ROUTINE] Afficher une date de validité par défaut et un compte-à-rebours dans le générateur. Fait = DevisGenerator.jsx affiche "expire dans N jours" à côté de dateValidite, calculé depuis quote_validity_days.
[x] [Devis/Générateur] [L1] [ROUTINE] Reporter le choix d'option recommandée et le scénario dans le devis enregistré pour la parité PDF. Fait = DevisGenerator.handleSubmit persiste le scenario/recommendedChoice (ex. dans etude_params) pour que l'option recommandée du PDF corresponde à l'écran au lieu du défaut "Avec batterie" codé dans le builder.
[ ] [Devis/Générateur] [L14] [ROUTINE] Afficher le repère prix/kWc vs la cible société dans le générateur. Fait = DevisGenerator.jsx colore la valeur pkwc en vert/rouge contre prixCible (ou prix_cible_kwc_defaut) avec un libellé FR, à l'écran seulement.
[x] [Devis/Générateur] [L7] [ROUTINE] Empêcher une étude industrielle enregistrée avec consommation nulle. Fait = DevisGenerator.jsx garde etude_params null (pas d'étude dégénérée) quand consoKwhDerivee=0 en mode industriel, errors.conso restant le seul blocage.
[ ] [Devis/Pompage] [L11] [ROUTINE] Rendre incontournable avant l'enregistrement le cas pompage "seules des pompes sans prix conviennent". Fait = DevisGenerator.jsx répète l'avertissement pompageSel.sansPrix près du bouton d'enregistrement.
[ ] [Devis/Pompage] [L4] [ROUTINE] Afficher m³/jour calculé et le résumé champ PV dans la confirmation d'auto-remplissage agricole. Fait = après handleAutoFill en agricole, DevisGenerator.jsx surface pompageSel.m3Jour et dims.champKwc dans une ligne de confirmation.
[x] [Devis/Pompage] [L3] [ROUTINE] Aligner les heures de pompage agricole par défaut entre auto-quote et générateur manuel. Fait = autoQuote.js createAutoQuote lit le réglage société agricole_pump_hours (comme le générateur) au lieu de toujours HEURES_POMPAGE_DEFAUT.
[x] [Devis/Étude] [L4] [ROUTINE] Prévisualiser les métriques d'étude dans le panneau auto-quote avant enregistrement. Fait = la branche industriel d'autoQuote.js montre taux d'autoconsommation, économies et payback (computeEtudeIndustrielle) avant d'enregistrer.
[x] [Devis/Générateur] [L5] [ROUTINE] Permettre de dupliquer un devis non-brouillon en un nouveau brouillon éditable depuis la liste. Fait = DevisList.jsx ajoute "Dupliquer" qui ouvre /ventes/devis/nouveau pré-chargé depuis un devis envoyé/clos sans muter l'original.
[x] [Devis] [L6] [ROUTINE] Ajouter une action explicite "Marquer envoyé" sur les devis brouillon dans la liste. Fait = DevisList.jsx affiche "Envoyer" sur les lignes brouillon qui PATCH statut:'envoye' (via le garde d'approbation de remise existant).
[x] [Devis] [L6] [ROUTINE] Ajouter les actions "Refuser" sur les devis envoyés dans la liste. Fait = DevisList.jsx expose "Refuser" sur les lignes envoye fixant statut:'refuse' avec confirm FR, en complément du bouton Accepter.
[x] [Devis] [L6] [ROUTINE] Distinguer visiblement les devis expirés on-the-fly dans la liste avec un tooltip de date. Fait = le badge effStatut==='expire' de DevisList.jsx porte un title montrant date_expiration.
[x] [Devis] [L9] [ROUTINE] Ajouter un filtre par statut et une recherche référence/client à la liste des devis. Fait = DevisList.jsx gagne un filtre segmenté statut (brouillon/envoyé/accepté/refusé/expiré) et une recherche sur reference et client_nom.
[x] [Devis] [L14] [ROUTINE] Afficher un en-tête de synthèse pipeline devis (nombre + total TTC par statut). Fait = DevisList.jsx rend une bande sommant total_affiche par statut pour les devis chargés.
[x] [Devis] [L10] [ROUTINE] Permettre la génération PDF en lot pour une sélection de devis. Fait = DevisList.jsx permet de multi-sélectionner et déclencher genererPdfDevis pour chacun avec un format partagé choisi dans le modal PDF.
[ ] [Devis] [L16] [ROUTINE] Ajouter un panneau historique des versions montrant la chaîne de révisions d'un devis. Fait = DevisList.jsx (ou un expander) liste les versions antérieures via version_parent/superseded_by_ref.
[ ] [Devis] [L1] [ROUTINE] Lier le badge lead/client de chaque ligne devis à un contexte quote-aware (profil énergétique). Fait = le tooltip du badge lead dans DevisList.jsx montre facture_hiver/type_installation du lead.
[x] [Devis] [L11] [ROUTINE] Remplacer alert(...)/JSON.stringify(err) des actions devis par des erreurs FR inline. Fait = les handlers de DevisList.jsx (handleConvertBC, handleDelete, accepter) rendent les erreurs en bannière inline en français.
[x] [Devis] [L5] [ROUTINE] Remplacer le flux d'acceptation par window.prompt par un formulaire inline. Fait = le bouton Accepter de DevisList.jsx ouvre une petite modale (nom/date/option) au lieu de window.prompt chaînés, postant à accepterDevis.
[x] [Devis/PDF] [L4] [ROUTINE] Ajouter un aperçu PDF in-app en un clic depuis la liste des devis. Fait = DevisList.jsx ouvre le PDF /proposal inline (via previewPdf.js proposalParams/pdfBlob) dans un visualiseur.
[ ] [Devis/PDF] [L11] [ROUTINE] Afficher l'erreur "pas d'onduleur → PDF options refusé" du builder en toast FR clair. Fait = quand generer-pdf/proposal lève le ValueError de builder.build_quote_data, DevisList.jsx surface "Ce devis n'a aucun onduleur — choisissez le format une page".
[x] [Devis/PDF] [L4] [ROUTINE] Présélectionner automatiquement l'option PDF étude pour les devis industriels. Fait = openPdfModal de DevisList.jsx met includeEtude=true par défaut quand mode_installation==='industriel' et etude_params existe.
[x] [Devis/PDF] [L2] [ROUTINE] Désactiver/masquer le toggle PDF étude pour les devis non industriels. Fait = la case includeEtude du modal PDF est masquée/désactivée avec une note FR quand le devis n'a pas d'etude_params.
[x] [Devis/PDF] [L2] [ROUTINE] Désactiver le PDF "premium full" pour les devis agricoles dans le modal de format. Fait = le radio pdf_mode='full' de DevisList.jsx est désactivé avec une note FR pour mode_installation==='agricole', reflétant la dégradation forcée du builder.
[x] [Devis] [L13] [ROUTINE] Surfacer une liste "devis expirant bientôt" sur l'écran des devis. Fait = DevisList.jsx met en avant les devis envoyés dont date_expiration est dans ≤7 jours dans une petite bande de rappel.
[ ] [Devis/Générateur] [L12] [ROUTINE] Rendre le tableau de lignes utilisable au pouce sur mobile. Fait = le tableau de lignes de DevisGenerator.jsx garde ses cellules data-label et ses inputs TVA/qté/prix dimensionnés pour l'édition sur téléphone sans zoom.
[ ] [Devis/Générateur] [L15] [ROUTINE] Avertir avant d'enregistrer des lignes dont la désignation a été éditée hors de son produit. Fait = DevisGenerator.jsx signale les lignes où designation ne correspond plus au produit choisi (la classification pourrait changer la répartition d'options PDF), indice FR non bloquant.
[ ] [Devis/Générateur] [L8] [ROUTINE] Garder les mots-clés de classification de solar.js et builder.py synchronisés via un test partagé. Fait = un test vérifie que les jeux de mots-clés de solar.js (réseau/injection, hybride, batterie, panneau) correspondent aux prédicats _is_* de quote_engine/builder.py.
[x] [Devis] [L14] [ROUTINE] Exposer la répartition des options acceptées (sans/avec batterie) dans un petit insight devis. Fait = DevisList.jsx (ou un en-tête) montre combien de devis acceptés ont choisi avec_batterie vs sans_batterie depuis option_acceptee.
[x] [Devis/Générateur] [L3] [ROUTINE] Reporter taille_souhaitee_kwc du lead dans la suggestion de nombre de panneaux. Fait = applyLead préfère lead.taille_souhaitee_kwc (converti en panneaux au panelW courant) sur l'estimation par facture quand présent et non touché.
[x] [Devis] [L9] [ROUTINE] Ajouter un repère de findability "leads sans devis". Fait = le select lead de DevisGenerator.jsx marque les leads ayant déjà un devis vs aucun.

### Facturation
[ ] [Facturation] [L8] [ROUTINE] Pré-remplir FactureForm depuis le BC sélectionné en recopiant ses lignes (produit/désignation/qté/PU/remise/taux_tva) au lieu d'une seule ligne vide. Fait = choisir un BC dans le select "Bon de commande" de /ventes/factures (nouvelle) injecte les lignes du devis lié dans le tableau, source unique devis→BC→facture.
[ ] [Facturation] [L17] [ROUTINE] Ajouter une colonne "TVA %" par ligne dans FactureForm.jsx (champ LigneFacture.taux_tva, N37) avec sélecteur 10/20 pré-rempli depuis le produit. Fait = chaque ligne du tableau de /ventes/factures porte son taux, transmis dans addLigneFacture/updateLigneFacture, et la ventilation tva_par_taux reflète le 10/20 saisi.
[ ] [Facturation] [L17] [ROUTINE] Exposer date_livraison et conditions_paiement dans FactureForm.jsx (champs modèle N30) pour lever l'avertissement Article 145 depuis l'écran. Fait = deux champs "Date de livraison/prestation" et "Conditions et mode de paiement" sont éditables et envoyés au payload, retirant les mentions correspondantes de mentions_manquantes.
[ ] [Facturation/Paiements] [L7] [ROUTINE] Bloquer le sur-paiement dans enregistrer_paiement (views.py) : refuser un montant qui dépasse montant_du. Fait = POST enregistrer-paiement avec montant > facture.montant_du renvoie 400 "Le paiement dépasse le reste à payer (X MAD)".
[ ] [Facturation/Avoirs] [L7] [ROUTINE] Plafonner un avoir au reste créditable de la facture dans creer_avoir (views.py). Fait = créer un avoir dont le TTC dépasse (facture.total_ttc − avoirs actifs) renvoie 400 "L'avoir dépasse le montant restant de la facture" ; un avoir partiel correct passe inchangé.
[ ] [Facturation/Avoirs] [L5] [ROUTINE] Ajouter un avoir PARTIEL depuis FactureList.jsx (le backend accepte déjà `lignes`) au lieu du seul avoir total via window.prompt. Fait = le bouton "Avoir" ouvre une modale listant les lignes avec qté à créditer, postée à creer-avoir avec `lignes`.
[ ] [Facturation/Paiements] [L9] [ROUTINE] Créer une page Encaissements listant PaiementViewSet (read-only, déjà exposé) avec filtre par mode/date et lien vers la facture. Fait = une route /ventes/paiements affiche tous les paiements de la société (référence facture, montant, date, mode, par qui).
[ ] [Facturation/Paiements] [L14] [ROUTINE] Afficher le total encaissé du mois en en-tête de FactureList.jsx, calculé on-the-fly depuis les paiements. Fait = un bandeau "Encaissé ce mois : X MAD" au-dessus du tableau, dérivé des montant_paye.
[ ] [Facturation] [L6] [ROUTINE] Calculer le statut "en retard" côté backend dans FactureSerializer (champ jours_retard existe) plutôt que de le déduire seulement côté front. Fait = is_overdue et un badge "En retard" s'appuient sur jours_retard du modèle, cohérents entre FactureList, Relances et balance âgée.
[ ] [Facturation] [L5] [ROUTINE] Permettre l'édition inline de la date d'échéance dans FactureList.jsx sans ouvrir le formulaire complet. Fait = un clic sur la cellule Échéance d'une facture émise ouvre un date-picker qui patch date_echeance via patchFacture.
[ ] [Facturation] [L11] [ROUTINE] Remplacer le fallback d'erreur JSON.stringify(error) de FactureList.jsx par un message FR lisible. Fait = un échec de fetchFactures affiche "Impossible de charger les factures. Réessayez.".
[ ] [Facturation] [L11] [ROUTINE] Donner un état de chargement/vide propre au tableau de paiements de la modale. Fait = la modale "Enregistrer un paiement" montre l'historique des paiements encaissés avec un message "Aucun paiement enregistré" si vide.
[ ] [Facturation/Comptable] [L14] [ROUTINE] Exposer l'option trimestre du journal des ventes (period_bounds gère déjà ?quarter=YYYY-Q). Fait = le bouton "Journal comptable" propose mois OU trimestre, et journalVentes envoie quarter quand choisi.
[ ] [Facturation/Comptable] [L14] [ROUTINE] Ajouter une colonne "Type de facture" (acompte/intermédiaire/solde/complète) au journal des ventes export (exports.py). Fait = la feuille "Journal des ventes" du .xlsx contient une colonne Type depuis get_type_facture_display.
[ ] [Facturation/Comptable] [L16] [ROUTINE] Inclure les avoirs (en négatif) dans le journal des ventes export pour réconcilier le CA. Fait = l'export journal-ventes liste les avoirs émis de la période en lignes négatives et le résumé TVA les déduit, borné société.
[ ] [Facturation/Relances] [L6] [ROUTINE] Afficher la balance âgée (buckets 0-30/31-60/61-90/90+) par ligne dans RelancesPage.jsx, dérivée de jours_retard. Fait = chaque impayé montre son bucket d'âge, cohérent avec /reporting/balance-agee.
[ ] [Facturation/Relances] [L2] [ROUTINE] Ajouter un filtre par niveau de relance et un tri par montant dû dans RelancesPage.jsx. Fait = des onglets/sélecteur "Niveau" filtrent les impayés et un clic d'en-tête trie par Dû décroissant.
[ ] [Facturation/Relances] [L13] [ROUTINE] Pré-remplir le niveau et le message de relance proposés dans la modale depuis le niveau courant (FollowupLevel.message). Fait = ouvrir "Relancer" montre le niveau courant et pré-remplit la note avec le message configuré.
[ ] [Facturation/Relances] [L14] [ROUTINE] Afficher un total d'encours impayé en en-tête de RelancesPage.jsx (somme des montant_du). Fait = un bandeau "Total à recouvrer : X MAD sur N factures".
[ ] [Facturation/Relances] [L10] [ROUTINE] Permettre de consigner une relance en lot pour les factures sélectionnées dans RelancesPage.jsx. Fait = des cases à cocher + "Consigner pour la sélection" appellent relancer sur chaque facture cochée.
[ ] [Facturation/Relances] [L16] [ROUTINE] Afficher l'historique des relances consignées (action relances/RelanceLog) dans RelancesPage.jsx. Fait = un clic "historique" sur un impayé liste les relances passées (date, niveau, note, par qui).
[ ] [Facturation/Relances] [L11] [ROUTINE] Distinguer "pas encore échue" de "en retard" (jours_retard=0 vs >0). Fait = les factures dues mais non échues affichent "À échoir" au lieu de "—".
[ ] [Facturation] [L14] [ROUTINE] Exporter la balance âgée en .xlsx (endpoint borné société comme journal_ventes). Fait = un bouton "Exporter" sur /reporting/balance-agee télécharge un classeur avec une ligne par client et les buckets 0-30/31-60/61-90/90+ + total.
[ ] [Facturation] [L4] [ROUTINE] Donner accès au relevé client (releve PDF) depuis RelancesPage.jsx en plus de la balance âgée. Fait = chaque impayé propose "Relevé" ouvrant le PDF de relevé via getClientRelevePdf.
[ ] [Facturation] [L14] [ROUTINE] Ajouter le détail des paiements et avoirs au relevé client (_releve_data dans recouvrement.py n'a que les totaux par facture). Fait = le relevé liste aussi les encaissements (date/mode/montant) et avoirs.
[ ] [Facturation/BonCommande] [L8] [ROUTINE] Afficher les totaux (HT/TVA/TTC) du BC dans BonCommandeList (VentesKanban.jsx), dérivés du devis lié. Fait = chaque bon de commande montre son Total TTC.
[ ] [Facturation/BonCommande] [L7] [ROUTINE] Pré-remplir date_livraison_prevue par défaut (ex. émission + délai) dans le formulaire BonCommande. Fait = un nouveau BC propose une date de livraison prévue par défaut éditable.
[ ] [Facturation/BonCommande] [L11] [ROUTINE] Afficher un message FR clair quand creer-facture échoue parce qu'une facture existe déjà (le backend renvoie ce 400). Fait = cliquer "Créer facture" sur un BC déjà facturé montre "Une facture existe déjà pour ce BC" et un lien vers la facture.
[ ] [Facturation] [L1] [ROUTINE] Afficher la référence du devis d'origine (devis_reference déjà sérialisé) dans la ligne facture de FactureList.jsx. Fait = une facture issue d'un échéancier montre "Devis DEV-…" cliquable vers le devis.
[ ] [Facturation] [L1] [ROUTINE] Afficher le pourcentage et le rang de tranche (1/3, acompte 30 %) pour les factures d'échéancier dans FactureList.jsx (champs pourcentage/type_facture existent). Fait = une facture acompte/solde montre "Acompte 30 % — tranche 1/3".
[ ] [Facturation] [L6] [ROUTINE] Proposer la prochaine action contextuelle sur chaque facture (next-best-action) dans FactureList.jsx. Fait = une brouillon met "Émettre" en avant, une émise échue "Relancer", une partiellement payée "Enregistrer paiement", d'après statut/montant_du/jours_retard.
[ ] [Facturation] [L9] [ROUTINE] Ajouter un onglet/filtre "Partiellement payées" dans FactureList.jsx (montant_paye>0 et montant_du>0). Fait = un nouvel onglet liste les factures à solde partiel, comptées dans le bandeau d'onglets.
[ ] [Facturation] [L9] [ROUTINE] Filtrer FactureList.jsx par type de facture (acompte/intermédiaire/solde/complète). Fait = un sélecteur "Type" restreint la liste, via type_facture.
[ ] [Facturation/Avoirs] [L9] [ROUTINE] Ajouter recherche et filtre par statut dans AvoirsPage.jsx. Fait = un champ de recherche (référence/facture/client) et un filtre Émis/Annulé restreignent la liste.
[ ] [Facturation/Avoirs] [L11] [ROUTINE] Rendre l'empty-state d'AvoirsPage.jsx actionnable et corriger les catch silencieux. Fait = sans avoir, un lien renvoie vers /ventes/factures ; un échec de annulerAvoir/telechargerAvoirPdf affiche un message FR.
[ ] [Facturation] [L15] [ROUTINE] Normaliser l'affichage monétaire de FactureList.jsx via le helper dh() (toLocaleString fr-MA) au lieu de toFixed(2)+"DH". Fait = "Payé / Dû" et "Total TTC" s'affichent en MAD formaté cohérent avec Relances/Avoirs/Balance âgée.
[ ] [Facturation] [L15] [ROUTINE] Afficher la ventilation TVA par taux (tva_par_taux sérialisé) dans les totaux de FactureForm.jsx quand 10/20 coexistent. Fait = le bloc totaux montre "TVA 10 % : X / TVA 20 % : Y", réconcilié au centime.
[ ] [Facturation] [L7] [ROUTINE] Avertir dans FactureForm.jsx quand date_echeance est antérieure à aujourd'hui à la création. Fait = saisir une échéance passée affiche un avertissement non bloquant "Échéance déjà dépassée".
[ ] [Facturation/Relances] [L13] [ROUTINE] Proposer une date de prochaine relance par défaut dans la modale Relancer depuis le délai du niveau suivant. Fait = la modale pré-remplit prochaine_relance à aujourd'hui + délai du niveau suivant.
[ ] [Facturation/Comptable] [L16] [ROUTINE] Afficher statut_teledeclaration (N39) en badge dans FactureList.jsx. Fait = chaque facture émise montre "DGI : Non soumise/Soumise/Validée", en lecture seule.
[ ] [Facturation] [L12] [ROUTINE] Rendre la grille de lignes de FactureForm.jsx utilisable sur mobile (scroll horizontal + actions empilées). Fait = sur écran étroit le tableau de lignes scrolle sans casser la mise en page.
[ ] [Facturation] [L12] [ROUTINE] Empiler la barre d'actions de FactureList.jsx (Émettre/Payée/Avoir/WhatsApp/UBL/PDF) en menu compact sur mobile. Fait = sur écran étroit les actions tiennent sans déborder, regroupées dans un menu "Actions".
[ ] [Facturation] [L3] [ROUTINE] Pré-remplir le taux TVA du formulaire facture à 20 % avec accès rapide 10 % (panneaux PV) via des chips préréglés. Fait = le champ TVA de FactureForm offre des chips "20 %" / "10 %" en plus de la saisie libre.
[ ] [Facturation] [L16] [ROUTINE] Tracer dans un chatter/log la création d'avoir et l'encaissement de paiement (acteur côté serveur). Fait = créer un avoir ou enregistrer un paiement consigne une ligne d'activité (qui, quand, montant) consultable sur la facture, jamais lue du corps de requête.
[ ] [Facturation/Relances] [L13] [GALLERY] Adapter la lettre de relance PDF au niveau courant (ton J+7 vs J+30) en variant le corps depuis FollowupLevel.message. Fait = generate_lettre_relance_pdf rend un texte distinct par niveau, sans changer la mise en page premium.
[ ] [Facturation] [L7] [DECISION] Décider si l'auto-passage en "En retard" des factures échues doit être matérialisé (aujourd'hui seulement calculé à la lecture). Fait = note tranchée — garder le on-the-fly (jours_retard) ou ajouter un job (gaté G9 scheduler), le choix consigné dans PLAN.md.
[ ] [Facturation/Comptable] [L17] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — export comptable) Exporter toutes les factures validées sur une plage de dates choisie par l'utilisateur, avec la TVA par ligne, l'ICE client et les totaux, aux formats Excel ET CSV. Fait = un export comptable scopé société sort, pour les factures validées d'une plage from→to, une feuille .xlsx ET un .csv listant par facture la ventilation TVA par ligne, l'ICE du client et les totaux HT/TVA/TTC (groundwork DGI, pas de transmission). Règles permanentes : FR ; FK company respectée ; additif ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.

### Chantiers / Installations
[ ] [Chantiers] [L6] [ROUTINE] Contraindre le select de statut chantier dans InstallationDetail.jsx aux mouvements avant/adjacents sur Installation.STATUT_ORDER au lieu de tout statut (bloquer ex. Signé→Clôturé). Fait = le dropdown statut n'offre que le statut courant ±1 (et toute valeur héritée déjà stockée), et un saut non-adjacent est empêché côté client avec un indice FR.
[ ] [Chantiers] [L6] [ROUTINE] Garder le drag Kanban (views/KanbanView.jsx) de déposer dans une colonne non adjacente (handleDragEnd accepte tout over.id). Fait = déposer un chantier à plus d'un pas snap back avec un message FR, les mouvements d'un pas persistant via onChangeStatus.
[ ] [Chantiers] [L6] [ROUTINE] Ajouter un badge "en retard"/stale quand date_pose_prevue est passée et date_pose_reelle vide (statut pas encore Installé/Réceptionné). Fait = ListView.jsx et KanbanView.jsx montrent une puce rouge "Pose en retard".
[ ] [Chantiers] [L6] [ROUTINE] Surfacer une "prochaine action" par chantier dérivée de statut + jalon manquant (Signé→commander matériel, Installé→planifier réception). Fait = InstallationDetail.jsx montre une bannière FR next-best-action sous l'en-tête.
[ ] [Chantiers] [L7] [ROUTINE] Valider le formulaire mise en service avant de passer le chantier à "Mise en service" : exiger une date et avertir si test de production / tension vides. Fait = saveMes dans InstallationDetail.jsx bloque (message FR) si date_mise_en_service vide et avertit si mes_production_test/mes_tension manquent.
[ ] [Chantiers] [L11] [ROUTINE] Remplacer le rendu d'erreur brut "Erreur : {JSON.stringify(error)}" dans InstallationsPage.jsx par un état d'erreur FR + bouton "Réessayer". Fait = un fetch chantiers en échec montre un texte FR lisible et un bouton retry.
[ ] [Chantiers] [L11] [ROUTINE] Afficher un retour explicite sur les actions équipement/intervention/ticket/besoin qui avalent les échecs (catch vides) dans InstallationDetail.jsx. Fait = un addEquipement/addIntervention/openTicket/commanderBesoin en échec surface un message FR.
[ ] [Chantiers] [L5] [ROUTINE] Remplacer le flux d'annulation par window.prompt (annuler dans InstallationDetail.jsx) par une modale/textarea inline pour le motif. Fait = annuler un chantier ouvre un formulaire FR in-app pour le motif, posté à l'endpoint annuler.
[ ] [Chantiers] [L10] [ROUTINE] Ajouter le changement de statut en lot et la réassignation d'installateur en lot à la ListView chantiers (cases + toolbar). Fait = sélectionner plusieurs lignes permet de fixer un statut ou un technicien_responsable pour tous, chaque changement journalisé.
[ ] [Chantiers] [L9] [ROUTINE] Étendre la recherche/filtre serveur chantiers à la référence devis et à l'installateur, + un toggle "mes chantiers". Fait = la FilterBar cherche devis_reference, et une pastille "Mes chantiers" filtre où technicien_responsable est l'utilisateur courant.
[ ] [Chantiers] [L3] [ROUTINE] Pré-remplir la date_prevue d'une nouvelle intervention "pose" depuis date_pose_prevue du chantier. Fait = choisir le type "pose" pré-remplit date_prevue avec current.date_pose_prevue.
[ ] [Chantiers] [L5] [ROUTINE] Permettre d'éditer en place une intervention existante (date_realisee / compte_rendu / technicien) — l'endpoint updateIntervention existe mais l'UI est add-only. Fait = chaque ligne du tableau Interventions devient éditable.
[ ] [Chantiers] [L6] [ROUTINE] Auto-tamponner date_realisee quand le compte_rendu est rempli ou l'intervention marquée faite (miroir de _stamp_statut_dates). Fait = enregistrer une intervention avec compte rendu mais sans date_realisee la fixe à aujourd'hui côté serveur.
[ ] [Chantiers] [L13] [ROUTINE] Ajouter une surface de rappel "poses à venir" dans l'en-tête des chantiers comptant les Planifié avec date_pose_prevue ≤7 jours. Fait = InstallationsPage.jsx montre un badge/lien FR des poses à venir, calculé à la lecture.
[ ] [Chantiers] [L14] [ROUTINE] Afficher une synthèse funnel (chantiers par statut + nb en retard) au-dessus des vues. Fait = InstallationsPage.jsx montre une bande de stats FR par statut et le nb en retard.
[ ] [Chantiers] [L16] [ROUTINE] Consigner une note de chatter explicite lors d'une mise en service incluant production/tension mesurées (aujourd'hui seule la date est notée). Fait = l'action mise_en_service journalise une note incluant mes_production_test et mes_tension.
[ ] [Chantiers] [L2] [ROUTINE] Faire lire au sélecteur "type" d'intervention la liste TypeIntervention gérée de la société au lieu du tableau en dur INTERVENTION_TYPES de statuses.js. Fait = le dropdown type liste les TypeIntervention non archivés via getTypesIntervention.
[ ] [Chantiers] [L15] [ROUTINE] Signaler les chantiers sans BOM gelé (bom vide, ex. créés sans devis). Fait = InstallationDetail.jsx montre un indice FR "Nomenclature absente" et le Parc marque ces systèmes.
[ ] [Chantiers] [L7] [ROUTINE] Bloquer/avertir avant d'annuler un chantier déjà Réceptionné/Clôturé et actif au parc (l'annulation le masque du parc). Fait = annuler avertit en FR qu'un système installé quittera le parc et exige confirmation quand est_parc=true.
[ ] [Chantiers] [L6] [ROUTINE] Conditionner les boutons PV de réception/handover jusqu'au jalon atteint (PV seulement une fois statut ≥ Installé et checklist quasi-complète). Fait = les boutons "Documents après-vente" sont désactivés avec un tooltip FR expliquant le prérequis.
[ ] [Chantiers] [L4] [GALLERY] Ajouter un aperçu in-app des PDF client après-vente (PV de réception, bon de livraison, dossier de remise, attestation) avant téléchargement dans InstallationDetail.jsx. Fait = chaque bouton "Documents après-vente" ouvre un aperçu PDF inline au lieu de seulement downloadBlob, sans changer le contenu du PDF généré.

#### Équipement
[ ] [Chantiers/Équipement] [L1+L2] [ROUTINE] Constrain the chantier equipment produit picker to that chantier's devis lines (Installation.devis → LigneDevis.produit) and filter each equipment slot to the correct product category. Fait = on a chantier created from a quote, the panneaux picker lists only that quote's panel products and the onduleur picker lists only inverter products, not the whole catalogue.
[ ] [Chantiers/Équipement] [L2] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — colonne additive) Ajouter un tag additif optionnel type_equipement (ou role) sur stock.Categorie (les catégories free-text gardent leurs noms FR) pour filtrer les slots par TYPE quel que soit le libellé. Fait = un nouveau champ Categorie nullable permet au slot onduleur de ne montrer que les produits typés onduleur et au slot panneaux que les panneaux, même si la société a renommé ses catégories ; les lignes existantes restent non typées et le comportement actuel est inchangé. Règles permanentes : FR ; FK company respectée ; migration additive uniquement ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [Chantiers/Équipement] [L8] [ROUTINE] Réconcilier les Equipement capturés contre le BOM gelé dans InstallationDetail.jsx (attendus BOM vs capturés par produit). Fait = le panneau Équipements liste chaque ligne BOM avec le nombre de séries capturées, en surlignant les composants encore sans unité enregistrée.
[ ] [Chantiers/Équipement] [L3] [ROUTINE] Pré-remplir le champ date_pose de l'équipement avec date_pose_reelle du chantier à l'ajout (miroir du fallback serveur de cocher_checklist). Fait = ouvrir le formulaire "Ajouter l'équipement" pré-remplit date_pose avec current.date_pose_reelle.
[ ] [Chantiers/Équipement] [L1+L2] [ROUTINE] Remplacer le <select> catalogue entier du formulaire "Ajouter un équipement" par le même ProduitPicker (scopé aux produits devis/BOM) que la checklist. Fait = le sélecteur produit d'ajout d'équipement utilise ProduitPicker alimenté des seuls produits du devis du chantier.
[ ] [Chantiers/Équipement] [L7] [ROUTINE] Avertir à l'ajout d'un équipement dont le produit n'a pas garantie_mois/garantie_production_mois (horloge de garantie vide). Fait = le formulaire montre un indice FR "garantie non renseignée" pour le produit choisi quand ses durées sont vides.
[ ] [Chantiers/Équipement] [L15] [ROUTINE] Signaler les numéros de série en doublon dans un chantier à la capture (checklist ou formulaire équipement). Fait = saisir un numero_serie déjà présent sur un autre Equipement du même chantier montre un avertissement FR de doublon avant création.
[ ] [Chantiers/Équipement] [L5] [ROUTINE] Permettre d'éditer/supprimer un Equipement capturé (série, statut En service/Remplacé/Hors service) depuis InstallationDetail.jsx au lieu de add-only. Fait = chaque ligne du tableau Équipements peut être éditée pour corriger une série ou poser son statut.
[ ] [Chantiers/Équipement] [L12] [ROUTINE] Empiler verticalement la ligne de capture série de la checklist (ChantierChecklist.jsx) avec inputs pleine largeur sur écrans étroits. Fait = sur largeur iPhone le ProduitPicker + N° de série s'empilent au lieu de déborder du form-row.
[ ] [Chantiers/Équipement] [L16] [ROUTINE] Consigner au chatter quels séries/produits ont été capturés sur une étape de checklist (cocher_checklist ne journalise qu'un compte "+N équipement(s)"). Fait = la note d'historique liste le produit et la série capturés.

#### Checklist
[ ] [Chantiers/Checklist] [L7] [ROUTINE] Avertir (non bloquant) avant de passer un chantier à Réceptionné si la complétion de checklist est < 100 %. Fait = poser le statut Réceptionné demande une confirmation FR quand checklist_completion < 100, l'override restant possible.
[ ] [Chantiers/Checklist] [L11] [ROUTINE] Améliorer l'état vide de la checklist quand aucune étape modèle n'existe en lien direct vers Paramètres → Chantiers. Fait = le message "Aucune étape" devient un lien/bouton FR ouvrant les réglages d'étapes.
[ ] [Chantiers/Checklist] [L16] [ROUTINE] Montrer qui a complété chaque étape ET quand (fait_le est retourné mais seul fait_par_nom est rendu). Fait = chaque étape faite affiche "par X le <date>" via item.fait_le formaté en FR.
[ ] [Chantiers/Checklist] [L12] [ROUTINE] Agrandir les cases et zones tactiles de la checklist pour usage terrain/gants sur mobile. Fait = les lignes de checklist utilisent des cases/labels tactiles plus grandes en largeur iPhone sans changer le desktop.

#### Photos
[ ] [Chantiers/Photos] [L4] [ROUTINE] Ajouter un lightbox/zoom au tap d'une vignette dans ChantierPhotos.jsx au lieu d'ouvrir l'URL brute dans un nouvel onglet. Fait = taper une image ouvre un visualiseur plein écran in-app avec préc/suiv dans la phase.
[ ] [Chantiers/Photos] [L12] [ROUTINE] Utiliser l'attribut capture caméra sur l'input d'upload photo pour la prise directe sur site. Fait = les inputs fichier de ChantierPhotos.jsx ajoutent capture="environment" pour ouvrir la caméra sur téléphone, la sélection desktop restant possible.
[ ] [Chantiers/Photos] [L11] [ROUTINE] Ajouter un retour de progression/erreur et un garde taille/type basique à ChantierPhotos.jsx (catch silencieux). Fait = un upload en échec ou trop volumineux montre un message FR et le bouton "＋" reflète la progression.
[ ] [Chantiers/Photos] [L5] [ROUTINE] Permettre de déplacer une pièce jointe entre phases (avant/pendant/après) sans supprimer + ré-uploader. Fait = chaque vignette propose un sélecteur de phase qui re-tague la pièce via l'endpoint records.

#### Planning
[ ] [Chantiers/Planning] [L4] [ROUTINE] Permettre le drag-to-reschedule sur la vue Calendrier des chantiers (CalendarView) pour mettre à jour date_pose_prevue. Fait = glisser une puce à un autre jour met à jour date_pose_prevue via updateInstallation et journalise le changement.
[ ] [Chantiers/Planning] [L14] [ROUTINE] Ajouter un indicateur de charge installateur (chantiers/poses par installateur sur la période). Fait = la vue Calendrier/Kanban montre un compte de poses à venir par installateur.
[ ] [Chantiers/Planning] [L11] [ROUTINE] Afficher un état vide clair sur la vue Calendrier quand aucun chantier du mois n'a de date_pose_prevue. Fait = CalendarView rend un indice FR "Aucune pose planifiée ce mois".

#### Parc installé
[ ] [Parc] [L4] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — Leaflet) Rendre la vue "Carte" du Parc installé en vraie carte tuilée interactive des systèmes géolocalisés au lieu d'un tableau de liens GPS, via **Leaflet** (pré-approuvé, voir bloc PRÉ-APPROUVÉ + N85). Fait = l'onglet Carte de ParcInstallePage.jsx montre des marqueurs pour chaque système localisé ; cliquer un marqueur ouvre le détail. Règles permanentes : FR ; FK company respectée ; additif ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [Parc] [L9] [ROUTINE] Ajouter un filtre régime loi 82-21 / statut de dossier à la barre de filtres du Parc installé (la donnée existe par système). Fait = ParcInstallePage.jsx gagne des selects régime + statut dossier pour isoler les systèmes en attente de déclaration/approbation.
[ ] [Parc] [L14] [ROUTINE] Ajouter une bande de synthèse Parc (total kWc installé, comptes par type d'installation, par tranche de puissance). Fait = ParcInstallePage.jsx montre des stats FR agrégées calculées depuis les lignes chargées.
[ ] [Parc] [L9] [ROUTINE] Rendre les colonnes du Parc triables (puissance, année, ville, client). Fait = cliquer un en-tête trie la table des systèmes installés, par défaut année d'installation la plus récente.
[ ] [Parc] [L6] [ROUTINE] Montrer un indicateur d'état de garantie par système dans la liste Parc (composant hors/proche fin de garantie). Fait = chaque ligne Parc montre un badge garantie FR dérivé du garantie_etat des équipements.
[ ] [Parc] [L11] [ROUTINE] Ajouter un skeleton de chargement et un état d'erreur au reload paginé de ParcInstallePage.jsx. Fait = un fetch en échec montre une erreur FR + retry, les pages partielles montrent la progression.
[ ] [Parc] [L8] [ROUTINE] Utiliser le filtre statut canonique pour que les systèmes legacy (mise_en_service) apparaissent dans le Parc et que l'année utilise date_mise_en_service à défaut de date_reception. Fait = les systèmes stockés en mise_en_service apparaissent dans le Parc avec une année correcte, conforme à l'inclusion ?parc=1 de MISE_EN_SERVICE.

#### Lifecycle / data quality / traçabilité
[ ] [Chantiers] [L15] [ROUTINE] Détecter et signaler un chantier dont le devis lié a été modifié depuis sa création (BOM/puissance gelés divergents). Fait = InstallationDetail.jsx montre un indice FR "le devis a changé depuis la création du chantier" quand les lignes diffèrent du bom.
[ ] [Chantiers] [L16] [ROUTINE] Journaliser les éditions/suppressions d'intervention au chatter (la création est journalisée mais pas updateIntervention/deleteIntervention). Fait = éditer/supprimer une intervention écrit une InstallationActivity via activity.log_note.
[ ] [Chantiers] [L17] [ROUTINE] Ajouter les dates clés du dossier réglementaire (dossier_date_depot, dossier_date_approbation) à la section Dossier réglementaire de InstallationDetail.jsx (champs modèle non exposés). Fait = la section permet de poser/effacer ces dates, qui apparaissent ensuite sur la timeline.
[ ] [Chantiers] [L6] [ROUTINE] Ajouter les jalons du dossier réglementaire (à déposer/déposé/approuvé/compteur posé) à ChantierTimeline.jsx. Fait = la timeline montre les jalons dépôt/approbation à côté des jalons de pose quand renseignés.
[ ] [Chantiers] [L5] [ROUTINE] Ajouter une action en un clic "Marquer réceptionné" qui pose statut Réceptionné, tamponne date_reception et le note. Fait = un bouton FR avance le chantier à Réceptionné avec sa date tamponnée et une note de chatter, à la forme de l'action mise en service.

### Parc & Équipements (registre SAV)
[ ] [Équipements] [L6] [ROUTINE] Ajouter une puce KPI/compteur "Expirant bientôt" dans l'en-tête d'EquipementsPage qui compte garantie_etat==='expire_bientot' et sert de filtre rapide. Fait = une puce près de "{n} équipement(s)" montre le compte expirant et le clic pose filters.garantie='expire_bientot'.
[ ] [Équipements] [L1] [ROUTINE] Surfacer le nombre de tickets SAV ouverts par équipement dans EquipementDetail (savApi.getTickets({equipement: eq.id})). Fait = la modale détail montre "N tickets SAV liés" avec le compte ouvert.
[ ] [Équipements] [L5] [ROUTINE] Rendre le filtre d'état garantie inline : cliquer le GarantieBadge d'une ligne pose filters.garantie à cet état. Fait = cliquer un badge de ligne filtre la table à cet état de garantie sans ouvrir la modale.
[ ] [Équipements] [L6] [ROUTINE] Ajouter un bouton "Créer un ticket SAV" dans EquipementDetail pré-rempli avec cet équipement + son installation + client. Fait = le bouton ouvre la création de ticket avec equipement, installation, client préremplis.
[ ] [Équipements] [L2] [ROUTINE] Ajouter un filtre statut (en_service/remplacé/hors_service) à la barre de filtres d'EquipementsPage — le backend supporte déjà ?statut= et filterEquipements lit f.statut. Fait = un select "Statut" filtre les lignes.
[ ] [Équipements] [L8] [ROUTINE] Rendre produit/installation corrigeables dans EquipementDetail via une action explicite "Corriger" (aujourd'hui readOnly, jamais re-postables). Fait = une édition gardée permet à un admin de corriger un produit/chantier erroné et recompute_garanties re-tourne à l'enregistrement.
[ ] [Équipements] [L9] [ROUTINE] Surfacer le lien remplace_par_ticket dans EquipementDetail quand statut='remplace'. Fait = un équipement remplacé montre "Remplacé via ticket SAV-XXXX" cliquable.
[ ] [Équipements] [L11] [ROUTINE] Distinguer "pas encore de parc" de "aucun match pour les filtres" dans EquipementsPage. Fait = avec filtres actifs l'empty-state lit "Aucun équipement ne correspond aux filtres." avec un lien Réinitialiser.
[ ] [Équipements] [L14] [ROUTINE] Ajouter une barre de synthèse d'état de garantie au-dessus de la table (sous_garantie / expire_bientot / hors_garantie / non_renseignée). Fait = quatre comptes rendus depuis items, alignés sur la légende GARANTIE_ETATS.
[ ] [Équipements] [L16] [ROUTINE] Montrer created_by/date_creation et date_modification en bas d'EquipementDetail. Fait = la modale montre "Ajouté le {date} · modifié le {date}".
[ ] [Équipements] [L17] [ROUTINE] Rendre la date de fin de garantie production en badge distinct labellisé (pas seulement un indice inline). Fait = date_fin_garantie_production s'affiche "Garantie production : {date FR}".
[ ] [Équipements] [L12] [ROUTINE] Rendre la table du parc mobile-usable en cartes empilées sous le breakpoint m-hide existant. Fait = sur écran étroit chaque équipement rend une carte empilée avec série/produit/garantie visibles.
[ ] [Équipements] [L9] [ROUTINE] Ajouter une option de tri sur date_pose à l'en-tête de liste (ordering_fields backend inclut déjà date_pose). Fait = un en-tête "Posé le" bascule sortEquipements('date_pose').
[ ] [Équipements] [L15] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — unicité série par société) Ajouter un garde/index d'unicité scopé société sur Equipement.numero_serie (nullable, vides permis) pour stopper la duplication silencieuse de séries. Fait = créer deux équipements à numero_serie non vide identique dans une société est rejeté avec un message FR clair. Additif uniquement — **si une contrainte d'unicité entrerait en collision avec des lignes existantes, la session est autorisée à sauter la contrainte DB et à imposer l'unicité au niveau du serializer.** Règles permanentes : FR ; FK company respectée ; additif ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.

### SAV
[ ] [SAV] [L4] [ROUTINE] Ajouter une vue Kanban (par statut, ordre funnel via statusOrder) à TicketsPage à côté de la table, sur le patron leads/chantiers. Fait = un view switcher rend les cartes ticket en colonnes Nouveau→Clôturé via TICKET_STATUS_COLORS.
[ ] [SAV] [L6] [ROUTINE] Imposer des changements de statut avant uniquement par STATUT_ORDER (bloquer le saut Nouveau→Clôturé sans En cours, ou avertir). Fait = le dropdown statut désactive/signale les sauts hors ordre avec un indice FR.
[ ] [SAV] [L3] [ROUTINE] Auto-tamponner date_resolution à aujourd'hui quand le statut passe à resolu/cloture et qu'elle est vide (TicketDetail.save). Fait = passer un ticket à Résolu sans date pré-remplit la date du jour avant submit.
[ ] [SAV] [L6] [ROUTINE] Ajouter un badge SLA/âge sur les tickets ouverts calculé à la lecture depuis date_ouverture (sans scheduler), surlignant les ouverts > N jours, surtout priorité haute/urgente. Fait = la liste montre "ouvert depuis X j" avec une escalade de couleur.
[ ] [SAV] [L1] [ROUTINE] Montrer le compte-à-rebours de garantie de l'équipement lié dans l'en-tête de TicketDetail (equipement_fin_garantie sérialisé) pas seulement l'indicateur oui/non. Fait = avec un équipement lié, le détail montre "Garantie jusqu'au {date} ({n} j restants)".
[ ] [SAV] [L7] [ROUTINE] Exiger un motif_annulation (ou confirmer le vide) au lieu d'accepter un blanc silencieux dans le window.prompt d'annulation. Fait = annuler avec motif vide demande confirmation FR avant de poser annule.
[ ] [SAV] [L5] [ROUTINE] Remplacer le window.prompt d'annulation/réactivation par un champ motif in-modal. Fait = "Annuler le ticket" ouvre un input motif inline dans la modale.
[ ] [SAV] [L11] [ROUTINE] Remplacer les erreurs JSON.stringify(err) de TicketDetail/EquipementDetail par des messages FR mappés au champ. Fait = un enregistrement en échec montre "Échec : {champ} — {raison}".
[ ] [SAV] [L11] [ROUTINE] Surfacer les échecs silencieux : les nombreux .catch(() => {}) de TicketDetail (interventions, pièces, notes, historique). Fait = une action pièce/intervention/note en échec affiche une brève erreur FR.
[ ] [SAV] [L2] [ROUTINE] Ajouter un contrôle de filtre annule à TicketsPage (le backend supporte ?annule=only|sans). Fait = un filtre permet de voir "Annulés seulement".
[ ] [SAV] [L9] [ROUTINE] Ajouter un tri/filtre rapide "urgent & sous garantie". Fait = une puce filtre en un clic priorite in (haute,urgente) et sous_garantie_effectif='oui'.
[ ] [SAV] [L14] [ROUTINE] Montrer les comptes de tickets par statut en colonnes/puces au-dessus de la table. Fait = une rangée de comptes (Nouveau/Planifié/En cours/Résolu/Clôturé) rendue depuis items, ordre funnel respecté.
[ ] [SAV] [L1] [ROUTINE] À la liaison d'un équipement dans TicketDetail, fixer l'affichage du champ manuel sous_garantie à la valeur calculée et le griser. Fait = sélectionner un équipement montre la garantie calculée dans le champ désactivé, pas la valeur manuelle obsolète.
[ ] [SAV] [L5] [ROUTINE] Permettre l'assignation du technicien_responsable même quand /users/ renvoie vide (endpoint admin), via les noms de techniciens déjà sur les tickets. Fait = le dropdown technicien est alimenté pour les non-admins depuis les assignés existants.
[ ] [SAV] [L7] [ROUTINE] Garder la quantité de pièce de dépasser le quantite_stock disponible quand "Décrémenter le stock" est coché. Fait = ajouter une pièce avec décrément et qté > stock surface un avertissement FR "stock insuffisant" avant post.
[ ] [SAV] [L16] [ROUTINE] Journaliser l'ajout/retrait de pièce et le décrément de stock en TicketActivity. Fait = consommer/retirer une pièce ajoute automatiquement "Pièce {produit} ×{qté} consommée (stock −)" à l'Historique.
[ ] [SAV] [L1] [ROUTINE] Pré-remplir le client d'un nouveau ticket depuis l'équipement/installation lié quand le ticket est créé depuis le parc ou un chantier. Fait = un ticket ouvert depuis un équipement porte client+installation+equipement sans sélection manuelle.
[ ] [SAV] [L16] [ROUTINE] Ajouter un lien depuis TicketDetail vers le chantier lié (installation_reference) et le détail équipement. Fait = les références chantier/équipement du détail naviguent vers leurs pages.
[ ] [SAV] [L12] [ROUTINE] Rendre la très large TicketDetail (modal-xl, beaucoup de sections) repliable par section pour usage terrain/mobile. Fait = sur écrans étroits les sections Interventions/Pièces/Historique sont des accordéons repliables.
[ ] [SAV] [L11] [ROUTINE] Remplacer le bare alert('Rapport indisponible.') à l'échec de PDF par le style d'erreur in-modal standard. Fait = un rapportPdf en échec montre le message form-error-box au lieu d'un alert navigateur.
[ ] [SAV] [L9] [ROUTINE] Ajouter un état vide (correctif/préventif) distinguant "aucun ticket ouvert" de "aucun match". Fait = filtrer vers un set vide montre "Aucun ticket ne correspond aux filtres" avec Réinitialiser.
[ ] [SAV] [L3] [ROUTINE] Pré-remplir la date_prevue de l'ajout d'intervention dans TicketDetail à aujourd'hui quand vide. Fait = le formulaire inline d'intervention pré-remplit date_prevue à aujourd'hui, éditable.
[ ] [SAV] [L10] [ROUTINE] Ajouter l'assignation technicien en lot / le changement de statut en lot à la liste TicketsPage. Fait = sélectionner plusieurs tickets et choisir un technicien/statut PATCH chacun via updateTicket.

### Maintenance
[ ] [Maintenance] [L5] [ROUTINE] Ajouter des contrôles édition/désactivation par ligne dans ContratsMaintenance (aujourd'hui création + PDF seulement ; saveContrat(id,...) et actif existent mais inutilisés). Fait = une ligne permet de basculer actif et d'éditer périodicité/prix inline via PATCH.
[ ] [Maintenance] [L3] [ROUTINE] Exposer prix, installation et duree_mois dans le formulaire de création de contrat (seuls client/périodicité/début/renouvellement sont saisis bien que tous sérialisés). Fait = le formulaire poste prix, installation optionnelle et duree_mois.
[ ] [Maintenance] [L6] [ROUTINE] Montrer un filtre/vue rapide "À renouveler" via le flag renouvellement_du déjà sérialisé. Fait = un toggle filtre les contrats où renouvellement_du===true, à côté de "À venir (dus)".
[ ] [Maintenance] [L14] [ROUTINE] Afficher l'équivalent revenu récurrent par ligne (mensuel/annuel) avec la même maths que l'insight recurring_revenue. Fait = chaque ligne montre "≈ {x} DH/mois" dérivé de prix et periodicite.
[ ] [Maintenance] [L13] [ROUTINE] Badger les contrats dont prochaine_visite est dans ~90 jours (à la lecture, comme T7) comme "Visite proche" distinct de déjà dû. Fait = un contrat bientôt dû mais pas encore dû montre un badge "Visite proche".
[ ] [Maintenance] [L17] [ROUTINE] Localiser la valeur de périodicité affichée dans la table (elle rend la clé brute c.periodicite, ex. "annuel") en libellé FR. Fait = la colonne Périodicité montre "Annuel/Mensuel/…" via la map PERIODES.
[ ] [Maintenance] [L7] [ROUTINE] Valider le formulaire de création avec erreurs FR claires au lieu d'un no-op silencieux quand client/date_debut manquent. Fait = cliquer "+ Ajouter" sans champs montre "Client et date de début requis.".
[ ] [Maintenance] [L1] [ROUTINE] Lier les tickets de visite générés par contrat : montrer combien de tickets préventif existent pour le client/installation du contrat. Fait = une ligne montre "N visite(s) générée(s)".
[ ] [Maintenance] [L6] [ROUTINE] Après "Générer les visites dues", refléter la derniere_visite/prochaine visite avancée sans race de reload et confirmer le compte inline. Fait = la génération met à jour en place la "Prochaine visite" de chaque ligne affectée et montre le compte généré.
[ ] [Maintenance] [L11] [ROUTINE] Donner à ContratsMaintenance un état de chargement et une vraie distinction vide-vs-erreur (aujourd'hui seul "Aucun contrat" avec erreurs avalées). Fait = la page montre "Chargement…" puis des lignes, "Aucun contrat", ou une erreur de chargement FR.
[ ] [Maintenance] [L16] [ROUTINE] Montrer date_creation et notes du contrat en ligne/détail (sérialisés, non affichés). Fait = un contrat expose ses notes et date de création dans un détail/colonne.
[ ] [Maintenance] [L4] [ROUTINE] Permettre de choisir la date du rapport PDF de maintenance en contexte avant téléchargement (l'endpoint accepte déjà ?date=). Fait = l'action "Rapport PDF" laisse choisir la date de visite, par défaut derniere_visite.

### Stock & Achats

#### Catalogue — liste & form
[x] [Stock/Catalogue] [L7] [ROUTINE] Bloquer le SKU en doublon dès la saisie dans ProduitForm.jsx avant submit (l'unicité ('company','sku') ne remonte qu'en errors.submit brut). Fait = saisir un SKU déjà utilisé affiche un message FR inline sous le champ SKU ("SKU déjà utilisé par <nom>") sans soumettre.
[x] [Stock/Catalogue] [L11] [ROUTINE] Remplacer le JSON.stringify(err) de errors.submit (ProduitForm.handleSubmit) par un message FR lisible. Fait = une erreur serveur affiche une phrase française, jamais {"sku":["..."]} brut.
[x] [Stock/Catalogue] [L3] [ROUTINE] Pré-remplir la TVA par défaut à 20 % pour un nouveau produit (aujourd'hui tva:'' = "Sans TVA" par défaut). Fait = ouvrir "Nouveau produit" affiche TVA 20 % ; l'édition garde la valeur existante.
[x] [Stock/Catalogue] [L7] [ROUTINE] Avertir dans ProduitForm quand prix_vente < prix_achat (marge négative, indicateur générateur interne). Fait = une marge négative affiche un avertissement FR non bloquant sous le prix de vente.
[x] [Stock/Catalogue] [L6] [ROUTINE] Afficher un indicateur de marge (prix_vente − prix_achat, %) dans ProduitForm en mode édition. Fait = la marge en DH et % s'affiche, jamais exportée ni client-facing.
[x] [Stock/Catalogue] [L9] [ROUTINE] Ajouter un filtre "Sans prix (à renseigner)" dans le rail de StockList pour isoler les produits où sansPrix(p) (11 pompes OSP). Fait = un bouton liste uniquement les produits sans prix de vente ; le compteur reflète leur nombre.
[x] [Stock/Catalogue] [L14] [ROUTINE] Afficher un total de valeur de vente du catalogue filtré (somme prix_vente×quantite) en pied de StockList. Fait = un bandeau montre "Valeur vente du catalogue affiché : X DH HT" recalculé au filtre/recherche.
[x] [Stock/Catalogue] [L15] [ROUTINE] Signaler les produits sans SKU dans StockList. Fait = un produit sans sku porte un badge "SKU manquant" et un filtre rail "Sans SKU" les regroupe.
[x] [Stock/Catalogue] [L5] [ROUTINE] Rendre le seuil d'alerte éditable en place (InlineEdit) dans CatalogueRow comme le prix et la quantité. Fait = cliquer le seuil dans la liste permet de le modifier via PATCH sans ouvrir le formulaire.
[x] [Stock/Catalogue] [L11] [ROUTINE] Distinguer l'état "vide réel" de "vide après filtre" dans l'empty-state de StockList quand actifs.length===0. Fait = catalogue totalement vide affiche un encart d'amorçage, distinct du "Aucun résultat pour …".

#### Catégories & marques
[x] [Stock/Catalogue] [L9] [ROUTINE] Ajouter une recherche/filtre marque au rail de StockList (les marques structurent l'affichage via groupCatalogue mais ne sont pas filtrables seules). Fait = on peut filtrer le catalogue par marque depuis le rail.
[ ] [Stock/Catalogue] [L8] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — UI gestion catégories) Offrir un écran Paramètres → Stock de gestion des catégories (renommer/ordre/archiver) — CategorieViewSet expose update/destroy mais aucune UI dédiée. Fait = un panneau Paramètres liste les catégories avec édition du nom et de ordre, conforme au free-text par société. Règles permanentes : FR ; FK company respectée ; additif ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [Stock/Catalogue] [L11] [ROUTINE] Afficher un message FR clair quand la suppression d'une marque est refusée (409 "utilisée par des produits"). Fait = tenter de supprimer une marque en usage montre "archivez-la plutôt".

#### Fournisseurs
[ ] [Stock/Achats] [L8] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — écran Fournisseurs) Créer un écran de gestion des fournisseurs (liste/édition coordonnées) — FournisseurViewSet expose list/update/destroy + contact/email/telephone/adresse mais aucune UI ne les édite. Fait = une page liste les fournisseurs et permet d'éditer personne de contact, email, téléphone, adresse. Règles permanentes : FR ; FK company respectée ; additif ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [Stock/Achats] [L7] [ROUTINE] Valider le format email du fournisseur à la création inline (le champ email existe). Fait = un email invalide saisi affiche une erreur FR avant enregistrement.
[ ] [Stock/Achats] [L14] [ROUTINE] Afficher sur un fournisseur le nombre de produits liés et de BCF associés (lecture seule). Fait = la fiche fournisseur montre "X produits · Y bons de commande".

#### Mouvements & low-stock
[ ] [Stock/Mouvements] [L9] [ROUTINE] Ajouter l'onglet "Transferts" à MouvementsPage (TYPE_META.transfert existe mais aucun onglet ne le filtre, et les TransfertStock ne sont pas des MouvementStock). Fait = un onglet liste les transferts entre emplacements avec source→destination.
[x] [Stock/Mouvements] [L4] [ROUTINE] Afficher un avertissement de stock négatif dans le preview de MouvementForm pour le cas ajustement<0 (previewApres calcule déjà la valeur négative). Fait = une saisie menant à un stock négatif est signalée avant submit pour tous les types.
[x] [Stock/Mouvements] [L16] [ROUTINE] Permettre de filtrer les mouvements par produit depuis CatalogueRow (lien "historique") vers MouvementsPage pré-filtré. Fait = cliquer "historique" sur un produit ouvre les mouvements filtrés sur ce produit.
[x] [Stock/Mouvements] [L3] [ROUTINE] Pré-sélectionner le produit dans MouvementForm si la page est ouverte depuis un produit donné. Fait = ouvrir le formulaire depuis un produit pré-remplit le select Produit.
[ ] [Stock/Mouvements] [L6] [ROUTINE] Ajouter un bouton "réapprovisionner" (next-best-action) sur les produits en stock bas de StockList ouvrant un BCF brouillon pré-rempli. Fait = un produit sous le seuil propose un BCF pré-rempli (produit + fournisseur catalogue/min) en un clic.
[x] [Stock/Catalogue] [L6] [ROUTINE] Afficher une suggestion de quantité à commander sur les produits en stock bas (ex. seuil − stock, ou seuil×2). Fait = le badge "stock bas" affiche un hint "commander ~N" dérivé de seuil_alerte et quantite_stock.

#### Multi-emplacements & transferts
[ ] [Stock/Emplacements] [L4] [ROUTINE] Afficher la ventilation par emplacement sur CatalogueRow (ou au survol) sans ouvrir le modal Transfert — l'endpoint produit/<id>/emplacements existe. Fait = un produit montre sa répartition dépôt/camionnette en lecture dans la liste.
[ ] [Stock/Emplacements] [L7] [ROUTINE] Pré-remplir et plafonner la quantité du transfert à la quantité disponible à la source dans TransfertModal (validateTransfert vérifie mais l'input n'a pas de max). Fait = le champ quantité du transfert porte max = stock source et affiche la dispo.
[ ] [Stock/Emplacements] [L11] [ROUTINE] Afficher un message FR explicite quand la suppression d'un emplacement est refusée (409 "détient du stock — transférez-le"). Fait = l'erreur de suppression s'affiche en clair dans le modal.
[ ] [Stock/Emplacements] [L9] [ROUTINE] Ajouter un filtre/colonne par emplacement à la valorisation et au catalogue pour voir le stock d'un emplacement précis. Fait = on peut afficher uniquement le stock d'un emplacement choisi (ex. Camionnette).

#### Inventaire
[ ] [Stock/Mouvements] [L4] [ROUTINE] Afficher l'écart (compté − stock) en direct par ligne dans InventaireModal pendant la saisie. Fait = chaque ligne montre un delta coloré (+/−) dès qu'une quantité comptée est tapée.
[ ] [Stock/Mouvements] [L9] [ROUTINE] Filtrer/limiter les produits de l'InventaireModal par recherche (aujourd'hui il prend `filtered` sans recherche interne sur grand catalogue). Fait = un champ de recherche dans le modal réduit la liste des lignes à compter.
[x] [Stock/Mouvements] [L11] [ROUTINE] Remplacer l'alert() de fin d'inventaire par un récapitulatif intégré (ajustés/inchangés) dans StockList. Fait = après inventaire, un bandeau FR résume le résultat sans popup navigateur.

#### Bons de commande fournisseur
[x] [Stock/Achats] [L11] [ROUTINE] Remplacer tous les JSON.stringify(err.response?.data) de BcfDetail (save/envoyer/recevoir/annuler) par des messages FR lisibles. Fait = une erreur de BCF affiche une phrase française.
[ ] [Stock/Achats] [L3] [ROUTINE] Pré-remplir le prix d'achat unitaire d'une ligne BCF depuis le PrixFournisseur du fournisseur choisi (ou prix_achat catalogue) à l'ajout de ligne. Fait = sélectionner un produit pour un fournisseur connu pré-remplit le prix d'achat U. interne.
[ ] [Stock/Achats] [L2] [ROUTINE] Rendre une ligne BCF sans prix d'achat possible mais signalée (BCF interne ≠ devis, les pompes sans prix de vente doivent rester commandables). Fait = un produit sans prix de vente est sélectionnable dans un BCF et n'est pas grisé "prix à renseigner" dans ce contexte d'achat.
[x] [Stock/Achats] [L6] [ROUTINE] Afficher l'avancement de réception sur la liste et le détail BCF (reçu/commandé, %). Fait = chaque BCF envoyé montre "X/Y reçus" et une barre dérivée de quantite_recue/quantite.
[x] [Stock/Achats] [L4] [ROUTINE] Ajouter un bouton "Tout recevoir" dans BcfDetail qui pré-remplit chaque saisie de réception au reste dû. Fait = un clic remplit toutes les colonnes "À recevoir" avec quantite_restante.
[x] [Stock/Achats] [L9] [ROUTINE] Afficher la date de création/d'envoi des BCF dans la liste (la colonne Date montre date_commande souvent vide). Fait = la liste BCF affiche une date pertinente (commande sinon création) jamais "—" quand un BCF existe.
[x] [Stock/Achats] [L13] [ROUTINE] Ajouter un compteur/onglet de BCF "envoyés non reçus" en attente de réception. Fait = la page BCF montre le nombre de commandes envoyées en attente, filtrable en un clic.
[ ] [Stock/Achats] [L1] [ROUTINE] Afficher la référence du chantier source sur un BCF créé depuis un besoin matériel (la note contient "chantier <ref>" mais non mise en avant). Fait = un BCF issu d'un chantier affiche un lien/étiquette vers le chantier d'origine.
[x] [Stock/Achats] [L7] [ROUTINE] Confirmer avant d'envoyer un BCF dont une ligne a un prix d'achat à 0 (cas pompes/placeholder). Fait = envoyer un BCF avec ligne(s) à prix 0 demande une confirmation FR.

#### Listes de prix fournisseur (multi-fournisseur)
[x] [Stock/Achats] [L6] [ROUTINE] Afficher l'écart de prix entre fournisseurs dans PrixFournisseursSection (% vs le moins cher ⭐). Fait = chaque ligne hors moins-cher montre "+X % vs le moins cher".
[x] [Stock/Achats] [L7] [ROUTINE] Empêcher dans PrixFournisseursSection d'ajouter un prix pour un fournisseur déjà listé (unicité ('produit','fournisseur')). Fait = tenter un doublon affiche un message FR, jamais une erreur brute.
[x] [Stock/Achats] [L5] [ROUTINE] Rendre le prix d'achat d'un PrixFournisseur éditable en place (aujourd'hui supprimer + recréer seulement). Fait = un prix fournisseur existant peut être modifié sans le supprimer.
[x] [Stock/Achats] [L16] [ROUTINE] Afficher la date du dernier achat formatée FR dans PrixFournisseursSection (aujourd'hui ISO brut ou "—"). Fait = la date du dernier achat s'affiche au format JJ/MM/AAAA.

#### Valorisation
[x] [Stock/Achats] [L11] [ROUTINE] Gérer l'état vide de ValorisationModal (stock à zéro → tables vides sans message). Fait = sans stock valorisable, le modal affiche "Aucun stock à valoriser".
[x] [Stock/Achats] [L14] [ROUTINE] Permettre d'exporter la valorisation en Excel (données internes admin) depuis ValorisationModal. Fait = un bouton télécharge un .xlsx des lignes de valorisation (jamais client-facing).
[x] [Stock/Achats] [L16] [ROUTINE] Indiquer la source du coût moyen par ligne de valorisation (réceptions BCF vs prix_achat catalogue de repli). Fait = chaque ligne signale si le coût vient d'achats reçus ou du prix catalogue.

#### Retours fournisseur
[x] [Stock/Achats] [L7] [ROUTINE] Plafonner la quantité retournée à la quantité reçue par ligne dans RetourModal (aujourd'hui input libre, le serveur décrémente sans plafond). Fait = le champ "À retourner" a max = quantite_recue et avertit si dépassé.
[ ] [Stock/Achats] [L9] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — liste Retours) Exposer une page/liste des retours fournisseur (RetourFournisseurViewSet existe mais aucune liste consultable). Fait = un écran liste les retours (référence RF, fournisseur, statut, date) avec consultation. Règles permanentes : FR ; FK company respectée ; additif ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[x] [Stock/Achats] [L11] [ROUTINE] Remplacer l'alert() de confirmation de retour par un retour intégré et afficher les erreurs FR (le catch fait JSON.stringify). Fait = valider un retour montre une confirmation/erreur FR sans popup ni JSON brut.

#### OCR import
[ ] [Stock/OCR] [L7] [ROUTINE] Bloquer/avertir avant d'appliquer un import OCR si plusieurs lignes "create" partagent le même SKU détecté (collision d'unicité). Fait = deux lignes au même SKU sont signalées avant application.
[ ] [Stock/OCR] [L3] [ROUTINE] Pré-remplir le prix de vente suggéré d'un produit créé via OCR (aujourd'hui nouveau_prix_vente vide → erreur "Prix de vente requis"). Fait = une ligne "create" propose un prix de vente par défaut (ex. = prix achat OCR) modifiable.
[ ] [Stock/OCR] [L11] [ROUTINE] Améliorer le journal de résultats OCR (Step3) : grouper succès/échecs et permettre de relancer uniquement les lignes en échec. Fait = l'écran résultats sépare réussis/échoués et propose de réessayer les échecs.
[ ] [Stock/OCR] [L1] [ROUTINE] Proposer de créer un BCF (entrée d'achat) à partir d'une facture/bon de livraison OCR plutôt que des mouvements isolés. Fait = pour un doc d'achat, une option crée un BCF reçu au lieu d'ENTRÉE directes.
[ ] [Stock/OCR] [L16] [ROUTINE] Renseigner automatiquement la référence document OCR comme reference du mouvement quand l'utilisateur ne l'a pas saisie. Fait = un mouvement OCR sans référence saisie reprend reference_document détecté.

#### Chatbot stock (agent IA / SQL)
[ ] [Stock] [L9] [ROUTINE] Ajouter des suggestions orientées stock à AgentChat (SUGGESTIONS a "en rupture de stock" mais pas valorisation/réappro). Fait = au moins deux suggestions stock supplémentaires (ex. "Valeur totale du stock ?", "Quels produits sous le seuil d'alerte ?").
[ ] [Stock] [L11] [ROUTINE] Afficher un message FR clair dans AgentChat quand la clé GROQ_API_KEY est absente (feature key-gated). Fait = sans clé, le chatbot affiche "Assistant indisponible (configuration manquante)".
[ ] [Stock] [L17] [ROUTINE] Garantir que les réponses du chatbot stock n'exposent jamais le prix d'achat/marge (le SQL agent peut interroger Produit.prix_achat). Fait = un test/garde confirme que prix_achat n'est pas restitué dans les réponses client-facing de l'agent.

#### Mobile & cohérence
[ ] [Stock] [L12] [ROUTINE] Rendre les tableaux larges (BCF lines-table, valorisation, inventaire) défilables/empilables sur mobile (~380px). Fait = sur viewport étroit, les tables stock défilent horizontalement ou s'empilent sans casser la mise en page.
[ ] [Stock] [L12] [ROUTINE] Adapter la barre d'actions de StockList (header-actions : 7+ boutons) en menu compact sur mobile. Fait = sur écran étroit, les actions stock se regroupent (menu "…").
[ ] [Stock] [L8] [ROUTINE] Aligner les mots-clés de classification de catalogue.js (Panneaux/Onduleurs/Variateurs/Batteries/Pompes/Câbles) avec quote_engine/builder.py et solar.js. Fait = un test vérifie la cohérence des familles entre l'affichage catalogue et la classification PDF/auto-fill.

### Paramètres & Référentiels
[ ] [Paramètres/Audit] [L16] [ROUTINE] Surfacer le journal d'audit des réglages existant (GET /parametres/audit/, N55) dans l'UI : ajouter getAudit() à parametresApi.js et un panneau "Journal des modifications" en lecture dans l'onglet Avancé (field_label/old→new/user/timestamp). Fait = un admin voit dans Paramètres → Avancé les derniers changements SettingsAuditLog, filtrables par section.
[ ] [Paramètres/Relances] [L5] [ROUTINE] Rendre le champ FollowupLevel.message en textarea dans la carte "Niveaux de relance" (déjà sauvé par saveNiveaux mais sans input). Fait = chaque niveau a une textarea message dont la valeur round-trip à l'enregistrement/rechargement.
[ ] [Paramètres/Relances] [L10] [ROUTINE] Permettre d'ajouter/supprimer des FollowupLevel depuis la carte Niveaux de relance (aujourd'hui seules les lignes existantes sont éditables). Fait = un bouton "+ Ajouter un niveau" crée un FollowupLevel et un contrôle de suppression en retire un, persistés via ventesApi.
[ ] [Paramètres/Relances] [L11] [ROUTINE] Remplacer le texte "Aucun niveau configuré." par une action de seed-defaults quand la société a zéro FollowupLevel. Fait = la carte vide propose "Créer les niveaux par défaut (J+7 / J+15 / J+30)".
[ ] [Paramètres/TVA] [L7] [ROUTINE] Garder tva_standard/tva_panneaux d'une coercition vide/zéro silencieuse à 20/10 à l'enregistrement (handleSave fait Number(...) || 20). Fait = vider un champ TVA et enregistrer montre une validation au lieu de re-snap au défaut, et un 0 délibéré est préservé.
[ ] [Paramètres/Numérotation] [L4] [ROUTINE] Afficher l'aperçu "prochain numéro" depuis la séquence réelle la plus haute de la société plutôt que toujours "0001" dans numberingPreview(). Fait = l'aperçu de numérotation reflète le vrai prochain numéro par type pour la période courante.
[ ] [Paramètres/Société] [L17] [ROUTINE] Marquer l'ICE comme requis pour la facturation dans l'onglet Société et avertir si vide. Fait = enregistrer un profil avec ICE vide montre une bannière légale non bloquante référençant les factures.
[ ] [Paramètres/Société] [L17] [ROUTINE] Masquer le SIRET français et le "N° TVA intracommunautaire" (inutiles au Maroc) derrière une section repliée "Champs hérités (France)". Fait = la carte Informations légales montre les IDs marocains par défaut et les champs français seulement dépliés.
[ ] [Paramètres/Société] [L15] [ROUTINE] Valider les formats ICE/IF/RC/Patente/CNSS côté client (longueurs de chiffres depuis les placeholders) sans rejeter la saisie. Fait = un ICE malformé montre un indice inline mais permet l'enregistrement.
[ ] [Paramètres/Messages] [L4] [ROUTINE] Ajouter un aperçu rendu de chaque template WhatsApp avec substitution d'exemples ({civilite}/{nom}/{reference}/{lien}) à côté des textareas FR/Darija. Fait = éditer un template montre un "Aperçu" en direct avec des valeurs d'exemple.
[ ] [Paramètres/Messages] [L7] [ROUTINE] Valider que les templates WhatsApp n'utilisent que les placeholders whitelistés par clé (_MESSAGE_PLACEHOLDERS) et signaler les inconnus comme {foo}. Fait = enregistrer un template avec un placeholder non supporté renvoie une erreur FR nommant le token fautif.
[ ] [Paramètres/Messages] [L11] [ROUTINE] Ajouter une action "Réinitialiser au modèle par défaut" par template WhatsApp (default_fr déjà retourné). Fait = un bouton par template restaure corps_fr à MESSAGE_TEMPLATE_DEFAULTS et efface l'override.
[ ] [Paramètres/Référentiels] [L11] [ROUTINE] Ajouter des états vides/chargement aux cartes Canaux, Étiquettes, Motifs, Types d'intervention et Marques. Fait = chaque carte montre un spinner pendant le fetch et une ligne "Aucun élément" si vide.
[ ] [Paramètres/Référentiels] [L10] [ROUTINE] Exposer le flag archived sur LeadTag/MotifPerte/Canal comme toggle "Archiver" au lieu du seul delete (N57 préserve l'historique via archived mais l'écran n'offre que ✕). Fait = chaque ligne offre Archiver/Réactiver, les éléments archivés étant grisés mais conservés.
[ ] [Paramètres/Référentiels/Motifs] [L7] [ROUTINE] Bloquer la suppression d'un MotifPerte utilisé par des leads (Lead.motif_perte) comme Canal le fait déjà, proposant l'archive. Fait = supprimer un motif en usage renvoie 409 FR et l'UI désactive le ✕.
[ ] [Paramètres/Référentiels/Tags] [L7] [ROUTINE] Bloquer la suppression d'un LeadTag référencé dans un Lead.tags et suggérer l'archive. Fait = supprimer une étiquette en usage renvoie un 409 gardé et la ligne montre un compteur d'usage.
[ ] [Paramètres/Référentiels/Tags] [L17] [ROUTINE] Exposer le champ LeadTag.couleur comme color-picker dans la carte Étiquettes (sérialisé mais sans input). Fait = chaque étiquette a un sélecteur de couleur persistant, reflété partout où les tags s'affichent.
[ ] [Paramètres/Référentiels/Canaux] [L5] [ROUTINE] Afficher le compte de leads en_usage à côté de chaque Canal en badge (serializer renvoie en_usage, UI ne l'utilise que pour désactiver delete). Fait = chaque canal affiche "N lead(s)".
[ ] [Paramètres/Référentiels/Canaux] [L7] [ROUTINE] Surfacer l'état protégé sur l'input libellé d'un Canal protégé (cle déjà gardé côté serveur). Fait = le libellé du canal protégé "Site web" est en lecture seule avec un indice "protégé (webhook site)".
[ ] [Paramètres/Checklist] [L5] [ROUTINE] Permettre de réordonner les étapes de checklist (ChecklistEtapeModele.ordre existe mais aucune UI ne le pose) via contrôles haut/bas. Fait = un admin déplace une étape haut/bas et le nouvel ordre persiste.
[ ] [Paramètres/Checklist] [L8] [ROUTINE] Exposer ChecklistEtapeModele.capture_serie comme toggle éditable (aujourd'hui seulement affiché en badge "série"). Fait = un admin marque/démarque une étape comme capture-série, persisté, affectant les nouveaux chantiers.
[ ] [Paramètres/Numérotation] [L8] [ROUTINE] Ajouter un aperçu de prochain numéro pour avoir et bon_commande aussi, depuis les mêmes clés DEFAULT_NUMBERING. Fait = chacun des 4 types de pièce montre son propre aperçu cohérent avec references.py.
[ ] [Paramètres/ROI] [L14] [ROUTINE] Ajouter une note d'impact inline sous onee_tarif_kwh/productible montrant comment une économie/payback d'exemple bouge à l'édition. Fait = la carte Hypothèses ROI montre "≈ X MAD/an économisés pour 1 kWc" recalculé en direct.
[ ] [Paramètres/Commission] [L7] [ROUTINE] Exiger commission_valeur quand commission_mode ≠ 'off' (aujourd'hui null possible avec un mode actif). Fait = sélectionner pct_devis/par_kwc avec valeur vide montre une erreur FR et bloque l'enregistrement.
[ ] [Paramètres] [L12] [ROUTINE] Faire passer les grilles multi-colonnes (pe-grid-2 / pe-grid-3 / pe-grid-relance) en une colonne sur mobile (seul param-grid a la media query). Fait = sur ~380px chaque carte Paramètres s'empile en une colonne sans scroll horizontal.
[ ] [Paramètres] [L9] [ROUTINE] Ajouter une recherche/filtre de réglage qui saute à l'onglet/carte contenant un libellé correspondant. Fait = taper "TVA" ou "ICE" surligne/scrolle vers le champ correspondant entre onglets.
[ ] [Paramètres/Clients] [L11] [ROUTINE] Remplacer le texte placeholder "apparaîtront ici" de l'onglet Clients par la vraie gestion des champs personnalisés client. Fait = l'onglet Clients gère les champs personnalisés client au lieu d'un "à venir".

### Rôles & RBAC
[ ] [RBAC] [L8] [ROUTINE] Synchroniser PERMISSION_GROUPS de RolesManagement.jsx avec ALL_PERMISSIONS backend — il manque installation_voir/installation_gerer, intervention_gerer, equipement_voir/equipement_gerer, sav_voir/sav_gerer. Fait = l'éditeur de rôle liste chaque code de roles/models.ALL_PERMISSIONS groupé par module.
[ ] [RBAC] [L8] [ROUTINE] Alimenter le catalogue de permissions de l'éditeur depuis l'endpoint /permissions-disponibles existant plutôt que le tableau en dur PERMISSION_GROUPS. Fait = RolesManagement fetch les codes au chargement et rend les libellés de tout code renvoyé.
[ ] [RBAC] [L11] [ROUTINE] Montrer le compte de permissions du rôle édité et un indicateur "modifié", et désactiver le champ nom pour les rôles est_systeme. Fait = éditer un rôle Système montre un nom en lecture seule avec un indice, les permissions restant éditables.
[ ] [RBAC] [L7] [ROUTINE] Garder contre l'enregistrement d'un rôle à zéro permission ou nom dupliqué avec un message FR clair dans le formulaire. Fait = soumettre un rôle vide ou nom dupliqué montre une erreur inline, pas un alert brut.
[ ] [RBAC] [L14] [ROUTINE] Afficher les utilisateurs assignés à chaque rôle (users_count déjà retourné) en survol/expand. Fait = cliquer le compte "Utilisateurs" d'un rôle révèle les noms le portant.
[ ] [RBAC] [L6] [ROUTINE] Quand la suppression d'un rôle est bloquée car des utilisateurs y sont assignés (perform_destroy lève PermissionDenied), proposer de réassigner ces utilisateurs dans le même dialogue. Fait = l'erreur de suppression de rôle lie vers la réassignation des N utilisateurs affectés.
[ ] [RBAC] [L16] [ROUTINE] Journaliser create/update/delete de rôle à l'audit des réglages (SettingsAuditLog section='roles'). Fait = changer les permissions d'un rôle écrit une ligne SettingsAuditLog visible dans le journal.
[ ] [RBAC] [L2] [ROUTINE] Dans UsersManagement, filtrer le dropdown de rôle aux rôles de la société courante et étiqueter Système vs Personnalisé. Fait = le sélecteur de rôle ne montre que les rôles scopés société avec un tag Système/Personnalisé.
[ ] [RBAC] [L7] [ROUTINE] Empêcher d'assigner un utilisateur à aucun rôle en édition quand c'est le propriétaire protégé, et surfacer is_protected dans la modale (openEdit permet role=''). Fait = éditer le propriétaire protégé garde un rôle admin et montre un avis "Propriétaire protégé".
[ ] [RBAC] [L11] [ROUTINE] Ajouter états vides/erreur et un indicateur de chargement à la table RolesManagement, cohérent avec UsersManagement. Fait = la liste des rôles montre un spinner et un retry à l'échec de fetch.
[ ] [RBAC] [L9] [ROUTINE] Ajouter un préréglage "Lecture seule" sélectionnant uniquement les codes *_voir. Fait = un bouton "Lecture seule" coche chaque permission de vue et décoche le reste.
[ ] [RBAC] [L12] [ROUTINE] Rendre la grille de permissions et la table users utilisables sur mobile (toutes deux en tables min-width/grilles fixes). Fait = sur ~380px les cases de permission wrap et la table users scrolle proprement.

### Champs personnalisés
[ ] [ChampsPerso] [L8] [ROUTINE] Câbler validate_custom_data dans les serializers produit (stock) et client — aujourd'hui appelé seulement pour Lead, donc les champs perso produit/client sont définis mais jamais validés/persistés. Fait = créer/éditer un Produit ou Client persiste son custom_data via le même chemin de validation que Lead.
[ ] [ChampsPerso] [L5] [ROUTINE] Permettre d'éditer un CustomFieldDef existant dans la carte Paramètres (libelle/type/options/obligatoire) — l'UI ne supporte qu'ajout et suppression. Fait = cliquer un champ défini ouvre un éditeur inline qui PATCH le CustomFieldDef.
[ ] [ChampsPerso] [L6] [ROUTINE] Exposer le flag CustomFieldDef.actif comme toggle de désactivation (le modèle supporte le soft-disable mais l'UI hard-delete seulement). Fait = un champ peut être désactivé/réactivé sans perdre custom_data, conforme au "rien n'est perdu".
[ ] [ChampsPerso] [L1] [ROUTINE] Exposer obligatoire à la création/édition d'un champ perso (modèle + serializer le supportent, addCf ne le pose pas). Fait = la ligne "+ Ajouter un champ" a une case "Obligatoire" et les champs requis rendent avec * et s'imposent à l'enregistrement.
[ ] [ChampsPerso] [L2] [ROUTINE] Surfacer les champs perso marqués visible_liste dans les listes/filtres du module — le champ existe sur CustomFieldDef mais n'est consommé nulle part. Fait = un champ marqué visible_liste apparaît en colonne/filtre sur la liste du module, et un toggle visible_liste est ajouté à l'éditeur.
[ ] [ChampsPerso] [L3] [ROUTINE] Laisser un admin poser CustomFieldDef.ordre (et réordonner) pour que CustomFieldsInput rende les champs dans l'ordre choisi. Fait = réordonner les champs en Paramètres change leur ordre dans le formulaire lead/client/produit.
[ ] [ChampsPerso] [L7] [ROUTINE] Protéger contre le renommage de CustomFieldDef.code (la clé JSON) après l'existence de données — n'autoriser que les changements de libelle. Fait = l'éditeur garde code en lecture seule dès qu'un enregistrement porte custom_data pour lui.
[ ] [ChampsPerso] [L15] [ROUTINE] Valider le type 'date' dans validate_custom_data (il passe sans contrôle de format, contrairement à number/choice/boolean). Fait = une valeur de date non-ISO pour un champ date est rejetée avec "Date invalide".
[ ] [ChampsPerso] [L7] [ROUTINE] Valider que les champs de type choice exigent des options non vides à la définition (addCf peut créer un choice sans options, select inutilisable). Fait = enregistrer un champ choice sans options montre une erreur FR et bloque la création.
[ ] [ChampsPerso] [L11] [ROUTINE] Ajouter un état vide et un compte par module à la carte Champs personnalisés. Fait = changer de module sans champ montre "Aucun champ pour ce module" et le compte de champs définis.
[ ] [ChampsPerso] [L16] [ROUTINE] Journaliser create/delete de définition de champ perso à SettingsAuditLog (section='champs'). Fait = ajouter ou supprimer un champ perso écrit une ligne d'audit visible dans le journal.

### Reporting & Tableaux de bord
[ ] [Reporting] [L11] [ROUTINE] Ajouter des états de chargement + erreur au Dashboard.jsx (les cartes KPI rendent des zéros silencieux pendant le fetch, et échouent en silence vers des tableaux vides). Fait = Dashboard montre un placeholder "Chargement…" et un message clair si un des 4 thunks rejette.
[ ] [Reporting] [L11] [ROUTINE] Remplacer les .catch(() => {}) silencieux des cartes de Rapports.jsx par un texte d'erreur FR par carte. Fait = une carte dont l'endpoint échoue montre "Rapport indisponible" (et job-costing/commissions "réservé admin"/"désactivé").
[ ] [Reporting] [L11] [ROUTINE] Ajouter un indicateur de chargement par carte dans Rapports.jsx (sales/stock/service/insights restent null jusqu'à résolution). Fait = chaque carte montre "Chargement…" jusqu'à résolution de son fetch.
[ ] [Reporting] [L14] [ROUTINE] Ajouter le bouton "Exporter Excel" manquant à la carte "Coût de revient par chantier" (job_costing) dans Rapports.jsx — chaque autre carte exporte sauf celle-ci, alors que insights.job_costing sert déjà ?export=xlsx (interne). Fait = la carte job-costing a un bouton Exporter Excel (admin, fichier interne).
[ ] [Reporting] [L14] [ROUTINE] Surfacer l'export .xlsx en lot sur Reporting.jsx (CA mensuel, top produits, créances) — aucune export aujourd'hui. Fait = la table créances-clients et le bloc KPI de /reporting exportent en .xlsx scopé société.
[ ] [Reporting] [L9] [ROUTINE] Rendre les cartes KPI de Dashboard/Reporting cliquables vers les enregistrements ("Devis acceptés", "Factures en retard", "Clients" sont des comptes sans issue). Fait = cliquer le KPI "Factures en retard" ouvre la liste factures pré-filtrée en_retard.
[ ] [Reporting] [L12] [ROUTINE] Envelopper les tables multi-colonnes de Rapports.jsx (par responsable, par catégorie, par technicien) dans overflowX:auto. Fait = chaque table scrolle horizontalement dans sa carte sur 375px.
[ ] [Reporting] [L11] [ROUTINE] Remplacer le "if (!p) return null" qui fait disparaître la section PipelineSection de Reporting.jsx en cas d'erreur par un avis FR visible. Fait = un échec de fetch pipeline montre "Valeur du pipeline indisponible".
[ ] [Reporting] [L14] [ROUTINE] Corriger le bucketing de mois glissant dans reporting/views.py _ca_mensuel (il recule de i*30 jours, ce qui dérive et peut sauter/dupliquer un mois). Fait = la série CA 12 mois montre exactement 12 mois calendaires distincts finissant ce mois.
[ ] [Reporting] [L6] [ROUTINE] Refléter l'expiry de devis on-the-fly (ventes.utils.expiry.is_expired) dans le funnel ventes de Rapports et les graphiques de statut devis de Reporting pour qu'un bucket "Expiré" apparaisse. Fait = les devis en attente expirés sont comptés sous "Expiré" dans le rapport ventes et la part statut Reporting.
[ ] [Reporting] [L14] [ROUTINE] Ajouter un filtre de période (mois/trimestre/dates) aux rapports sales/stock/service (reports.py + Rapports.jsx) — tout est all-time aujourd'hui. Fait = le rapport ventes accepte ?from=&to= et l'UI expose un sélecteur de période, l'export .xlsx l'honorant.
[ ] [Reporting] [L11] [ROUTINE] Montrer un état vide explicite sur CalendarPage.jsx quand un mois n'a aucun évènement. Fait = un mois sans évènement montre "Aucun évènement ce mois-ci" sous la grille.
[ ] [Reporting] [L13] [ROUTINE] Marquer visuellement les évènements en retard/passés sur CalendarPage.jsx (poses passées, activités en retard) par comparaison de date à la lecture. Fait = les évènements de date < aujourd'hui rendent en style atténué/rouge, sans scheduler.
[ ] [Reporting] [L2] [ROUTINE] Corriger le filtrage par responsable du CalendarPage : calendar.py saute les visites_maintenance calculées dès qu'assignee_id est posé, masquant en silence toutes les visites de maintenance. Fait = filtrer par responsable garde les visites de maintenance visibles (sans assignee) ou l'UI indique qu'elles ne sont pas filtrées.
[ ] [Reporting] [L8] [ROUTINE] Réconcilier les deux dashboards qui se chevauchent (Reporting.jsx /reporting/dashboard vs Rapports.jsx pipeline + insights) pour que créances/CA ne soient pas calculés de deux façons. Fait = Reporting et Rapports montrent le même total créances-clients pour les mêmes données (agrégation partagée).

### Recherche & Notifications
[ ] [Recherche] [L11] [ROUTINE] Distinguer une recherche en échec de zéro résultat dans GlobalSearch.jsx (le .catch() pose groups:[] donc une erreur réseau ressemble à "Aucun résultat"). Fait = un /reporting/search en échec montre "Recherche indisponible, réessayez" tandis qu'un vrai vide garde "Aucun résultat pour « … »".
[ ] [Recherche] [L9] [ROUTINE] Ajouter la navigation clavier (↑/↓ pour déplacer, Entrée pour ouvrir, Échap pour fermer) au dropdown de GlobalSearch (souris seulement aujourd'hui). Fait = flécher les résultats groupés surligne un item et Entrée l'ouvre sans souris.
[ ] [Recherche] [L9] [ROUTINE] Ajouter une mémoire des recherches récentes à GlobalSearch (montrer les derniers résultats ouverts quand la boîte est focalisée vide). Fait = focaliser la boîte vide liste les choix récents (localStorage), effacés à la déconnexion.
[ ] [Recherche] [L9] [ROUTINE] Étendre search.py global_search aux objets omis aujourd'hui — bons de commande, contrats de maintenance, dossiers réglementaires, parc installé — listés par N83. Fait = chercher une référence BC ou un client de contrat de maintenance renvoie un résultat groupé.
[ ] [Recherche] [L11] [ROUTINE] Ajouter un "voir tout" par groupe dans GlobalSearch quand un type est plafonné à PER=6 résultats. Fait = un groupe à plus de 6 matches montre "+N autres" liant vers la liste du type filtrée par la requête.
[ ] [Recherche] [L12] [ROUTINE] Rendre la boîte GlobalSearch utilisable sur mobile (entre le titre et le bloc user dans Header.jsx, max-width 460px, sans règle small-screen). Fait = sur 375px l'input/dropdown est atteignable et pleine largeur (ex. une icône qui se déplie).
[ ] [Notifications] [L11] [ROUTINE] Surfacer les échecs de fetch dans NotificationBell.jsx (le .catch(() => setData(null)) fait passer une erreur pour "Rien à signaler 🎉"). Fait = un /reporting/notifications en échec montre "Notifications indisponibles".
[ ] [Notifications] [L13] [ROUTINE] Ajouter les renouvellements de contrats de maintenance expirants et les visites dues à la cloche (search.py couvre activités en retard, garanties ≤90j, impayés mais pas les signaux maintenance). Fait = la cloche compte contrats à renouveler ≤90j et visites dues, calculés à la lecture, lignes cliquables.
[ ] [Notifications] [L11] [ROUTINE] Ajouter un état de chargement à NotificationBell (panneau vide jusqu'au premier fetch). Fait = ouvrir la cloche avant l'arrivée des données montre "Chargement…".
[ ] [Notifications] [L13] [ROUTINE] Grouper/trier les notifications par urgence dans la cloche (le plus en retard d'abord par catégorie) via les dates déjà retournées. Fait = dans "Factures impayées" et "Activités en retard" les items sont triés du plus en retard.
[ ] [Recherche] [L17] [ROUTINE] Remplacer la map PAGE_TITLES périmée/anglaise de Header.jsx ("Dashboard", "Reporting & Analytics", et aucune entrée pour /rapports, /calendrier, /chantiers, /parc, /sav, /stock/bons-commande-fournisseur). Fait = chaque route montre un titre FR correct ("Tableau de bord", "Rapports", "Calendrier") au lieu du fallback "ERP Agentique".

### WhatsApp & Liens publics
[ ] [WhatsApp] [L3] [ROUTINE] Exposer le choix de langue FR/Darija dans l'UI d'envoi WhatsApp (LeadForm devis send, FactureList, RelancesPage codent tous langue:'fr' alors que backend + MessageTemplate supportent 'darija'). Fait = un petit toggle FR/Darija sur "Envoyer par WhatsApp" passe la langue choisie, défaut FR.
[ ] [WhatsApp] [L4] [ROUTINE] Montrer un aperçu du message WhatsApp avant d'ouvrir wa.me (le backend renvoie déjà message et links). Fait = cliquer "Envoyer par WhatsApp" montre le message rendu FR/Darija + le(s) lien(s) public(s) dans une étape de confirmation, puis ouvre wa.me.
[ ] [WhatsApp] [L7] [ROUTINE] Valider le téléphone côté front avant d'appeler l'endpoint WhatsApp pour un message clair immédiat plutôt qu'un 400 aller-retour. Fait = un lead/facture à téléphone non normalisable désactive le bouton avec un tooltip FR "Numéro invalide" miroir de normalize_ma_phone.
[ ] [Liens publics] [L6] [ROUTINE] Gérer le cas lien expiré côté devis de public_views.py plus utilement (un ShareLink de 30 jours renvoie un "Lien expiré ou introuvable" nu). Fait = la page publique d'un devis/facture expiré montre un avis FR clair invitant à demander un lien frais (aucune donnée interne).
[ ] [Liens publics] [L7] [ROUTINE] (UNGATED 2026-06-18, pré-approuvé — noindex + throttle) Sur les PDF ShareLink publics, ajouter un en-tête no-index et un rate-limiting par token (aujourd'hui public_views.py sert le PDF client à quiconque a le token, sans throttle). Fait = chaque PDF public porte X-Robots-Tag: noindex + un throttle par IP/token basique, sans toucher au rendu et sans jamais exposer de prix d'achat. Règles permanentes : FR ; additif ; aucun prix d'achat client-facing ; STAGES.py/funnel intouchés ; DEBUG/déploiement/sécurité inchangés.
[ ] [WhatsApp] [L16] [ROUTINE] Journaliser l'action d'envoi WhatsApp au chatter de l'enregistrement (lead Historique / facture activity) — l'envoi n'écrit rien aujourd'hui. Fait = construire un lien WhatsApp devis/facture ajoute "Lien WhatsApp généré pour <réf> par <user>" au chatter, côté serveur.
[ ] [WhatsApp] [L11] [ROUTINE] Donner aux boutons WhatsApp une affordance busy/disabled claire sur les listes factures (FactureList montre "..." mais le bouton lead n'a pas de spinner). Fait = chaque "Envoyer par WhatsApp" montre un libellé busy FR pendant la construction du lien.

### Documents & Archives
[ ] [Documents] [L11] [ROUTINE] Remplacer les alert('Document indisponible.')/alert('Relevé indisponible.') de DocumentsArchive.jsx et BalanceAgeePage.jsx par une UI d'erreur FR inline. Fait = un fetch PDF/relevé en échec montre un message inline dans la ligne/carte.
[ ] [Documents] [L12] [ROUTINE] Envelopper la table d'archive (DocumentsArchive.jsx) et la table balance-âgée 7 colonnes (BalanceAgeePage.jsx) dans overflowX:auto. Fait = les deux tables scrollent horizontalement dans leur carte sur 375px.
[ ] [Documents] [L9] [ROUTINE] Ajouter le filtrage par type/date à l'archive documents (archive.py renvoie tout à plat sans filtre). Fait = la vue archive filtre par type de document et trie par date, le compte reflétant le filtre.
[ ] [Documents] [L1] [ROUTINE] Donner au bon de commande client son téléchargement dans l'archive — archive.py émet download_url:None pour les bons de commande, donnant une affordance "Ouvrir le PDF" morte. Fait = les lignes bon-de-commande lient vers un document fonctionnel ou montrent "Pas de PDF".
[ ] [Documents] [L14] [ROUTINE] Ajouter un "Tout exporter (.xlsx)" par client/chantier à l'archive listant les documents et références. Fait = la vue archive offre un .xlsx de sa liste (type/référence/date), scopé société, sans prix d'achat.
[ ] [Documents] [L4] [ROUTINE] Réutiliser le renderer PDF.js de PdfCanvas.jsx pour prévisualiser les PDF d'archive/après-vente inline au lieu d'ouvrir un nouvel onglet. Fait = cliquer un document rend dans un canvas PDF.js same-origin (comme l'aperçu devis) avec fallback téléchargement, jamais une icône cassée.
[ ] [Documents] [L11] [ROUTINE] Gérer les images de pièces jointes cassées/manquantes dans AttachmentsPanel.jsx (un objet MinIO supprimé donne un glyphe d'image cassée). Fait = une pièce dont le fetch 404 montre une tuile FR "Aperçu indisponible".
[ ] [Documents] [L15] [ROUTINE] Montrer les contraintes d'upload en amont dans AttachmentsPanel.jsx (le backend cap à 10 Mo et PDF/PNG/JPEG/WebP, mais le panneau ne le surface qu'après rejet). Fait = le contrôle d'ajout indique "PDF/PNG/JPEG/WebP — 10 Mo max" avant l'upload et valide côté client.
[ ] [Documents] [L11] [ROUTINE] Ajouter la progression d'upload à AttachmentsPanel.jsx pour les gros fichiers (seul un label "Envoi…" existe). Fait = uploader un fichier de plusieurs Mo montre un indicateur de progression.
[ ] [Documents] [L11] [ROUTINE] Montrer un retour par erreur clair dans ExcelImport.jsx quand un fichier parse mais ne mappe aucune colonne (commit désactivé sans explication). Fait = un fichier dont les en-têtes ne mappent rien montre "Aucune colonne reconnue — vérifiez les en-têtes".
[ ] [Documents] [L15] [ROUTINE] Reporter quelles lignes ont été ignorées et pourquoi dans le résumé de commit d'import (services.commit renvoie une liste skipped avec ligne+raison, mais ExcelImport.jsx ne montre que le compte). Fait = le résultat liste les numéros de ligne ignorés et raisons (doublon/vide/nom manquant).
[ ] [Documents] [L12] [ROUTINE] Rendre la table d'aperçu dry-run 10 lignes d'ExcelImport.jsx mobile-usable (12px, maxWidth:640, scroll horizontal seulement). Fait = l'aperçu est lisible et scrollable sur 375px sans rognage.

### Transversal (mobile, états, traçabilité)
[ ] [Transversal] [L11] [ROUTINE] Établir un patron partagé états vide/chargement/erreur et l'appliquer où les pages échouent en silence (Dashboard, Rapports, BalanceAgeePage font tous .catch(() => {})). Fait = un helper "état" FR réutilisable est utilisé par ces pages pour qu'aucune liste ne rende vide en silence à un échec.
[ ] [Transversal] [L16] [ROUTINE] Ajouter des liens profonds object_ref au Journal d'activité unifié (insights.audit_log renvoie object_ref en simple chaîne). Fait = chaque ligne du Journal lie vers le lead/devis/chantier/ticket concerné.
[ ] [Transversal] [L16] [ROUTINE] Rendre le détail du champ modifié cohérent dans audit_log entre SettingsAuditLog et les modèles de chatter (le formatage diverge entre _activity_summary et la branche SettingsAuditLog inline). Fait = changements de réglages et d'enregistrements rendent un format "champ : ancien → nouveau" identique dans le Journal.
[ ] [Transversal] [L9] [ROUTINE] Ajouter des contrôles de filtre à l'UI du Journal d'activité (l'endpoint audit_log accepte déjà ?user, ?type, ?since mais la carte Rapports ne les expose pas). Fait = la carte Journal a des filtres user/type/date qui pilotent l'endpoint et l'export .xlsx.
[ ] [Transversal] [L12] [ROUTINE] Auditer les tables de données des pages reporting/archive pour le PWA iPhone-first — envelopper les tables larges restantes dans des conteneurs de scroll et garder les actions principales atteignables au pouce. Fait = chaque table auditée scrolle dans sa carte et son bouton principal est atteignable sans scroll horizontal de page sur 375px.
[ ] [Transversal] [L8] [ROUTINE] Unifier les deux constructeurs de réponse .xlsx (dataimport.exports_view + crm.exports.build_xlsx_response tous deux utilisés) pour que chaque export de liste ait en-têtes/locale/encodage identiques. Fait = les exports devis/factures/chantiers/équipements/tickets partagent le seul builder, format fr-MA cohérent.
[ ] [Transversal] [L11] [ROUTINE] Ajouter une error-boundary de route globale pour qu'une erreur de rendu/fetch montre un écran FR de récupération au lieu d'une app blanche (Layout.jsx a une barre de progression mais pas d'UI d'erreur). Fait = une erreur non capturée rend "Une erreur est survenue — recharger" avec action de rechargement.
[ ] [Transversal] [L12] [ROUTINE] Vérifier que le drawer mobile (Layout.jsx drawerOpen / hamburger Header) garde la recherche globale et la cloche atteignables sur iPhone. Fait = sur 375px la recherche et la cloche restent utilisables depuis le header (ou le drawer) sans chevaucher le titre, et le drawer se ferme à la navigation.
[ ] [Transversal] [L14] [ROUTINE] Ajouter une ligne de synthèse compte + valeur à chaque table Rapports ayant des totaux (funnel, par catégorie, créances). Fait = chaque table applicable montre un pied "Total : N — X MAD" calculé depuis les mêmes données.

---

## GATED — needs Reda's decision before building (agent does NOT auto-build)

The agent must **not** start these. They cost money, change auth, add a new module/architecture, or
need Reda's taste. Reda decides; then I write a focused task and move it into the BUILD QUEUE.

- **G1 — Real email sending** (devis/facture/relance by email). **UNGATED 2026-06-18 → BUILD QUEUE
  (N87/N88, Brevo).** Provider chosen = **Brevo** (SDK or SMTP), key from settings/env, no-op
  without a key; pre-approved (see the PRE-APPROVED block). No longer a blocker.
- **G2 — WhatsApp Business Cloud API** (true auto-send + PDF *attached*, message templates). Cost +
  Meta Business setup. Already your Month-2 roadmap.
- **G3 — Full document visual redesign** (devis/facture/bon de commande). **Still GATED** — the
  full visual redesign **needs your gallery approval** (taste) and is a deliberate non-goal for an
  unattended run; `PDF_ENGINE=legacy` stays as the fallback. **PARTIAL UNGATE 2026-06-18 → BUILD
  QUEUE (N106):** only the two additive deliverables in the EXISTING premium visual language — the
  three escalating-tone relance letters and the one-page after-sale handover/warranty sheet — were
  ungated; the redesign itself stays here.
- **G4 — Custom per-module roles/permissions.** New architecture; becomes important at multi-tenant
  Phase 6. Today Commerciale vs admin works.
- **G5 — Supplier procurement module** (bons de commande fournisseur, goods-in/receiving, supplier
  invoices / accounts payable). A real new module — a multi-session project of its own.
- **G6 — Stock auto-decrement on installation** (a chantier consumes its equipment from stock).
  **UNGATED 2026-06-18 → folded into BUILD QUEUE N14.** The exact rule is now confirmed and
  pre-approved: **reserve on chantier create → decrement on « Installé » → release on
  cancel/close** (see the PRE-APPROVED block). No longer a blocker.
- **G7 — Quote e-signature.** External dependency / provider.
- **G8 — 2FA / SSO.** Auth change.
- **G9 — Automation engine / scheduler.** **UNGATED 2026-06-18 → BUILD QUEUE (G9).** Decision made:
  **Celery Beat (in-app)**, two scheduled jobs in Africa/Casablanca time — scheduled relance
  reminders + a daily facture-overdue check (overdue = échéance passed & not fully paid → « En
  retard »; default échéance = issue + 30 days). Pre-approved (see the PRE-APPROVED block). The
  broader no-code automation engine (N72/N73) and n8n workflows stay separate.
- **G10 — CAPI service** (Meta Conversions API, sends `SignedQuote` on Signé, EMQ ≥ 7.0). Roadmap;
  **gated on fbclid/UTM capture landing in the site form first**.
- **G11 — Chatbot → Reda's Claude API key.** Small, but a cost change — needs the key present + your OK.
- **G12 — MCP server + M365** (Entra ID, Outlook, OneDrive, Teams). Roadmap.
- **G13 — One-off 619-lead Odoo import.** Uses the reusable importer (T9) once built, but **gated on a
  2nd Odoo backup** before any real extraction. File holds PII → gitignored, never in chat/GitHub.
- **G14 — DGI e-invoicing readiness (Morocco).** Mandatory ~**Jan 2027** for businesses with CA >
  500k DH — likely Taqinor's wave. **PARTIAL UNGATE 2026-06-18 → BUILD QUEUE (N105):** only the
  **silent, backend-only local capability** was ungated — on-demand **UBL 2.1 / CII** XML export
  (recipient ICE on every line) + a conformity validator, both behind a master toggle that defaults
  OFF and is completely invisible while off. **STILL GATED here** (blocked on the unpublished DGI
  implementing decree — no API spec exists yet, not a decision of Reda's): the **Simpl-TVA portal
  transmission** and the **certified e-signature** (a PDF is explicitly NOT compliant; clearance
  needs the live DGI platform). Start those when the specs publish.
- **G15 — Arabic / Darija UI** (full interface localization, not just message templates). Decide if
  needed and for whom.

### From the 2026-06-18 refinement audit — gated copies (do NOT auto-build)

Mirror of every non-ROUTINE task in the REFINEMENT QUEUE above, kept here so an unattended run
never auto-builds them. Each still lives in its module section above; build only after Reda's call.

**SCHEMA — needs an additive DB migration (still gated)**
_(none — the three SCHEMA refinements (client ICE export, client `created_by`/`date_modification`,
`type_equipement` tag on `stock.Categorie`) were UNGATED 2026-06-18 to `[ROUTINE]` in their module
sections; additive migrations are pre-approved — see the PRE-APPROVED block.)_

**DEP — needs a dependency beyond openpyxl / vite-plugin-pwa**
_(none — the Parc « Carte » Leaflet refinement was UNGATED 2026-06-18 to `[ROUTINE]`; Leaflet is
pre-approved — see the PRE-APPROVED block + N85.)_

**DECISION — needs Reda's product call**
[ ] [CRM/Leads] [L17] [DECISION] Capturer/afficher la langue de communication préférée du lead (FR/Darija) pour préparer l'envoi WhatsApp bilingue (whatsapp_devis prend déjà un paramètre langue, mais rien ne le mémorise sur le lead). Fait = un champ "Langue préférée" sur la fiche pré-sélectionne la langue du message WhatsApp. [cf. N93]
[ ] [Facturation] [L7] [DECISION] Décider si l'auto-passage en "En retard" des factures échues doit être matérialisé (aujourd'hui seulement calculé à la lecture). Fait = note tranchée — garder le on-the-fly (jours_retard) ou ajouter un job (gaté G9 scheduler), le choix consigné dans PLAN.md.
_(UNGATED 2026-06-18 to `[ROUTINE]` in their module sections — see the PRE-APPROVED block:
saved named views (local, no email); comptable export; per-company `Equipement` serial uniqueness;
Stock-category management UI; Fournisseurs management screen; Retours list; public ShareLink
noindex + per-token throttle.)_

**GALLERY — changes a client-facing PDF/visual (needs gallery approval)**
[ ] [Facturation/Relances] [L13] [GALLERY] Adapter la lettre de relance PDF au niveau courant (ton J+7 vs J+30) en variant le corps depuis FollowupLevel.message. Fait = generate_lettre_relance_pdf rend un texte distinct par niveau, sans changer la mise en page premium.
[ ] [Chantiers] [L4] [GALLERY] Ajouter un aperçu in-app des PDF client après-vente (PV de réception, bon de livraison, dossier de remise, attestation) avant téléchargement dans InstallationDetail.jsx. Fait = chaque bouton "Documents après-vente" ouvre un aperçu PDF inline au lieu de seulement downloadBlob, sans changer le contenu du PDF généré.

---

## MANUAL — Reda's / Meryem's tasks (NOT code; agent never does these)

Tracked here so they aren't lost:
- Enter the real **ICE / IF / RC** on the server (Paramètres → Identifiants légaux) — live invoices
  currently lack the legally-required seller ICE until then.
- Enter the **11 OSP pump prices** on the server (the agricole pump box stays red; don't send an agri
  quote before).
- Enter **real stock quantities** on the server (the ~283 M DH dashboard value is demo quantities).
- **Article 33 / Loi 82-21 outreach** to the install portfolio (decree in force since 9 June —
  overdue, still sendable; Meryem sends; tag replies « Régularisation 82-21 »).
- **Confirm Sami's GitHub org access is removed.**
- **Confirm the PC cleanup ran** (taqinor-secrets.txt + test-leads JSON deleted).
- **Set the default lead responsable** to Meryem (Paramètres → Leads).
- **Personalize the WhatsApp templates** (Paramètres → Messages WhatsApp).
- Optional: add `PUBLIC_BASE_URL=https://api.taqinor.ma` to the server `.env` for cleaner WhatsApp
  links (they already work via auto-redirect).
- **DEBUG:** turn it off when you decide the OS is ready (your call — the agent will not raise it).

---

## DONE LOG (agent appends one plain-language line per completed task)

- *(seeded baseline — see "ALREADY LIVE" above for the full pre-plan state)*
- _next: the agent adds entries here, e.g. "2026-06-15 — T1 done: devis preview renders + downloads in all 3 formats; cache-busting added; deployed."_
- 2026-06-18 — F1 + F2 livrés (fondation du module field-execution / outillage — lane unique « Outillage » : F2 dépend de F1, mêmes fichiers, donc une seule lane séquentielle ; orchestrateur = build + revue adversariale + CI locale). **F1** : nouvelle app `apps.outillage` (modèle `Outillage` — équipement DURABLE strictement séparé du stock vendable : nom/catégorie/asset_tag/n° série/emplacement FK `stock.EmplacementStock`/statut Disponible·En intervention·En réparation·Perdu/date d'achat/note). Endpoints `/outillage/outils/` (TenantMixin, lecture tout rôle, écriture responsable/admin) filtrables `?statut=`/`?emplacement=` + recherche nom/asset_tag/n° série/catégorie. Page `/outillage` (menu CHANTIERS) : liste + filtres + création/édition en panneau (Sheet) + photo optionnelle via la pièce jointe générique (`outillage.outillage` ajouté à `records.ALLOWED_TARGETS`). Jamais consommé, jamais client-facing. **F2** : `KitOutillage` + `KitOutillageItem` (liste ordonnée d'outils du catalogue, `type_intervention` pré-sélecteur) ; 3 kits par défaut semés à la 1ʳᵉ liste (idempotent), pleinement éditables (renommer/réordonner/désactiver) ; endpoints `/outillage/kits/` + `/outillage/kit-items/` (écriture admin, société de l'item suivant le kit) ; onglet Paramètres → « Kits d'outillage ». Gate vert localement : flake8 0, eslint 0 erreur (6 warnings legacy Landing.jsx), build vite+PWA OK, 12 tests outillage + 8 records, makemigrations --check propre, check_stages OK. Additif, FR (identifiants EN), multi-tenant (société forcée serveur), aucune nouvelle dépendance, aucun PDF/STAGES.py/prix d'achat touché. F3–F24 + refinement queue + N gatés non entamés (fenêtre d'usage — reprise idempotente).
- 2026-06-18 — N58 + N74 livrés (couche d'éditabilité, 2 lanes worktree parallèles à fichiers quasi-disjoints, orchestrateur = revue + intégration des registres partagés). **N58** : statuts chantier/SAV/bon-de-commande configurables (libellé, ordre, visibilité) par société via un nouveau `StatutConfig` (parametres, fichiers isolés `models_statuses.py`/`serializers_statuses.py`/`views_statuses.py`/`statuses_defaults.py`) ; couche d'AFFICHAGE seule — clés canoniques + machine à états intactes, défauts lus à la source ⇒ identique tant que rien n'est édité ; onglet Paramètres → Statuts ; endpoints `/parametres/statuts/` + `/effective/` + `/bulk/` ; écriture réservée admin/responsable, société forcée serveur (TenantMixin). **N74** : checklists chantier en modèles nommés (`ChecklistTemplate`, installations) auto-sélectionnés par type d'installation, repli « Défaut » protégé portant les 7 étapes actuelles (migration additive rattachant l'historique — comportement préservé) ; onglet Paramètres → Checklists ; endpoint `/installations/checklist-templates/`. Gate vert localement : flake8 0, eslint 0 erreur (6 warnings legacy Landing.jsx), 56 tests backend parametres+installations, 159+5 tests front, check_stages OK. **N14 bloqué** (= périmètre G6, décision comptable du founder requise). Aucune nouvelle dépendance, additif, FR, multi-tenant, aucun PDF/STAGES.py/prix d'achat touché. Reste de la file (F1–F24, refinement queue, N gatés) non entamé — pacé sur la fenêtre d'usage, reprise idempotente.
- 2026-06-18 — F3 + F4 livrés (spine du module field-execution / interventions — lane unique « Interventions » : F4 dépend de l'API F3, mêmes fichiers backend/front couplés, donc une seule lane séquentielle ; orchestrateur = build + revue adversariale + CI locale). **F3** : le modèle `installations.Intervention` existant (« sortie chantier ») est ÉTENDU de façon ADDITIVE — nouveau `statut` (machine à états PROPRE `a_preparer/prete/en_route/sur_site/terminee/validee` + `STATUT_ORDER`, défaut `a_preparer`), `equipe` M2M→users (défaut = installateur du chantier, posé serveur), `camionnette` FK→`stock.EmplacementStock` (scopée société). Nouveau modèle chatter `InterventionActivity` + helper `intervention_activity.py` (création + changements de champs suivis dont le statut + notes, acteur/société serveur) ; endpoints `/installations/interventions/<id>/historique/` + `/noter/` ; filtres `?statut=`/`?type_intervention=`. Réf/client/devis/ville/GPS tirés du chantier (lecture seule). **GARANTIE clé : changer le statut intervention NE TOUCHE JAMAIS le statut chantier ni STAGES.py** (couvert par test). Migration additive `0007`. **F4** : nouvelle page `/interventions` (menu CHANTIERS) — vue Liste + vue Kanban par statut intervention (glisser-déposer via @dnd-kit, mêmes classes `kb-*`/`kc-*` que le kanban chantiers, AUCUNE nouvelle dépendance), réassignation du technicien par carte, filtres statut + type, panneau de détail (Sheet) avec Historique + ajout de note + changement statut/technicien ; cartes = chantier/client/ville/type/date prévue/équipe/statut. Gate vert localement : flake8 backend 0, 51 tests installations (dont 10 F3) + 122 tests installations/sav/reporting/outillage verts, `manage.py check` propre, makemigrations --check propre, eslint 0 erreur (6 warnings legacy Landing.jsx), build vite+PWA OK, codemap_fingerprint --check OK (structure + plan), check_stages OK. Additif, FR (identifiants EN), multi-tenant (société forcée serveur, camionnette scopée), aucune nouvelle dépendance, aucun PDF/PdfCanvas/page publique/STAGES.py/prix d'achat touché. F5 dépend de la réservation de stock (= N14/G6 gaté) ; F6–F24 + refinement queue + N gatés non entamés — pacé sur la fenêtre d'usage, reprise idempotente.
- 2026-06-19 — 5 lanes worktree parallèles à fichiers DISJOINTS (orchestrateur = revue + fold + bookkeeping centralisé ; les 2 régressions d'abord, en priorité). **N103 (régression — changement de rôle)** : un Directeur/Administrateur ne pouvait plus changer le rôle d'un utilisateur. Cause réelle : le palier de menu qui garde `/users/` et `/roles/` (`IsAdminOrResponsableTier`) était dérivé du seul `nom` + `est_systeme` du rôle, donc un rôle système « Directeur »/« Administrateur » ayant dérivé à `est_systeme=False` retombait en palier « normal » → 403 sur les deux endpoints → écran Utilisateurs cassé (aucun rôle sélectionnable) + attribution superviseur cassée. Correctif : `role_tiers` dérive d'abord du signal d'autorité (`roles_gerer`→admin, `users_voir`→responsable) avec repli par nom ; le seeding (`_create_system_roles`/`init_roles`) auto-répare un rôle système dérivé. Backend seul, additif, tests de régression (dont négatifs : Viewer/Commercial restent bloqués). **N104 (régression — pièces jointes)** : ajout de fichier cassé PARTOUT. Cause réelle : la primitive partagée `FileUpload` validait la liste `accept` au seul `file.type` (`matchesAccept` dans `ui/file-utils.js`), or le navigateur renvoie souvent un type MIME vide pour des PDF/images valides → rejet côté client AVANT toute requête. Correctif : repli sur l'extension quand le MIME est vide (strict : l'extension doit mapper un MIME autorisé ; la validation magic-byte serveur reste l'autorité) + test de régression backend qui upload/download les octets exacts contre MinIO. Backend disculpé par reproduction. **N14** : `installations.StockReservation` (migration 0008) semée du BOM gelé à la création du chantier, réutilise le mouvement SORTIE existant ; idempotent (flag `consomme` sous `select_for_update()`/atomic — repasser « Installé » n'émet rien) ; annuler/clôturer libère le restant ; `ProduitSerializer` expose réservé/disponible. **N105** : capacité DGI silencieuse — module `apps/ventes/dgi/` (`build_ubl_xml` + `validate_dgi_conformity`, XML stdlib, aucune dépendance) + commande + 2 actions, armées par le seul `CompanyProfile.dgi_export_actif` (défaut OFF, migration parametres 0019) ; OFF ⇒ endpoints 404, modèle/sérialiseur/listes Facture identiques, aucun prix d'achat dans le XML. **N107** : commande `manage.py import_odoo_leads <fichier> --company <…>` idempotente (réutilise le parser dataimport CSV/XLSX + JSON, rapprochement email/téléphone + clé `(company, external_system, external_id)`, stages depuis STAGES.py, société forcée serveur, no-op sans fichier). Validation : flake8 backend 0 ; eslint frontend 0 erreur (6 warnings legacy Landing.jsx) ; build vite+PWA OK ; `file-utils.test.mjs` 7/7 ; tests backend de chaque lane verts sur sqlite (suite complète en CI avec Postgres+MinIO) ; codemap_fingerprint --check OK (structure + plan), check_stages OK. Additif, FR (identifiants EN), multi-tenant (société forcée serveur), aucune nouvelle dépendance, STAGES.py/funnel/DEBUG/déploiement intouchés, prix d'achat jamais client-facing. Poussé sur `claude/elegant-fermat-9gu98c` (pas de merge `main`, pas de PR — conformément aux consignes de branche). RESTE OUVERT : reste de la file N/F + refinement queue (reprise idempotente).
- 2026-06-19 — Batch de 6 lanes worktree parallèles (orchestrateur = fold cherry-pick + revue + bookkeeping centralisé). **Incident de base corrigé** : l'outil d'isolation a basé 5/6 worktrees sur un commit `main` périmé (083191a, 231 commits de retard) ; détecté par contrôle d'ancêtre + absence de `apps/outillage`, rendu déterministe (refs `main`/`origin/main` locales alignées sur HEAD), 5 lanes relancées sur la bonne base avec auto-contrôle, puis foldées (cherry-pick, 0 conflit réel ; 2 corrections lint mineures à l'intégration). **DOCLIT (D2/N60/N67/N26 ; N59 partiel)** : littéraux du devis premium câblés vers des réglages versionnés par société (`parametres.DocumentTemplates`, migration 0020) — CGV, « Validité : 30 jours », garanties (titre/détail/perf), tampon d'acceptation N26 (rendu UNIQUEMENT si `accepte_par_nom`+`date_acceptation`) ; défauts reproduisant EXACTEMENT les littéraux entité-HTML → **PDF byte-identique vérifié** (HTML premium-full 2 943 910 car. + one-page identiques à HEAD avec réglages par défaut) ; onglet Paramètres → « Modèles de documents ». N59 : éditeur livré + littéraux DEVIS faits ; autres types (facture/acompte/avoir/PV/BL/dossier/attestation) différés. **FIELD (F5–F8)** : module field-execution — liste de préparation (BOM gelé + kit, %, gate « tout chargé », manquant→réservation stock), check-in GPS (départ/arrivée/retour + distance haversine, aucun service externe), capture photos guidée par shot-list configurable (Avant/Pendant/Après, stockage via `records.store_attachment`), enforcement photos obligatoires avant Terminée ; **le statut intervention ne touche jamais le statut chantier ni STAGES.py** (test). **MAP (N85)** : endpoint geo company-scopé (reporting) + carte Leaflet `/carte` (leads/chantiers/installés/visites, filtres type+statut, marqueurs cliquables) ; classification installé via `Installation.canonical_statut`. **LABELS (N20)** : générateur QR + CODE128 SVG autonome (zéro dépendance) + étiquettes imprimables (WeasyPrint) + résolveur de scan `PRODUIT:<id>`/`SYSTEME:<id>` (installations en lecture seule). **EXPORT (N97)** : export company-scopé CSV/XLSX/JSON + ZIP sauvegarde (apps.dataimport, fichiers neufs, code d'import intact ; `apps.dataimport` enregistré dans INSTALLED_APPS pour la commande) ; `prix_achat` exclu ; gardé `IsAdminRole`. **CRM-leads (refinements ROUTINE)** : normalisation téléphone serveur, avertissement doublon en direct (création+édition, endpoint `check-duplicates`), validation email, canal par défaut, garde SIGNED (SigneDialog), motif de perte requis, lien GPS Maps, champs lead web en lecture, retours d'erreur FR, +8 champs au chatter `TRACKED_FIELDS`. Validation locale (Postgres + mock S3 moto + Redis) : flake8 backend 0 ; eslint frontend 0 erreur (6 warnings legacy Landing.jsx) ; build vite+PWA OK + Leaflet bundlé ; 167 tests node front ; tests backend par lane verts (DOCLIT 47+27+33, FIELD 104, MAP 33, LABELS 81, EXPORT 28, CRM 148) ; suite backend agrégée 751 tests verte (9 « erreurs » initiales = Redis local absent, 0 après démarrage Redis) ; `codemap_fingerprint --check` OK (structure+plan), `check_stages` OK. Additif, FR (identifiants EN), multi-tenant (société forcée serveur) ; Leaflet (pré-approuvé) seule nouvelle dépendance front, openpyxl (pré-approuvé) pinné ; STAGES.py/funnel/DEBUG/déploiement intouchés ; prix d'achat jamais client-facing ; moteur premium byte-identique. `labels.test.mjs` ajouté à la liste de tests CI. Poussé sur `claude/wonderful-carson-ltcl7q` (pas de merge `main`, pas de PR — consignes de branche). RESTE OUVERT : N64/N65 (tarif ONEE/ROI), N87/N88/G9 (Brevo+scheduler), N106, F9–F24, refinement queue (reprise idempotente).
- 2026-06-19 — Wave 2 : 3 lanes worktree parallèles foldées sur le batch précédent (apps libérées parametres/ventes ; base déterministe alignée sur le HEAD du batch 1 ; cherry-pick, seul `ventes/urls.py` en conflit trivial — routes des deux lanes conservées). **TARIFF (N64/N65)** : moteur tarifaire ONEE + ROI en réglages versionnés par société (`parametres.TariffSettings`, migration 0021) — facturation PROGRESSIVE ≤150 kWh/mois (chaque tranche à son tarif) vs SÉLECTIVE >150 (mois entier au tarif unique de la tranche, NON progressif) avec tolérance 10 kWh (bornes 210/310/510) ; barème résidentiel semé en TTC (0,9010/1,0732/1,0732/1,1676/1,3817/1,5958, jamais de TVA en plus) ; classe force-motrice/agricole séparée moins chère (0,95) ; `surplus_injecte_compense` défaut FAUX ; ROI conservateur ; client PVGIS (urllib stdlib, azimut Sud 0/Est −90/Ouest +90/Nord +180, timeout 6 s) avec repli productible manuel éditable hors-ligne (ne lève jamais). Fondation réglages+endpoints ; pas encore branché dans le générateur de devis (hors périmètre, `solar.js` intouché). **EMAILSCHED (N87/N88/G9)** : intégration email Brevo (backend anymail `sendinblue` sinon SMTP/console), **no-op si pas de clé (comportement actuel préservé)** ; envoi des documents + relances routé via l'intégration, journalisé sur un `EmailLog` company-scopé (migration ventes 0019) + chatter ; capture entrante N88 (parse réf `DEV-…/FAC-…`, rattache au fil, no-op sans clé) ; **Celery Beat** (Africa/Casablanca, `enable_utc=False`) 2 jobs : bascule quotidienne des factures échues impayées → `en_retard` (échéance ou défaut +30 j, idempotent) 00:30, rappels de relance 07:00 ; onglet Paramètres → Email. **N106** : 3 lettres de relance premium (courtois/ferme/mise en demeure, `?niveau=`) + fiche de remise/garantie 1 page, à la demande, RÉUTILISANT les helpers visuels premium (sans modifier `generate_devis_premium.py`) ; client-facing, aucun prix d'achat (vérifié), cross-tenant 404 ; boutons sur Relances + InstallationDetail. Validation locale (Postgres + moto S3 + Redis) : flake8 backend 0 ; eslint frontend 0 erreur (6 warnings legacy Landing.jsx) ; build vite+PWA OK ; tests node front verts ; tests backend par lane verts (TARIFF 62 dont 35 neufs, EMAILSCHED 233 dont 18, N106 222 dont 7) ; suite parametres+ventes agrégée verte ; `codemap_fingerprint --check` OK, `check_stages` OK. Additif, FR (identifiants EN), multi-tenant (société forcée serveur), aucune nouvelle dépendance pip (anymail déjà présent, urllib stdlib), Africa/Casablanca pour toute logique horaire, STAGES.py/funnel/DEBUG intouchés, prix d'achat jamais client-facing, moteur premium non redessiné. Poussé sur `claude/wonderful-carson-ltcl7q`. RESTE OUVERT : F9–F24, N50–N53/N72/N73/N75/N86/N89/N91–N101 (gatés ou architecture), refinement queue (reprise idempotente).
- 2026-06-19 — Batch « 6 lanes worktree parallèles » (orchestrateur = fold séquentiel + revue + bookkeeping centralisé ; validation locale Postgres + mock S3 moto). **MONITORING (N50/N51/N52)** : nouvelle app `apps.monitoring` — cadre de supervision à interface fournisseur SWAPPABLE (registre + `NoOpProvider` par défaut + squelette `FusionSolarProvider` qui no-ope sans identifiants), modèles `MonitoringConfig`/`ProductionReading`/`UnderperformanceFlag`/réglages, endpoints `/api/django/monitoring/` (configs + `sync-now`, readings + saisie manuelle, settings) ; vue `/production` (relevés récents + ajout manuel) + onglet Paramètres « Supervision » (seuil sous-performance + auto-ticket SAV idempotent, défaut OFF) ; aucune dépendance, défauts = comportement actuel. **AUTOMATION (N72/N73)** : nouvelle app `apps.automation` — moteur règles sans code (8 déclencheurs / 7 actions via signals best-effort qui ne cassent jamais la sauvegarde, chaque exécution journalisée `AutomationRun`) + étape d'approbation (`AutomationApproval`, owner-tier) ; endpoints `/api/django/automation/` (rules/runs/approvals) ; onglet Paramètres « Automatisations » ; opt-in, aucune dépendance, canaux WhatsApp/email réutilisés (no-op si non configurés). **NOTIFY (N75)** : nouvelle app `apps.notifications` — moteur de notifications unifié (`Notification`/`NotificationPreference`, service `notify()` best-effort respectant les préférences par utilisateur×événement, in-app toujours + canaux existants si configurés), centre de notifications (cloche dans le Header + feed persistant) + page `/parametres/notifications` ; endpoints `/api/django/notifications/`. **PUBLICAPI (N89)** : nouvelle app `apps.publicapi` — clés d'API hashées (scopes), authentification `Api-Key`, throttle DRF par clé (120/min), endpoints publics read-only company-scopés `/api/public/{leads,devis,factures,chantiers}` (jamais de prix d'achat), webhooks signés HMAC-SHA256 (httpx, best-effort, `WebhookDelivery` journalisé) sur 4 événements ; gestion `/api/django/publicapi/` + onglet Paramètres « API & Webhooks ». **CHATBOT (N86)** : extension de l'agent FastAPI — lecture élargie aux nouveaux objets (chantiers à planifier, garanties qui expirent, factures en retard, production) avec scoping société conservé ; 3 outils d'ACTION (ouvrir ticket SAV, brouillon BC chantier, planifier visite maintenance) passant par l'API Django (jamais de SQL en écriture — règle #1), gatés par rôle (read-only/viewer = aucun outil), prix d'achat masqué sans permission ; aucune dépendance, no-op sans clé. **FIELD (F9–F20, F22, F23 ; F21 sauté)** : suite du module field-execution sur `apps.installations` (migrations linéaires 0010 + parametres 0022) — capture n° de série (OCR swappable no-op, n° vide ne bloque jamais, flux vers le parc installé), annotation photo, réconciliation matériel consommé (prévu vs utilisé, lignes extra, justification texte ou mémo vocal, mouvements stock réels + libération réservation) + revue dépassement (seuil éditable), mémos vocaux (stockage objet) + transcription swappable no-op « Non transcrit », suivi du temps d'équipe → jours-homme réels, réserves (punch-list → intervention de suivi et/ou ticket SAV), réconciliation retour d'outillage, validation consignes de sécurité (checklist EPI/consignation éditable), PDF compte-rendu d'intervention (moteur existant, aucun prix d'achat), QA photo IA swappable (no-op, ne bloque jamais), vue « Ma journée » (`/ma-journee`, rôle Technicien scopé à ses propres interventions, jamais de prix d'achat), QR/short code `INTERV:<id>`. **Validation intégrée** : `manage.py check` propre, `makemigrations --check` « No changes », flake8 backend 0, **920 tests backend verts** (suite complète Postgres + mock S3), eslint frontend 0 erreur (6 warnings legacy Landing.jsx), 167 tests node front, build vite+PWA OK, `check_stages` OK, `codemap_fingerprint --check` OK après refresh. Additif, FR (identifiants EN), multi-tenant (société forcée serveur), AUCUNE nouvelle dépendance pip/npm, toutes les interfaces swappables no-op par défaut, STAGES.py/funnel/DEBUG/déploiement/moteur premium intouchés, prix d'achat jamais client-facing. **GATÉS ce run (skip + flag, déplacés en raison documentée)** : N91/F21 (architecture de synchronisation hors-ligne), N92 (dép. pywebpush + VAPID), N93/N94 (i18n+RTL pan-application : nouvelle dép. + tout le frontend), N96 (2FA : changement d'auth + pyotp), N100/N101 (facturation par tenant + auto-inscription : coût/auth/architecture), N102 (dépend de N100/N101). Poussé sur `claude/parallel-task-execution-yd3hra` (pas de merge `main`, pas de PR — consignes de branche). RESTE OUVERT : N53/N76/N79 (gatés en amont) + la file gatée ci-dessus + refinement queue déjà drainée.
