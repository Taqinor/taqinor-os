# Taqinor OS — Build Plan & Progress

This file is the **single source of truth** for the Taqinor OS build backlog and the
**memory between Claude Code sessions**. Each run first works through EVERY unchecked task in `docs/PLAN2.md` (if that file exists) from top to bottom — not just one — ticking each off as it lands, then does the same for this file, and only stops when both queues are clear (or a usage limit pauses it, in which case re-running resumes from the next unchecked task). The next session reads this file and
continues. Nothing relies on the agent's own memory — the file on disk is the memory.
Each run partitions its unchecked tasks into independent **lanes** (grouped by which real
source files they write) and builds them with **up to 8 concurrent worktree subagents**, in
waves of 8 when there are more lanes — see HOW TO RUN.

---

## HOW TO RUN · STANDING RULES · ALREADY LIVE → `docs/PLAN_HOWTO.md`

The run procedure (HOW TO RUN), the STANDING RULES every task obeys, and the
ALREADY-LIVE “do not rebuild” inventory now live in **`docs/PLAN_HOWTO.md`** —
read it once per session before draining this queue. The five non-negotiable
rules (#1–#5) live in `CLAUDE.md`; `docs/LANE_BRIEF.md` is the per-lane-agent
distillate. This file keeps the BUILD QUEUE + DONE LOG below.

## BUILD QUEUE (do top-down — highest value first)

### EVERY CATEGORY IS BUILDABLE (auto-gating OFF, 2026-06-21)

> **Founder standing consent (2026-06-21, Reda).** The former auto-skip of
> `ARCH` / `DECISION` / `AUTH` / `COST` / `GALLERY` / `DEP` is **LIFTED**. A run builds tasks of
> EVERY category without pausing — including brand-new greenfield modules (compta, paie, flotte,
> qhse, contrats, gestion_projet, ged, kb, litiges), auth changes, new paid/external dependencies,
> additive **and** destructive-but-revertable migrations, and new architecture. `scripts/plan_lanes.py`
> reflects this in code (`GATED_KEYWORDS` is empty → it reports `0 gated`). The planner still LABELS
> each category, and the run **NOTES in the DONE LOG** whenever a built task introduced a new
> paid/external dependency, an auth change, a destructive migration, or a brand-new architectural
> component — so you keep visibility. The five non-negotiable rules (#1–#5) are unaffected and still
> bind (rule #5's `tos_risk/` process still applies to any scraping task). Every change must stay
> revertable via `git revert` and pass the four required CI checks before merge.

A task is held back ONLY by a genuine **external prerequisite a run cannot satisfy** — a credential /
secret / account / real-world data **you** must provide, or a conflict with a non-negotiable rule.
Those are parked under **NEEDS YOUR INPUT** (below) each with a recommendation; everything else
builds. (Historical note: the long list of "specifically pre-approved" deps — Leaflet, Celery Beat,
Brevo, the additive columns, ONEE seeds, the stock-reserve rule, the silent DGI export, the devis
literal→setting wiring — all shipped; they are now just instances of the general rule above.)


---

## BUILD QUEUE — N1–N102 (post-sale, procurement, Moroccan billing, editability, platform)

Re-homed from the recovered `docs/PLAN2.md` (the overflow queue) on 2026-06-16. The ticks
below were **re-verified against the live `main` code**, task by task — the recovered file's
own ticks were written on a branch that never merged and were unreliable. `[x]` here means the
feature genuinely exists **and is usable** on `main` today; `[ ]` means missing, backend-only
with no usable screen, or only partially meeting the spec. Same STANDING RULES as the rest of
this file apply (additive only, multi-tenant, French UI, STAGES.py pipeline never edited, buy
prices never client-facing). **Reconciliation result: 30 of 102 done, 72 open.**

Two tasks have a working backend on `main` but no usable screen yet — flagged inline as
_Backend ready, frontend pending_: **N11** (supplier-PO management page) and **N29** (facture
conformity warning banner).

### Growth / multi-tenant platform
- [x] N100 — Build out multi-tenant operation on the existing tenant_id foundation (strict per-tenant isolation verification, tenant onboarding flow, per-tenant branding/white-label of client-facing documents, configurable per-plan feature limits, tenant-level billing). (UNGATED 2026-06-21 per "ungate all". **RECOMMENDATION: KEEP DEFERRED until a 2nd paying installer — the `company` foundation is ready so no debt accrues by waiting; building SaaS billing for zero customers adds cost + the biggest auth surface in the app. Do NOT let a drain build this unless you've decided to sell TAQINOR as a product.** See NEEDS YOUR INPUT.) (ARCH) (@lane: backend/authentication)
- [x] N101 — Tenant administration console (manage tenants/plans/usage/support) + self-serve signup for design-partner installers. (UNGATED 2026-06-21. **RECOMMENDATION: KEEP DEFERRED — pairs with N100; self-serve signup is a major auth surface, build only when going multi-installer.**) (ARCH) (@lane: backend/authentication) (@after: N100)
- [x] N102 — After the modules above are built, update the master project document + PLAN + DONE log in plain language to reflect the new post-sale, procurement/inventory, Moroccan billing/compliance, full-editability, and platform additions, noting which shipped and which were skipped. (UNGATED 2026-06-21 — depends on N100/N101; do last.) Files: docs/CODEMAP.md, docs/PLAN.md, docs/DONE.md. (@after: N100, N101)

---

## BUILD QUEUE — Parité « best ERP du monde » + découpage modules façon Odoo — audit 2026-07-02 (ODX1–ODX23 + groupes X*)

Provenance : audit multi-agents du 2026-07-02 (62 agents — recherche web par domaine sur Odoo 18/19,
SAP Business One, Dynamics 365 Business Central, NetSuite, ERPNext + leaders spécialisés de chaque
domaine ; gap-analysis contre le code réel ET tous les plans — tout ce qui figure dans
PLAN/PLAN2/DONE/ERROR_PLAN, coché ou non, a été compté comme déjà couvert ; revue adversariale
anti-doublon par domaine, dédoublonnage inter-domaines, puis critique de complétude). **489 tâches**
= les vrais manques restants sur ~1 100 features benchmarkées (l'écrasante majorité est déjà couverte
par l'existant + les plans). Règles du lot : contenu/fonctionnel uniquement (le câblage frontend
fonctionnel est inclus ; le design visuel viendra dans le chantier UI dédié) ; tout est
additif / multi-tenant / frontières services-selectors ; intégrations externes key-gated OFF par
défaut ; `/proposal` et le moteur premium intouchés. Les tags (DECISION)/(COST) marquent les appels
du fondateur mais restent dans la file. **Groupe ODX en premier** (découpage modules façon Odoo — ses
étapes -1/-2 sont ordonnées par (@after)) ; les groupes X* sont indépendants par app et
lane-drainables comme d'habitude.

### Groupe ODX — Découpage en modules façon Odoo (ODX1–ODX23)

Constats vérifiés dans le repo (2026-07-02) :
- `apps/compta/models.py` = monolithe de 6 084 lignes / ~85 classes : la vraie compta CGNC y cohabite avec le marketing (Campagne, SequenceRelance…), les appels d'offres (AppelOffre…), le portail client (ComptePortailClient…), les partenaires/territoires, la config de vente (CodePromotion, ModeleDevis…), les notes de frais.
- La facturation (Facture/Paiement/Avoir/relances) vit dans `ventes` ; les achats fournisseurs (BCF/réceptions/FactureFournisseur/PaiementFournisseur/retours/PrixFournisseur) vivent dans `stock` — Odoo les sépare (Invoicing, Purchase).
- FG391 existe : `core.ModuleToggle` + `core/feature_flags.module_actif` + ViewSet `core/module-toggles/` — mais AUCUN manifest par app, aucune fermeture de dépendances, aucun usage frontend (grep = 0 appel).
- UX1 a livré le registre frontend `router/moduleRoutes.jsx` + `features/<module>/module.config.jsx` — mais seulement pour les 10 modules récents ; STOCK/CRM/VENTES/CHANTIERS/SAV… restent codés en dur dans `Sidebar.jsx`.
- La maintenance interne a déjà ses foyers Odoo-conformes (flotte = PlanEntretien/EcheanceEntretien, outillage = statut EN_REPARATION) ; `sav` = Helpdesk+Repairs (équipements CLIENTS + O&M) — pas de move nécessaire, à documenter dans la carte.

- [ ] ODX14 — **Rapatrier la config de vente dans ventes.** Déplacer de compta vers `apps/ventes` (chez Odoo : Sales — quotation templates, pricelists, online quotes) : CodePromotion, ModeleDevis, SessionGuidedSelling, DemandeApprobationConfig, ECatalogue, DocumentProposition, SimulationPublique, SimulationFinancement, OffreFinancement, LigneIncitation, EcheancierPaiement, TranchePaiement (FG209–221) — recette state-only ODX9 (db_table figé, SeparateDatabaseAndState, shims compta), puis vues/urls sous `/api/django/ventes/…` avec anciennes routes compta conservées. Invariants : `/proposal` reste l'unique voie PDF devis (les DocumentProposition sont des ANNEXES du même moteur, pas un second chemin) ; l'e-catalogue n'expose JAMAIS `prix_achat` ; toute numérotation reste sur `apps/ventes/utils/references.py`. **Done =** données intactes, doubles URLs vertes, tests devis/PDF existants verts (pages counts inchangés), garde no-prix_achat testée, import-linter vert. Files: apps/ventes/ + migrations, apps/compta/{models,urls}.py + migrations, tests. (ARCH) (@lane: backend/ventes) (@after: ODX2) (@blocked: app-split state-only migration needs live makemigrations verification — no docker/DB in this lane, local Django 6.x incompatible with pinned 5.1.4; matches 2026-07-11 precedent 'ODX14/17-22 — à valider en DB')
- [ ] ODX18 — **App Facturation — étape 2 (vues/urls/recouvrement/frontend).** Déplacer vers `apps/facturation` les serializers/views/services du périmètre ODX17 + le recouvrement (relances, balance âgée, relevés clients, lettres de relance PDF, niveaux de relance) et l'export UBL/DGI facture : nouvelles routes `/api/django/facturation/…`, les routes historiques `/api/django/ventes/factures|paiements|avoirs|relances|balance-agee|niveaux-relance/…` RESTENT servies à l'identique (mêmes ViewSets inclus sous les deux préfixes) — aucun client cassé, `/api/django/public/document/{token}/` intouché. Le flux devis→facture reste orchestré par `apps/facturation/services.py` appelé depuis l'action ventes existante (frontière services). Frontend : `features/facturation/` + `module.config.jsx` (section « Facturation » : Factures, Avoirs, Encaissements, Recouvrement) extraite de la section VENTES — regroupement fonctionnel only. **Done =** doubles URLs byte-identiques (tests des deux préfixes), génération facture depuis devis verte, PDFs facture inchangés, nav montre Ventes et Facturation comme deux modules, e2e vertes. Files: apps/facturation/{views,serializers,services,urls}.py, apps/ventes/{views,urls}.py, frontend/src/features/facturation/, tests. (ARCH) (@lane: backend/facturation) (@after: ODX17) (@blocked: app-split state-only migration needs live makemigrations verification — no docker/DB in this lane, local Django 6.x incompatible with pinned 5.1.4; matches 2026-07-11 precedent 'ODX14/17-22 — à valider en DB')
- [ ] ODX20 — **App Achats — étape 2 (vues/urls/flux stock/frontend).** Déplacer les serializers/views/services/urls du périmètre ODX19 vers `apps/achats` : nouvelles routes `/api/django/achats/…`, routes historiques `/api/django/stock/bons-commande-fournisseur|receptions-fournisseur|factures-fournisseur|retours-fournisseur|prix-fournisseurs/…` conservées à l'identique. Les MOUVEMENTS DE STOCK à la réception/au retour passent désormais par une fonction de `apps/stock/services.py` (entrée/sortie + MouvementStock) appelée par achats — jamais d'import direct des modèles stock (hors shims transitoires) ; l'intégration compta (écritures fournisseurs) continue via `apps/compta/services.py`. Frontend : `features/achats/` + `module.config.jsx` (section « Achats » : Commandes fournisseur, Réceptions, Factures fournisseur, Retours, Import OCR) extraite de la section STOCK — fonctionnel only, hooks e2e préservés. **Done =** doubles URLs vertes, réception incrémente le stock à l'identique (test bout-en-bout), nav montre Stock et Achats séparés, import-linter vert, e2e vertes. Files: apps/achats/{views,serializers,services,urls}.py, apps/stock/{views,urls,services}.py, frontend/src/features/achats/, tests. (ARCH) (@lane: backend/achats) (@after: ODX19) (@blocked: app-split state-only migration needs live makemigrations verification — no docker/DB in this lane, local Django 6.x incompatible with pinned 5.1.4; matches 2026-07-11 precedent 'ODX14/17-22 — à valider en DB')
- [ ] ODX22 — **Étendre les contrats import-linter au graphe post-découpage.** Mettre à jour `backend/django_core/.importlinter` : ajouter les nouvelles apps (marketing, ao, portail, frais, facturation, achats) au contrat d'indépendance mutuelle des MODÈLES de domaine (string-FKs only, pin M1), garder `core-foundation-is-a-base-layer` intact, et ajouter un contrat « les apps satellites n'importent pas les views d'une autre app ». Retirer les shims de ré-export transitoires devenus sans référence (grep prouvant zéro appelant avant chaque retrait — étape par étape, révertable). **Done =** `lint-imports` vert avec les nouveaux contrats, chaque shim retiré n'a plus aucun référent (grep committé dans le message), tests complets verts. Files: backend/django_core/.importlinter, apps/{compta,ventes,stock}/models.py (retrait shims). (ROUTINE) (@lane: backend/core) (@after: ODX10, ODX18, ODX20) (@blocked: app-split state-only migration needs live makemigrations verification — no docker/DB in this lane, local Django 6.x incompatible with pinned 5.1.4; matches 2026-07-11 precedent 'ODX14/17-22 — à valider en DB')

Notes de non-duplication (vérifié dans docs/PLAN.md, docs/PLAN2.md, docs/DONE.md) :
- FG391 (ModuleToggle + feature_flags) est DÉJÀ construit — ODX2/3 n'ajoutent que le manifest, le catalogue et la fermeture de dépendances manquants.
- UX1–47 ont DÉJÀ livré le registre frontend `module.config.jsx` + les écrans des 10 modules récents — ODX7 ne fait qu'y raccorder les sections legacy hardcodées ; aucun écran n'est reconstruit.
- WR12 (flags métier dans Paramètres) et QC1/QC2 (autocomplete sociétés) sont distincts et non dupliqués.
- DC34 (refonte AP sous-traitant) interagit avec ODX19/20 — noté dans ODX19.
- La maintenance interne (Odoo Maintenance) existe déjà dans flotte (PlanEntretien/EcheanceEntretien/OrdreReparation) et outillage — aucun move nécessaire, documenté dans ODX1.

### Groupe XACC — Comptabilité & Finance (compta : TVA, trésorerie, budgets, immobilisations, multi-devise, fiscalité DGI)

Le socle compta est très complet (COMPTA1–40, FG107–153, DC20–DC30, écrans UX2–UX9, G14 gaté pour la télétransmission DGI) — les tâches ci-dessous couvrent UNIQUEMENT les vrais manques identifiés face au benchmark best-in-world (Odoo/SAP B1/BC/NetSuite/Sage 100 Maroc), du plus prioritaire au moins prioritaire.

- [ ] XACC12 — **Position fiscale des tiers (exonérations avec attestation).** Aucune substitution de taxe par tiers : ajouter une position fiscale sur Client/Fournisseur (assujetti par défaut / exonéré avec attestation n°+date de validité / hors champ / zone franche) qui substitue automatiquement le taux TVA (0 %) et les comptes à la création du devis/facture, avec mention légale d'exonération sur le document et attestation jointe (records). Très pertinent : clients agricoles (pompage solaire exonéré) et exports. **Done =** un client exonéré (attestation valide) produit une facture à TVA 0 % avec la mention, attestation expirée → warning et taux normal, la compta mappe sur les bons comptes via `MappingCompte`, tests. Files: apps/crm + apps/ventes (champ + application), apps/compta (mapping), migrations additives. (SCHEMA) (@lane: backend/ventes) (@blocked: money/GL path needs DB validation + founder decision — matches 2026-07-11 precedent 'XACC12/XPOS19/YCASH5/XPLT20 — validation DB + décision fondateur')

### Groupe XPOS — Point de vente comptoir & commerce client (accessoires solaires)

Combler l'écart « vente au comptoir » : aujourd'hui vendre un simple câble/disjoncteur au magasin exige la chaîne devis→BC→facture ; rien n'encaisse en 30 secondes, aucune grille tarifaire revendeur, pas de retour client re-stocké ni de n° série capturé à la vente comptoir. Tout réutilise l'existant (Facture classique sans devis, Paiement multi-modes, `compta.Caisse`/`ClotureCaisse` FG124, étiquettes QR N20, e-catalogue FG214, portail FG228, PaymentLink FG53, timbre fiscal FG144) — jamais de doublon.

- [ ] XPOS19 — **E-commerce transactionnel : checkout direct des petits articles (panier → paiement CMI → commande → expédition).** Aujourd'hui le parcours public s'arrête à « Demander un devis » (XPOS14 : e-catalogue FG214 → lead + devis brouillon) : aucun achat direct des petits articles vendables (MC4, coffrets, chauffe-eau). Côté ERP UNIQUEMENT (la vitrine/panier web = tâche WEB_PLAN séparée, à queuer là-bas) : modèle `CommandeEnLigne` + lignes dans `apps/ventes` (company résolue par le token du catalogue FG214, client rapproché/créé via `crm.services` — pattern `resolve_client_for_lead`, jamais de doublon ; seuls les produits marqués `vendable_en_ligne`, bool additif sur `stock.Produit`, avec stock disponible) ; paiement par la passerelle CMI EXISTANTE et gated (FG370/FG53 : `apps/ventes/payments/providers.py` `HostedGatewayProvider`/NoOp + `PaymentLink` — réutiliser `create_session`/`verify_webhook`, AUCUN nouveau code passerelle) ; à la confirmation vérifiée du webhook : `Facture` via `ventes.services` (référence via `utils/references.py`, jamais count()+1) + `Paiement` rapproché, décrément stock via `stock.services`, puis suivi d'expédition `a_preparer → expediee → livree` (n° de suivi transporteur libre) avec notifications client aux transitions ; sans clé CMI = endpoint checkout 404/no-op (le catalogue reste devis-only) ; jamais `prix_achat` dans aucune réponse publique. **Done =** un panier public payé (webhook mock) crée commande+facture+paiement+mouvement stock et traverse les 3 statuts d'expédition avec notifications, webhook non vérifié → aucune commande, sans clé → no-op, produit non `vendable_en_ligne` ou stock insuffisant refusé, tests multi-tenant + test d'absence de prix d'achat. Files: `apps/ventes/models.py` (+migration additive) + `public_views.py` + `services.py`, `apps/stock/models.py` (`vendable_en_ligne` +migration additive) + `services.py`, tests. (DECISION) (COST) (@lane: backend/ventes) (@after: XPOS14) (@blocked: money/GL path needs DB validation + founder decision — matches 2026-07-11 precedent 'XACC12/XPOS19/YCASH5/XPLT20 — validation DB + décision fondateur')

### Groupe XKB — Connaissance & collaboration interne (base de connaissances, chat d'équipe, approbations, annonces, to-dos)

Combler les vrais manques face à Odoo Knowledge/Discuss/Approvals, Notion, Confluence et Slack — en s'appuyant sur l'existant (apps/kb KB1–KB7, apps/chat Groupe S1–S20, apps/notifications N75/N92/BSP, records.Activity + « Mes activités », chatter FG7, approbations éparses contrats/GED/automation/achats, agent AG1–AG12) sans jamais le reconstruire.

- [BLOCKED: décision fondateur requise — construire (WebRTC auto-hébergé) vs s'en tenir aux memos vocaux + WhatsApp] XKB35 — **Appels audio/vidéo internes (huddles).** Appels voix/vidéo WebRTC depuis un DM/canal (partage d'écran, invitation par lien) — grosse brique d'infra temps réel du même ordre que S21 (signalisation, TURN/STUN, sessions collantes au déploiement) pour une petite équipe interne qui a déjà les memos vocaux transcrits et WhatsApp. À trancher par le fondateur : construire (auto-hébergé, pas de service payant par défaut) ou s'en tenir aux memos vocaux + WhatsApp. **Done =** décision fondateur documentée ; si oui, spec dédiée puis découpage en tâches. Files: n/a (décision). (DECISION) (@lane: backend/chat)

## BUILD QUEUE — Round 2 : câblage bout-en-bout, bonnes pratiques mondiales & parité profonde Odoo — audit 2026-07-03 (groupes Y* / Z*)

Provenance : 2ᵉ audit multi-agents (2026-07-03, ~90 agents sur Opus/Fable — 29 lanes recherche→audit→revue
adversariale + dédoublonnage inter-lanes + critique de complétude). Là où le round 1 (section ci-dessus,
ODX*/X*) faisait l'inventaire des **features** manquantes, ce round attaque ce qu'une checklist ne voit pas :
comment les processus sont **CÂBLÉS** de bout en bout, les **bonnes pratiques d'ingénierie** de niveau
mondial, et la parité Odoo à **grain fin** (rapports, assistants, réglages, actions planifiées, documents
imprimables). Chaque tâche cite le code RÉEL vérifié (modèle/service/événement/fichier). **336 tâches** =
les vrais manques après recoupement contre tout l'existant ET tous les plans (round 1 inclus). Mêmes règles :
additif / multi-tenant / frontières services-selectors / bus d'événements `core/events.py` / intégrations
key-gated OFF / `/proposal` intouché ; (DECISION)/(COST)/(AUTH)/(DEP) marquent les appels du fondateur.

**Trois axes + un durcissement :**
- **Axe A — Câblage bout-en-bout (Y\*, 10 processus)** : Lead-to-Cash, Procure-to-Pay, Install-to-Service,
  Record-to-Report (exhaustivité comptable), Hire-to-Retire, Intégrité des flux de stock, Machines d'états
  des documents, Marketing-to-Lead, Revenu récurrent, Couverture bus d'événements/notifications/approbations/audit.
- **Axe B — Bonnes pratiques mondiales (Y\*, 5 lanes)** : couverture RBAC (matrice endpoint×rôle + masquage
  champ-niveau), cohérence & complétude de l'API (OpenAPI/idempotence/webhooks), ops/perf/résilience
  (sauvegardes testées, N+1, Celery), stratégie de test (e2e par processus), patterns d'intégrité des données.
- **Axe C — Parité profonde Odoo (Z\*, 14 apps)** : le grain fin que le round 1 a manqué, app par app.
- **Durcissement plateforme & gouvernance IA (YHARD)** : les 11 écarts transverses trouvés par la critique
  de complétude (chiffrement au repos, i18n du contenu, reconstruction temporelle, secrets/rotation,
  observabilité/SLO, perf & a11y du front ERP, staging anonymisé, déploiement sans coupure, audit/rollback IA).


### — Axe A : câblage des processus bout-en-bout —

### Groupe YCASH — Câblage Lead-to-Cash

Le socle Lead-to-Cash est déjà remarquablement câblé de bout en bout via le bus `core/events.py` (M6) : la capture (webhook site, OCR FG106, simulateur, dédup QJ8, SLA première réponse FG28, round-robin XSAL11 planifié), le funnel STAGES.py (chatter LeadActivity, expiration/hygiène QJ5, scoring FG27/QJ6), le devis (générateur, révisions, variantes, `resolve_client_for_lead`), l'acceptation (`devis_accepted` → CRM avance le lead SIGNED, `installations` auto-crée le chantier ET réserve le stock `seed_reservations`), l'envoi (`devis_sent` → QUOTE_SENT), le refus (`devis_refused` → COLD/perdu), la réception (`_apply_reception_handover` FG70 → équipements au parc SAV + garantie), la facturation par tranches (échéancier avec cumul « déjà facturé », acompte/solde, avoirs, PV de livraison FG51), l'encaissement (FG53 `PaymentLink` → `Paiement` rapproché, liens CMI gated FG370), les relances cadencées (`relance_reminders` beat, reset sur solde U10), et la comptabilité GL (FG107-153 : auto-écritures, TVA, clôture de période, lettrage, FEC). Les situations de travaux BTP (XPRJ4), le blocage crédit dur (XFAC28), l'e-invoicing DGI sortant (XFAC29), la TVA sur encaissement (XACC1) et les écritures de stock (XACC6) sont déjà planifiés. Les écarts ci-dessous sont les rares hand-offs RÉELLEMENT non câblés que j'ai vérifiés dans le code — surtout deux systèmes de paiement/écriture parallèles jamais reliés à leur consommateur.





- [ ] YCASH5 — **Annulation d'une facture après acompte : réversion de l'acompte tracée mais AUCUNE contre-passation comptable ni ré-ouverture du chantier.** FG50 (fait) gère déjà, à l'annulation d'une facture, le transfert/remboursement de l'acompte (re-pointe le `Paiement` ou pose un `Paiement` négatif de contre-passation + chatter) — MAIS le blueprint « cancellation cascade » et « refund flow » exige que (a) l'écriture d'acompte (4191/avance) soit contre-passée au GL et (b) le chantier lié soit ré-ouvert/annulé avec motif. Vérifié : l'annulation `views/facture.py::annuler` ne poste aucune écriture (dépend de YCASH1) et n'émet aucun événement vers `installations` (le chantier auto-créé sur `devis_accepted` reste en l'état). Après YCASH1, abonner l'annulation de facture (nouvel événement `facture_annulee` sur `core/events.py`) côté compta (contre-passation idempotente de l'écriture d'acompte, avance 4191 soldée) ET côté installations (récepteur qui, si le chantier n'a pas démarré, le repasse en attente/annulé avec motif chatter — garde : jamais si des équipements sont déjà posés). **Done =** annuler une facture d'acompte réglée contre-passe l'écriture d'avance (équilibrée, idempotente, verrou de période respecté) et, si le chantier n'a pas démarré, le ré-ouvre avec un motif au chatter ; un chantier déjà en pose n'est pas touché ; toggle compta OFF = pas d'écriture ; tests. Files: `core/events.py`, `apps/ventes/views/facture.py` (émission), `apps/compta/receivers.py`, `apps/installations/receivers.py`. (ARCH) (@after: YLEDG1) (@lane: backend/ventes) (@blocked: money/GL path needs DB validation + founder decision — matches 2026-07-11 precedent 'XACC12/XPOS19/YCASH5/XPLT20 — validation DB + décision fondateur')

## BUILD QUEUE — App « Appel d'offres » : calepinage prouvé + dossier de dépôt — conception 2026-08-01 (Groupe AOF : AOF1-AOF194)

> **Provenance.** Distillation intégrale de la session AO FRDISI du 27/07/2026 (`docs/ao-frdisi/releve-2026-07-27/` : `dessin.py`, `solveur.py`, `calepinage.py`, `planche_05H_bat_C_ecole.py`, `planche_06H_residence_aile_L.py`, `planche_06I_implantation_residence_aile_arc.py`, `ENVOI_ACCORDIA_MAJ/`, brief de passation) + inventaire du réutilisable réel du dépôt + benchmark marché (PVcase, Aurora, HelioScope, PVsyst, OpenSolar, Solargis, archelios PRO, Virto, RatedPower, PVfarm ; Vecteur Plus / écosystème Libel / BidTender Maroc). **Le trou de marché est établi et c'est le produit :** aucun outil de calepinage solaire ne traite le formalisme administratif d'un AO public marocain (bordereau à prix unitaires, montant en lettres, acte d'engagement, caution), et aucun outil BTP de réponse aux AO n'a de capacité technique solaire. La couture entre les deux n'existe nulle part.
> **DÉCISION FONDATEUR 2026-08-01 — WIR166 EST ROUVERTE.** `frontend/src/features/ao/module.config.jsx` porte aujourd'hui une décision écrite « le module reste BACKEND-ONLY … requiert une confirmation explicite du fondateur », actée en WIR166 (`docs/PLAN.md:1820`). La demande du fondateur qui a produit ce groupe **EST** cette confirmation explicite : les écrans AO sont autorisés. **WIR166 reste cochée `[x]` et n'est PAS réécrite** (une tâche done ne se réouvre pas par édition) : la réouverture vit (i) dans cet en-tête, (ii) dans le commentaire daté de `module.config.jsx` réécrit par AOF7, (iii) dans une ligne DONE LOG. Tant qu'AOF7 n'est pas fait, chaque agent qui ouvre ce fichier lit une consigne qui lui interdit de travailler.
> **Les 3 portes d'entrée du plan de toiture (exigence fondateur), CUMULABLES et non exclusives :** (1) **plan fourni, complet ou à compléter** — PDF/image calibré à 2 points, ou DXF ; (2) **from scratch** — tracé au clavier et à la souris avec chaînes de cotes et fermetures topo ; (3) **lecteur de cartes existant** — reprise du contour via `bootCaptureOnly` de roofPro11 par l'alias Vite `@roofpro` (précédent établi : `/devis-design/:id`). Les trois produisent le MÊME objet `ToitureAO` et ouvrent le MÊME éditeur ; un plan importé se complète au tracé et un tracé reçoit un underlay a posteriori. Aucune porte n'a d'écran spécialisé, sinon on maintient trois éditeurs.
> **Le moteur est PARTAGÉ avec les villas (exigence fondateur).** Le paquet pur `core/calepinage/` (stdlib + numpy, zéro Django, zéro I/O, zéro globale mutable) a DEUX consommateurs qui ne peuvent pas s'importer l'un l'autre : `apps/ao` et `apps/ventes`. La villa est simplement un kit à `modules_par_table = 1` (720 Wc, 2,384×1,303, 13°) avec une politique de pas `AntiOmbrage` ; l'AO est un kit table dos-à-dos à 2 modules (PORTRAIT 1,134/4,70 · PAYSAGE 2,382/2,25 à 2×625 Wc) avec `AlleeFixe`. Le modèle AO d'obstacle est strictement plus riche que celui de la villa (rectangle + dégagement PAR obstacle + PROVENANCE) : **c'est LUI le format canonique**, la villa s'y adapte par adaptateur. `apps/web` n'est JAMAIS modifié ici (règle de run « web plan ») — le cerveau TypeScript reste intact et la bascule ERP se fait derrière un flag avec comparaison A/B.
> **CHOIX MAXIMUM + RECOMMANDATIONS (exigence fondateur).** Le moteur ne rend pas un plan : il rend une **Étude** (variantes retenue / alternative / sensibilité / marche d'échelle), un **faisceau de recommandations APPLIQUABLES à gain RECALCULÉ** (`patch_entree` rejoué par le moteur, jamais estimé) et une **question à poser au client** pré-remplie de son impact chiffré. L'atelier offre trois étages : barre de verdict permanente, tiroirs de paramètres avec impact chiffré, mode expert. Règle produit gravée : **ne JAMAIS publier l'allée minimale quand une allée large est gratuite** (bâtiment C : compte identique 314 de 0,60 à 1,94 m — 1,90 m de maintenance offerts).
> **QUALITÉ DOCUMENTAIRE NIVEAU FRDISI (exigence fondateur).** Un seul `contexte_dossier` calculé alimente les 9+ pièces ; aucun gabarit ne contient de chiffre littéral ; la **cascade de prix se calcule à l'envers** (coût de revient + bénéfice net visé → total HT/TTC → répartition sur les PU par homothétie + arrondi métier + ligne d'ajustement, invariant `Σ q×PU == cible` au centime) et propage aux **6 cibles déclarées** ; la **bascule d'équipement** est atomique (référence + prix + grandeurs dérivées recalculées + fiche annexée AJOUTÉE + ancienne RETIRÉE) ; la **sanitisation** est bloquante avant tout rendu client ; le **contrôleur de cohérence croisée** (~14 invariants) est une PORTE, pas un rapport ; les **montants en lettres** sont recalculés, jamais recopiés ni stockés.
> **L'ÉCONOMIE EST RÉSERVÉE AU DIRECTEUR (exigence fondateur).** `EconomieAO` / `LigneCoutRevient` / l'Excel de rentabilité vivent dans des tables et des endpoints SÉPARÉS derrière `ao_rentabilite_voir` placée dans `ELEVATED_PERMISSIONS` (non octroyable par un non-admin), mappée dans AUCUNE liste Responsable/Commercial/Technicien/Viewer. **Régression de confidentialité EXISTANTE à corriger d'abord :** les 8 ViewSets AO héritent aujourd'hui de `_ComptaBaseViewSet` → `IsResponsableOrAdmin`, donc tout le palier Responsable voit tout l'AO, et il n'existe AUCUNE permission `ao_*`. Y déposer un coût de revient avant AOF2/AOF3 exposerait la marge à toute l'équipe. La « simulation de rentabilité 25 ans » du pack est une pièce CLIENT distincte qui ne porte AUCUN coût — les fusionner « parce que ça parle de rentabilité » est le chemin le plus court vers la fuite.
> **Primitives plateforme JAMAIS recodées (règle de composition).** Chatter et pièces jointes = `records` (déjà branché sur `ao.appeloffre` par `apps/ao/platform.py`) — jamais une classe `*Activity`. Références = `core.numbering.create_with_reference` — jamais `count()+1`. Jobs lourds = `core.jobs.submit(kind, task, company=…, user=…)` + `BackgroundJob` — jamais une file maison. Documents métier = kit `core/documents.py` (**c'est un MODULE, un seul fichier — ne pas en faire un paquet**), réservé aux NOUVEAUX types. PDF HTML = `core.pdf.render_pdf` (ARC11) — jamais un import direct de WeasyPrint. Fusion PDF = `apps.ged.services.fusionner_pdf` en appel cross-app. Approbation = `core.models.WorkflowDefinition` (`core/models.py:364`) — jamais un moteur d'approbation local. Import de fichiers = `apps.dataimport.parsing.iter_rows` + `FIELD_MAPS`. Aucun NOUVEAU `FileField` (le garde `apps/records/platform_guards.py:114` gèle `apps/ao/models.py` à `{"fichier": 1}`) : tout artefact passe par `records.Attachment` / `ged.Document`. Un signal sans abonné réel = CI rouge (`core/event_coverage.py`) : **deux signaux, deux abonnés, point** (`ao_depose`, `ao_gagne`).
> **Contraintes héritées par CHAQUE tâche AOF :** FK `company` via `core.models.TenantModel` + `core.viewsets.CompanyScopedModelViewSet` (sinon `scripts/check_platform.py` casse la CI) ; cross-app via `selectors.py`/`services.py`/string-FK uniquement — le contrat import-linter `ao-models-decoupled` interdit déjà à `apps.ao.models` d'importer `apps.{crm,ventes,stock,sav,installations,compta}.models` ; **`AppelOffre.lead_id` reste un ENTIER OPAQUE — ne jamais le « réparer » en FK** ; les 8 modèles legacy gardent leurs `db_table = 'compta_*'` (migrations `SeparateDatabaseAndState` d'ODX11 — toute migration sur eux doit être state-only ou purement additive, un `AlterModelTable` régénéré renommerait une table en production) ; règle #4 — `/proposal` reste le SEUL chemin PDF devis client, la fabrique AO est un domaine NEUF sans aucun couplage à `quote_engine` (pas même en lecture des jetons visuels) ; `prix_achat`, coût de revient, marge et bénéfice ne sortent JAMAIS dans un rendu remis au maître d'ouvrage ; migrations additives et revertables, **groupées en 6-8 migrations** (`0002_tenantmodel`, `0003_projet`, `0004_geometrie`, `0005_calepinage`, `0006_equipements_bordereau`, `0007_economie`, `0008_documents`, `0009_administratif`) — chaque migration nouvelle force un cache-MISS du gate CI ; UI en français.
> **CONVENTION DE CHEMINS — délibérée, ne pas la « corriger ».** Tous les `Files:` backend de ce groupe sont écrits en forme COURTE et UNIFORME (`apps/ao/models.py`, `core/calepinage/optimum.py`, `templates/ao/…`), relative à `backend/django_core/`. La raison est mécanique : `plan_lanes.py` unionne les lanes par COMPARAISON DE CHAÎNES, donc mélanger `apps/ao/views.py` et `backend/django_core/apps/ao/views.py` dans le même groupe ferait passer deux tâches du MÊME fichier dans deux lanes sans union — exactement le défaut que la convention supprime. Les chemins frontend (`frontend/src/…`), docs (`docs/…`) et scripts (`scripts/…`) sont déjà relatifs à la racine et restent tels quels.
> **Ordre de build et gating.** Le préfixe `AOF` doit être déclaré dans `docs/BUILD_ORDER.yml` (AOF6) sous peine de préfixe orphelin rouge dans le job `stage-names`. Prérequis **vérifiés atteints** : `ARC-noyau` 56/56 = 100 % et `SCA` 49/49 = 100 % — **aucune lane AOF ne sera refusée par le gate SCA6**. L'alias réel du kit document est **`SCA-kit-document`** (et non « kit-document ») : `check_build_order.py` refuse un `after:` non résolvable. W2 (moteur pur) n'a AUCUN prérequis plateforme et peut démarrer en parallèle de W0/W1. **Composition :** toute tâche qui aurait besoin d'une primitive plateforme non encore sur `main` se marque `[BLOCKED: attend <ID>]` — jamais de substitut local (le #1 poste de dette mesuré du dépôt : 13 chatters hand-rollés).
> **Plan de lanes RÉEL (mesuré sur les `Files:` déclarés, après auto-union de `plan_lanes.py`) : 15 groupes, pas 12.** `backend/ao` et `backend/ao-fabrique` écrivent tous deux `apps/ao/models.py`, `services.py`, `serializers.py`, `views.py`, `urls.py`, `tasks.py` : ils **fusionnent nécessairement** en UNE lane séquentielle de ~88 tâches — et c'est la bonne réponse, pas un défaut, car **la chaîne de migrations `apps/ao` est mono-écrivain de toute façon**. C'est LA lane critique. La vraie seconde lane est `backend/core-calepinage` (~31 tâches, **zéro fichier partagé avec le backend AO — vérifié**, aucun prérequis plateforme) : **elle démarre EN MÊME TEMPS, dès la première heure.** Les 13 autres : `backend/core-calepinage-rendu` (8, file-disjointe du solveur, ne dépend que du squelette de feuille), `frontend/ao-toiture` (14), `frontend/ao-calepinage` (10), `frontend/ao-socle` (9), `frontend/ao-dossier` (9), `frontend/ao-qualite` (6), `frontend/ao-studio` (5), `frontend/ao-variantes` (4), `backend/core-foundation` (3), `gov/build-order` (3), `frontend/ao-qr` (2), `frontend/ao-directeur` (1), `backend/bascule-villa` (1). **`frontend/ao-socle`, `frontend/ao-studio` et `backend/core-calepinage` partent en premier**, sinon six agents attendent le même canvas et le même contrat JSON.
> **Les deux grandes lanes dépassent DÉLIBÉRÉMENT le seuil de `plan_lanes.py`.** L'avertissement « Oversized lanes (>15 tasks — consider splitting via @lane: tags) » est ATTENDU sur `backend/ao(+fabrique)` (~88) et `backend/core-calepinage` (~31) : la première parce que sa chaîne de migrations est mono-écrivain, la seconde parce que ses dépendances internes sont strictement séquentielles (types → surfaces → moteur → optimum → garde-fous). **Ce n'est pas un défaut à corriger par un split artificiel** — chaque run redécouvrirait sinon la même question.
> **Notes de non-duplication.** **Ne pas créer d'app** : `apps/ao` existe, est routée (`/api/django/ao/`), porte son `module_manifest`, son `platform.py` et son contrat import-linter — le travail est de la faire grandir. **ODX22 (`docs/PLAN.md:414`) reste OUVERT, conserve son marqueur de blocage, et n'est PAS atterri par ce groupe** : ODX22 est « étendre les contrats import-linter au graphe post-découpage + RETIRER les shims de ré-export transitoires » — AOF1 fait l'inverse (il ajoute un shim de ré-export dans `compta`) et exécute seulement le relogement annoncé par le docstring d'`apps/ao/views.py` (« ODX22 re-logera le corps ici ») ; AOF1 annote ODX22 pour signaler que le shim compta↔ao a été INVERSÉ, à prendre en compte le jour de son déblocage. **WIR166 reste `[x]`** (réouverture actée en en-tête + `module.config.jsx` + DONE LOG). **Six tâches NTMAR de `docs/plans/PLAN_FINANCE.md` visent `apps/ao` et « reviennent au run plateforme » (leur propre DONE LOG) = CE run :** NTMAR22 (`:655`, bordereau/DQE avec `LigneBordereauPrix`) est **superseded par AOF120/127/128** — ne créer AUCUN `LigneBordereauPrix`, `LigneBordereau` est étendu ; NTMAR27 (`:660`, tableau de bord marchés) est **superseded par AOF166** qui reprend son nom d'endpoint `tableau-marches` et ses KPI ; NTMAR21 (`:654`, import CSV des avis) est **absorbé par AOF169** ; NTMAR23/25/26 (`:656/:658/:659` — révision de prix, ordres de service et décomptes, pénalités) restent OUVERTS et hors périmètre AOF, **mais ils écrivent dans `apps/ao/models.py` : la chaîne de migrations `apps/ao` est MONO-ÉCRIVAIN, ils ne se construisent JAMAIS en parallèle de ce run.** **NTASS19 (`:710`) est déjà `[x]`** : `ExigenceAssuranceMarche` vit dans `apps/assurances` et possède les exigences d'assurance — AOF14 ne les modélise PAS, il s'y réfère par `marche_ref`. **Ne pas confondre** avec `NTOBS17` (export PDF du trust center *pour* un dossier d'AO) ni `NTP2P41` (export XLSX des offres RFQ) : là on ÉMET un appel d'offres, ici on y RÉPOND — ce ne sont pas des doublons et il ne faut pas les fusionner lors d'un futur audit. `core.numbering` / `records` / `core.jobs` / `core/documents.py` / `core.pdf` / `core.models.WorkflowDefinition` / `ged.fusionner_pdf` / `dataimport.parsing` / `stock.selectors` sont CONSOMMÉS, jamais réimplémentés. Le moteur TypeScript d'`apps/web` (`roofPro2.ts`, `estimatorBrainV2.ts`, `roofPro11/`) reste intact — toute convergence côté site public sera une tâche du **web plan**, jamais d'ici. Aucune dépendance nouvelle n'est requise pour le cœur : **matplotlib 3.9.2, numpy 1.26.4, PyMuPDF 1.28.0, openpyxl 3.1.5, weasyprint, celery, boto3 sont déjà en production** ; quatre tâches seulement sont `[GATED]` sur une décision ou une dépendance (`ezdxf` pour le DXF en import et en export, `python-docx` pour la checklist partenaire éditable, l'export bancable PVsyst).

## REFINEMENT QUEUE — existing-feature polish (audit 2026-06-18)

Atomic refinements of **already-shipped** features, found by reading the real frontend + backend
source surface by surface (not the docs). Each line is one focused session in the format
`[ ] [<MODULE>] [L<lens#>] [<GATE>] <imperative>. Fait = <observable acceptance>`. **Build order:**
drain the existing BUILD QUEUE first (T1–T17, then the N/F tasks), **then** work this queue
top-down — build the **ROUTINE** tasks unattended and **skip + report** every gated task
(`SCHEMA`/`DEP`/`DECISION`/`GALLERY`); the gated ones are mirrored into the GATED section below so an
unattended run never auto-builds them. Every task obeys the STANDING RULES (French UI, additive only,
company-scoped, `STAGES.py` contract untouched, buy prices never client-facing, DEBUG untouched,
client-facing PDFs are GALLERY-gated).

### CRM / Leads

#### Fiche & formulaire

#### Funnel / étapes & cycle de vie

#### Vues (kanban / liste / calendrier / graphique)

#### Filtres / recherche / findability

#### Actions en masse (bulk)

#### Doublons

#### Activités / Mes activités / rappels

#### Gagné (Won)

#### Mobile / ergonomie

#### Cohérence cross-module / export / localisation
[x] [CRM/Leads] [L17] [ROUTINE] Capturer/afficher la langue de communication préférée du lead (FR/Darija) pour préparer l'envoi WhatsApp bilingue (whatsapp_devis prend déjà un paramètre langue, mais rien ne le mémorise sur le lead). Fait = un champ "Langue préférée" (nullable FR/Darija) sur la fiche pré-sélectionne la langue du message WhatsApp. (UNGATED 2026-06-20 — migration additive nullable, FK société forcée serveur.) [cf. N93] [DONE 2026-06-20: champ nullable `Lead.langue_preferee` (crm 0019), exposé au serializer, `whatsapp_devis` retombe sur la langue préférée du lead quand aucune n'est passée, sélecteur sur LeadForm + pré-sélection dans l'envoi WhatsApp, 4 tests.]

### Clients

### Devis (génération & PDF)

### Facturation
[x] [Facturation/Relances] [L13] [ROUTINE] Adapter la lettre de relance PDF au niveau courant (ton J+7 doux → J+30 ferme) en variant le corps depuis FollowupLevel.message. Fait = generate_lettre_relance_pdf rend un texte distinct par niveau, sans changer la mise en page premium. (UNGATED 2026-06-20 — approuvé par Reda ; construire sur le rendu de relance existant (N106), NE PAS toucher generate_devis_premium.py.) [DONE 2026-06-20: `render_lettre_relance_pdf`/`build_lettre_relance_html` acceptent un `message` optionnel ; sans message ils résolvent le `FollowupLevel.message` par niveau (1/2/3 → seuils croissants), sinon retombent sur le corps par défaut (rétro-compatible). Mise en page premium inchangée. 3 tests ajoutés ; generate_devis_premium.py non touché.]

### Chantiers / Installations
[x] [Chantiers] [L4] [ROUTINE] Ajouter un aperçu in-app des PDF client après-vente (PV de réception, bon de livraison, dossier de remise, attestation) avant téléchargement dans InstallationDetail.jsx. Fait = chaque bouton "Documents après-vente" ouvre un aperçu PDF inline au lieu de seulement downloadBlob, sans changer le contenu du PDF généré. (UNGATED 2026-06-20 — approuvé par Reda ; aperçu seulement, le PDF généré est inchangé.) [DONE 2026-06-20: les 5 boutons après-vente (PV de réception, bon de livraison, dossier de remise, fiche de remise, attestation) ouvrent un aperçu inline (réutilise PdfCanvas + previewPdf.js comme LeadDevisPanel) avec un bouton "Télécharger" ; octets PDF et endpoints inchangés.]

#### Équipement

#### Checklist

#### Photos

#### Planning

#### Parc installé

#### Lifecycle / data quality / traçabilité

### Parc & Équipements (registre SAV)

### SAV

### Maintenance

### Stock & Achats

#### Catalogue — liste & form

#### Catégories & marques

#### Fournisseurs

#### Mouvements & low-stock

#### Multi-emplacements & transferts

#### Inventaire

#### Bons de commande fournisseur

#### Listes de prix fournisseur (multi-fournisseur)

#### Valorisation

#### Retours fournisseur

#### OCR import

#### Chatbot stock (agent IA / SQL)

#### Mobile & cohérence

### Paramètres & Référentiels

### Rôles & RBAC

### Champs personnalisés

### Reporting & Tableaux de bord

### Recherche & Notifications

### WhatsApp & Liens publics

### Documents & Archives

### Transversal (mobile, états, traçabilité)

## Groupe PACT — Le pacte front↔back : rendre la rupture entre l'écran et le serveur IMPOSSIBLE, puis rattraper le passif

> **Pourquoi ce groupe existe.** Le 03/08/2026 l'écran « Appels d'offres — Tableau de bord » plante en
> production. Cause exacte : le serveur renvoie `echeances_dues` en **nombre** et l'écran fait `.map()`
> dessus ; il renvoie `marches_en_execution` en **objet** et l'écran le rend comme un enfant React ; et
> 4 autres champs lus n'existent nulle part. **Zéro clé sur six ne concordait.** Les deux suites de tests
> étaient VERTES et se contredisaient : le test frontend mockait l'inverse exact de ce que le test backend
> affirmait. Chacun testait sa propre hypothèse ; personne ne testait le lien.
>
> **Ce n'est pas une négligence d'agent, c'est un défaut de structure.** `scripts/plan_lanes.py` force les
> lanes à être disjointes en fichiers — c'est ce qui permet à 8 agents de travailler sans conflit. Or un
> contrat front↔back ne partage **aucun fichier par construction** (`backend/…/urls.py` d'un côté,
> `frontend/src/api/*.js` de l'autre). **La règle qui protège des conflits de fusion garantit mécaniquement
> que les deux moitiés d'un contrat travaillent en aveugle l'une de l'autre.** L'en-tête de
> `frontend/src/api/aoApi.js` le dit en toutes lettres : « ce fichier PUBLIE le contrat que le backend
> enregistre ensuite » — une obligation adressée à une lane parallèle qui ne l'a jamais reçue. Personne
> n'a menti ; l'obligation n'avait aucun porteur.
>
> **C'est au moins le 4ᵉ rattrapage front↔back en un mois** : 2026-07-03 « Round 2 : câblage bout-en-bout »
> (8 groupes) · 2026-07-06 `docs/FRONTEND_GAP_PLAN.md` 147 tâches, **dont 145 sont encore ouvertes** ·
> 2026-07-18 groupe WIR, 443 constats consolidés en 168 tâches, 168/168 cochées · 2026-08-03 l'incident
> actuel. Pire : **WIR166 avait regardé ce module précisément** et l'avait refermé « hors périmètre » le
> 19/07 ; le groupe AOF a rouvert la décision quinze jours plus tard et livré 8 ressources fantômes plus un
> tableau de bord qui plante. **Chaque rattrapage a vidé un stock ; aucun n'a fermé le robinet.**
>
> **D'où l'ordre de ce groupe, et il n'est pas négociable :** les gardes (§A) et la méthode (§B) d'abord —
> elles gèlent le présent —, le passif vérifié (§C) ensuite, le backlog des écrans manquants (§E) en
> dernier. Drainer §E sans §A et §B serait le 5ᵉ rattrapage, et il récidiverait en deux semaines.
>
> **Les chiffres sont calibrés, pas bruts.** La première mesure annonçait « 470 routes sans écran » : elle
> était **fausse à 51 %** (239 faux positifs — le script ne lisait que `frontend/src/api/*.js` et ignorait
> 1 137 autres fichiers, ne résolvait ni les fabriques `makeResourceFactory`/`crud()`, ni les constantes de
> base `const P = '/gestion-projet'`, ni les répartiteurs génériques ; et 139 ressources étaient comptées
> deux fois parce que `apps/compta/urls.py` les remonte sous un second préfixe). **Chiffre à retenir et à
> citer : sur 1 045 ressources backend distinctes, 809 (77 %) ont un écran, 30 sont techniques par design,
> et 206 (599 endpoints, 31 apps) sont du backend construit que personne ne voit.** Ne plus jamais citer
> le 470.

### PACT §A — Les gardes permanentes : ce qui rend la récidive impossible (PACT1-PACT9)

- [x] PACT5 — **Élargir la garde de contrat aux appels axios écrits DIRECTEMENT dans un écran.** Constat : la garde 1 ne couvre aujourd'hui que les clients API (`frontend/src/api/*.js` + les 5 `*Api.js` vivant dans `features/`, dont `adsengineApi.js`). Un `api.post('/rh/pointages/export-paie/')` écrit dans le corps d'un composant lui échappe — et c'est précisément la forme de deux défauts avérés (`features/rh/Temps.jsx`, `features/ao/equipements`). L'élargissement à tout `frontend/src` est mécanique (une constante), **mais il exige une passe de vérification des faux positifs qui n'a pas été menée** : elle n'a délibérément pas été faite à l'aveugle, parce qu'une garde qui crie au loup finit désactivée et la récidive serait la cinquième. Cette tâche fait cette passe : élargir, lister chaque nouveau rouge, statuer un par un (vrai défaut → corriger ; faux positif → mécanisme de résolution à ajouter au script, jamais une ligne d'exception). **Done =** périmètre élargi à tout `frontend/src`, zéro faux positif restant, chaque mécanisme de résolution ajouté est couvert par un test unitaire du script. Files: `scripts/check_api_contract.py`, `scripts/tests/test_check_api_contract.py`, `scripts/api_contract_allow.txt`. (ARCH) (@lane: gardes) (@model: opus) (@after: PACT1)
- [x] PACT6 — **Rendre BLOQUANTE la dérive de l'instantané OpenAPI.** Constat mesuré : `scripts/check_openapi_schema.py` déclare lui-même que la comparaison à `docs/openapi-schema.yml` est « advisory, never fails ». Résultat : l'instantané a été régénéré pour la dernière fois le 31/07, **avant** la fusion du module Appels d'offres du 02/08 — **un module entier est entré en production sans que le fichier de contrat bouge d'une ligne** (29 ressources présentes dans le code en sont absentes, dont 21 dans `apps/ao`). Tant que l'instantané est en retard, aucune garde front↔back ne peut s'appuyer dessus. Faire échouer le contrôle quand le schéma généré diffère de l'instantané versionné, et documenter la commande de régénération dans le message d'échec lui-même. **Done =** une PR qui ajoute une route sans régénérer l'instantané est rouge, avec le nom des routes manquantes dans le message ; l'instantané committé est à jour du code au moment du merge. Files: `scripts/check_openapi_schema.py`, `docs/openapi-schema.yml`. (ARCH) (@lane: gardes) (@model: opus)
- [x] PACT7 — **Interdire `responses={200: OpenApiResponse(response=dict)}` sur les endpoints agrégés.** Constat qui disqualifie la solution évidente : **le schéma OpenAPI n'aurait rien vu de l'incident.** `GET /ao/tableau-marches/` y est documenté « type: object » **sans aucune propriété**, parce que la vue déclare `response=dict` (`apps/ao/kpis.py`). Pire, le schéma **ment** sur certains agrégats : `/flotte/vehicules/tableau-bord/` est documenté avec le `VehiculeSerializer` alors que la vue renvoie `{vehicules, engins, echeances, couts, entretien, pool}` — un garde-fou naïf produirait des ROUGES sur du code CORRECT. La génération émet 2 241 avertissements dont **406 « unable to guess serializer »**, et ce sont exactement les vues-fonctions et les `@action` agrégées, c'est-à-dire les tableaux de bord, c'est-à-dire la classe qui plante. Exiger un sérialiseur réel (ou un sérialiseur en ligne dans `@extend_schema`) sur tout endpoint agrégé, avec une base de référence qui ne peut que rétrécir. **Done =** aucune nouvelle vue ne peut déclarer `response=dict` ; le compteur d'« unable to guess serializer » est gelé puis décroissant ; les deux agrégats menteurs (flotte, litiges) sont corrigés en premier. **C'est le seul geste qui protège aussi les fonctionnalités FUTURES** : une fois la forme déclarée, lire un champ fantôme devient impossible à écrire. Files: `scripts/check_openapi_shapes.py`, `backend/django_core/apps/ao/kpis.py`, `backend/django_core/apps/flotte/views.py`, `backend/django_core/apps/litiges/views.py`. (ARCH) (@lane: gardes) (@model: opus)
- [x] PACT8 — **Le meilleur rapport valeur/coût du groupe : une spec e2e « fumée des écrans » qui visite TOUS les écrans.** Constat : `.github/workflows/ci.yml` affirme « the e2e browser suite is the cross-surface net », mais au merge le job `e2e` ne lance que `devis.spec.js` et `health.spec.js`. Les 4 specs du module AO ne tournent que dans `release-verify.yml`, déclenché à la main ou la nuit, et dont l'en-tête précise « ne garde PAS main ». Pire : `frontend/e2e/helpers.js` **déclare** `AO_ROUTES.dashboard = '/ao'` et **aucune spec ne le visite** — l'écran qui a planté n'est ouvert par aucun test, nulle part. Écrire UNE spec qui se connecte, visite **chaque route déclarée par les 44 `module.config.jsx`** (317 routes aujourd'hui, lues automatiquement : aucune liste à tenir à jour) et échoue sur toute erreur console non rattrapée ou tout ErrorBoundary affiché. **Done =** la spec est dans la ligne `playwright test` qui tourne DÉJÀ à chaque merge sur un vrai Django et une vraie base ; elle attrape d'un coup, sur tout l'ERP, `.map is not a function`, « objects are not valid as a React child » et le `#undefined` déjà corrigé deux fois à la main le 01/08 ; **elle couvre les fonctionnalités DÉJÀ livrées sans écrire une seule tâche de rattrapage**. L'infrastructure existe et tourne : il manque une spec et une ligne. Files: `frontend/e2e/fumee-ecrans.spec.js`, `frontend/e2e/helpers.js`, `.github/workflows/ci.yml`. (ARCH) (@lane: gardes) (@model: opus)
- [x] PACT9 — **Garde écran → champ : attraper l'écran qui lit un champ fantôme même SANS test.** Constat : la garde 2 contrôle les **mocks**, donc un écran qui lit un champ inexistant et n'a pas de test reste invisible — c'est le cas des 4 KPI muets de `features/rh/Recrutement.jsx` et des 4 lignes vides de `features/flotte/VehiculeDetail.jsx`, qui n'affichent **aucune erreur**, juste des tirets pour toujours. Un balayage a trouvé 70 fichiers d'écran sur 881 lisant au moins un champ `snake_case` absent du backend ; vérification manuelle sur 10 tirages : **40 à 50 % de vrais défauts**, soit 28 à 35 écrans réellement fautifs. **Ce taux interdit d'en faire une garde en l'état.** La condition pour qu'elle devienne fiable est écrite et mesurée : **apparier par ENDPOINT, jamais par nom de champ.** Le détecteur qui suit écran → fonction API → URL → vue → dictionnaire retrouve les DEUX plantages de l'AO sans faux positif ; le même détecteur apparié par nom de champ seul tombe sous 10 % de précision (3 faux positifs vérifiés sur POS, paie et flotte : homonymies entre deux endpoints). **Done =** la garde suit la chaîne complète écran → endpoint, zéro faux positif sur les 3 cas d'homonymie connus, et les deux plantages de l'AO sont retrouvés depuis l'écran (pas depuis son test). Ne pas livrer cette tâche tant que le zéro faux positif n'est pas atteint : une garde bruyante sera désactivée. Files: `scripts/check_api_shapes.py`, `scripts/tests/test_check_api_shapes.py`. (ARCH) (@lane: gardes) (@model: opus) (@after: PACT2)

### PACT §B — La méthode : ce qui change dans la façon de construire (PACT10-PACT14)

- [x] PACT10 — **Le contrat part EN PREMIER et atterrit SEUL sur `main`, avant que les deux lanes démarrent.** C'est le renversement qui ferme le robinet. Aujourd'hui la lane frontend **écrit** le contrat et décrète que le backend l'implémentera (en-tête d'`aoApi.js`) : c'est la faille elle-même. Nouvelle règle : dès qu'une fonctionnalité a une moitié front et une moitié back, **une tâche de contrat part la première et atterrit seule sur `main`**. Elle contient les noms de routes **et un exemple de réponse JSON par endpoint agrégé**, committé dans l'app (`apps/<x>/contract_samples/`). Les deux lanes branchent ensuite depuis un `main` qui le contient déjà. Le test backend affirme que sa vraie réponse **égale** l'exemple ; le test frontend **importe** cet exemple au lieu d'inventer son `PAYLOAD`. Si le backend change la forme, l'exemple change, et le test frontend casse tout seul. **Done =** le lien manquant est rétabli par un fichier partagé, sans réunion et sans discipline humaine ; un module pilote (le prochain construit) livre son contrat d'abord et ses deux lanes ensuite. Files: `backend/django_core/apps/*/contract_samples/`, `CLAUDE.md`, `scripts/check_api_shapes.py`. (ARCH) (@lane: methode) (@model: opus)
- [x] PACT11 — **`plan_lanes.py` refuse une lane frontend dont la source de données est dans une autre lane sans `@after` déclaré.** Constat, ligne à ligne : la tâche de l'écran du tableau de bord AO (AOF172) déclarait dépendre de la tâche de la **liste** des affaires (AOF170) — et **PAS** de AOF166, la tâche backend qui produit exactement les données qu'il affiche. `scripts/plan_lanes.py` sait déjà lire ces déclarations de dépendance. La dépendance existait dans la réalité, elle n'a jamais été écrite, donc les deux tâches ont été planifiées **simultanément**. Cette seule vérification aurait bloqué le départ en parallèle. **Done =** une tâche dont la ligne `Files:` cite `frontend/src/api/` ou un écran, et dont le run contient une tâche backend touchant l'`urls.py` ou les `selectors.py` de la même app, est refusée avec un motif en français tant qu'elle ne porte pas `@after` sur elle ; le refus est testé sur le cas réel AOF172/AOF166. Files: `scripts/plan_lanes.py`, `scripts/tests/test_plan_lanes.py`. (ARCH) (@lane: methode) (@model: opus)
- [x] PACT12 — **Une tâche dont `Files:` cite à la fois `backend/` et `frontend/` compte pour DEUX moitiés : si une seule est livrée, elle reste `[ ]`.** C'est la règle qui manquait quand `docs/FRONTEND_GAP_PLAN.md` a constaté, en juillet, que « the paired frontend half of each task was never built, **EVEN THOUGH** most tasks named a `frontend/` file in their own `Files:` line ». La ligne `Files:` **annonçait déjà les deux moitiés** — et rien, ni CI ni harnais, ne vérifiait qu'elles avaient été touchées toutes les deux. **Done =** un contrôle refuse de cocher `[x]` une tâche mixte dont le diff ne touche qu'un côté, en nommant le côté manquant ; vérifié sur un échantillon des 145 tâches encore ouvertes de `FRONTEND_GAP_PLAN.md`. Files: `scripts/check_plan_taches_mixtes.py`, `scripts/tests/test_check_plan_taches_mixtes.py`, `.github/workflows/ci.yml`. (ARCH) (@lane: methode) (@model: opus)
- [x] PACT13 — **Interdire les mocks de forme écrits à la main sur les endpoints agrégés.** C'est la cause racine **prouvée** : le test vitest de l'AO mockait l'inverse exact du backend et restait vert. Pour tout endpoint agrégé, la charge utile d'un test frontend doit provenir d'une **fixture dérivée du contrat** (l'exemple de PACT10 ou le contrat de PACT2), jamais d'un littéral tapé dans le fichier de test. **Done =** la garde de forme refuse un `mockResolvedValue` littéral sur un endpoint sous contrat et indique la fixture à importer à la place ; les tests existants sont migrés app par app, la base de référence ne pouvant que rétrécir. Files: `scripts/check_api_shapes.py`, `frontend/src/test/fixtures/`, `scripts/api_shapes_allow.txt`. (ARCH) (@lane: methode) (@model: opus) (@after: PACT10)
- [x] PACT14 — **Écrire les deux règles dans `CLAUDE.md`, à l'endroit où un run les lit.** Les gardes empêchent la récidive après coup ; ces deux lignes l'empêchent avant. (a) Toute fonctionnalité à deux moitiés livre son **contrat d'abord**, seul, sur `main`. (b) Une tâche frontend qui consomme les données d'une tâche backend du même run **doit** porter `@after` sur elle. Les deux sont appliquées par la machine (PACT10, PACT11) — le texte de `CLAUDE.md` dit **pourquoi**, pour qu'une session future ne les défasse pas en croyant simplifier. **Done =** les deux règles figurent dans la section « How a plan run works », datées et rattachées à l'incident du 03/08, en deux lignes chacune maximum. Files: `CLAUDE.md`. (DECISION) (@lane: methode) (@model: opus)

### PACT §C — Le passif vérifié : les défauts réellement constatés, du plus grave au plus bénin (PACT15-PACT27)

- [x] PACT17 — **Écran entier mort : Ventes → « Action requise ».** `frontend/src/pages/ventes/DevisActionBoardPage.jsx` appelle `GET /ventes/devis/action-requise/` **qui n'existe pas**, et lit `r.data.buckets` d'une réponse qui n'arrivera jamais. L'entrée de menu est pourtant **publiée aux rôles responsable et admin** (`features/ventes/module.config.jsx`) : un utilisateur peut cliquer dessus aujourd'hui. Et `frontend/src/components/layout/Sidebar.WIR23.test.jsx:52` **épingle** le fait que le lien pointe bien vers `/ventes/devis/action-requise` — un test vert garantit donc l'existence du **lien** vers un écran mort. Trou de type **(c) — le backend n'a jamais été construit** : il faut écrire l'agrégat côté serveur (les devis nécessitant une action, groupés par motif), pas renommer un appel. **Done =** l'endpoint existe avec un sérialiseur déclaré (pas `response=dict`, cf. PACT7), l'écran l'affiche, et l'entrée de menu ne mène plus à un écran vide. Files: `backend/django_core/apps/ventes/selectors.py`, `backend/django_core/apps/ventes/views.py`, `backend/django_core/apps/ventes/urls.py`, `frontend/src/pages/ventes/DevisActionBoardPage.jsx`. (ROUTINE) (@lane: correctifs) (@model: sonnet)
- [x] PACT18 — **Un seul caractère tue un onglet : Comptabilité → États → « Grand-livre ».** `frontend/src/api/comptaApi.js:46` appelle `/compta/etats/grand-livre/` avec un **tiret** ; la route réelle est `/compta/etats/grand_livre/` avec un **souligné**, parce que `def grand_livre` (`apps/compta/views.py:410`) est **la seule `@action` de ce ViewSet à ne pas déclarer d'`url_path`** — ses vingt voisines en déclarent une (`balance-referentiel`, `cockpit-cloture`…), elle non, donc son chemin retombe sur le nom de la méthode Python, souligné compris. Trou de type **(b) — désaccord de nom**, dans une app **ancienne** : le défaut n'est pas propre au module AO, il traîne depuis longtemps sans que personne ne l'ait vu. **Done =** l'onglet affiche le grand-livre ; la garde de contrat (PACT1) couvre déjà cette classe et resterait rouge si le tiret revenait. Files: `frontend/src/api/comptaApi.js`. (ROUTINE) (@lane: correctifs) (@model: haiku)
- [x] PACT19 — **RH → Temps : deux actions en 404.** `features/rh/Temps.jsx` appelle `/rh/pointages/export-paie/` et `/rh/pointages/calendrier-equipe/`. Statuer d'abord lequel des trois cas s'applique en lisant `apps/rh/views.py` — **(b)** si l'action existe sous un autre `url_path`, **(c)** si elle n'a jamais été écrite — et le dire dans le commit. Ces deux appels sont écrits **directement dans l'écran**, donc invisibles à la garde 1 tant que PACT5 n'est pas fait : c'est un exemple concret de son utilité. **Done =** les deux boutons font ce qu'ils annoncent, ou l'action est retirée de l'écran si la fonction n'a pas de sens côté serveur — jamais un bouton qui échoue en silence. Files: `frontend/src/features/rh/Temps.jsx`, `backend/django_core/apps/rh/views.py`. (ROUTINE) (@lane: correctifs) (@model: sonnet)
- [x] PACT20 — **RH → Recrutement : 4 tuiles affichent « — » pour toujours.** L'écran lit `delai_embauche_moyen`, `total_candidatures`, `total_embauches`, `ouvertures_actives` ; `apps/rh/selectors.py` renvoie `delai_embauche_moyen_jours`, `entonnoir`, `candidatures_par_ouverture`, `sources`. **Aucune erreur, aucune alerte** : juste quatre tirets que personne ne remarque. Trou de type **(b)** pour un champ (le suffixe `_jours`) et de type **(a')** pour les trois autres : la donnée existe mais sous une forme différente (un entonnoir, pas un total) — donc l'écran doit être **repensé sur ce que le serveur sait dire**, pas forcé à afficher un chiffre qu'il n'a pas. **Done =** les 4 tuiles affichent une valeur réelle ; le test importe la forme du serveur (PACT13) au lieu de la réinventer. Files: `frontend/src/features/rh/Recrutement.jsx`, `frontend/src/features/rh/Recrutement.test.jsx`. (ROUTINE) (@lane: correctifs) (@model: sonnet)
- [x] PACT21 — **Flotte → fiche véhicule : le bloc éco-conduite est muet par quasi-homonymie.** L'écran lit `eco_score` / `co2_total_kg` / `consommation_moyenne` / `valeur_acquisition` ; `apps/flotte/selectors.py` renvoie `score_eco` / `co2_kg` / `conso_l_100km` et **rien** pour `valeur_acquisition`. Trois inversions de mots et un champ qui n'existe pas. Trou de type **(b)** pour trois, **(c)** pour le quatrième : décider franchement si la valeur d'acquisition doit être ajoutée au sélecteur ou la ligne retirée de l'écran. **Done =** les 4 lignes affichent une valeur ou disparaissent ; aucune ligne ne reste vide sans explication. Files: `frontend/src/features/flotte/VehiculeDetail.jsx`, `backend/django_core/apps/flotte/selectors.py`. (ROUTINE) (@lane: correctifs) (@model: sonnet)
- [x] PACT22 — **Gestion de projet → Ressources : un chiffre FAUX est affiché comme vrai.** L'écran lit `res.data?.nb_appliquees ?? nb` ; `services.auto_affecter` renvoie `{simule, deplacements, creations, non_resolues}`. **Le message de confirmation annonce donc le nombre SIMULÉ comme s'il avait été appliqué.** Bénin techniquement, grave en confiance : l'utilisateur croit qu'une action a eu lieu. **Done =** le message distingue explicitement une simulation d'une application, et nomme le nombre de cas non résolus quand il y en a. Files: `frontend/src/features/gestion_projet/pages/RessourcesPage.jsx`. (ROUTINE) (@lane: correctifs) (@model: haiku)
- [x] PACT23 — **Les 2 dernières dettes de forme : Stock → fiche fournisseur 360.** Le serveur renvoie `solde_total_du` et `total_achats_ht` en **texte** (`str(Decimal)`), le test les mocke en **nombres** — et son propre test backend fait `Decimal(resp.data[...])`, ce qui confirme la chaîne. Découvert par la garde 2, pas par un utilisateur. Statuer : soit le sérialiseur renvoie un nombre, soit l'écran et son test traitent explicitement du texte décimal — **jamais une comparaison numérique sur une chaîne**. **Done =** base `scripts/api_shapes_allow.txt` vidée (2 → 0). Files: `backend/django_core/apps/stock/selectors.py`, `frontend/src/pages/stock/FournisseurFiche360.test.jsx`, `scripts/api_shapes_allow.txt`. (ROUTINE) (@lane: correctifs) (@model: sonnet)
- [x] PACT24 — **Les 2 dernières dettes de contrat.** (1) `POST /rh/demandes-conge/{id}/soumettre/` — `DemandeCongeViewSet` a `valider`, `refuser`, `annuler`, `calendrier-equipe`, **pas** `soumettre` : décider si l'action manque côté serveur (c) ou si le client doit appeler autre chose (b). (2) `GET /api/public/openapi.json` — la route réelle est `/api/public/v1/openapi.json` : simple (b). **Done =** base `scripts/api_contract_allow.txt` vidée (2 → 0) ; **à partir de là, toute nouvelle ligne dans cette base exige `--autoriser-croissance`, donc un accord explicite du fondateur.** Files: `frontend/src/api/rhApi.js`, `frontend/src/api/publicapiApi.js`, `backend/django_core/apps/rh/views.py`, `scripts/api_contract_allow.txt`. (ROUTINE) (@lane: correctifs) (@model: sonnet)
- [x] PACT25 — **Le trou central du dossier de soumission AO : `services.producteurs_de_pack`.** Trou de type **(c) franc** : toute la machinerie basse existe (écriture en flux, refus si un contrôle est rouge, orchestration idempotente, suivi de tâche) mais **rien ne lui fournit les pièces**. Trois chemins en dépendent (`generer-piece`, `zip`, `statut-de-job`) et ont été laissés délibérément non exposés : ouvrir la porte aurait produit une tâche se terminant « terminée » avec **zéro pièce**, donc un écran affichant « pack prêt » sur une **archive vide**. Un faux succès est pire qu'un 404 **parce qu'il se dépose**. Écrire le registre qui associe chaque type de pièce à son producteur réel, puis ouvrir les trois actions. **Done =** générer un pack produit les pièces attendues et le ZIP les contient toutes ; un pack incomplet **échoue** au lieu de se terminer vert ; un test vérifie qu'un pack vide ne peut pas être marqué prêt. Files: `backend/django_core/apps/ao/services.py`, `backend/django_core/apps/ao/fabrique/`, `backend/django_core/apps/ao/tests/test_pack.py`, `frontend/src/api/aoApi.js`. (ARCH) (@lane: correctifs) (@model: opus)
- [x] PACT26 — **Nettoyer `apps/compta/urls.py` : 139 doubles montages.** Ce routeur fourre-tout historique (149 `router.register`) **ré-enregistre les ViewSets de `ao`, `marketing` et `portail` sous `/compta/`**. Conséquences mesurées : toute mesure automatique compte double (c'est ce qui a faussé le dénominateur de l'audit), et **tout écran futur doit choisir arbitrairement entre deux URLs pour la même donnée**. Retirer les ré-enregistrements en vérifiant qu'aucun appelant frontend n'utilise la variante `/compta/` — la garde de contrat (PACT1) le prouve mécaniquement. **Done =** chaque ressource est servie sous un seul préfixe ; la garde de contrat reste verte ; aucun appel frontend n'est cassé (vérifié, pas supposé). Files: `backend/django_core/apps/compta/urls.py`. (ARCH) (@lane: correctifs) (@model: opus) (@after: PACT1)
- [ ] PACT27 — **Le rapport mensuel « backend sombre », non bloquant, avec un marqueur d'intention.** Constat : la classe « le backend existe, l'écran manque » **sur-compte structurellement** — portails publics ouverts par un lien, `calendar.ics` collé dans un agenda, PDF ouverts par `window.open` : **aucun extracteur d'appels axios ne les verra jamais.** Elle ne doit donc **pas** devenir un rouge de CI. En faire un rapport mensuel, et lui appliquer au niveau ENDPOINT la distinction que `scripts/check_modules.py` fait déjà au niveau APP : une route déclarée volontairement sans écran par un marqueur `# headless: <raison>` sort du décompte, le reste est de la dette à arbitrer. **Done =** le rapport distingue « sans écran par design » (30 ressources aujourd'hui, toutes justifiées) de « sans écran par oubli » (206), et le chiffre publié est celui-là, jamais le brut. Files: `scripts/rapport_backend_sombre.py`, `backend/django_core/apps/*/urls.py`. (ROUTINE) (@lane: gardes) (@model: sonnet)

### PACT §E — Le backend construit que personne ne voit : 206 ressources, 599 endpoints, 31 apps

> **Lire ceci avant de drainer §E.** Ces tâches sont les DERNIÈRES du groupe, et l'ordre est délibéré :
> les gardes (§A) et la méthode (§B) doivent être posées d'abord. **Quatre campagnes de rattrapage en
> un mois ont produit 443 constats et 315 tâches cochées, et le défaut est revenu à chaque fois** —
> parce qu'aucune n'a fermé le robinet. Drainer §E sans §A serait la cinquième.
>
> **Ce que les chiffres veulent dire exactement.** Sur 1 045 ressources backend distinctes, 809 (77 %)
> ont un écran, 30 sont techniques par design, et **206 sont du métier construit, testé, exposé — et
> invisible**. Ce n'est pas de la dette cachée : le dépôt l'avoue lui-même par endroits (« déjà complets
> côté API », « aucune surface frontend jusqu'ici »). Regroupées par famille métier, elles font
> **120 écrans ou onglets**, dont une trentaine de modules entiers jamais exposés — sous-traitance,
> consultation fournisseur, immobilisations, rapprochement bancaire, chantier BTP, configurateur CPQ,
> coffre et rétention GED, moteur publicitaire, abonnements et relance d'impayé, administration du
> portail client, et **l'inscription d'une nouvelle société, dont le backend est entier et la porte
> inexistante**.
>
> **Chaque tâche dit de quel trou il s'agit** — c'est la distinction exigée, et elle change le travail :
> **(a)** le backend existe, l'écran n'a jamais été fait · **(b)** le nom diverge entre les deux moitiés ·
> **(c)** le backend lui-même est incomplet. Un renommage et une construction ne sont jamais présentés
> comme la même chose.
>
> **Ce que les quatre auditeurs ont écarté, et qu'il ne faut pas remettre.** 11 ressources sont des
> **faux positifs vérifiés** (l'écran existe, parfois sous un autre chemin) ; les endpoints purement
> techniques sont listés « sans écran par design » avec leur raison ; et surtout **8 ressources
> d'`installations` sont GELÉES par la décision WIR86** — la famille Programme multi-chantiers est
> volontairement dépréciée au profit de `gestion_projet`, chaque réponse HTTP portant déjà son en-tête
> de dépréciation. L'audit initial la comptait comme « un écran à faire » : lui en construire un
> créerait un nouveau consommateur d'une surface qu'une décision d'architecture a fermée.
### PACT §E1 — Écrans manquants : finance, ventes, crédit, achats, fiscal, FP&A, e-facturation (PACT28-PACT54)

- [x] PACT28 — **Fiscalité avancée : acomptes IS, conventions fiscales, TVA non déductible.** Trou **(a)**. Trois obligations fiscales invisibles : `AcompteIS` (`apps/compta/models.py:8259`) matérialise les 4 acomptes provisionnels d'IS et sait les marquer payés ; `ConventionFiscale` (`models.py:8213`) porte le taux de retenue à la source réduit par pays pour les prestataires étrangers ; `FamilleTvaNonDeductible` (`models.py:746`) est le référentiel des familles à TVA non récupérable (véhicules de tourisme, missions et réceptions). Aucun des trois n'a d'écran ni même de wrapper client. Un écran à trois onglets. **Done =** les trois listes chargent leurs données réelles et « Marquer payé » change le statut visible sans recharger la page. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/FiscaliteAvanceePage.jsx`, `frontend/src/features/compta/pages/FiscaliteAvanceePage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT29 — **Immobilisations avancées : composants, dépréciation, mutations, encours.** Trou **(a)**, cas d'école du regroupement : 5 ressources = 1 écran à onglets, greffé sur le module Immobilisations existant. `ComposantImmobilisation` (`models.py:7751`, décomposition d'un actif en composants amortis séparément), `DepreciationImmobilisation` (`7800`, test de dépréciation avec `poster`/`reprendre`), `MutationImmobilisation` (`7855`, transfert entre centres de coût), `ImmobilisationEnCours` (`7914`, actif en construction, action `mettre-en-service` qui bascule vers un actif amortissable) et son enfant `LigneImmobilisationEnCours` (`7956` — **un onglet, pas une tâche**). **Done =** un actif reçoit composants, test de dépréciation postable et mutation ; un encours reçoit ses lignes de montants engagés et « Mis en service » le bascule en immobilisation classique. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/ImmobilisationsAvanceesPage.jsx`, `frontend/src/features/compta/pages/ImmobilisationsAvanceesPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT30 — **Rapprochements de comptes de bilan (contrôle à quatre yeux).** Trou **(a)**. `RapprochementCompte` (`models.py:7615`) et son enfant `LigneJustificationCompte` (`7699`) implémentent la justification périodique du solde d'un compte de bilan : report de l'exercice précédent, recalcul de l'écart, puis **séparation des tâches** préparateur → réviseur (403 serveur si le réviseur est le préparateur). C'est un contrôle de clôture **distinct** du rapprochement bancaire déjà écranté — le nom seul prêtait à confusion. **Done =** un utilisateur ne peut pas valider un rapprochement qu'il a lui-même soumis, et le message de refus du serveur est affiché tel quel, pas une erreur générique. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/RapprochementsComptePage.jsx`, `frontend/src/features/compta/pages/RapprochementsComptePage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT31 — **Référentiels comptables parallèles et analytique multi-axes.** Trou **(a)**, avec un aveu dans le code : ces 4 ressources sont marquées « API-only » dans le commentaire même du client `comptaApi.js` — elles ont un wrapper et **zéro écran**. `ReferentielComptable` (`models.py:6852`, livres parallèles + amorçage du CGNC), `AjustementGaap` (`6896`, retraitements entre référentiels), `AxeAnalytique` (`6939`, axes configurables type Chantier/Région/Activité), `ImputationAxe` (`6967`). **Done =** un ajustement posté crée une écriture visible dans le référentiel parallèle, jamais dans le plan comptable principal. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/ReferentielsAnalytiquePage.jsx`, `frontend/src/features/compta/pages/ReferentielsAnalytiquePage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT32 — **Clés de répartition et engagements budgétaires.** Trou **(a)**, 5 ressources : `CleRepartition`/`LigneCleRepartition` (`models.py:7019`/`7067`, coefficients devant totaliser 100 %), `RunAllocation` (`7108`, déverse un compte source vers ses cibles), `AllocationRecurrente` (`7170`), `EngagementComptable` (`7235` — « le budget est réservé dès le devis d'achat, avant la facture »). **Piège de nommage vérifié** : une page « Engagements & clôtures avancées » existe déjà mais couvre les retenues de garantie et cautions — **homonyme français, backend totalement différent**, aucun wrapper pour celui-ci. **Done =** une clé dont les coefficients ne totalisent pas 100 % est rejetée visiblement ; un engagement liquidé n'est plus modifiable. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/AllocationsEngagementsPage.jsx`, `frontend/src/features/compta/pages/AllocationsEngagementsPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT33 — **Consolidation groupe multi-sociétés.** Trou **(a)**, 7 ressources également marquées « API-only » dans le client. Le `CycleConsolidation` (`models.py:6419`) pilote tout : ouvrir, verrouiller, collecter les liasses des filiales, apparier les intercos, générer les éliminations réciproques, les intérêts minoritaires et les états consolidés. S'y ajoutent `LiasseRemontee`, `MappingConsolidation`, `OperationInterco`, `MargeInterneStock`, `EliminationTitres`, et — mécanisme **séparé et plus ancien** — `EntiteConsolidation` (`3973`, périmètre de filiales + CPC consolidé direct), à ne pas fondre dans le cycle. **Done =** un cycle verrouillé refuse toute modification de collecte et l'écran affiche ce refus au lieu de planter. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/ConsolidationGroupePage.jsx`, `frontend/src/features/compta/pages/ConsolidationGroupePage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT34 — **Reconnaissance du revenu (IFRS 15).** Trou **(a)**, 3 ressources « API-only » : `ContratRevenu` (`models.py:7996`, répartit le prix de transaction au prorata des obligations), `ObligationPerformance` (`8044`), `EcheancierReconnaissance` (`8108`). Utile pour les contrats pluriannuels — maintenance solaire, monitoring — où le revenu doit être étalé plutôt que reconnu à la facturation. **Done =** la somme des montants alloués aux obligations d'un contrat égale son prix de transaction total, et l'écran le montre. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/RevenuIfrs15Page.jsx`, `frontend/src/features/compta/pages/RevenuIfrs15Page.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT35 — **Import guidé de la balance d'ouverture.** Trou **(a)**. `BalanceOuvertureViewSet` (`apps/compta/views.py:7925`) télécharge un gabarit CSV et importe une balance d'ouverture en une écriture « À Nouveau » unique et équilibrée, idempotente par exercice, avec rejet détaillé ligne à ligne. **C'est l'outil dont a besoin toute société qui migre sa comptabilité vers TAQINOR en cours d'exercice** — aujourd'hui inaccessible autrement qu'en appelant l'API à la main. **Done =** un second import sur le même exercice est refusé avec un message clair, pas une erreur 500. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/BalanceOuverturePage.jsx`, `frontend/src/features/compta/pages/BalanceOuverturePage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT36 — **Comparateurs commerciaux : versions de devis, cash contre financement.** Trou **(a)**, deux ressources de calcul pur sans stockage : le comparateur de devis (`views.py:6965`, différence champ à champ entre deux versions) sert au back-office ; le comparateur cash/financement (`7161`, coût total et retour sur investissement) sert au commercial en rendez-vous pour lever l'objection prix. **Done =** deux identifiants de devis affichent la différence champ par champ, et un montant/durée/taux affiche le comparatif — sans rien écrire en base. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/ComparateursPage.jsx`, `frontend/src/features/compta/pages/ComparateursPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT37 — **Codes promotionnels datés sur devis.** Trou **(a)**. `CodePromotion` (`models.py:4155`) porte des codes de remise à dates de validité, traçables au retour sur investissement. Zéro écran, zéro wrapper. **Done =** un code expiré apparaît **visiblement** comme tel dans la liste, jamais filtré silencieusement. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/CodesPromotionPage.jsx`, `frontend/src/features/compta/pages/CodesPromotionPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT38 — **Assistant de vente guidée.** Trou **(a)**. `SessionGuidedSelling` (`models.py:4255`) fait répondre un commercial junior étape par étape, valide la cohérence technique (par exemple kWc contre onduleur) et propose une composition — sans créer le devis lui-même. **Done =** une réponse incohérente affiche l'alerte renvoyée par le serveur, jamais un écran silencieusement « complet ». **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/GuidedSellingPage.jsx`, `frontend/src/features/compta/pages/GuidedSellingPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT39 — **Catalogue public à jeton.** Trou **(a)**. `ECatalogue` (`models.py:4356`) génère un catalogue public en prix TTC derrière un jeton opaque — lien partageable sans connexion. **Done =** le lien copié ouvre une page qui n'affiche **que le prix TTC** : ni `prix_achat`, ni marge, jamais (règle du dépôt). **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/ECataloguePage.jsx`, `frontend/src/features/compta/pages/ECataloguePage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT40 — **Bibliothèque d'annexes de proposition.** Trou **(a)**. `DocumentProposition` (`models.py:4395`) porte des annexes réutilisables (titre, contenu, type, ordre) attachables au PDF de proposition commerciale. **Done =** un document créé ici apparaît dans la liste des annexes disponibles au moment de générer un devis. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/DocumentsPropositionPage.jsx`, `frontend/src/features/compta/pages/DocumentsPropositionPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT41 — **Échéanciers de paiement en tranches.** Trou **(a)**. `EcheancierPaiement` et `TranchePaiement` (`models.py:4661`/`4694`) découpent une facture en tranches datées avec suivi des versements — indispensable pour les gros chantiers payés en plusieurs fois. **Done =** le solde restant de la facture se met à jour visiblement dès qu'une tranche est réglée. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/EcheanciersPaiementPage.jsx`, `frontend/src/features/compta/pages/EcheanciersPaiementPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT42 — **Approbation des configurations non standard.** Trou **(a)**. `DemandeApprobationConfig` (`models.py:4297`) fait partir en validation toute composition qui sort des règles, traçable (demandeur, décideur, motif). **Aujourd'hui une composition non standard ne PEUT PAS être formellement approuvée.** **Done =** une demande refusée conserve son motif visible dans l'historique. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/compta/pages/ApprobationsConfigPage.jsx`, `frontend/src/features/compta/pages/ApprobationsConfigPage.test.jsx`, `frontend/src/features/compta/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/compta) (@model: sonnet) (@after: PACT13)
- [x] PACT43 — **Mandats de paiement récurrents (carte tokenisée).** Trou **(a)**, confirmé par le code lui-même (`ventesApi.js` note « reste en attente »). Le client tokenise sa carte depuis son portail (flux déjà géré), mais la vue **interne** — lister les mandats actifs, expirés, révoqués par client et révoquer — n'a aucun écran. Aucun numéro de carte n'est jamais stocké : seulement un jeton opaque et les 4 derniers chiffres. **Done =** un mandat révoqué repasse le client sur encaissement manuel et le statut « Révoqué » est visible ; **aucune donnée de carte n'est jamais saisie dans l'ERP**. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/ventes/MandatsPaiementPage.jsx`, `frontend/src/pages/ventes/MandatsPaiementPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ventes) (@model: sonnet) (@after: PACT13, PACT17)
- [x] PACT44 — **Paramétrage des relances par client.** Trou **(a)** d'un genre subtil : le serveur **renvoie déjà** `relance_mode` et `relance_responsable_id` dans la liste des relances, mais l'écran existant ne les affiche ni ne les édite nulle part (vérifié : zéro occurrence dans le fichier). `ParametrageRelanceClient` (`apps/ventes/models.py:933`) choisit entre relance automatique et relance manuelle assignée à un responsable. **Sans cet écran, tout client est silencieusement en mode automatique sans responsable.** **Done =** un client passé en manuel disparaît de la file automatique et apparaît dans « Mes relances » de son responsable. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/ventes/RelancesPage.jsx`, `frontend/src/pages/ventes/RelancesPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ventes) (@model: sonnet) (@after: PACT13, PACT17)
- [x] PACT45 — **Promesses de paiement client.** Trou **(a)**, même mécanique : `PromessePaiement` (`apps/ventes/models.py:896`) enregistre « le client s'engage à payer le [date] », ce qui **suspend automatiquement les relances** jusqu'à cette date (et les reprend avec un indicateur « promesse rompue » si l'échéance passe sans encaissement). Le serveur renvoie déjà cette donnée par facture ; l'écran ne l'affiche pas et n'en crée aucune. **Done =** enregistrer une promesse fait disparaître la facture de la relance automatique jusqu'à la date promise, visiblement à l'écran. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/ventes/RelancesPage.jsx`, `frontend/src/pages/ventes/RelancesPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ventes) (@model: sonnet) (@after: PACT13, PACT17)
- [x] PACT46 — **Remises d'encaissement terrain.** Trou **(a)**, et le plus sensible du bloc : `RemiseEncaissement` (`apps/ventes/models.py:1875`) permet à un technicien qui encaisse espèces ou chèques sur le terrain de déclarer sa collecte, puis au responsable de la clôturer — ce qui génère un bordereau PDF et **calcule l'écart entre le déclaré et la somme des lignes, jamais masqué**. **Zéro écran aujourd'hui = aucun contrôle sur l'argent liquide collecté sur le terrain.** **Done =** une remise avec écart non nul affiche l'alerte à la clôture et le bordereau PDF se télécharge. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/ventes/RemisesEncaissementPage.jsx`, `frontend/src/pages/ventes/RemisesEncaissementPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ventes) (@model: sonnet) (@after: PACT13, PACT17)
- [ ] PACT47 — **Import en masse des limites de crédit.** Trou **(a)**. `importer_limites` (`apps/credit/views.py:275`) accepte un CSV ou XLSX, valide ligne à ligne, propose un mode **aperçu qui n'écrit rien** et un mode écrasement en opt-in explicite (le remplissage seul est le défaut sûr). Aujourd'hui, initialiser les limites de tous les clients existants n'est possible que client par client. **Done =** l'aperçu montre les erreurs ligne à ligne **sans avoir écrit en base**, et confirmer applique exactement ce qui a été prévisualisé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/credit/ImportLimitesCreditPage.jsx`, `frontend/src/features/credit/ImportLimitesCreditPage.test.jsx`, `frontend/src/features/credit/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/credit) (@model: sonnet) (@after: PACT13)
- [ ] PACT48 — **Assurance-crédit : polices et encours garantis.** Trou **(a)**. `PoliceAssuranceCredit` (`apps/credit/models.py:249`) et `EncoursGarantiClient` (`282`) forment un registre **déclaratif** (aucun appel à l'assureur : Allianz Trade, Coface, Atradius saisis à la main) avec alerte d'échéance. **Faux positif écarté après vérification** : un écran « Polices » existe déjà mais gère les polices **d'entreprise** (RC pro, décennale) — modèle et backend totalement différents malgré le nom proche. **Done =** un client sans encours garanti déclaré apparaît explicitement « non couvert », jamais par hypothèse silencieuse. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/credit/PolicesAssuranceCreditPage.jsx`, `frontend/src/features/credit/PolicesAssuranceCreditPage.test.jsx`, `frontend/src/features/credit/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/credit) (@model: sonnet) (@after: PACT13)
- [ ] PACT49 — **Rapport des dérogations de crédit.** Trou **(a)**. `rapport_derogations_view` (`apps/credit/views.py:68`) produit un rapport sur période avec le délai de traitement en heures et un export XLSX/CSV. L'écran existant gère le **workflow** (approuver, rejeter) mais pas ce **rapport agrégé**. **Done =** l'export contient exactement les colonnes documentées côté serveur. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/credit/DerogationsPage.jsx`, `frontend/src/features/credit/DerogationsPage.test.jsx`, `frontend/src/features/credit/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/credit) (@model: sonnet) (@after: PACT13)
- [ ] PACT50 — **Segments crédit : rattacher un client à son segment.** Trou **(a)**. `SegmentClientCredit` (`apps/credit/models.py:153`) affecte un client à un segment. L'écran existant configure les **conditions par segment** (délai, acompte) — mais **sans rattachement, cette configuration reste décorative** pour un client donné. **Done =** changer le segment d'un client fait apparaître les conditions du nouveau segment sur son devis suivant. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/credit/SegmentsClientPage.jsx`, `frontend/src/features/credit/SegmentsClientPage.test.jsx`, `frontend/src/features/credit/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/credit) (@model: sonnet) (@after: PACT13)
- [x] PACT51 — **Relevé RAS-TVA fournisseurs et registre consolidé des paiements.** Trou **(c) partiel — nuance importante à ne pas écraser** : enregistrer un paiement fournisseur **existe déjà à l'écran** (via une action imbriquée sur la facture, qui crée le même modèle). Ce n'est donc **pas** un écran à construire de zéro. Ce qui manque réellement, exposé uniquement par la ressource autonome jamais appelée (`apps/stock/views/paiement_fournisseur.py:45`) : l'export **RAS-TVA** pour la télédéclaration Simpl-TVA (`ligne 150`, aucun bouton nulle part) ; un **registre consolidé** de tous les paiements tous fournisseurs confondus (aujourd'hui visible uniquement facture par facture, jamais en vue trésorerie) ; et le **flag d'escompte pour paiement anticipé**, calculé côté serveur mais silencieusement perdu par le chemin d'écran actuel. **Done =** le fichier exporté correspond au relevé attendu par Simpl-TVA, et saisir un paiement dans la fenêtre d'escompte affiche le pourcentage disponible. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/stock/PaiementsFournisseurLedgerPage.jsx`, `frontend/src/pages/stock/PaiementsFournisseurLedgerPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/achats) (@model: sonnet) (@after: PACT13)
- [x] PACT52 — **Registre des bénéficiaires effectifs.** Trou **(a)**. `BeneficiaireEffectif` (`apps/fiscal/models.py:156`) est le registre légal marocain (nom, pièce d'identité, nationalité, pourcentage de détention, contrôle direct ou indirect) avec alerte de complétude et export au format attendu par l'OMPIC. **Obligation de conformité** — et le client `fiscalApi.js` documente explicitement ne pas l'exposer. **Done =** tant que la somme des pourcentages déclarés est sous le seuil, l'alerte de complétude est affichée au lieu d'être masquée. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/fiscal/BeneficiairesEffectifsPage.jsx`, `frontend/src/pages/fiscal/BeneficiairesEffectifsPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/fiscal) (@model: sonnet) (@after: PACT13)
- [ ] PACT53 — **Soumissions budgétaires par département.** Trou **(a)**. `SoumissionBudgetDepartement` (`apps/fpa/models.py:195`) porte le workflow de validation d'un budget de département (en saisie → soumis → validé ou rejeté) avec son fil de discussion horodaté. Le module FP&A existe entièrement (tableau de bord, saisie, prévisions, scénarios, écarts) mais **aucune de ses 5 pages ne mentionne « soumission »** : la saisie fonctionne, son statut de validation formel est invisible. **Done =** une note ajoutée à une soumission apparaît dans son fil avec son auteur et son horodatage. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/fpa/SoumissionsBudgetPage.jsx`, `frontend/src/pages/fpa/SoumissionsBudgetPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/fpa) (@model: sonnet) (@after: PACT13)
- [x] PACT54 — **Transmission DGI : contrôle avant envoi et historique.** Trou **(a)**, plus large que le seul slug : `TransmissionDGI` (`apps/einvoice/models.py:113`) est la file d'attente de transmission (en attente, envoyé, accepté, rejeté, réponse brute, compteur de tentatives), inerte tant qu'aucune credential DGI n'est configurée mais prête. Et le composant d'actions e-facture existant ne fait **que** « Générer (à blanc) » et « Télécharger XML » : l'action `controler` (anomalies bloquantes) est **wrappée dans le client mais jamais appelée**, et `transmettre` n'a aucun wrapper. Un historique sans bouton pour transmettre serait inutile : les deux vont ensemble. **Done =** « Contrôler » affiche la liste des anomalies bloquantes si la facture n'est pas conforme, et une transmission déclenchée apparaît dans l'historique avec son statut. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/components/EinvoiceActions.jsx`, `frontend/src/pages/fiscal/TransmissionsDGIPage.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/einvoice) (@model: sonnet) (@after: PACT13)

> **Faux positif écarté (1).** `paiements-fournisseur` : la capacité de base « enregistrer qu'on a payé une facture » **existe** via une action imbriquée sur la facture fournisseur, qui crée le même modèle avec les mêmes garde-fous. Seule la ressource autonome est sans appelant — voir PACT51 pour le sous-ensemble réellement manquant.
>
> **Sans écran par design.** Webhooks entrants d'intégrations tierces ; portail client à jeton (mon relevé, contester une facture) ; flux publics à jeton (enquêtes, désinscription, redirecteur de lien tracké, inscription à un événement). Aucun n'est destiné à un écran ERP.
### PACT §E2 — Écrans manquants : chantier, sous-traitance, appels d'offres, immobilier, projet, agricole, éducation (PACT55-PACT80)

- [x] PACT55 — **Sous-traitance chantier : ordres, factures, règlements, attestations, évaluations, retenues.** Trou **(a)**, le plus gros bloc du périmètre. Le backend sait déjà tout faire — ordres de travaux avec cycle émettre/réceptionner/clôturer, PDF et chatter (`apps/installations/views/ordre_soustraitance.py`) ; factures et paiements en façade sur la chaîne comptes-à-payer (`views/facture_soustraitant.py`) ; attestations obligatoires avec contrôle d'affectabilité (`views/attestation_soustraitant.py`) ; évaluations qualité/délai/sécurité avec tableau de bord (`views/evaluation_soustraitant.py`) ; retenue de garantie BTP marocaine de 5 à 10 % (`models_retenue_garantie.py`). **Six ressources, aucun fichier frontend ne les appelle, pas même le client API.** Un écran à onglets par sous-traitant. **Done =** depuis la fiche d'un sous-traitant on crée un ordre, on l'émet, on y attache une facture et un règlement, on voit ses attestations et son évaluation, et on suit ses retenues — sans quitter l'écran. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/SousTraitanceChantier.jsx`, `frontend/src/features/installations/SousTraitanceChantier.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT56 — **Import et douane : dossiers, frais, coût débarqué.** Trou **(a)**. `DossierImport` (`models_dossier_import.py` — incoterm, connaissement, conteneur, statut douanier de commandé à livré), `FraisImport` et `LandedCostLigne` (`models_landed_cost.py` — coût débarqué par référence, réparti au prorata de la valeur FOB) : 3 ressources, **aucune trace nulle part** dans le frontend, pas même un wrapper. **Done =** un dossier d'import est suivi statut par statut, ses frais saisis, et le coût de revient débarqué par produit calculé — **donnée interne, jamais montrée au client**. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/SuiviImport.jsx`, `frontend/src/features/installations/SuiviImport.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT57 — **Prix négociés fournisseurs : commandes-cadres et contrats de prix.** Trou **(a)** élargi aux en-têtes. L'écran d'approvisionnement existant affiche déjà ces documents **en lecture seule**, la colonne « Lignes » réduite à un simple compte : aucun écran ne permet de créer ou modifier une commande-cadre, un contrat de prix, ni leurs lignes de prix négocié par référence. Sans écriture sur les en-têtes, les lignes n'ont d'ailleurs rien à rejoindre. **Done =** un responsable crée une commande-cadre ou un contrat de prix, y ajoute des lignes avec prix négocié, et voit le volume restant engagé — capacité qui n'existe nulle part aujourd'hui. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/PrixNegocies.jsx`, `frontend/src/features/installations/PrixNegocies.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT58 — **Contrôle documentaire de projet : registre et révisions.** Trou **(a)**. `DocumentProjetViewSet` (schéma unifilaire, calepinage, note de calcul par chantier) et `RevisionDocumentViewSet` (indice, date, auteur, fichier) dans `views/document.py` — aucun appelant. **Done =** un document technique est enregistré sur un chantier, une nouvelle révision ajoutée (l'indice courant s'affiche), et l'historique complet reste consultable. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/DocumentsProjet.jsx`, `frontend/src/features/installations/DocumentsProjet.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT59 — **Suivi projet du chantier : jalons, modèles, comptes-rendus.** Trou **(a)**. `JalonProjetViewSet` (phases étude, approvisionnement, pose, mise en service, réception — dates cible contre réelles), `ModeleProjetViewSet` (patron de chantier type, action qui pré-crée jalons et nomenclature) et `ReunionChantierViewSet` (comptes-rendus horodatés) dans `views/projet.py` — 3 ressources sans appelant. **Done =** un jalon est marqué atteint, un modèle de projet instancié sur un chantier (jalons et nomenclature pré-créés), un compte-rendu rédigé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/SuiviProjetChantier.jsx`, `frontend/src/features/installations/SuiviProjetChantier.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT60 — **Consultation fournisseurs et comparatif d'offres.** Trou **(a)**. `views/rfq.py` porte le cycle complet — envoyer, clôturer, retenir une offre (avec adjudication possible d'un bon de commande) — plus les offres reçues et la liste des fournisseurs consultés avec relance. Trois ressources sans appelant. **Done =** une consultation est créée depuis une demande d'achat, envoyée à plusieurs fournisseurs, ses offres saisies et comparées côte à côte, une offre retenue. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/RFQ.jsx`, `frontend/src/features/installations/RFQ.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT61 — **Paramétrage des kits d'assemblage : nomenclature, gamme, contrôle qualité.** Trou **(a)** élargi au kit lui-même. L'écran Atelier existant ne fait que **sélectionner** un kit déjà créé — aucun écran ne crée le kit (pas même de wrapper `create`), sa nomenclature, sa gamme d'étapes ni son modèle de contrôle qualité. **L'Atelier suppose donc en amont une capacité qui n'existe pas.** **Done =** un kit est créé, sa nomenclature éditée ligne par ligne, sa gamme d'étapes définie et son modèle de contrôle configuré. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/installations/ParametrageKits.jsx`, `frontend/src/features/installations/ParametrageKits.test.jsx`, `frontend/src/features/installations/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/installations) (@model: sonnet) (@after: PACT13)
- [x] PACT62 — **Réserves de chantier : punch-list géolocalisée sur plan.** Trou **(a)**. `ReserveChantier` (`apps/btp_chantier/models.py`) pose un point x/y sur un plan de la GED avec gravité (mineure, majeure, bloquante) et cycle ouverte → levée ou contestée, historique de transitions. **L'app `btp_chantier` n'a AUCUN fichier client : ses 7 ressources sont toutes invisibles.** L'écran doit être une **carte avec pastilles cliquables colorées par gravité**, pas une liste plate. **Done =** une réserve est posée sur un point précis du plan, assignée avec date limite, puis levée ou contestée avec preuve. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/ReservesChantier.jsx`, `frontend/src/features/btp_chantier/ReservesChantier.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT63 — **Demandes d'information technique.** Trou **(a)**. Question numérotée par chantier, délai en jours ouvrés converti en date limite **côté serveur**, alerte de retard, fil de réponses, clôture. **Done =** une question est posée avec délai, répondue, close, et les demandes en retard sont signalées visuellement. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/RFI.jsx`, `frontend/src/features/btp_chantier/RFI.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT64 — **Visas de documents techniques.** Trou **(a)**. Cycle soumis → en revue → approuvé ou refusé, délai de revue en jours ouvrés, et **resoumission automatique dès qu'une nouvelle version du document arrive dans la GED**. **Done =** un plan est soumis, approuvé (avec ou sans réserve) ou refusé avec observations, et une nouvelle version rouvre automatiquement le visa. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/VisasDocuments.jsx`, `frontend/src/features/btp_chantier/VisasDocuments.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT65 — **Journal de chantier quotidien.** Trou **(a)**. Une entrée par jour et par chantier : météo, effectif interne et sous-traitant, matériel, événements, visiteurs — avec export PDF. **Done =** une entrée est saisie chaque jour (contrainte d'unicité côté serveur) et exportée en PDF. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/JournalChantier.jsx`, `frontend/src/features/btp_chantier/JournalChantier.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT66 — **Avenants de chantier : chiffrage et approbation client.** Trou **(a)**. Chiffrage avec impact sur le budget ou facture d'acompte auto-générée, puis approbation client par lien public à jeton avec signature électronique (loi 53-05). L'écran construit ici est **l'écran interne** : lignes chiffrées, envoi au client, suivi de signature — la page que le client ouvre reste hors périmètre. **Done =** un avenant est chiffré, envoyé, et son approbation signée ou son refus suivis depuis l'écran. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/AvenantsChantier.jsx`, `frontend/src/features/btp_chantier/AvenantsChantier.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT67 — **Décompte général et définitif, déboursé sec contre facturé.** Trou **(a)**. Solde dû recalculé côté serveur, **verrouillage définitif avec déverrouillage administrateur journalisé**, cycle notifier → contester ou accepter → finaliser, export PDF, plus la comparaison déboursé sec contre facturé du chantier. **Done =** un décompte est généré, notifié, contesté ou finalisé (verrouillage tracé), et le déboursé sec comparé au facturé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/DecompteGeneral.jsx`, `frontend/src/features/btp_chantier/DecompteGeneral.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT68 — **Diffusion contrôlée de plans.** Trou **(a)**. Diffusion tracée d'une version de plan avec accusé de réception par destinataire, et **détection d'un plan périmé encore consulté** — le risque le plus concret sur un chantier. **Done =** une version est diffusée à des destinataires internes et externes, les accusés suivis un par un, et les diffusions périmées signalées. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/btp_chantier/DiffusionPlans.jsx`, `frontend/src/features/btp_chantier/DiffusionPlans.test.jsx`, `frontend/src/features/btp_chantier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/btp_chantier) (@model: sonnet) (@after: PACT13)
- [x] PACT69 — **Bordereau des prix de l'appel d'offres.** Trou **(a)**. `BordereauPrix` (`apps/ao/models.py`) porte 4 sections types, la **clause de réserve obligatoire** en prix unitaires, et des totaux HT / remise / TVA par taux / TTC recalculés côté serveur ; `SectionBordereau` et `LigneBordereau` (dont la quantité est tracée jusqu'à la variante de calepinage d'origine) sont ses enfants. Aucun appelant. **Done =** un bordereau est monté section par section, ses totaux visibles en direct, et l'absence de clause de réserve est signalée avant remise. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/BordereauPrix.jsx`, `frontend/src/features/ao/BordereauPrix.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT70 — **Suivi administratif de l'AO : cautions, échéances, résultat.** Trou **(a)** avec une conséquence directe : le tableau de bord AO (celui-là même qui a planté, corrigé en PACT15) **agrège** ces trois données — taux de réussite, cautions immobilisées, échéances dues — mais **aucun écran ne permet d'en créer une seule**. Ses indicateurs resteront donc à zéro tant que cet écran n'existe pas. `CautionSoumission` alerte si l'échéance tombe avant l'ouverture des plis ; `EcheanceAO` porte les dates clés ; `ResultatAO` enregistre gagné ou perdu, le classement, l'attributaire et l'écart de prix. **Done =** on enregistre caution, échéances et résultat d'un AO — et le tableau de bord cesse d'être vide. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/SuiviAdministratifAO.jsx`, `frontend/src/features/ao/SuiviAdministratifAO.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT71 — **Checklist partenaire du dossier de dépôt.** Trou **(a)**. `LigneChecklistPartenaire` couvre les 7 blocs d'une remise réelle (case, responsable, commentaire) et **un point obligatoire non coché BLOQUE la transition « prêt à déposer »**. L'écran du dossier affiche déjà pièces et états, mais pas cette checklist : **la porte de blocage est donc invisible, et on la contourne par l'API en croyant bien faire** — exactement le motif qui a justifié l'écran de contrôles avant dépôt. **Done =** les points sont visibles et cochables avec leur responsable, et un point obligatoire ouvert apparaît clairement comme la cause du blocage. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/dossier/ChecklistPartenaire.jsx`, `frontend/src/features/ao/dossier/ChecklistPartenaire.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT72 — **Pièces fournies du dossier de dépôt.** Trou **(a)**. Une pièce du dossier est **soit** générée par la fabrique **soit** fournie (par le partenaire ou l'acheteur). L'écran affiche déjà les pièces générées mais n'offre aucun moyen de marquer une pièce fournie « présente » ni d'y attacher son fichier — pas même un wrapper client. **Done =** une pièce fournie est marquée présente et son fichier attaché, visible dans le même dossier que les pièces générées. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/dossier/PiecesFournies.jsx`, `frontend/src/features/ao/dossier/PiecesFournies.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT73 — **Bibliothèque des pièces administratives.** Trou **(a)**. `PieceAdministrative` porte les pièces datées réutilisables d'un AO à l'autre : déclaration sur l'honneur, attestation fiscale de moins d'un an, CNSS de moins de trois mois, registre de commerce, RIB, assurances. Le modèle pose la règle qui compte : **la date de référence est celle de la remise des plis, pas celle du jour**. **Done =** une pièce est enregistrée une fois et réutilisée sur plusieurs AO, avec alerte explicite si elle expirera **avant une remise de plis donnée**. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/PiecesAdministratives.jsx`, `frontend/src/features/ao/PiecesAdministratives.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT74 — **Pièces du dossier de consultation reçu de l'acheteur.** Trou **(c) partiel**, à énoncer franchement : `PieceConsultation` (CPS, règlement, plans d'architecte, additifs et errata pointant la pièce qu'ils modifient) n'est exposé nulle part. Conséquence mesurable : **un additif reçu après téléchargement ne marque rien « à revérifier »**, et l'écran des exigences CPS — pourtant déjà câblé — référence « la page du CPS » alors que **le CPS lui-même n'est jamais stocké**. L'écran existant est donc lui-même incomplet tant que cette pièce n'existe pas. **Done =** le CPS et les pièces reçues sont enregistrés avec leur version, un additif rattaché à la pièce qu'il modifie et signalé comme tel. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/PiecesConsultation.jsx`, `frontend/src/features/ao/PiecesConsultation.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT75 — **Économie directeur de l'AO : coût de revient et cibles de marge.** Trou **(a)** élargi au parent. Le client de rentabilité est **déjà câblé au bon endpoint** mais **consommé par aucun écran** : la fiche Économie n'a pas d'écran, donc ses deux enfants non plus — `LigneCoutRevient` (poste, quantité, prix unitaire, régime TVA 10 % ou 20 %) et `CibleFinanciere` (bénéfice net visé, **versionnée avec auteur et motif**, seuils psychologiques). Gardé par une permission élevée, jamais mêlé aux vues AO générales, **jamais client-facing**. **Done =** un directeur saisit les postes de coût, verse une nouvelle version de cible avec son motif, et produit le classeur — et aucune donnée de marge n'apparaît dans une vue non gardée. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/economie/EconomieDirecteur.jsx`, `frontend/src/features/ao/economie/EconomieDirecteur.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [x] PACT76 — **Plan source de la toiture : le calibrage n'est jamais persisté.** Trou **(b) déjà corrigé + (a) résiduel** — la distinction compte. La réparation du 03/08 a corrigé le **nom** côté client, mais **aucun écran ne l'appelle encore** : les outils de calibration, d'import DXF et de fond de plan travaillent sur une image **jamais enregistrée** comme plan source (origine plan fourni / tracé manuel / carte, état brut / calibré / vectorisé). Conséquence : **un rechargement de page perd le calibrage**, et aucune liste des plans attachés à une toiture n'existe. **Done =** un plan importé et calibré survit à un rechargement, et la toiture affiche la liste de ses plans sources avec leur état. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ao/toiture/PlanSourcePanel.jsx`, `frontend/src/features/ao/toiture/PlanSourcePanel.test.jsx`, `frontend/src/features/ao/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ao) (@model: sonnet) (@after: PACT13)
- [ ] PACT77 — **États des lieux d'entrée et de sortie.** Trou **(a)**. `EtatLieuxImmo` est **pré-rempli depuis la grille standard du type de local à la création** ; ses pièces et éléments sont créés automatiquement (modifiables, jamais créables directement) et chaque élément accepte une photo comparative. Trois ressources sans aucun appelant. **Done =** un état d'entrée est créé pour un bail avec sa grille pré-remplie, chaque élément noté et photographié, et un état de sortie comparé au même référentiel. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/immobilier/EtatsLieux.jsx`, `frontend/src/features/immobilier/EtatsLieux.test.jsx`, `frontend/src/features/immobilier/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/immobilier) (@model: sonnet) (@after: PACT13)
- [ ] PACT78 — **Paramètres avancés du module Projet.** Trou **(a)**, trois ressources : verrouillage d'un mois sur les feuilles de temps ; création et révocation du lien public de portail projet (**le portail public fonctionne déjà, mais rien côté administration ne le pilote**) ; gabarits de tâches récurrentes (la génération restant une commande serveur). **Done =** un mois est verrouillé, un lien portail généré ou révoqué pour un projet, un gabarit de tâche récurrente créé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/gestion_projet/ParametresAvances.jsx`, `frontend/src/features/gestion_projet/ParametresAvances.test.jsx`, `frontend/src/features/gestion_projet/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/gestion_projet) (@model: sonnet) (@after: PACT13)
- [x] PACT79 — **Lots de récolte.** Trou **(a)**. `LotRecolte` porte le lot par campagne (date, quantité en quintaux, calibre, grille de qualité libre) avec rattachement optionnel à un lot de stock physique en lecture seule. Le client agricole câble ses 11 autres ressources et **n'a aucune entrée pour celle-ci**. **Done =** un lot est créé pour une campagne (numéro anti-collision généré côté serveur) et retrouvé depuis sa traçabilité stock quand il en a une. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/agriculture/LotsRecolte.jsx`, `frontend/src/features/agriculture/LotsRecolte.test.jsx`, `frontend/src/features/agriculture/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/agriculture) (@model: sonnet) (@after: PACT13)
- [ ] PACT80 — **Transport scolaire : circuits, arrêts, affectations.** Trou **(a)**. `CircuitTransport` référence un véhicule du parc par clé étrangère réelle et lit sa disponibilité **via les sélecteurs de la flotte** (jamais par import de ses modèles) ; les arrêts sont ordonnés ; l'affectation d'un élève sur une période émet un **avertissement doux, jamais bloquant**, si le véhicule est indisponible. Le client éducation n'a aucune entrée pour ce sujet. **Done =** un circuit est créé avec ses arrêts ordonnés et son véhicule, un élève y est affecté, et l'indisponibilité avertit sans jamais bloquer. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/education/TransportScolaire.jsx`, `frontend/src/features/education/TransportScolaire.test.jsx`, `frontend/src/features/education/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/education) (@model: sonnet) (@after: PACT13)

> **Faux positifs écartés (6), tous dans le module AO.** `chaines-cotes`, `series-questions`, `variantes-calepinage`, `kits-calepinage`, `presets-calepinage` et `dossiers-ao` sont **désormais correctement nommés ET consommés par un écran réel** — l'audit datait d'avant les corrections du 03/08 (PACT16). Aucun désaccord de nom ne subsiste dans ce périmètre.
>
> **Sans écran par design — la découverte la plus importante de ce bloc (8 ressources).** La famille **Programme multi-chantiers** d'`installations` (`programmes` et ses 6 satellites) est **GELÉE par la décision WIR86 du 18/07/2026** : le système Projet canonique est `apps/gestion_projet`, déjà câblé, et chaque réponse HTTP de la famille Programme porte déjà un en-tête de dépréciation pointant son successeur. **L'audit ERP la présentait comme « 7 ressources = 1 écran à faire » : lui construire un écran créerait un nouveau consommateur d'une surface volontairement gelée, contre une décision d'architecture déjà actée.** Même raison pour le **contrôle budgétaire** : c'est un garde-fou consultatif conçu pour être appelé *par* un écran d'achat existant, et son seul paramètre utile pointe vers le budget de cette même famille gelée.
### PACT §E3 — Écrans manquants : RH, paie, portail, CRM, marketing, publicité, santé (PACT81-PACT116)

- [x] PACT81 — **Affectation véhicule & permis de conduire.** `AffectationVehicule` (`apps/rh/models.py:4348`) lie un conducteur à un véhicule du parc sur une période et refuse déjà, côté serveur (`services.controler_permis_affectation`), toute affectation sans `PermisConduire` (`models.py:4267`) valide — mais personne ne voit ni la garde ni le permis lui-même. Trou **(a)** : le backend existe, l'écran n'a jamais été fait. L'écran liste les permis par employé (catégorie, expiration) et les affectations actives, avec création d'une affectation qui remonte le refus serveur si le permis est absent ou expiré. **Done =** une affectation créée sans permis valide affiche le message de refus renvoyé par le serveur, jamais un filtrage client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/VehiculesPermis.jsx`, `frontend/src/features/rh/VehiculesPermis.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT82 — **Dépôt des bulletins de paie.** `BulletinPaie` (`apps/rh/models.py:4198`) stocke le PDF mensuel produit par le prestataire de paie externe (un fichier par employé/année/mois, MinIO via `records.Attachment`) ; le collaborateur le consulte déjà depuis son portail self-service, mais Administrateur/Responsable n'a aucun écran pour le DÉPOSER. Trou **(a)** : le backend existe (dépôt multipart, liste filtrable employé/année/mois), l'écran n'a jamais été fait. **Done =** un dépôt réussi apparaît immédiatement dans la liste filtrée, sans filtrage côté client d'une donnée que le serveur a déjà scopée. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/DepotsBulletinsPaie.jsx`, `frontend/src/features/rh/DepotsBulletinsPaie.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT83 — **Demandes d'allocation de congés.** `DemandeAllocation` (`apps/rh/models.py:5866`) permet de demander une allocation EXCEPTIONNELLE de jours (RTT, ancienneté, don de jours), distincte d'une demande de congé classique ; validée, elle crédite `SoldeConge.acquis` via `services.valider_allocation` — mais l'écran Congés existant (`features/rh/Conges.jsx`) ne charge que `DemandeConge`/`SoldeConge`, jamais celle-ci. Trou **(a)** : les actions `valider`/`refuser` existent, l'écran de traitement n'a jamais été fait. **Done =** valider une demande met à jour le solde affiché à partir de la réponse serveur, sans recalcul côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/DemandesAllocation.jsx`, `frontend/src/features/rh/DemandesAllocation.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT84 — **Guichet des demandes RH (attestations).** `DemandeRH` (`apps/rh/models.py:4504`) est le guichet self-service d'attestations (travail / salaire / domiciliation) : l'employé demande déjà depuis le portail, mais Administrateur/Responsable n'a aucun écran pour voir la file et déclencher `traiter` (génère le PDF via le renderer paie existant) ou `refuser`. Trou **(a)**. **Done =** un traitant sans la permission `salaires_voir` voit le refus 403 du serveur affiché tel quel, jamais un bouton masqué côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/DemandesRh.jsx`, `frontend/src/features/rh/DemandesRh.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT85 — **Modèles d'intégration (onboarding).** `ModeleIntegration`/`ElementIntegration` (`apps/rh/models.py:663-739`) définissent le gabarit de checklist d'onboarding ciblé par poste/département ; la fiche employé coche déjà les items PAR employé, mais personne ne peut créer ni modifier le MODÈLE lui-même. Trou **(a)**. **Done =** un modèle nouvellement créé est proposé à la prochaine embauche correspondante sans redéploiement. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/ModelesIntegration.jsx`, `frontend/src/features/rh/ModelesIntegration.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT86 — **Éléments variables de paie (bordereau externe).** `ElementsVariablesPaie` (`apps/rh/models.py:3779`) agrège par employé et par mois heures, absences, congés, primes et retenues — SEUL bordereau destiné à un prestataire de paie externe (le moteur interne a ses propres sélecteurs), cycle brouillon → validé → exporté, mais aucun écran ne l'alimente. Trou **(a)**. **Done =** un bordereau exporté passe visiblement en statut « exporté » avec sa date serveur, sans recalcul des totaux côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/ElementsVariablesPaie.jsx`, `frontend/src/features/rh/ElementsVariablesPaie.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT87 — **Entretiens de sortie.** `EntretienSortie` (`apps/rh/models.py:5273`) capture, un par employé sorti, un questionnaire structuré (motif principal, JSON libre, recommanderait) distinct du simple `motif_sortie` déjà saisi à l'offboarding — aucun écran ne le crée ni ne le consulte. Trou **(a)**. **Done =** un second entretien sur le même employé est refusé avec le message d'erreur serveur (contrainte d'unicité), pas par une validation front. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/EntretiensSortie.jsx`, `frontend/src/features/rh/EntretiensSortie.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT88 — **Grilles salariales.** `GrilleSalariale` (`apps/rh/models.py:4895`) définit les bandes min/max par poste et échelon, gatées en lecture ET en écriture par `HasPermission('salaires_voir')`, et sert de référence à `selectors.compa_ratio` — aucun écran ne l'affiche. Trou **(a)**. **Done =** un compte sans `salaires_voir` reçoit le 403 serveur affiché tel quel : l'écran ne masque JAMAIS côté client une donnée salariale déjà envoyée, il relaie l'erreur serveur. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/GrillesSalariales.jsx`, `frontend/src/features/rh/GrillesSalariales.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT89 — **Horaires de travail.** `HoraireTravail` (`apps/rh/models.py:126`) rend explicite l'horaire appliqué (44 h standard, Ramadan, saisonnier, temps partiel) sur une fenêtre de dates avec retour automatique au standard hors fenêtre, et alimente déjà le seuil d'heures supplémentaires — mais aucun écran ne le configure. Trou **(a)**. **Done =** une fenêtre Ramadan créée pour une période future passe de « à venir » à « active » sans recalcul de dates côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/HorairesTravail.jsx`, `frontend/src/features/rh/HorairesTravail.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT90 — **Jours bloqués (congés).** `JourBloqueConge` (`apps/rh/models.py:5732`) interdit la soumission d'une demande de congé chevauchant une période bloquée, forçable via `?forcer=1` par un Responsable — l'écran Congés existant ne le gère pas. Trou **(a)**. **Done =** une demande chevauchant une période bloquée est refusée avec le motif serveur affiché, jamais par une validation de dates côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/JoursBloquesConge.jsx`, `frontend/src/features/rh/JoursBloquesConge.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT91 — **Parcours (timeline) des employés.** `LigneParcours`/`TypeLigneParcours` (`apps/rh/models.py:5934-5960`) forment la timeline chronologique d'un employé, déjà affichée en lecture seule dans l'annuaire self-service — mais ni le catalogue de types ni les lignes n'ont d'écran d'édition. Trou **(a)**. **Done =** un type ajouté au catalogue est immédiatement sélectionnable pour une nouvelle ligne, sans redéploiement. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/ParcoursEmploye.jsx`, `frontend/src/features/rh/ParcoursEmploye.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT92 — **Fermetures collectives.** `PeriodeFermeture` (`apps/rh/models.py:4798`) modélise une fermeture imposée qui, à l'action `appliquer`, GÉNÈRE une `DemandeConge` validée par employé concerné (idempotent) — aucun écran ne la crée ni ne déclenche `appliquer`. Trou **(a)**. **Done =** ré-appliquer une fermeture déjà appliquée n'affiche aucun doublon : l'idempotence serveur est visible à l'écran. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/FermeturesCollectives.jsx`, `frontend/src/features/rh/FermeturesCollectives.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT93 — **Primes & indemnités.** `TypePrime` (`apps/rh/models.py:3887`, catalogue) et `PrimeAttribuee` (`models.py:3943`, attribution proposée → validée → payée) alimentent le bordereau de paie une fois validées — aucun des deux n'a d'écran. Trou **(a)**. Le catalogue de types vit comme onglet du même écran. **Done =** un montant pré-rempli depuis le type reste modifiable, et le statut affiché vient du serveur, jamais dérivé côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/PrimesIndemnites.jsx`, `frontend/src/features/rh/PrimesIndemnites.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [x] PACT94 — **Réglages RH.** `ReglageRH` (`apps/rh/models.py:4722`, une ligne par société) porte le rayon de géo-clôture (`geofence_metres`) du pointage chantier — l'action `mon-reglage` (GET/PATCH) n'est appelée par personne. Trou **(a)**. **Done =** désactiver le rayon GPS redevient effectif immédiatement sur le prochain pointage, sans redéploiement. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/rh/ReglagesRh.jsx`, `frontend/src/features/rh/ReglagesRh.test.jsx`, `frontend/src/features/rh/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/rh) (@model: sonnet) (@after: PACT13)
- [ ] PACT95 — **Types d'entrées ponctuelles de paie.** `TypeEntreePonctuelle` (`apps/paie/models.py:2086`) est un catalogue par société d'entrées ponctuelles hors rubriques récurrentes (pourboire, remboursement non imposable…), avec sens gain/retenue, imposable, soumis CNSS/AMO, référençable par `ElementVariable.type_entree` ; une action `seed-standard` provisionne un catalogue de départ. Trou **(a)**. **Done =** un type créé est immédiatement sélectionnable comme `type_entree` sur une entrée variable, sans redéploiement, et l'écran ne filtre jamais côté client une donnée déjà scopée par le serveur. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/paie/TypesEntreePonctuelle.jsx`, `frontend/src/features/paie/TypesEntreePonctuelle.test.jsx`, `frontend/src/features/paie/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/paie) (@model: sonnet) (@after: PACT13)
- [x] PACT96 — **Comptes portail (accès client).** Rappel de cadrage : le portail CLIENT a bien ses écrans ; ce qui manque est son ADMINISTRATION côté ERP. `ComptePortailClient` (`apps/portail/models.py:49-96`) porte le jeton d'accès et le statut actif d'un client, avec une action `provisionner-acces` (crée le vrai compte portail, mot de passe temporaire envoyé par email, jamais de réactivation silencieuse d'un compte révoqué) — aucun écran ERP ne liste ces comptes ni ne déclenche le provisioning. Trou **(a)**. **Done =** un compte provisionné apparaît immédiatement actif, une révocation empêche la connexion suivante, et **aucun mot de passe ne transite côté client**. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/portail/admin/ComptesPortailAdmin.jsx`, `frontend/src/features/portail/admin/ComptesPortailAdmin.test.jsx`, `frontend/src/features/portail/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/portail) (@model: sonnet) (@after: PACT13)
- [x] PACT97 — **Acceptations de devis (portail) : la preuve légale côté ERP.** `AcceptationDevisPortail` (`apps/portail/models.py:101-152`) enregistre la signature client (option choisie, nom du signataire, IP, date) créée en coulisse quand le client accepte depuis son portail — mais l'ERP n'a aucun écran pour consulter cette preuve liée au devis. Trou **(a)** côté ERP. **Done =** l'IP et la date affichées viennent telles quelles du serveur, jamais reformatées côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/portail/admin/AcceptationsDevisPortailAdmin.jsx`, `frontend/src/features/portail/admin/AcceptationsDevisPortailAdmin.test.jsx`, `frontend/src/features/portail/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/portail) (@model: sonnet) (@after: PACT13)
- [x] PACT98 — **Paiements de facture (portail) : le rapprochement impossible aujourd'hui.** `PaiementFacturePortail` (`apps/portail/models.py:157-218`) est créé quand un client initie un virement depuis son portail (statut `initie`) ; le seul workflow serveur est `rapprocher` (marque PAYÉ, idempotent depuis `initie` uniquement) — **sans écran, un virement reçu ne peut jamais être confirmé dans l'ERP**. Trou **(a)**, avec un trou **(c)** à signaler franchement : le statut `echoue` existe dans le modèle mais n'est posé par AUCUN code serveur — l'écran ne doit donc proposer QUE « rapprocher », **jamais un bouton « rejeter » sans service derrière**. **Done =** rapprocher un paiement le retire de la file « à rapprocher » sans recalcul de statut côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/portail/admin/PaiementsFacturePortailAdmin.jsx`, `frontend/src/features/portail/admin/PaiementsFacturePortailAdmin.test.jsx`, `frontend/src/features/portail/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/portail) (@model: sonnet) (@after: PACT13)
- [x] PACT99 — **Documents clients (portail).** `DocumentClientPortail` (`apps/portail/models.py:223-300`) dépose déjà automatiquement en GED tout document lié à un client ou un lead, avec une action `marquer_traite`. À noter honnêtement : contrairement à la règle générale du portail, **ici ni le client ni l'équipe n'ont d'écran** ; le dépôt côté client reste hors périmètre de cette tâche, qui construit uniquement le côté ERP. Trou **(a)**. **Done =** un document marqué traité change de statut visible sans dupliquer le fichier GED déjà déposé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/portail/admin/DocumentsClientPortailAdmin.jsx`, `frontend/src/features/portail/admin/DocumentsClientPortailAdmin.test.jsx`, `frontend/src/features/portail/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/portail) (@model: sonnet) (@after: PACT13)
- [x] PACT100 — **Jalons de chantier (portail).** `JalonChantierPortail` (`apps/portail/models.py:305-346`) trace une timeline de jalons par chantier avec une action `marquer_atteint`, prévue pour être vue par le client — mais, même remarque honnête qu'au-dessus, ni le client ni l'équipe n'ont d'écran aujourd'hui ; le partage client reste hors périmètre. Trou **(a)** côté ERP. **Done =** marquer un jalon atteint pose la date du jour côté serveur si elle est absente, jamais calculée côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/portail/admin/JalonsChantierPortailAdmin.jsx`, `frontend/src/features/portail/admin/JalonsChantierPortailAdmin.test.jsx`, `frontend/src/features/portail/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/portail) (@model: sonnet) (@after: PACT13)
- [x] PACT101 — **Demandes de ticket (portail).** `DemandeTicketPortail` (`apps/portail/models.py:351-418`) capture une demande avec une action `prendre_en_charge` qui crée et lie le vrai `sav.Ticket` — ni le client ni l'équipe n'ont d'écran. Trou **(a)** côté ERP ; le formulaire client d'ouverture reste hors périmètre. **Done =** prendre en charge affiche le lien vers le ticket SAV **réellement créé côté serveur**, jamais un ticket fictif côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/portail/admin/DemandesTicketPortailAdmin.jsx`, `frontend/src/features/portail/admin/DemandesTicketPortailAdmin.test.jsx`, `frontend/src/features/portail/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/portail) (@model: sonnet) (@after: PACT13)
- [x] PACT102 — **Partenaires : fiche, soumissions, commissions.** `Partenaire` (`apps/crm/models.py:1824`, onboarding prospect → agréé, action `activer`), `SoumissionLeadPartenaire` (`models.py:1904`, soumis → qualifié → converti → rejeté, action `qualifier`) et `CommissionPartenaire` (`models.py:1960`, action `marquer_payee` + relevé agrégé) sont liés au même partenaire. Le portail externe montre déjà ce résumé EN LECTURE au partenaire, mais **l'équipe interne n'a AUCUN écran** pour agréer un partenaire, qualifier ses soumissions ou régler ses commissions. Trou **(a)** sur les trois ; un seul écran à onglets. **Done =** qualifier une soumission fait apparaître le lead réel créé par le serveur, jamais un lead fictif construit côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/crm/Partenaires.jsx`, `frontend/src/features/crm/Partenaires.test.jsx`, `frontend/src/features/crm/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/crm) (@model: sonnet) (@after: PACT13)
- [x] PACT103 — **Concurrents sur affaires perdues.** `ConcurrentPerte` (`apps/crm/models.py:1350`) capture, sur un lead marqué perdu, le concurrent gagnant et son prix — aucun écran ne l'appelle, y compris le popover « perdu » existant. Trou **(a)**. À signaler comme dette distincte (trou **(c)**, hors périmètre ici) : `litiges.Reclamation.concurrent_nom/concurrent_prix` capture déjà le MÊME besoin métier sur un autre objet, sans unification — cette tâche construit l'écran de `crm.ConcurrentPerte` tel qu'il existe et ne touche pas à `litiges`. **Done =** le concurrent saisi apparaît dans l'analyse par lead perdu sans dupliquer une saisie déjà faite côté litiges. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/crm/ConcurrentsPerte.jsx`, `frontend/src/features/crm/ConcurrentsPerte.test.jsx`, `frontend/src/features/crm/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/crm) (@model: sonnet) (@after: PACT13)
- [x] PACT104 — **Points de contact : la saisie manuelle manque, pas la lecture.** `PointContact` (`apps/crm/models.py:1432`) enregistre chaque touchpoint d'un lead (ordre auto-incrémenté, auteur et société posés serveur). La lecture agrégée premier/dernier contact par lead est **déjà câblée** et consommée par la timeline — mais la collection racine `/crm/points-contact/` (créer, modifier, supprimer, action d'attribution) n'a aucun appelant. Trou **(a) d'ÉCRITURE uniquement** : ce n'est pas un renommage, et la moitié lecture n'est pas à refaire. **Done =** un point de contact ajouté manuellement apparaît immédiatement dans la timeline déjà affichée, sans dupliquer son calcul d'attribution. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/crm/workspace/TimelineTab.jsx`, `frontend/src/features/crm/workspace/TimelineTab.test.jsx`, `frontend/src/features/crm/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/crm) (@model: sonnet) (@after: PACT13)
- [x] PACT105 — **Revues de compte : la création manque, pas l'affichage.** `RevueCompte` (`apps/crm/models.py:2274` — réunion structurée : participants, décisions, prochaine action) s'affiche **déjà en lecture** dans le plan de compte, mais **aucun formulaire n'écrit jamais** sur `/crm/revues-compte/` : l'historique reste vide tant que rien n'est créé. Trou mixte, à énoncer tel quel : la lecture existe, l'ÉCRITURE est le vrai trou **(a)**. **Done =** une revue créée apparaît dans la liste déjà affichée sans recharger la page. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/crm/comptes/PlanComptePage.jsx`, `frontend/src/pages/crm/comptes/PlanComptePage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/crm) (@model: sonnet) (@after: PACT13)
- [ ] PACT106 — **Avis clients et routage Google.** `AvisClient` (`apps/marketing/models.py:1159`) capture une note de 1 à 5 et un texte, avec les actions `recevoir` et `pousser_google` (routage Google Reviews si `GOOGLE_REVIEW_URL` est configuré, sans effet sinon) — aucun écran ne l'appelle. Trou **(a)**. **Done =** l'action « pousser vers Google » reste un bouton neutre sans erreur visible quand la clé n'est pas configurée, exactement comme le serveur la traite — jamais un bouton mort ni une erreur brute. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/marketing/AvisClients.jsx`, `frontend/src/features/marketing/AvisClients.test.jsx`, `frontend/src/features/marketing/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/marketing) (@model: sonnet) (@after: PACT13)
- [ ] PACT107 — **Enquêtes NPS post-installation.** `EnqueteNPS` (`apps/marketing/models.py:1100`) est une enquête de satisfaction CLIENT post-chantier (score 0-10, catégorisation promoteur/passif/détracteur) — distincte du Pulse eNPS interne aux employés (déjà câblé) et du générique `Enquete` (où NPS n'est qu'un type de question). Aucun écran n'appelle ce modèle. Trou **(a)**. **Done =** le score NPS affiché vient de l'action serveur, jamais recalculé côté client à partir des réponses brutes. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/marketing/EnquetesNps.jsx`, `frontend/src/features/marketing/EnquetesNps.test.jsx`, `frontend/src/features/marketing/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/marketing) (@model: sonnet) (@after: PACT13)
- [ ] PACT108 — **Journal des messages WhatsApp entrants.** `MessageWhatsAppEntrant` (`apps/marketing/models.py:993`) journalise en LECTURE SEULE chaque message entrant capturé par le webhook Meta, qui crée déjà un lead brouillon — distinct de l'aperçu sortant avant `wa.me` déjà câblé ailleurs. Aucun écran ne consulte ce journal. Trou **(a)**. **Done =** la liste reste purement en lecture : aucune action d'écriture n'est ajoutée côté client sur une ressource que le serveur expose en lecture seule. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/marketing/MessagesWhatsapp.jsx`, `frontend/src/features/marketing/MessagesWhatsapp.test.jsx`, `frontend/src/features/marketing/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/marketing) (@model: sonnet) (@after: PACT13)
- [ ] PACT109 — **Détail des participations et certificat d'enquête.** `ReponseEnquete` (`apps/marketing/models.py:1431`) n'est délibérément PAS une collection CRUD : ce sont l'action `participations` déjà exposée (liste individuelle filtrable réussi/échoué) et la route isolée du **certificat PDF** qui ne sont exploitées par aucun écran — l'écran de résultats n'affiche que l'agrégat. Trou **(b)/(c)** mêlé, **pas** une reconstruction depuis zéro : on complète un écran existant. **Done =** le certificat téléchargé est le PDF renvoyé tel quel par le serveur, jamais généré côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/marketing/EnqueteResultats.jsx`, `frontend/src/features/marketing/EnqueteResultats.test.jsx`, `frontend/src/features/marketing/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/marketing) (@model: sonnet) (@after: PACT13)
- [ ] PACT110 — **Pilotage du bandit publicitaire : bras, décisions, statistiques.** `ExperimentArm`/`DecisionLog`/`ArmDailyStat` (`apps/adsengine/models.py:1210/1306/1252`) forment la hiérarchie réelle du bandit. L'écran des expériences a **DÉJÀ** des sections « Bras » et « Journal des décisions » — mais elles lisent des noms de champs **qu'aucun sérialiseur ne renvoie** : ces sections sont **structurellement vides en production**, sans aucune erreur. Trou mixte à ne pas confondre : un **câblage cassé** (bras, décisions) à corriger, PLUS deux vues jamais construites (série quotidienne par bras, collection des décisions toutes expériences). **Done =** la section « Bras » affiche un bras réel dès qu'il en existe un, sans dépendre d'un champ que l'API ne renvoie jamais. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/adsengine/ExperimentsScreen.jsx`, `frontend/src/features/adsengine/ExperimentsScreen.test.jsx`, `frontend/src/features/adsengine/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet) (@after: PACT13)
- [ ] PACT111 — **Fabrique créative : génération ancrée, lots, items hors-lot.** Le pipeline réel est `generation/variantes-ancrees/` (variantes ancrées aux faits, gatée par clé) → `CreativeGenerationBatch` (lot, actions approuver/rejeter, `models.py:1565`) → `CreativeBacklogItem` (`models.py:1625`). Or le bouton « Générer des variantes » appelle en réalité **un autre endpoint, plus simple et sans ancrage aux faits** ; l'écran de backlog n'a **aucun bouton pour REJETER** un lot (seule l'approbation existe) ; et tout item déposé sans lot est **silencieusement ignoré** par la vue groupée. Trou **(a)** sur les trois. **Done =** un item déposé sans lot apparaît en interrogeant directement la collection, sans modifier la vue groupée existante. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/adsengine/BacklogScreen.jsx`, `frontend/src/features/adsengine/BacklogScreen.test.jsx`, `frontend/src/features/adsengine/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet) (@after: PACT13)
- [ ] PACT112 — **Policy créative : la vraie, pas la constante codée en dur.** `CreativePolicy` (`apps/adsengine/models.py:1111`, une par société) permet à chaque société de définir SES règles interdites et autorisées — mais l'écran lit un champ que le sérialiseur ne renvoie **jamais** et retombe systématiquement sur **une constante écrite en dur dans le frontend** : le fondateur ne peut donc jamais personnaliser ses règles réelles alors que le backend est prêt. Trou **(a)**, et précision qui compte : ce n'est PAS un renommage — **la donnée affichée aujourd'hui est une constante, pas la policy**. **Done =** une règle interdite ajoutée est celle réellement appliquée à la génération suivante, pas la valeur par défaut. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/adsengine/ConnectionScreen.jsx`, `frontend/src/features/adsengine/ConnectionScreen.test.jsx`, `frontend/src/features/adsengine/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet) (@after: PACT13)
- [ ] PACT113 — **Plan de vol : le plan composé n'est jamais sauvegardé.** `FlightPhase` (`apps/adsengine/models.py:1716`) doit porter les phases d'une feuille de route créative de 3 à 6 mois — mais l'écran compose ses phases dans un état client **100 % éphémère** et n'appelle jamais la ressource : **fermer l'onglet perd tout le plan**. Trou **(a)** : le backend existe, l'écran affiche un plan qui n'est jamais persisté. **Done =** recharger la page après avoir composé un plan affiche le MÊME plan, pas un formulaire vide. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/adsengine/FlightPlanScreen.jsx`, `frontend/src/features/adsengine/FlightPlanScreen.test.jsx`, `frontend/src/features/adsengine/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet) (@after: PACT13)
- [ ] PACT114 — **Stérilisation des instruments : cycles et traçabilité sanitaire.** `CycleSterilisation`/`InstrumentSterilise` (`apps/sante/models.py:736-801`) tracent chaque cycle d'autoclave (conforme ou non) et les instruments qui y sont passés ; un cycle non conforme émet déjà l'événement repris par QHSE, et une action serveur retrouve **les patients exposés** pour un rappel sanitaire — mais le client API `sante` ne référence ni l'un ni l'autre, contrairement à toutes ses autres ressources. Trou **(a)**. **Done =** un cycle non conforme affiche, sans aucun recalcul client, la liste des patients renvoyée par l'action serveur. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/sante/SterilisationScreen.jsx`, `frontend/src/features/sante/SterilisationScreen.test.jsx`, `frontend/src/features/sante/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/sante) (@model: sonnet) (@after: PACT13)
- [ ] PACT115 — **Créneaux disponibles à la prise de rendez-vous.** La vue `disponibilites` (`apps/sante/viewsets.py:426-464`) calcule déjà les créneaux libres d'un praticien pour un jour donné, mais l'agenda crée un rendez-vous **sans jamais l'appeler** — le praticien saisit une heure à l'aveugle. Trou **(a)** : le backend existe, l'intégration à l'écran existant n'a jamais été faite. **Done =** changer le praticien ou la date recharge les créneaux depuis le serveur, jamais un calcul de disponibilité côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/sante/SanteAgenda.jsx`, `frontend/src/features/sante/SanteAgenda.test.jsx`, `frontend/src/features/sante/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/sante) (@model: sonnet) (@after: PACT13)
- [ ] PACT116 — **Inscription d'une nouvelle société : le backend est entier, la porte n'existe pas.** `POST /auth/register-company/` (`authentication/views.py:526-603`) crée déjà tout en une requête publique : société, profil, rôles système, compte Directeur, hooks de démarrage. Mais **il n'existe AUCUNE route ni aucun lien pour l'atteindre** : pas de `/register` dans le routeur, aucun appel depuis l'écran de connexion, et le bouton « Démarrer gratuitement » de la page d'accueil repointe vers `/login`. Trou **(a)** dans sa forme la plus nette : la fonctionnalité SaaS d'inscription existe et est inatteignable. **Done =** une inscription réussie crée la société, les erreurs 400 par champ du serveur sont affichées telles quelles, et aucune règle d'unicité n'est dupliquée côté client. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/RegisterCompany.jsx`, `frontend/src/pages/RegisterCompany.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/auth) (@model: sonnet) (@after: PACT13)

> **Faux positifs écartés après vérification (4).** `users/avatar-image` : l'écran existe (téléversement dans la gestion des utilisateurs, affichage via le composant Avatar). `adsengine/connexions`, `adsengine/garde-fous`, `adsengine/reconciliations` : ces trois ViewSets CRUD sont des **doublons morts par construction** — l'écran couvre déjà intégralement chaque sujet via le singleton correspondant (le code source note lui-même que le ViewSet `garde-fous` est distinct et inutilisé).
>
> **Sans écran par design (1).** `rh/carrieres` : endpoints PUBLICS destinés aux candidats, désactivés par défaut (`CAREERS_ENABLED`) et renvoyant 404 tant qu'ils le sont ; le code précise que la page qui les consomme relève du plan WEB, pas de l'ERP.
### PACT §E4 — Écrans manquants : socle, configuration, CPQ, GED, abonnements, IA (PACT117-PACT147)

- [x] PACT117 — **Modèles brandés (PDF, email, WhatsApp).** Trou **(a)**. `core.BrandedTemplate` (`core/models.py:1211`) et son ViewSet (`core/views.py:619`, avec une action d'aperçu qui rend le modèle sur un contexte d'exemple) sont exposés et **aucun fichier frontend ne les appelle**. Lecture ouverte, écriture réservée aux profils élevés — le serveur revérifie. **Done =** un modèle « email / relance devis » est créé, l'aperçu affiche le rendu sur le contexte de test avant enregistrement. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/parametres/ModelesBrandesSection.jsx`, `frontend/src/pages/parametres/ModelesBrandesSection.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/parametres) (@model: opus) (@after: PACT13)
- [ ] PACT118 — **Édition en masse : brancher le registre du socle plutôt qu'un dixième endpoint maison.** Constat à double face, à ne pas confondre. **(a)** le moteur générique existe et est testé (`core/bulk_edit.py`) — mais **(c)** **aucune app ne l'a jamais branché** : la fonction d'enregistrement d'une cible n'est appelée que depuis les tests du socle, jamais depuis le démarrage d'une app réelle. Pendant ce temps le frontend a résolu le besoin **autrement** : quatre écrans appellent déjà chacun **leur propre** endpoint de mise à jour en masse. **Ce n'est donc ni un écran manquant ni un renommage : c'est une capacité de fondation inutilisée, en concurrence avec une solution déjà déployée ailleurs.** Le travail réel : enregistrer 2 ou 3 cibles réelles, et exposer un accès générique que les listes peuvent consommer **sans écrire leur propre endpoint** — à réserver aux écrans qui n'en ont pas déjà un, **sans retoucher les quatre existants**. **Done =** un écran de liste sans endpoint propre affiche « Modifier en masse » et applique un changement via le registre du socle. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/core/useBulkEditCible.js`, `frontend/src/features/core/useBulkEditCible.test.js`, `frontend/src/features/core/module.config.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/core) (@model: opus) (@after: PACT13)
- [x] PACT119 — **Registre de consentement (loi 09-08 / CNDP).** Trou **(a)**. `core.ConsentRecord` (`core/models.py:1277` — sujet, finalité, accordé ou non, version du texte, IP de confirmation : la preuve du double opt-in) n'a jamais d'écran. **Ne pas confondre** : l'onglet Confidentialité existant couvre déjà le registre des traitements et les demandes d'exercice de droits, mais **pas** celui-ci ; et son bloc « benchmarking » est un simple booléen d'entreprise, un tout autre mécanisme. Un quatrième bloc à ajouter à l'écran existant, pas un nouvel écran. **Done =** l'onglet Confidentialité liste les consentements par sujet et finalité avec leur statut donné ou révoqué, et permet d'en ajouter un. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/parametres/ConfidentialiteSection.jsx`, `frontend/src/pages/parametres/ConfidentialiteSection.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/parametres) (@model: opus) (@after: PACT13)
- [ ] PACT120 — **Partage interne d'un tableau de bord.** Trou **(a)**. `core.DashboardPartageInterne` (`core/models.py:1842`) partage à des **personnes ou rôles choisis** avec un niveau lecture ou édition. À ne pas confondre avec l'écran de partage existant, qui ne couvre que les **liens publics anonymes à jeton** — deux mécanismes distincts qui cohabitent sur le **même** écran. **Done =** un second bloc « Partage interne » permet de choisir un utilisateur ou un rôle, un niveau, et de le retirer. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/reporting/DashboardSharePage.jsx`, `frontend/src/pages/reporting/DashboardSharePage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/reporting) (@model: opus) (@after: PACT13)
- [x] PACT121 — **Paiement en ligne d'une facture.** Trou **(a)**. `core.PaymentTransaction` (`core/models.py:884`) initie un paiement et sait rafraîchir son statut auprès du prestataire — **zéro écran ne l'appelle**. À distinguer de « Encaisser », qui enregistre un paiement **déjà reçu** : ce n'est pas le même chemin. Sans compte marchand configuré, la création reste sans effet et propre (transaction « initiée », aucun appel réseau). **Done =** depuis une facture, « Payer en ligne » crée la transaction et affiche soit le lien de paiement, soit le message « prestataire non configuré » — **jamais une erreur brute**. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ventes/PaiementEnLigneDialog.jsx`, `frontend/src/features/ventes/PaiementEnLigneDialog.test.jsx`, `frontend/src/features/ventes/module.config.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/ventes) (@model: opus) (@after: PACT13, PACT17)
- [ ] PACT122 — **Explorateur de données (requêtes sauvegardées).** Trou **(a)**. `core.SavedQuery` et son ViewSet (`core/views.py:326` — catalogue de jeux de données, exécution, sauvegarde) existent, testés, sans écran. **Fait à afficher honnêtement, pas à masquer** : un seul jeu de données est enregistré en production aujourd'hui — le moteur fonctionne de bout en bout mais le catalogue sera mince au lancement. L'écran doit montrer une liste courte, pas prétendre le contraire. **Done =** l'écran liste les jeux de données réels, permet de construire une requête simple, de l'exécuter et de la sauvegarder en personnel ou en société. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/core/ExplorateurDonneesPage.jsx`, `frontend/src/features/core/ExplorateurDonneesPage.test.jsx`, `frontend/src/features/core/module.config.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/core) (@model: opus) (@after: PACT13)
- [x] PACT123 — **Exports planifiés.** Trou **(a)**. `core.ScheduledExport` (`core/views.py:391`) planifie un export vers SFTP ou S3 — à ne pas confondre avec l'export manuel ponctuel déjà présent dans Paramètres, sans lien avec ce modèle. Sans identifiants provisionnés, l'exécution reste sans effet et le statut vaut « non configuré ». **Done =** création complète (titre, jeu de données, format, destination, planification), exécution immédiate, et **affichage explicite du dernier statut y compris « non configuré »** — jamais un plantage. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/parametres/ExportsPlanifiesSection.jsx`, `frontend/src/pages/parametres/ExportsPlanifiesSection.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/parametres) (@model: opus) (@after: PACT13)
- [ ] PACT124 — **Modifier une définition de workflow existante.** Trou **(a)** avec un symptôme concret : l'écran des workflows **ne fait que créer** une nouvelle définition avec ses étapes puis réinitialise son formulaire — **il n'existe aucun chemin pour rouvrir une définition déjà créée.** Le ViewSet d'étapes (`core/views.py:226`) qui le permettrait n'est jamais appelé (une simple mention en commentaire dans le client). **Done =** cliquer une définition existante ouvre ses étapes en édition — ajout, retrait, réordonnancement — et enregistre. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/workflow/WorkflowsScreen.jsx`, `frontend/src/features/workflow/WorkflowsScreen.test.jsx`, `frontend/src/features/core/module.config.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/core) (@model: opus) (@after: PACT13)
- [ ] PACT125 — **Configurateur guidé (assistant produit).** Trou **(a)**, et le module est **entièrement absent côté frontend** : aucun fichier `features/cpq` n'existe. Le backend pilote pourtant une session questions-réponses complète (`apps/cpq/views.py:164-265`) qui résout produits et offres groupées via le moteur de règles et peut créer un devis brouillon ; l'administration des questions est un **onglet du même écran**, pas une tâche séparée. **Done =** une session est démarrée, les questions actives sont répondues, le résultat résolu s'affiche et un devis brouillon est généré ; un onglet gère les questions. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/cpq/module.config.jsx`, `frontend/src/features/cpq/ConfigurateurPage.jsx`, `frontend/src/features/cpq/ConfigurateurPage.test.jsx`. (ROUTINE) (@lane: frontend/cpq) (@model: sonnet) (@after: PACT13)
- [ ] PACT126 — **Compatibilité produits : règles et test.** Trou **(a)**. Les règles entre deux produits (incompatible, requiert, recommandé) et la validation d'une liste de produits — qui **sépare les violations bloquantes des simples avertissements** — existent sans écran. **Done =** la grille des règles est éditable et un panneau « Tester » poste une liste de produits en affichant bloquantes et avertissements séparément. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/cpq/CompatibilitePage.jsx`, `frontend/src/features/cpq/CompatibilitePage.test.jsx`, `frontend/src/features/cpq/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/cpq) (@model: sonnet) (@after: PACT13)
- [ ] PACT127 — **Offres groupées.** Trou **(a)**. Un prix total optionnel réparti au prorata, des lignes en mode prix fixe / remise en pourcentage / prix du composant, et une action qui insère les lignes dans un devis. **Done =** un ensemble est créé avec ses lignes et appliqué à un devis existant depuis l'écran. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/cpq/OffresGroupeesPage.jsx`, `frontend/src/features/cpq/OffresGroupeesPage.test.jsx`, `frontend/src/features/cpq/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/cpq) (@model: sonnet) (@after: PACT13)
- [ ] PACT128 — **Options de configuration d'un produit.** Trou **(a)**. Les groupes d'options d'un produit (« Onduleur », « Batterie », obligatoires ou non) n'ont pas d'écran. La fiche produit a **déjà** un système d'onglets : c'est là que celui-ci s'ajoute, pas dans un écran séparé. **Done =** un onglet « Options » sur la fiche produit liste et ajoute les groupes d'options de ce produit. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/stock/ProduitOptionsTab.jsx`, `frontend/src/pages/stock/ProduitOptionsTab.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/stock) (@model: sonnet) (@after: PACT13)
- [x] PACT129 — **Prix contractuels négociés par client.** Trou **(a)**. Prix HT négocié par client et par produit, avec fenêtre de validité et motif, réservé aux profils élevés. La fiche client a déjà des onglets : « Tarifs négociés » y est plus cohérent qu'un écran autonome. **Done =** un onglet sur la fiche client liste les prix négociés avec leurs dates de validité et permet d'en créer un. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/crm/ClientPrixContractuelsTab.jsx`, `frontend/src/pages/crm/ClientPrixContractuelsTab.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/crm) (@model: sonnet) (@after: PACT13)
- [ ] PACT130 — **Paramètres CPQ : seuils de marge et paliers d'approbation de remise.** Trou **(a)**, et le regroupement n'est pas arbitraire — **le code lui-même le demande** : les deux ViewSets portent le commentaire « pour **un** écran Paramètres CPQ, plus de dépendance au Django admin ». `SeuilMargeFamille` porte la marge minimale par catégorie (**donnée interne : ne doit jamais apparaître dans un PDF client**) et `RegleApprobationRemise` les paliers d'approbation par intervalle de remise. Les deux ne sont gérables **qu'en passant par le Django admin** aujourd'hui. **Done =** un écran à deux onglets remplace tout besoin de Django admin sur ces deux sujets. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/cpq/ParametresCpqPage.jsx`, `frontend/src/features/cpq/ParametresCpqPage.test.jsx`, `frontend/src/features/cpq/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/cpq) (@model: sonnet) (@after: PACT13)
- [x] PACT131 — **Coffres-forts documentaires.** Trou **(a)**. `Coffre` (`apps/ged/models.py:360`) est un espace confidentiel appartenant à un employé **ou** à un client, avec contrôle d'accès propriétaire et administrateur. Zéro écran, zéro entrée de navigation. **Done =** l'écran liste les coffres accessibles à l'utilisateur, permet d'en créer un (propriétaire employé **ou** client, jamais les deux) et d'y voir les documents classés. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/CoffresPage.jsx`, `frontend/src/features/ged/advanced/CoffresPage.test.jsx`, `frontend/src/features/ged/module.config.jsx`. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [x] PACT132 — **Règles de dossier : action automatique au dépôt.** Trou **(a)**. `RegleDossier` (`apps/ged/models.py:2711`) évalue une condition au dépôt d'un document et enchaîne des actions, avec journal d'exécution. **Le dépôt documente lui-même ce trou** : un commentaire d'un écran GED existant le décrit comme « non prioritaire en attendant l'arbitrage du fondateur » — **cette tâche EST cet arbitrage**. **Done =** les règles par dossier sont listées (condition simple champ/valeur en première version, actions en séquence), activables, avec un journal des dernières exécutions. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/ReglesDossierPage.jsx`, `frontend/src/features/ged/advanced/ReglesDossierPage.test.jsx`, `frontend/src/features/ged/module.config.jsx`. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [x] PACT133 — **Règles d'accès par métadonnée.** Trou **(a)**, même commentaire d'attente dans le dépôt. `RegleAclMetadonnee` (`apps/ged/models.py:2919`) accorde un niveau d'accès à un **rôle** selon une condition sur les tags, le type ou les données personnalisées, **recalculée à chaque lecture** sans matérialiser de ligne. Sujet d'accès documentaire : à manier avec soin même si l'écran reste simple. **Done =** les règles sont listées (condition, rôle cible, niveau, priorité) et activables. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/ReglesAclPage.jsx`, `frontend/src/features/ged/advanced/ReglesAclPage.test.jsx`, `frontend/src/features/ged/module.config.jsx`. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [x] PACT134 — **Demandes de disposition en fin de rétention.** Trou **(a)**. `DemandeDisposition` (`apps/ged/models.py:3005`) organise la revue humaine avant de détruire ou archiver un lot de documents échus, avec circuit demandeur → approbateur, **certificat de destruction immuable** à l'exécution, et **exclusion d'office des documents sous conservation légale**. L'écran de rétention existe déjà avec un onglet « Échus » qui liste précisément les documents visés : la disposition est la **suite de ce même écran**. **Opération irréversible** : confirmation explicite obligatoire et certificat affiché après exécution. **Done =** un onglet « Dispositions » crée une demande depuis les documents échus, l'approuve ou la rejette, et affiche les certificats émis. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/RetentionPage.jsx`, `frontend/src/features/ged/advanced/RetentionPage.test.jsx`, `frontend/src/features/ged/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [x] PACT135 — **Envoi en masse pour signature.** Trou **(a)**. `LotEnvoi` (`apps/ged/models.py:3134`) fusionne un modèle de document avec N destinataires (CSV ou sélection de clients) et produit une demande de signature par destinataire, avec compteurs envoyé / vu / signé / refusé. L'écran d'approbation gère déjà les demandes unitaires et le circuit multi-signataires : l'envoi en masse est **un mode supplémentaire du même écran**. **Done =** un onglet « Envoi en masse » choisit un modèle et une liste, puis affiche les compteurs du lot créé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/ApprobationPage.jsx`, `frontend/src/features/ged/advanced/ApprobationPage.test.jsx`, `frontend/src/features/ged/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [x] PACT136 — **Routage documentaire automatique.** Trou **(a)**, trouvé indépendamment (absent du commentaire d'attente du dépôt). `RoutageDocumentaire` (`apps/ged/models.py:3185`) fait résoudre à un code de module un dossier cible avec des jetons de substitution et des tags par défaut ; **sans réglage, il ne fait rien** — comportement strictement inchangé. **Done =** un écran de paramètres GED liste les routages source → dossier cible avec un aperçu des segments résolus, et permet d'en créer ou d'en désactiver. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/RoutagesPage.jsx`, `frontend/src/features/ged/advanced/RoutagesPage.test.jsx`, `frontend/src/features/ged/module.config.jsx`. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [x] PACT137 — **Planifications de document.** Trou **(a)**. `PlanificationDocument` (`apps/ged/models.py:2881`) porte une échéance, un assigné et une notification — **volontairement local à la GED** plutôt que passé par l'activité générique. **Done =** les planifications sont listées par échéance (faites, à venir), créables sur un document et marquables faites. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/ged/advanced/PlanificationsPage.jsx`, `frontend/src/features/ged/advanced/PlanificationsPage.test.jsx`, `frontend/src/features/ged/module.config.jsx`. (ROUTINE) (@lane: frontend/ged) (@model: sonnet) (@after: PACT13)
- [ ] PACT138 — **Abonnements : plans, options, paliers d'usage, compteurs.** Trou **(a)**, 4 ressources qui n'ont **rien** côté frontend et forment **un seul écran** : le plan vendable (code, nom, prix de base, engagement, cadence), les options payantes rattachables (leurs lignes de rattachement sont un **onglet**, pas une tâche), la tarification par palier de volume et le compteur d'usage idempotent par période. **Piège de nommage à ne pas reproduire en construisant** : le plan d'abonnement **vendu aux clients** n'a aucun rapport avec le plan de licence **du tenant à l'ERP lui-même** — le modèle le signale explicitement. **Done =** un écran à onglets gère plans, options avec leurs rattachements, paliers et compteurs. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/contrats/AbonnementsPage.jsx`, `frontend/src/features/contrats/AbonnementsPage.test.jsx`, `frontend/src/features/contrats/module.config.jsx`. (ROUTINE) (@lane: frontend/contrats) (@model: sonnet) (@after: PACT13)
- [ ] PACT139 — **Relance d'impayé : séquences et étapes.** Trou **(a)**. Une séquence nommée rattachable à un contrat, et ses étapes (décalage en jours après l'échéance impayée, canal email / WhatsApp / notification interne, déclenchement ou non de la suspension) — une seule surface, une séquence n'ayant aucun sens sans ses étapes. Sans séquence rattachée, le comportement actuel reste **strictement inchangé** : ajout pur. **Done =** une séquence est créée avec ses étapes ordonnées, et le journal d'exécution par contrat est consultable. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/contrats/DunningPage.jsx`, `frontend/src/features/contrats/DunningPage.test.jsx`, `frontend/src/features/contrats/module.config.jsx`. (ROUTINE) (@lane: frontend/contrats) (@model: sonnet) (@after: PACT13)
- [x] PACT140 — **Objets métier personnalisés.** Trou **(a)**. La définition d'un objet sans code (code, libellé, icône) et le CRUD dynamique de ses enregistrements — **avec schémas de liste et de formulaire auto-générés** — n'ont aucun écran. Le mécanisme sous-jacent de définition de champs **est déjà utilisé ailleurs** pour les modules natifs : il faut le **réutiliser** en le pointant sur l'objet personnalisé (la propriété nécessaire est déjà prévue par le modèle), **pas réécrire un second moteur de champs**. Capacité transversale : tout futur besoin métier peut créer son objet sans code. **Done =** un objet personnalisé est créé, ses champs ajoutés avec l'éditeur existant, et un écran générique liste et édite ses enregistrements à partir du schéma auto-généré. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/parametres/ObjetsPersonnalisesPage.jsx`, `frontend/src/features/customobjects/CustomObjectRecordsPage.jsx`, `frontend/src/features/customobjects/CustomObjectRecordsPage.test.jsx`, `frontend/src/features/parametres/module.config.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/parametres) (@model: opus) (@after: PACT13)
- [x] PACT141 — **Brouillon de relance ou de réponse assisté.** Trou **(a)**. L'endpoint aplatit le fil de discussion et les activités d'une fiche et renvoie un **brouillon français éditable, jamais envoyé automatiquement** — réutilisé par la relance commerciale et la réponse SAV. Zéro appelant. **Sans clé d'API configurée**, le serveur répond explicitement « rédaction manuelle requise » sans aucun appel réseau : l'écran doit afficher **ce message**, jamais un bouton mort ni une erreur brute. **Done =** depuis la fiche lead, un brouillon éditable est proposé (jamais envoyé seul) ; sans clé, un message explicite remplace le bouton. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/crm/leads/LeadDetailPage.jsx`, `frontend/src/pages/crm/leads/LeadDetailPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/crm) (@model: sonnet) (@after: PACT13)
- [x] PACT142 — **Compte rendu d'intervention depuis un mémo vocal.** Trou **(a)**. L'endpoint transcrit puis structure en diagnostic, travaux, pièces et recommandations pour pré-remplir le rapport SAV — **ne change jamais le statut du ticket et ne conserve jamais l'audio**. Même exigence de clé absente que PACT141 : message explicite, **jamais un téléversement silencieux qui échoue**. **Done =** un mémo vocal pré-remplit le formulaire de rapport, éditable avant sauvegarde. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/sav/TicketsPage.jsx`, `frontend/src/pages/sav/TicketsPage.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/sav) (@model: sonnet) (@after: PACT13)
- [ ] PACT143 — **Description commerciale d'un produit.** Trou **(a)**. L'endpoint produit une description et une variante courte à valider, avec une **liste blanche de champs qui empêche `prix_achat` d'être transmis au fournisseur** — règle du dépôt appliquée au niveau du service. Même exigence de clé absente. **Done =** un brouillon et sa variante courte sont proposés à la validation avant sauvegarde. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/stock/ProduitForm.jsx`, `frontend/src/pages/stock/ProduitForm.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/stock) (@model: sonnet) (@after: PACT13)
- [ ] PACT144 — **Rapport d'activité périodique.** Trou **(a)**, avec un garde-fou qui mérite d'être visible à l'écran : **les chiffres viennent des sélecteurs serveur, le modèle ne fait que les mettre en phrases**, et un narratif contenant un chiffre absent des métriques est **refusé côté serveur, jamais rendu**. Même exigence de clé absente. **Done =** l'écran affiche les métriques serveur **et** le narratif éditable, et le refus serveur est visible s'il survient. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/reporting/RapportPeriodePage.jsx`, `frontend/src/features/reporting/RapportPeriodePage.test.jsx`, `frontend/src/features/reporting/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/reporting) (@model: sonnet) (@after: PACT13)
- [x] PACT145 — **Assistant de paramétrage.** Trou **(a)**, cas particulier utile : l'endpoint répond en français avec des **liens profonds vers les écrans de paramètres concernés, sans jamais rien modifier**, et **dégrade sur une FAQ statique quand aucune clé n'est configurée** — il renvoie donc toujours un lien. L'écran ne désactive donc rien : il affiche simplement une réponse plus sommaire. **Done =** un champ « Où régler… ? » renvoie une réponse et un lien cliquable vers le bon écran, avec ou sans clé configurée. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/parametres/AssistantConfigWidget.jsx`, `frontend/src/pages/parametres/AssistantConfigWidget.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/parametres) (@model: sonnet) (@after: PACT13)
- [ ] PACT146 — **Générateur de rapports croisés.** Trou **(a)**. `RapportDefinitionViewSet` (`apps/reporting/rapport_builder.py`) rejoue une requête et la croise en tableau croisé quand une spécification est fournie. **Chevauchement à signaler au fondateur — ce n'est pas une divergence de nom** : ce modèle est le frère structurel de la requête sauvegardée de PACT122 (même moteur, même visibilité), avec en plus le croisement et l'abonnement d'envoi planifié. Les deux écrans peuvent être construits séparément mais **gagneraient à converger plus tard**. **Done =** une définition est créée, exécutée, et son résultat affiché à plat ou croisé. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/features/reporting/RapportBuilderPage.jsx`, `frontend/src/features/reporting/RapportBuilderPage.test.jsx`, `frontend/src/features/reporting/module.config.jsx` + l'écran parent qui le monte. (ROUTINE) (@lane: frontend/reporting) (@model: sonnet) (@after: PACT13)
- [x] PACT147 — **Politiques d'approbation configurables.** Trou **(a)**. `ApprovalPolicy` (`apps/parametres/models_approvals.py`) définit quel type d'action — remise, montant de devis, bon de commande, dépense, contrat, avoir — déclenche une approbation, à partir de quel seuil et vers quel palier, tracé au journal d'audit. **Ne pas confondre avec l'écran d'approbations déjà construit** : celui-ci est la **boîte de réception** des approbations en attente — il consomme des décisions, il **ne configure pas** ce qui les déclenche. Cette tâche construit la configuration en amont, qui n'existe nulle part. Opt-in strict : sans politique activée, rien ne change. **Done =** une politique est créée et activée par type d'action avec son seuil et son palier, et chaque changement apparaît au journal d'audit. **Et l'écran est ATTEIGNABLE** : route déclarée + entrée de nav (ou onglet monté dans son écran parent), et `python scripts/check_ecrans_atteignables.py` ne signale aucune nouvelle dette — un écran livré mais injoignable ne compte PAS comme fait. Files: `frontend/src/pages/parametres/ApprobationsPolitiquesSection.jsx`, `frontend/src/pages/parametres/ApprobationsPolitiquesSection.test.jsx`, `frontend/src/router/index.jsx` + l'écran parent qui le monte. (ARCH) (@lane: frontend/parametres) (@model: opus) (@after: PACT13)

> **Aucun faux positif dans ce bloc**, mais quatre pièges de nommage ont été écartés **un par un** — ils auraient tous fait conclure à tort qu'un écran existait : la requête sauvegardée du socle n'est pas la vue de liste sauvegardée du CRM ; le registre de consentement n'est pas le booléen de benchmarking d'entreprise ; le partage interne d'un tableau de bord n'est pas son lien public ; et la politique d'approbation n'est pas la boîte de réception des approbations.
>
> **Signalé hors périmètre, sans tâche créée :** les règles produit CPQ (`cpq/regles/`) sont elles aussi sans aucun appelant — trouvé en creusant, mais hors du bloc confié, donc laissé à l'arbitrage.

### PACT §F — Ce que l'audit du 03/08/2026 a trouvé APRÈS les gardes (PACT148-PACT187)

> **Pourquoi ce §F existe, et pourquoi il passe AVANT §E.** Le fondateur a ouvert l'app Appels
> d'offres et n'a pas pu créer un appel d'offres. La réparation a été faite le jour même. Il a
> ensuite demandé une chose plus difficile : *« maintenant que tu connais mieux, certaines tâches
> du plan ne sont peut-être pas bonnes »*. Six sondes indépendantes ont mesuré les angles morts des
> trois gardes posées ce jour-là, puis un juge adversarial a instruit le plan lui-même. **Il avait
> raison, et les tâches en cause étaient les miennes.**
>
> **LE VERDICT, vérifié en exhaustif.** Sur les 120 tâches d'écrans de §E : **0 sur 120** n'exigeait
> que l'écran livré soit ATTEIGNABLE ; **112 sur 120** ne nommaient pas le fichier qui le monte —
> or la règle du dépôt interdit de toucher un fichier non listé, donc **112 tâches n'avaient pas le
> DROIT de câbler leur propre écran** ; 13 visaient des apps dépourvues de fichier de module
> qu'aucune tâche ne créait, donc **injoignables par construction** ; et l'ordre « les gardes
> d'abord » n'existait **que dans ma prose** : `BUILD_ORDER.yml` place PACT en vague A sans aucun
> prérequis et aucune tâche §E ne portait de dépendance, si bien que `plan_lanes.py` aurait
> distribué le backlog **dès la première vague, avant que les gardes n'existent**. Drainé tel quel,
> §E produisait **120 écrans de plus que personne ne pouvait ouvrir** — le défaut même qu'il
> prétendait réparer. **Les 120 tâches ont été amendées** (clause d'atteignabilité, fichier de
> montage, `@after: PACT13`) : c'est fait, pas à faire.
>
> **CE QUE LES GARDES NE VOIENT PAS — mesuré, pas supposé.** La garde d'atteignabilité n'inventorie
> que `features/**` : les **258 écrans de `pages/**` ne sont comptés nulle part**. Elle vérifie
> qu'un écran est relié à une ROUTE, jamais qu'un humain peut l'ATTEINDRE — preuve vivante,
> `/parametres/achats` est un écran réel de 182 lignes, routé, vers lequel **aucun lien ne mène**.
> Et la garde de contrat a un trou : son joker d'identifiant avale **4 appels en 404** sur 2 880,
> dont trois que le plan croyait déjà couverts — elle reste **verte** dessus.
>
> **LA CLASSE LA PLUS DANGEREUSE reste la moins visible.** Pas le 404, qui se voit : le **champ
> fantôme**, qui ne se voit pas. Trouvés cette fois, chacun vérifié des deux côtés : le contrôle
> d'intégrité de la piste d'audit comptable **ne peut mécaniquement pas signaler une altération** ;
> le blocage légal d'envoi de masse (loi 09-08) **ne bloque jamais** ; la reprise des cumuls de paie
> annonce « 0 ligne à importer » **juste avant d'écrire pour de vrai** ; une demande d'attestation RH
> part en 201 et **enregistre toujours autre chose**. Aucun de ces écrans n'affiche d'erreur.
>
> **ET DES COCHES QUI MENTENT.** Plusieurs tâches marquées faites livrent une fonction que **rien
> n'appelle** : approbation d'un changement de RIB fournisseur, moteur de séquences marketing qui
> n'inscrit personne, avances sur salaire absentes de l'export paie — de l'argent réel. Sur 2 984
> fonctions publiques, **314 n'ont aucun appelant hors tests**. Elles ne sont pas toutes à câbler :
> §F ne demande QUE le tri, jamais le câblage de masse.
>
> **CE QUI A ÉTÉ REFUSÉ, et il faut le savoir avant de le reproposer.** Une garde de vocabulaire par
> ressemblance : **72 % de faux positifs mesurés**, et structurellement aveugle au cas fondateur.
> Recopier la sonde des champs fantômes en garde bloquante : son zéro erreur est mesuré sur le
> corpus qui a servi à l'écrire, elle ne couvre que 17 % de la surface d'API. Brancher les 38
> orphelins AO « tels quels » : au moins trois appellent un endpoint qui n'existe pas — on
> obtiendrait des écrans **atteignables ET morts**, un cran pire qu'invisibles.
>
> **Enfin, ce groupe corrige aussi mes propres chiffres.** J'ai annoncé « 53 écrans AO » : c'est
> **38** (le 53 était le total toutes apps). J'avais déjà annoncé 470 pour 206, puis 122 pour 73.
> Chaque chiffre de §F a été vérifié sur échantillon à graine fixée, avec son taux d'erreur rendu.

#### §F1 — Ce qui doit etre corrige AVANT de drainer quoi que ce soit

- [x] (déjà présent — 120/120 vérifiées) PACT148 — **Atteignabilité exigée sur les 120 tâches §E — l'amendement qui empêche le 5ᵉ rattrapage.** Constat vérifié sur origin/main@023db8c8 : 0/120 tâches PACT28-147 n'exigent l'atteignabilité dans leur Done= (échantillon graine 20260803, 12/12 concordants, erreur résiduelle 0/12), 112/120 ne nomment pas le fichier de montage, PACT62-68 et PACT96-101 visent des apps sans module.config.jsx qu'aucune tâche ne crée, et BUILD_ORDER.yml (vague A1 « NO prerequisite ») + zéro @after §E n'imposent pas l'ordre §A/§B→§E. Amender docs/PLAN.md : fichier de montage dans chaque Files:, clause d'atteignabilité dans chaque Done=, @after: PACT13 sur les 120, création des module.config.jsx manquants confiée à PACT62/PACT96/PACT125 avec chaînage @after. **Done =** un grep de docs/PLAN.md compte 120/120 tâches §E portant la clause d'atteignabilité ET leur fichier de montage ; `python scripts/plan_lanes.py docs/PLAN.md` refuse toute tâche §E tant que PACT13 est ouverte. Files: `docs/PLAN.md`, `docs/BUILD_ORDER.yml`. (ARCH) (@lane: plan) (@model: opus)
- [x] PACT149 — **La garde d'atteignabilité couvre frontend/src/pages/** et les îlots hors features/.** `scripts/check_ecrans_atteignables.py:470` n'inventorie que FEATURES.rglob('*.jsx') : les 258 écrans non-test de pages/** ne sont comptés nulle part (mesuré : 257 atteignables, 1 orphelin — erreur 0 % sur 38 cas vérifiés au grep). L'orphelin `frontend/src/pages/credit/ExpositionCreditPage.jsx` est un jumeau périmé du vivant `frontend/src/features/credit/ExpositionCreditPage.jsx` routé /credit/exposition (module.config.jsx:61) : le SUPPRIMER, jamais le brancher. Étendre l'inventaire à pages/** (et aux .tsx), et arbitrer l'îlot mort de `frontend/src/ui/datatable/` (ViewBuilderWizard.jsx + FilterBuilder.jsx livrés par NTUX25 cochée [x], zéro importateur externe ; BulkEditDialog.jsx, BulkNoteDialog.jsx, TagChipInput.jsx). **Done =** un écran pages/ non routé fait échouer la garde (contrôle négatif ajouté puis retiré) ; zéro ligne ajoutée à la base (l'orphelin est supprimé, l'îlot arbitré ou branché) ; les compteurs --stats affichent le total features+pages. Files: `scripts/check_ecrans_atteignables.py`, `scripts/ecrans_atteignables_allow.txt`, `frontend/src/pages/credit/ExpositionCreditPage.jsx`. (ARCH) (@lane: gardes) (@model: opus)
- [x] PACT150 — **Route ≠ menu : la garde vérifie aussi l'entrée de navigation.** L'en-tête de la garde promet « atteignable depuis le menu » mais son code ne mesure que la route : un écran routé sans aucune entrée de nav ni lien entrant la satisfait. Cas vivant vérifié : `/parametres/achats` (AchatsParametresPage, écran réel de 182 lignes) est déclaré à `frontend/src/features/parametres/module.config.jsx:155` et AUCUN Link/navigate n'y mène nulle part (seul le commentaire ligne 54 « hors périmètre ODY23 »). Croiser chaque route littérale avec nav.items[].to et les liens entrants réels hors tests et commentaires (le grep naïf a compté un fichier de test comme lien — mesuré) ; segments dynamiques exemptés ; routes volontairement sans nav marquées `# contextuelle: <raison>` ; passe de triage un par un des rouges initiaux, discipline PACT5. **Done =** /parametres/achats a une entrée de nav ou son marqueur justifié ; contrôle négatif (route ajoutée sans nav → rouge) ; zéro faux positif restant après triage. Files: `scripts/check_ecrans_atteignables.py`, `frontend/src/features/parametres/module.config.jsx`. (ARCH) (@lane: gardes) (@model: opus)
- [ ] PACT151 — **Le joker `<pk>` de la garde de contrat n'avale plus un nom d'action manqué.** `scripts/check_api_contract.py:462` ajoute inconditionnellement `base + (ANY,)` pour chaque router.register (même sans retrieve/list), et `compatible()` (:920-923) traite ce joker symétriquement : un segment final kebab-case littéral codé en dur y matche toujours. Mesuré en exhaustif : 4 appels réels sur 2880 sont avalés (action-requise/PACT17, grand-livre/PACT18, export-paie et calendrier-equipe/PACT19) — la garde reste VERTE sur ces quatre 404. Refuser qu'un littéral `^[a-z]+(-[a-z]+)+$` en dernier segment matche la route générique `<pk>`. **Done =** les 4 cas historiques sont des tests unitaires du script (rouges sans leurs correctifs, verts avec) ; le balayage complet des 2880 appels est rejoué et chaque nouveau rouge statué un par un — zéro fausse alerte gelée dans la base. Files: `scripts/check_api_contract.py`, `scripts/tests/test_check_api_contract.py`. (ARCH) (@lane: gardes) (@model: opus)
- [x] PACT152 — **Compta : le contrôle d'intégrité de la piste d'audit ne peut mécaniquement pas signaler une altération.** `frontend/src/features/compta/pages/EngagementsPage.jsx:497,521,523` lit `intacte` et `detail` ; `verifier_integrite_piste` (`backend/django_core/apps/compta/services.py:10216-10226`) renvoie `{valide, nb_maillons, rupture}` : `intacte === false` est TOUJOURS faux, la carte verte « Chaîne d'audit intacte » et le toast s'affichent même sur rupture (vérifié des deux côtés). Lire les vraies clés et afficher le maillon en rupture. **Done =** avec une réponse `valide: false`, l'écran affiche « Rupture détectée » et le maillon ; le test importe la forme réelle du serveur (fixture, jamais un mock inventé). Files: `frontend/src/features/compta/pages/EngagementsPage.jsx`, `frontend/src/features/compta/pages/EngagementsPage.test.jsx`. (ROUTINE) (@lane: correctifs-compta) (@model: sonnet)
- [x] PACT153 — **Marketing : le blocage légal d'envoi de masse ne bloque jamais.** `frontend/src/features/marketing/CampagneDetail.jsx:171-179` lit `precheck.bloquants` ; `precheck_sante_campagne` (`backend/django_core/apps/compta/services.py:8393`) renvoie `{bloque, avertissements}` : `disabled={(precheck.bloquants||[]).length>0}` vaut toujours false — « Confirmer l'envoi » reste cliquable même quand le serveur a posé `bloque=True` (lien de désinscription manquant, loi 09-08). Même écran ligne 153 : `nb_envoyes ?? envoyes` fantômes (le serveur renvoie `{seeds, corps_fusionne}`) — « Test envoyé (ok). » toujours. **Done =** `bloque: true` désactive le bouton et affiche les avertissements ; le nombre réel de seeds s'affiche après un envoi de test. Files: `frontend/src/features/marketing/CampagneDetail.jsx`, `frontend/src/features/marketing/CampagneDetail.test.jsx`. (ROUTINE) (@lane: correctifs-marketing) (@model: sonnet)
- [x] PACT154 — **Paie : deux faux zéros affichés avant des écritures réelles.** `frontend/src/features/paie/PaieDeclarations.jsx:1085` lit `apercu.lignes?.length ?? 0` ; le serveur (`backend/django_core/apps/paie/services.py:6349-6356`) renvoie `{colonnes, mapping, non_mappees, total_lignes, matricules_inconnus, apercu}` : « 0 ligne(s) à importer. » toujours, juste avant un commit de reprise des cumuls de go-live. `frontend/src/features/paie/PaieRunWizard.jsx:242` lit `data?.crees ?? 0` ; le serveur (`backend/django_core/apps/paie/views.py:670-671`) renvoie `{bulletins, nombre}` : « 0 bulletin(s) de 13e mois généré(s). » alors que N viennent d'être créés. **Done =** les deux écrans affichent les comptes réels du serveur, tests sur fixtures de forme réelle. Files: `frontend/src/features/paie/PaieDeclarations.jsx`, `frontend/src/features/paie/PaieRunWizard.jsx`. (ROUTINE) (@lane: correctifs-paie) (@model: sonnet)
- [x] PACT155 — **RH Portail : la demande d'attestation part en 201 et enregistre TOUJOURS autre chose.** `frontend/src/features/rh/Portail.jsx:439` poste `{type_demande, motif}` ; `DemandeRHSerializer` (`backend/django_core/apps/rh/serializers.py:2374`) n'accepte que `type` et `message` : DRF ignore les deux champs, `type` retombe sur le défaut modèle `attestation_travail` — le choix de l'employé est perdu SANS erreur. Même écran : option `domiciliation` (ligne 427) inexistante côté serveur (la vraie valeur est `attestation_domiciliation`), et lecture `type_demande_display` (ligne 245) au lieu de `type_display` : chaque ligne de « mes demandes » affiche le repli générique. **Done =** le type choisi arrive dans la demande créée (assertion sur le payload envoyé ET la réponse), l'option invalide est corrigée, la liste affiche le libellé serveur. Files: `frontend/src/features/rh/Portail.jsx`, `frontend/src/features/rh/Portail.test.jsx`. (ROUTINE) (@lane: correctifs-rh) (@model: sonnet)
- [x] PACT156 — **RH : la sortie d'employé est impossible sur 3 des 7 motifs du menu.** `frontend/src/features/rh/EmployeDetail.jsx:652` (sous le commentaire « Motifs alignés sur DossierEmploye.MotifSortie » — promesse FAUSSE) propose `fin_cdd`, `rupture_conventionnelle`, `deces` ; `MotifSortie` (`backend/django_core/apps/rh/models.py:209`) déclare `fin_contrat`, `rupture_essai`, et pas `deces` ; `rh/views.py:518` renvoie 400 explicitement sur toute valeur hors TextChoices. Corriger les valeurs et statuer `deces` avec le modèle (l'ajouter au TextChoices OU retirer l'option — jamais un menu qui 400). **Done =** chaque option du menu aboutit à une sortie enregistrée ; un test affirme que les valeurs du menu ⊆ MotifSortie.values. Files: `frontend/src/features/rh/EmployeDetail.jsx`, `frontend/src/features/rh/EmployeDetail.test.jsx`. (ROUTINE) (@lane: correctifs-rh) (@model: sonnet)
- [x] PACT157 — **Adsengine : le journal d'actions est illisible de bout en bout.** `frontend/src/features/adsengine/ActionsLogScreen.jsx:173` lit `a.type` (le sérialiseur envoie `kind` — `backend/django_core/apps/adsengine/serializers.py:181` ; la rustine existe déjà dans ApprovalsScreen.jsx:117 et n'a pas été appliquée ici) : chaque ligne s'intitule « Action ». `frontend/src/features/adsengine/adsengine.js:300` lit `action.statut ?? action.result` (serveur : `status` ; `result` est un JSONField) : badge « — » partout et filtre par statut toujours vide ; `:291` nomme `en_attente`/`echec` au lieu de `proposee`/`echouee` ; `:134` (ACTION_TYPE_LABELS) ignore 4 `kind` réellement produits (`create_adset`, `rotate_creative`, `rebalance_budget`, `pause`) et porte 10 clés mortes ; `TreeScreen.jsx:46` déclare `audience` au lieu de `audience_structure`. **Done =** le journal affiche libellé et statut réels pour chaque `kind` du modèle (`adsengine/models.py:755,762`), les clés mortes sont retirées, le test s'appuie sur les valeurs du modèle, jamais un dictionnaire inventé. Files: `frontend/src/features/adsengine/ActionsLogScreen.jsx`, `frontend/src/features/adsengine/adsengine.js`, `frontend/src/features/adsengine/TreeScreen.jsx`. (ROUTINE) (@lane: correctifs-adsengine) (@model: sonnet)
- [x] PACT158 — **QHSE : le chatter n'affiche jamais un changement de champ.** `frontend/src/features/qhse/NcrChatter.jsx:16,24` teste `e.kind === 'field_change'` ; le serveur écrit `kind='modification'` (`backend/django_core/apps/qhse/chatter.py:59`, valeurs `creation`/`modification`/`note` dans `qhse/models.py:757`) : la branche ne se déclenche JAMAIS — chaque changement automatique s'affiche « Enregistrement créé » et la pastille montre le mot brut. **Done =** une entrée `modification` affiche « Champ : ancienne → nouvelle » avec sa pastille libellée, test sur la valeur réelle du modèle. Files: `frontend/src/features/qhse/NcrChatter.jsx`, `frontend/src/features/qhse/NcrChatter.test.jsx`. (ROUTINE) (@lane: correctifs-qhse) (@model: haiku)
- [ ] PACT159 — **Garde vocabulaire à source déclarée — opt-in, zéro faux positif par construction.** La détection par ressemblance est disqualifiée par la mesure (18 candidats machine → 13 faux positifs = 72 % ; et le cas fondateur ExigencesPage partageait ZÉRO valeur avec son champ : invisible à toute similarité). La forme viable : un marqueur machine `// source-choix: <app>.<Modele>.<champ>` (ou chemin de constante/module pour les vocabulaires hors TextChoices, ex. `ao.fabrique.approvisionnement.GRAVITES`) posé sur chaque liste de choix front, vérifié en CI par extraction AST des modèles en nom QUALIFIÉ (le piège `Modele.Statut` vs nom simple est mesuré : 6 faux candidats). Seules les listes marquées sont vérifiées — une liste sans marqueur n'est jamais un rouge. Convertir les ~80 promesses en prose (« aligné sur », « miroir de ») et les listes des écrans de PACT807-810. **Done =** contrôle négatif : une valeur inventée ajoutée à une liste marquée → rouge en citant modèle et champ ; les promesses en prose sont converties ou refusées une par une ; le commentaire faux d'EmployeDetail.jsx:651 est remplacé par un marqueur vérifié. Files: `scripts/check_choices_declares.py`, `scripts/tests/test_check_choices_declares.py`. (ARCH) (@lane: gardes) (@model: opus)
- [x] PACT160 — **Compta : l'approbation de changement de RIB fournisseur (XACC24, cochée à tort) n'a ni route ni écran.** `approuver_demande_rib` (`backend/django_core/apps/compta/services.py:11567`) n'a que des appelants de test (vérifié par grep sur origin/main) ; aucune classe DemandeApprobationRib* dans views.py, urls.py ou serializers.py : la surface anti-fraude « valider un changement de coordonnées bancaires fournisseur » est inutilisable par l'API. Construire ViewSet + serializer + route + écran de file d'approbation atteignable, ou faire décocher XACC24 par le fondateur avec motif au DONE LOG. **Done =** une demande de RIB est approuvée ou refusée depuis un écran atteignable (route + entrée de nav), la décision tracée ; check_ecrans_atteignables et check_api_contract restent verts. Files: `backend/django_core/apps/compta/views.py`, `backend/django_core/apps/compta/urls.py`, `backend/django_core/apps/compta/serializers.py`, `frontend/src/features/compta/pages/ApprobationsRibPage.jsx`. (ROUTINE) (@lane: compta) (@model: sonnet)
- [x] PACT161 — **Marketing : le moteur de séquences n'inscrit jamais personne (XMKT1 cochée à tort).** `inscrire_leads_pour_stage` (`backend/django_core/apps/compta/services.py:8602`) n'a que des appelants de test (vérifié) ; `compta/receivers.py` n'a aucun abonnement au changement d'étape pipeline ; `executer_sequences_relance_task` (`compta/tasks.py:14`) n'exécute que des participants déjà inscrits — qui n'existent donc jamais. Abonner un receiver au changement d'étape via le bus `core/events` (pattern `crm/receivers.py`) qui appelle `inscrire_leads_pour_stage`. **Done =** un lead qui change d'étape est inscrit aux séquences actives de cette étape — test d'intégration signal→inscription, pas un appel direct au service. Files: `backend/django_core/apps/compta/receivers.py`, `backend/django_core/apps/compta/apps.py`, `backend/django_core/apps/compta/tests/test_xmkt1_inscription_sequence.py`. (ROUTINE) (@lane: compta) (@model: sonnet)
- [x] PACT162 — **Paie : les avances sur salaire approuvées ne partent jamais chez le prestataire (FG192 inachevée).** `avances_a_deduire` (`backend/django_core/apps/rh/selectors.py:1671`) n'a aucun appelant hors tests (vérifié — la migration 0033 affirme pourtant qu'il « alimente l'export paie ») ; `export_paie_csv` (`backend/django_core/apps/rh/views.py:4399-4429`) écrit la colonne Retenues depuis EvpVariable seulement : le CSV envoyé au prestataire omet les avances approuvées du mois — de l'argent réel. **Done =** l'export du mois inclut les avances approuvées dans Retenues ; test : approuver une avance change la ligne CSV de l'employé. Files: `backend/django_core/apps/rh/views.py`, `backend/django_core/apps/rh/tests/test_avances_salaire.py`. (ROUTINE) (@lane: rh) (@model: sonnet)
- [x] PACT163 — **Compta : trois capacités XACC cochées sans aucune vue (XACC15/16/22).** `etaler_charge_avance` (`compta/services.py:10660`), `generer_dotations_derogatoires` (`:10818`) et `generer_ligne_budget_repartie` (`:11518`) n'ont ni appelant hors tests ni route (vérifié) : charges constatées d'avance, double plan comptable/fiscal et budget par courbe n'existent que comme fonctions. Exposer chacune (action de ViewSet + écran ou onglet atteignable) ou faire décocher sa tâche d'origine par le fondateur — jamais laisser la coche mentir. **Done =** chaque capacité est déclenchable depuis un écran atteignable, ou sa tâche d'origine est décochée avec motif au DONE LOG. Files: `backend/django_core/apps/compta/views.py`, `backend/django_core/apps/compta/urls.py`, `frontend/src/features/compta/`. (ROUTINE) (@lane: compta) (@model: sonnet)
- [x] PACT164 — **Adsengine : câbler ou archiver les 6 producteurs d'actions livrés que rien n'invoque (ADSDEEP36/50/51/52).** `propose_internal_dayparting_pause` (:311), `propose_pause_for_month` (:402), `propose_edit_post` (:619), `propose_create_post` (:642), `propose_boost_post` (:667), `propose_keyword_hides` (:762) dans `backend/django_core/apps/adsengine/services.py` : appelants = tests seulement (vérifié) ; ni views.py, ni rules_engine.py, ni le frontend ne les invoquent — contrairement à propose_action/propose_duplicate qui SONT câblés. Les brancher au circuit propose→approve réel (règle du moteur ou bouton du journal), ou les archiver et décocher leurs tâches d'origine. **Done =** chaque producteur est déclenché par un chemin réel avec son action visible dans le journal d'approbation, ou archivé avec décochage motivé. Files: `backend/django_core/apps/adsengine/rules_engine.py`, `backend/django_core/apps/adsengine/views.py`, `frontend/src/features/adsengine/ApprovalsScreen.jsx`. (ROUTINE) (@lane: adsengine) (@model: sonnet)
- [x] PACT165 — **Trier les ~290 fonctions orphelines restantes AVANT d'en câbler une seule.** Mesure vérifiée (deux échantillons à graines fixées 42 et 1337, 0/34 erreurs) : 314/2984 fonctions publiques de apps/*/services.py + selectors.py + fabrique/ sans appelant hors tests ; seules ~20 sont classées. Classer les restantes app par app — (a) à câbler / (b) désaccord de nom / (c) doublon mort à supprimer — dans un tableau committé, en réutilisant la méthode corrigée (registre `core.platform.kpi_providers` et alias d'import identifiés comme les deux mécanismes dynamiques réels), puis dériver les tâches (a) au format PLAN. Ne PAS câbler en masse : les 70 orphelines d'apps/ao/fabrique sont TOUTES en aval du registre unique manquant de PACT25. **Done =** chaque orpheline a un verdict motivé dans docs/rapport-orphelines.md ; les (a) deviennent des lignes de tâche ; aucune fonction câblée dans cette tâche-ci. Files: `docs/rapport-orphelines.md`, `docs/PLAN.md`. (ROUTINE) (@lane: audit) (@model: sonnet)

#### §F2 — Les 38 ecrans AO encore injoignables

- [x] PACT166 — **Assembler la coquille de l'atelier de traçage de toiture (canvas).** Constat vérifié : frontend/src/features/ao/toiture/ToituresPage.jsx:378-388 affiche un EmptyState « Atelier de traçage » disant que le canvas interactif existe dans une autre lane ; studio/StudioShell.jsx (357 lignes, AOF73), studio/CanvasSvg.jsx (387 lignes, AOF74), studio/Selection.jsx (400 lignes, AOF76), studio/BarreEtat.jsx (74 lignes, AOF74), studio/TableauGeometrie.jsx (374 lignes, AOF77) et components/ProvenanceBadge.jsx (AOF9) ne sont importés par AUCUN fichier réel (grep : seuls leurs propres tests les importent). Assembler ces 6 composants en un atelier fonctionnel (StudioShell comme coquille à slots canvas/état/tableau, CanvasSvg en surface SVG, Selection en voie souris, TableauGeometrie en voie clavier, ProvenanceBadge pour les pastilles), et le monter à la place de l'EmptyState dans ToituresPage.jsx (branche desktop), en persistant la géométrie via aoApi.toitures.update (champ contour_local_m, déjà utilisé par le wizard de création). **Done =** l'atelier (canvas, sélection, tableau de géométrie) est visible et utilisable depuis l'onglet « Toitures & relevés » de la fiche affaire ou /ao/toitures sur écran large, et `python scripts/check_ecrans_atteignables.py` ne signale plus studio/StudioShell.jsx, studio/CanvasSvg.jsx, studio/Selection.jsx, studio/BarreEtat.jsx, studio/TableauGeometrie.jsx ni components/ProvenanceBadge.jsx comme dette. Files: `frontend/src/features/ao/toiture/ToituresPage.jsx`, `frontend/src/features/ao/studio/StudioShell.jsx`, `frontend/src/features/ao/studio/CanvasSvg.jsx`, `frontend/src/features/ao/studio/Selection.jsx`, `frontend/src/features/ao/studio/BarreEtat.jsx`, `frontend/src/features/ao/studio/TableauGeometrie.jsx`, `frontend/src/features/ao/components/ProvenanceBadge.jsx`. (ARCH) (@lane: frontend/ao-studio) (@model: opus)
- [x] PACT167 — **Brancher les outils spécialisés de l'atelier de toiture (calibration, DXF, obstacles, zones, cotes).** Constat vérifié : 16 fichiers du même atelier ne sont importés par aucun écran réel — toiture/Calibration.jsx (AOF80), ImportDxf.jsx (AOF81), RepriseCarte.jsx (AOF82), OutilTrace.jsx (AOF84), ChainesCotes.jsx+Cote.jsx (AOF85, Cote importé seulement par ChainesCotes, lui-même orphelin), FermeturesPanel.jsx (AOF86), PointsALever.jsx (AOF87), OutilsObstacles.jsx+ObstacleInspecteur.jsx (AOF88, même chaîne orpheline), OutilsZones.jsx (AOF89), ObstaclesList.jsx (AOF90), EnveloppeArc.jsx+EnveloppeL.jsx (AOF91), UnderlayImage.jsx+UnderlayPdf.jsx (AOF79). Dépend de PACT900 (la coquille doit exister). Brancher au minimum le calage (Calibration), le fond de calque (UnderlayImage/UnderlayPdf), le tracé (OutilTrace) et la pose d'obstacles (OutilsObstacles+ObstacleInspecteur+ObstaclesList) comme boîte à outils du canvas assemblé en PACT900. **Done =** calibration, tracé et pose d'obstacles sont utilisables depuis l'atelier monté, et `python scripts/check_ecrans_atteignables.py` ne signale plus aucun des 16 fichiers toiture/* listés ci-dessus comme dette. Files: `frontend/src/features/ao/toiture/Calibration.jsx`, `frontend/src/features/ao/toiture/ImportDxf.jsx`, `frontend/src/features/ao/toiture/RepriseCarte.jsx`, `frontend/src/features/ao/toiture/OutilTrace.jsx`, `frontend/src/features/ao/toiture/ChainesCotes.jsx`, `frontend/src/features/ao/toiture/Cote.jsx`, `frontend/src/features/ao/toiture/FermeturesPanel.jsx`, `frontend/src/features/ao/toiture/PointsALever.jsx`, `frontend/src/features/ao/toiture/OutilsObstacles.jsx`, `frontend/src/features/ao/toiture/ObstacleInspecteur.jsx`, `frontend/src/features/ao/toiture/OutilsZones.jsx`, `frontend/src/features/ao/toiture/ObstaclesList.jsx`, `frontend/src/features/ao/toiture/EnveloppeArc.jsx`, `frontend/src/features/ao/toiture/EnveloppeL.jsx`, `frontend/src/features/ao/toiture/UnderlayImage.jsx`, `frontend/src/features/ao/toiture/UnderlayPdf.jsx`. (ARCH) (@lane: frontend/ao-toiture) (@model: opus)
- [x] PACT168 — **Monter le Mode Expert et les Suggestions dans l'atelier de calepinage.** Constat vérifié : frontend/src/features/ao/calepinage/CalepinageStudio.jsx:6-11 ne monte que les 5 tiroirs débutant (Kits/Allées/Rives/Orientation/Électrique) ; calepinage/ModeExpert.jsx (AOF101, 182 lignes, importe RobustesseBadges.jsx) et calepinage/SuggestionsPanel.jsx (AOF100, 167 lignes, purement présentationnel : props suggestions=[]/historique=[]/enCours=null) ne sont importés par aucun fichier hors leurs propres tests. Monter ModeExpert (toggle qui révèle RobustesseBadges) et SuggestionsPanel à côté des 5 tiroirs existants, avec suggestions=[] tant que le serveur ne publie pas /ao/calepinage/<id>/suggestions/ (nonConstruit, frontend/src/api/aoApi.js:153) — même principe que les tiroirs qui « restent montés » déjà appliqué dans ce fichier. **Done =** le toggle « Mode expert » et le panneau « Suggestions du moteur » sont visibles dans l'atelier de calepinage, et `python scripts/check_ecrans_atteignables.py` ne signale plus calepinage/ModeExpert.jsx, calepinage/RobustesseBadges.jsx ni calepinage/SuggestionsPanel.jsx comme dette. Files: `frontend/src/features/ao/calepinage/CalepinageStudio.jsx`, `frontend/src/features/ao/calepinage/ModeExpert.jsx`, `frontend/src/features/ao/calepinage/RobustesseBadges.jsx`, `frontend/src/features/ao/calepinage/SuggestionsPanel.jsx`. (ROUTINE) (@lane: frontend/ao-calepinage) (@model: sonnet)
- [x] PACT169 — **Brancher les contrôles avant dépôt, les échéances et le bouton ZIP dans l'onglet Dossier.** Constat vérifié : frontend/src/features/ao/dossier/DossierPage.jsx:70-76 déclare 3 emplacements injectables (renderApercu, renderEcheances, actions) restés vides (lignes 190, 201-203, 204-209) aux deux points de montage réels — AffaireDetail.jsx:293 et module.config.jsx:192 — qui rendent <DossierPage dossierId=...} sans aucun de ces props ; conséquence directe : dossier/ControlesAvantDepot.jsx (AOF176), dossier/EcheancesDossier.jsx (AOF178) et dossier/ZipButton.jsx (AOF177) ne sont importés par aucun fichier hors leurs propres tests. Passer actions={() => <ControlesAvantDepot/><ZipButton/>} et renderEcheances={() => <EcheancesDossier/>} aux deux points de montage. ControlesAvantDepot est pleinement fonctionnel dès aujourd'hui (GET /ao/dossiers-ao/<id>/controles-avant-depot/ est un endpoint RÉEL, aoApi.js:225) ; le clic sur ZipButton restera un refus honnête tant que aoApi.dossiers.zip reste nonConstruit (aoApi.js:245, « aucun monteur ne leur passe les pièces » — hors périmètre frontend, ne pas le masquer ni le simuler). **Done =** les contrôles avant dépôt, les échéances et le bouton ZIP sont visibles dans l'onglet « Dossier » de la fiche affaire (et sur /ao/dossiers/:id), et `python scripts/check_ecrans_atteignables.py` ne signale plus dossier/ControlesAvantDepot.jsx, dossier/EcheancesDossier.jsx ni dossier/ZipButton.jsx comme dette. Files: `frontend/src/features/ao/dossier/DossierPage.jsx`, `frontend/src/features/ao/AffaireDetail.jsx`, `frontend/src/features/ao/module.config.jsx`, `frontend/src/features/ao/dossier/ControlesAvantDepot.jsx`, `frontend/src/features/ao/dossier/EcheancesDossier.jsx`, `frontend/src/features/ao/dossier/ZipButton.jsx`. (ROUTINE) (@lane: frontend/ao-dossier) (@model: sonnet)
- [x] PACT170 — **Brancher l'annotateur de photo, la fiche question et l'export dans Questions terrain.** Constat vérifié : questions/Annotateur.jsx (AOF106) importe questions/RepereMarker.jsx (Annotateur.jsx:4) mais aucun des deux, ni questions/QuestionFiche.jsx (AOF107 1/3) ni questions/ExportQR.jsx (AOF107 2/3), n'est importé par questions/SeriesPage.jsx — l'écran « Questions terrain » réellement monté (AffaireDetail.jsx:448). Les 4 fichiers sont de purs composants props/callbacks (aucun appel aoApi direct, vérifié par grep), donc branchables sans attendre de backend. Brancher le flux déjà écrit dans leurs en-têtes : poser un repère sur une photo (Annotateur) ouvre sa fiche (QuestionFiche), avec un bouton d'export de l'image annotée + la liste (ExportQR), depuis SeriesPage.jsx. **Done =** depuis l'onglet « Questions terrain » de la fiche affaire, on peut poser un repère sur une image, ouvrir sa fiche question et exporter l'image annotée, et `python scripts/check_ecrans_atteignables.py` ne signale plus questions/Annotateur.jsx, questions/RepereMarker.jsx, questions/QuestionFiche.jsx ni questions/ExportQR.jsx comme dette. Files: `frontend/src/features/ao/questions/SeriesPage.jsx`, `frontend/src/features/ao/questions/Annotateur.jsx`, `frontend/src/features/ao/questions/RepereMarker.jsx`, `frontend/src/features/ao/questions/QuestionFiche.jsx`, `frontend/src/features/ao/questions/ExportQR.jsx`. (ROUTINE) (@lane: frontend/ao-questions) (@model: sonnet)
- [x] PACT171 — **Ajouter l'onglet « Variantes » à la fiche affaire.** Constat vérifié : AffaireDetail.jsx déclare 10 onglets (lignes 390-498 : synthese/toitures/calepinages/bordereau/dossier/questions_terrain/historique/administratif/equipements/cps) et aucun « variantes » ; variantes/VariantesCompare.jsx (ligne 85, signature {affaireId, exporterImage}) et variantes/VarianteColonne.jsx (importé uniquement par VariantesCompare, ligne 8) ne sont montés sur aucune route ni onglet (grep : zéro importeur hors tests). Ajouter un 11e onglet « Variantes » au tableau `tabs` de AffaireDetail.jsx, même patron que les 3 onglets ajoutés le 03/08/2026 : `<PanneauDiffere><VariantesCompare affaireId={id} /></PanneauDiffere>`. **Done =** l'onglet « Variantes » est visible et affiche le comparateur depuis la fiche affaire, et `python scripts/check_ecrans_atteignables.py` ne signale plus variantes/VariantesCompare.jsx ni variantes/VarianteColonne.jsx comme dette. Files: `frontend/src/features/ao/AffaireDetail.jsx`, `frontend/src/features/ao/variantes/VariantesCompare.jsx`, `frontend/src/features/ao/variantes/VarianteColonne.jsx`. (ROUTINE) (@lane: frontend/ao-variantes) (@model: sonnet)
- [x] PACT172 — **Brancher le détail d'une variante (diff, décomposition, sensibilités, historique) et corriger l'appel API cassé de SensibilitesPanel.** Constat vérifié : VariantesCompare.jsx n'importe que VarianteColonne (grep confirmé, ligne 8) ; 4 fichiers frères ne sont montés nulle part — variantes/DiffPlan.jsx, variantes/DecompositionWaterfall.jsx (AOF104), variantes/SensibilitesPanel.jsx et variantes/HistoriqueVersions.jsx. BUG RÉEL trouvé au passage : SensibilitesPanel.jsx:84 appelle aoApi.calepinages.sensibilites(calepinageId), un endpoint nonConstruit (aoApi.js:156) ; le vrai endpoint existe sous un autre nom — aoApi.calepinage.variantes.sensibilites(varianteId), aoApi.js:112 — brancher ce panneau SANS corriger l'appel produirait un refus serveur permanent au lieu de vraies données. Dépend de PACT905 (l'onglet Variantes doit exister). Ajouter une action « Détails » par colonne de VarianteColonne qui ouvre DiffPlan + DecompositionWaterfall + SensibilitesPanel (avec son appel corrigé) + HistoriqueVersions ; HistoriqueVersions restera un refus honnête tant que le serveur ne modélise pas l'historique de versions (nonConstruit, aoApi.js:141,144 : « l'historique de versions d'un calepinage n'est pas modélisé ») — le monter quand même (refus motivé, jamais une donnée inventée) est ce qui le rend atteignable. **Done =** DiffPlan, DecompositionWaterfall, SensibilitesPanel (avec l'appel vers aoApi.calepinage.variantes.sensibilites) et HistoriqueVersions sont tous atteignables depuis l'onglet Variantes, et `python scripts/check_ecrans_atteignables.py` ne signale plus aucun des 4 comme dette. Files: `frontend/src/features/ao/variantes/VariantesCompare.jsx`, `frontend/src/features/ao/variantes/VarianteColonne.jsx`, `frontend/src/features/ao/variantes/DiffPlan.jsx`, `frontend/src/features/ao/variantes/DecompositionWaterfall.jsx`, `frontend/src/features/ao/variantes/SensibilitesPanel.jsx`, `frontend/src/features/ao/variantes/HistoriqueVersions.jsx`, `frontend/src/api/aoApi.js`. (ROUTINE) (@lane: frontend/ao-variantes) (@model: sonnet)

#### §F3 — Angles morts residuels des gardes

- [x] PACT173 — **Un `lazy()` jamais placé dans une route est compté atteignable (trou structurel, 0 défaut aujourd'hui).** `scripts/check_ecrans_atteignables.py:452` ajoute `config.path` lui-même aux racines, donc `pile.extend(imports_de(courant))` crédite tous les imports d'un `module.config.jsx`, y compris un composant `lazy()` absent de `routes:` ; le tri `ecrans_routes` (ligne 456) ne protège que la classe 2. Le trou est réel mais ne saigne pas encore : sondage du 03/08/2026 = 4 candidats, 4 FAUX positifs vérifiés à la main (`PortalClientLayout`/`PortalFournisseurLayout`/`PortalPartenaireLayout` sont passés en prop `shell={…}` à `frontend/src/router/index.jsx:350`, et `AdsCockpitScreen` est rendu par le wrapper routé `AdsCockpitScreenPrintable` à `frontend/src/features/adsengine/module.config.jsx:81`). Fermer le trou : pour un config NON opaque, ne plus mettre `config.path` en racine mais garder ses `ecrans_routes`, ET créditer explicitement les composants utilisés en prop JSX (`shell={X}`) ou rendus par un wrapper déclaré dans le même fichier — sans ces deux exceptions la garde produirait ces 4 fausses alertes le jour de sa pose. **Done =** la garde sort 0 sur `main` avec une base inchangée, ET un composant `lazy()` ajouté dans un `module.config.jsx` sans entrée `routes:` correspondante la fait sortir 1. Files: `scripts/check_ecrans_atteignables.py`. (ARCH) (@lane: garde-ecrans) (@model: opus)
- [x] PACT174 — **Cinq composants livrés que rien n'importe, hors des deux dossiers inventoriés.** Le même graphe montre 5 `.jsx` non-test morts hors `features/` et `pages/`, chacun confirmé au grep (zéro importateur non-test) : `frontend/src/components/TagChipInput.jsx`, `frontend/src/ui/datatable/BulkEditDialog.jsx`, `frontend/src/ui/datatable/BulkNoteDialog.jsx`, `frontend/src/ui/datatable/ViewBuilderWizard.jsx` et `frontend/src/ui/datatable/FilterBuilder.jsx` — ce dernier n'est importé QUE par `ViewBuilderWizard.jsx:19`, lui-même orphelin : un îlot mort de deux fichiers, qu'un contrôle naïf « a-t-il un importateur ? » déclarerait vivant. Décider fichier par fichier : brancher dans l'écran qui devait les recevoir (le tiroir d'édition en masse / le constructeur de vues de la DataTable), ou supprimer. **Done =** aucun de ces 5 fichiers n'est à la fois présent et dépourvu d'importateur relié à une route ; `python scripts/check_ecrans_atteignables.py --stats` ne les liste plus comme non atteints. Files: `frontend/src/ui/datatable/`, `frontend/src/components/TagChipInput.jsx`. (ROUTINE) (@lane: ui-datatable) (@model: sonnet)
- [ ] PACT175 — **Étendre le lecteur de forme : 3 classes d'endpoints agrégés lui échappent aujourd'hui, dont le cas fondateur.** Mesure : `scripts/check_api_shapes.py` met 321 endpoints sous contrat ; les 3 extensions ci-dessous le portent à 418 (+30 %). (a) `check_api_contract._expand_router` ne remplit `self.views` que pour les `@action` : toute route de liste/détail d'un routeur DRF n'a AUCUNE vue associée, donc tout `viewsets.ViewSet` d'agrégation servant depuis `list()`/`retrieve()` est invisible — c'est le cas de `RecrutementStatistiquesViewSet` (`backend/django_core/apps/rh/urls.py:147`, `backend/django_core/apps/rh/views.py:2911`), l'endpoint du défaut fondateur. Enregistrer `base -> (viewset, list)` et `base + (ANY,) -> (viewset, retrieve)`, et mapper le verbe HTTP vers la méthode DRF (get->list/retrieve, post->create, ...). (b) `ShapeReader._shape_of_expression` ne résout qu'un appel NU `foo(...)` : `data = selectors.foo(...)` puis `return Response(data)` — la convention de TOUT le dépôt — n'est pas lu. Résoudre l'alias de module via `_imports_of` et suivre `x = <Call>` -> `return Response(x)`. (c) Un dictionnaire muté après construction (`payload['devis_reference'] = ...`, `backend/django_core/apps/publicapi/views.py:97-101`) est sous-lu : absorber les clés de subscript LITTÉRALES dans la forme, et déclarer la forme incertaine (endpoint retiré du contrat) sur une mutation non littérale ou un `.update(...)`. **Done =** `python scripts/check_api_shapes.py --stats` annonce au moins 415 endpoints sous contrat contre 321 aujourd'hui ; `docs/api-contracts.md` régénéré contient `/api/django/rh/recrutement/statistiques` avec ses 4 clés réelles ; contrôle négatif fait dans les deux sens sur chacune des 3 extensions ; aucune ligne ajoutée à `scripts/api_shapes_allow.txt`. Files: `scripts/check_api_contract.py`, `scripts/check_api_shapes.py`, `scripts/tests/test_check_api_shapes.py`, `docs/api-contracts.md`. (ARCH) (@lane: gardes) (@model: opus) (@after: PACT2)
- [ ] PACT176 — **Mesurer les 83 % de la surface d'API que l'étage 2 ne voit PAS, avant d'affirmer quoi que ce soit dessus.** Constat chiffré : 418 endpoints seulement sur 2 511 fonctions de client API ont une forme de réponse certaine statiquement (16,6 %), et 181 écrans sur 725 touchent au moins un de ces 418. Le reste est majoritairement du CRUD servi par un serializer DRF — une classe où la forme est parfaitement connaissable (`Meta.fields` / champs déclarés) mais qu'aucune garde ne lit. Cette tâche ne CONSTRUIT pas la garde : elle MESURE d'abord, sur un échantillon aléatoire à graine fixée d'au moins 30 écrans lisant une ressource CRUD, quelle proportion de lectures de champ ne correspond à aucun champ du serializer, et rend le taux d'erreur résiduel de cette mesure, cas par cas, vérifié à la main. Interdit d'extrapoler un chiffre non vérifié (deux mesures fausses à 51 % et 40 % ont déjà été annoncées dans ce dépôt). **Done =** un chiffre unique, son dénominateur, la méthode d'échantillonnage (graine + tirage) et le taux d'erreur résiduel mesuré, plus un verdict explicite « garde CRUD constructible / non constructible » motivé par ce taux — et rien de plus si la mesure ne le soutient pas. Files: `docs/PLAN.md`. (ROUTINE) (@lane: gardes) (@model: opus) (@after: PACT910)
- [ ] PACT177 — **Le contrat de forme ne couvre que 14 % des endpoints appelés, et pas ceux où vit le vocabulaire.** `python scripts/check_api_contract.py --stats` résout 2880 appels vers 2262 endpoints distincts ; `docs/api-contracts.md` n'en fige que 321, parce que `scripts/check_api_shapes.py` ne lit que les vues renvoyant un dictionnaire littéral — les ressources servies par un sérialiseur (`EngineAction`, `DemandeRH`, `QhseChatterEntry`, `AssumptionNode`, soit les 9 défauts de cette mesure) en sont absentes. Étendre le générateur : pour une route de routeur DRF, résoudre `serializer_class` → `Meta.fields` → modèle, et écrire dans le contrat les NOMS de champs exposés plus, pour chaque champ à `choices`, ses valeurs. La garde reste anti-faux-positif : une vue dont le sérialiseur n'est pas résoluble statiquement (`get_serializer_class` dynamique) reste absente du contrat. **Done =** `docs/api-contracts.md` contient l'entrée `/api/django/adsengine/actions/` avec `kind ∈ {…9 valeurs…}` et `status ∈ {…5 valeurs…}`, et `python scripts/check_api_shapes.py` échoue sur un mock de test qui déclare `type`/`statut` pour cet endpoint. Files: `scripts/check_api_shapes.py`, `docs/api-contracts.md`. (ARCH) (@lane: gardes) (@model: opus)

#### §F4 — Capacites construites et injoignables (hors celles deja jugees)

- [x] PACT178 — **AOF16 : dériver et surveiller la caution définitive, jamais appelé.** Constat vérifié : `deriver_caution_definitive` (apps/ao/services.py:479) et `cautions_expirant_avant_ouverture` (apps/ao/services.py:518) sont testés (5+2 tests) mais `CautionSoumissionViewSet` (apps/ao/views.py:992-995) est un CRUD nu et `apps/ao/controles.py` n'a aucune règle de caution. Ajouter une action `deriver-definitive/` sur le ViewSet appelant le service, et une règle bloquante dans `controles.py` (registre `apps.ao.controles`) qui appelle `cautions_expirant_avant_ouverture`. **Done =** poser le taux CPS calcule et MET À JOUR (idempotent) la caution définitive via l'API ; un dossier avec une caution expirant avant l'ouverture des plis échoue au contrôle de cohérence en le citant. Files: `backend/django_core/apps/ao/views.py`, `backend/django_core/apps/ao/controles.py`, `backend/django_core/apps/ao/tests/`. (ROUTINE) (@lane: backend/ao) (@model: sonnet)
- [x] PACT179 — **AOF20 : l'upload de plan de toiture n'a aucun endpoint.** Constat vérifié : `attacher_fichier_plan_source` (apps/ao/services.py:1061, empreinte SHA-256 + dédup + stockage `records.Attachment`, jamais un FileField) est testé mais `PlanSourceViewSet` (apps/ao/views.py:650-678) n'a qu'un CRUD JSON — aucune action multipart ne l'invoque, et le frontend (`ImportDxf.jsx`) confirme l'absence d'endpoint de traitement de plan. Ajouter une action `upload/` (multipart) sur le ViewSet appelant le service. **Done =** un PDF/DXF/image envoyé à `/plans-source/{id}/upload/` (ou en création) produit un `Attachment` référencé, un second envoi du même contenu réutilise l'`Attachment` existant (dédup prouvée par test). Files: `backend/django_core/apps/ao/views.py`, `backend/django_core/apps/ao/serializers.py`, `backend/django_core/apps/ao/tests/`. (ARCH) (@lane: backend/ao) (@model: opus)
- [x] PACT180 — **PACT25 doit nommer son registre : 70 fonctions de fabrique/ attendent `producteurs_de_pack`.** Constat vérifié : la sonde chiffre à 70 le nombre de fonctions publiques de `apps/ao/fabrique/**` sans aucun appelant hors tests (ex. `rendre_checklist` apps/ao/fabrique/rendus/checklist_docx.py:230, `rendre_page_garde` apps/ao/fabrique/rendus/garde_sommaire.py:191, `rendre_note_calcul` apps/ao/fabrique/rendus/note_calcul.py:208, `fusionner_pack` apps/ao/fabrique/pack_pdf.py:159, `ecrire_pack_zip` apps/ao/fabrique/pack_zip.py:130) — TOUTES en aval du trou déjà décrit par PACT25 (docs/PLAN.md:2252, encore `[ ]`). Avant de fermer PACT25, vérifier que son « registre qui associe chaque type de pièce à son producteur réel » couvre nommément ces 70 fonctions (liste complète dans les notes de cette sonde) — sinon PACT25 se ferme en laissant une partie du pack silencieusement vide. **Done =** le Done de PACT25 est complété par la liste vérifiée ci-dessus ; un test paramétré vérifie qu'aucun producteur nommé n'est orphelin après le fold. Files: `backend/django_core/apps/ao/services.py`, `backend/django_core/apps/ao/fabrique/`, `backend/django_core/apps/ao/tests/test_pack.py`. (ARCH) (@lane: correctifs) (@model: opus) (@after: PACT25)
- [x] PACT181 — **XQHS15 : « mes lectures en attente » — l'endpoint que le code lui-même annonce n'existe pas.** Constat vérifié : `lectures_en_attente` (apps/qhse/services.py:1974) nomme dans son propre docstring l'endpoint « mes lectures en attente », absent de `apps/qhse/views.py`. Ajouter l'action/endpoint qui l'appelle. **Done =** un utilisateur voit la liste de ses diffusions non lues via l'API, scopée à lui + sa société (test). Files: `backend/django_core/apps/qhse/views.py`, `backend/django_core/apps/qhse/urls.py`, `backend/django_core/apps/qhse/tests/`. (ROUTINE) (@lane: backend/qhse) (@model: sonnet)
- [x] PACT182 — **XQHS7 : l'analyse 5-pourquoi/8D d'une NCR n'a aucune route.** Constat vérifié : `enregistrer_analyse_ncr` (apps/qhse/services.py:1487) n'a aucun appelant ; `NonConformiteViewSet` (apps/qhse/views.py:197-349) n'a que depuis-reserve/depuis-ticket-sav/creer-intervention/cloturer/taux-defaillance-produit/poser-disposition, aucune action d'analyse. Ajouter une action `analyse/` appelant le service. **Done =** une NCR reçoit son analyse 5-pourquoi et/ou 8D via l'API (merge sur les disciplines fournies, les autres conservées), test. Files: `backend/django_core/apps/qhse/views.py`, `backend/django_core/apps/qhse/tests/`. (ROUTINE) (@lane: backend/qhse) (@model: sonnet)
- [x] PACT183 — **XQHS14 : la revue périodique des risques/opportunités et le lien CAPA n'ont aucune route.** Constat vérifié : `risques_opportunites_revue_due` (apps/qhse/services.py:1908) et `lier_capa_risque_opportunite` (apps/qhse/services.py:1878) n'ont aucun appelant. Ajouter un endpoint « revues dues » + une action de liaison CAPA sur le ViewSet concerné. **Done =** la liste des risques/opportunités en revue due se lit via l'API, lier une CAPA existante est idempotent (re-appel = pas de doublon, test). Files: `backend/django_core/apps/qhse/views.py`, `backend/django_core/apps/qhse/tests/`. (ROUTINE) (@lane: backend/qhse) (@model: sonnet)
- [x] PACT184 — **XQHS12 : le rappel légal de réunion CSH trimestrielle n'a aucune route.** Constat vérifié : `csh_relance_due` (apps/qhse/services.py:1851, obligation Code du travail) n'a aucun appelant. Câbler dans le cockpit QHSE ou une tâche périodique qui notifie quand la cadence est dépassée. **Done =** sans réunion antérieure, la relance est due (première réunion) ; après une réunion tenue, la relance n'est due qu'après la cadence (test). Files: `backend/django_core/apps/qhse/services.py` (appelant), `backend/django_core/apps/qhse/tasks.py` ou vue cockpit, `backend/django_core/apps/qhse/tests/`. (ROUTINE) (@lane: backend/qhse) (@model: sonnet)
- [x] PACT185 — **XGED3 : le PDF signé final aplati n'a aucune route.** Constat vérifié : `rendre_pdf_signe_avec_champs` (apps/ged/services.py:3482) n'a aucun appelant. Ajouter l'action de téléchargement qui l'invoque sur la vue de demande de signature. **Done =** le PDF final avec champs aplatis (valeurs + signature) se télécharge via l'API pour une demande complétée ; best-effort inchangé (jamais de 500) en cas de contenu introuvable, test. Files: `backend/django_core/apps/ged/views.py`, `backend/django_core/apps/ged/tests/`. (ROUTINE) (@lane: backend/ged) (@model: sonnet)
- [x] PACT186 — **XSAL6 : le résolveur de plan de commission cross-app n'est jamais consommé.** Constat vérifié : `resoudre_plan_commission` (apps/ventes/selectors.py:988), documenté comme « point d'entrée cross-app pour reporting/insights », n'a aucun appelant réel. Câbler le point de reporting/insights annoncé (ou, si le consommateur prévu n'existe pas encore, le brancher dans le calcul de commission d'un devis via le sélecteur, jamais en réimportant `PlanCommission`). **Done =** un commercial avec un plan dédié actif voit sa commission résolue par ce plan plutôt que par `CompanyProfile.commission_mode` (test), comportement historique inchangé en son absence. Files: `backend/django_core/apps/reporting/` ou `backend/django_core/apps/ventes/services.py` (appelant), `backend/django_core/apps/ventes/tests/`. (ROUTINE) (@lane: backend/ventes) (@model: sonnet)

#### §F5 — Champs fantomes restants

- [x] PACT187 — **Onglet « Amortissement » d'un véhicule : 3 lignes sur 4 muettes.** `frontend/src/features/flotte/VehiculeDetail.jsx:649-651` lit `valeur_acquisition`, `amortissement_cumule`, `vnc` alors que `backend/django_core/apps/flotte/selectors.py:2337-2345` sert `valeur_origine`, `cumul_amortissements`, `valeur_nette_comptable`. La 4e ligne (`part_non_deductible`, `frontend/src/features/flotte/VehiculeDetail.jsx:652`) est CORRECTE — la clé est ajoutée par mutation dans `backend/django_core/apps/flotte/views.py:267` — donc ne la touchez pas. Aligner les 3 lectures fautives. **Done =** sur un véhicule immobilisé les 4 lignes affichent une valeur ; un test échoue si l'une des 3 corrigées rend « — » sur une réponse `amortissable: true`. Files: `frontend/src/features/flotte/VehiculeDetail.jsx`. (ROUTINE) (@lane: flotte) (@model: haiku)

---

## NEEDS YOUR INPUT — ungated; each waits on something only you can give (with my recommendation)

**Auto-gating is OFF (2026-06-21).** Per your standing consent, NO task is gated by category any
more — `ARCH` / `AUTH` / `COST` / `DECISION` / `GALLERY` / `DEP` are now just **labels**, never a
stop-and-ask, and `scripts/plan_lanes.py` schedules them like any other task (the planner reports
`0 gated` by design). The only things that still hold a task are the five non-negotiable rules
(#1–#5) and a genuine **external prerequisite a run cannot satisfy** — a credential / account /
paid service **you** must provide, real-world data only you have, or a taste / strategic call that
is yours to make.

The items below are no longer auto-skipped; they are parked here **with my recommendation** because
each genuinely needs you. To act on one: provide the credential/data, or say "build it" and a run
ships the safe **no-op scaffold now** (it lights up the moment the key/data lands). Effort tags:
S/M/L. The cross-app safety rules (#1–#5, multi-tenant, buy-prices-never-client-facing) still bind.

- **G1 — Real email sending** (devis/facture/relance by email). **UNGATED 2026-06-18 → BUILD QUEUE
  (N87/N88, Brevo).** Provider chosen = **Brevo** (SDK or SMTP), key from settings/env, no-op
  without a key; pre-approved (see the PRE-APPROVED block). No longer a blocker.
- **G2 — WhatsApp Business Cloud API** (true auto-send + PDF *attached*, message templates).
  Needs: Meta Business **verification** + a WhatsApp Cloud API **access token** + **phone-number
  ID** + an **approved template name**. Today WhatsApp is link-only (`wa.me`, manual tap) and works
  well. **MY RECOMMENDATION: defer until you provision the Meta token — the manual link covers the
  daily need; don't pre-build a dead scaffold. FG207 (inbound WhatsApp → lead) is the SAME Meta
  credential — bundle the two.** Effort M once unblocked. Use Meta Cloud API directly (skip BSP
  resellers/their markup). Verification can take days–weeks; that, not code, is the bottleneck.
- **G3 — Full document visual redesign** (facture + bon de commande; the premium **devis** engine
  `generate_devis_premium.py` / `/proposal` stays OFF-LIMITS per rule #4). The facture/BC still use
  the plainer legacy WeasyPrint templates, which undercut the brand next to the premium devis.
  Needs: a **gallery review** (taste) — I generate 2–3 redesigned facture/BC drafts in the premium
  visual vocabulary, you pick one. **MY RECOMMENDATION: worth doing — clients see the facture as
  often as the devis. Say the word and I'll produce the gallery; keep `PDF_ENGINE=legacy` working.**
  Effort M. (N106 relances + handover sheet already shipped in the premium language.)
- **G5 — Supplier procurement module** (bons de commande fournisseur, goods-in/receiving, supplier
  invoices / accounts payable). **UNGATED 2026-06-20 → BUILD QUEUE (G5, under « Procurement & inventory »).** Approved as a dedicated multi-session module.
- **G6 — Stock auto-decrement on installation** (a chantier consumes its equipment from stock).
  **UNGATED 2026-06-18 → folded into BUILD QUEUE N14.** The exact rule is now confirmed and
  pre-approved: **reserve on chantier create → decrement on « Installé » → release on
  cancel/close** (see the PRE-APPROVED block). No longer a blocker.
- **G7 — Quote e-signature (certified third-party).** Needs your choice of a **paid e-signature
  provider**. The lightweight in-OS acceptance already shipped (Q7: tokenized public accept stamps
  name + timestamp + IP, flips the devis to `accepte`, renders on the PDF) — that is legally
  adequate for residential/SME solar quotes and effectively satisfies FG229. **MY RECOMMENDATION:
  defer — only add a certified provider (Yousign > DocuSign for an MA/FR SME: cheaper, EU-based,
  simpler API) when a high-ticket contract or dispute actually needs eIDAS-grade signing.** Effort M.
- **G8 — 2FA / SSO.** 2FA shipped (N96, opt-in TOTP). **SSO** still needs your choice of an IdP
  (Google Workspace / Microsoft Entra / Okta) + that tenant's app credentials. **MY RECOMMENDATION:
  defer — opt-in 2FA already covers the security need for a small internal team; add SSO only if you
  standardise on one IdP (and then it pairs naturally with G12 if that IdP is Microsoft).** Effort M.
- **G9 — Automation engine / scheduler.** **UNGATED 2026-06-18 → BUILD QUEUE (G9).** Decision made:
  **Celery Beat (in-app)**, two scheduled jobs in Africa/Casablanca time — scheduled relance
  reminders + a daily facture-overdue check (overdue = échéance passed & not fully paid → « En
  retard »; default échéance = issue + 30 days). Pre-approved (see the PRE-APPROVED block). The
  broader no-code automation engine (N72/N73) and n8n workflows stay separate.
- **G10 — CAPI send** (Meta Conversions API, sends `SignedQuote` on Signé, EMQ ≥ 7.0). Capture half
  shipped (fbclid + UTM on the lead + apps/web). The send hook is a stub at two known sites
  (`apps/crm/services.py` SIGNED transition). Needs your **Meta pixel/dataset access token**. CAPI
  itself is **free** (ad-attribution signal, not messaging). **MY RECOMMENDATION: low effort to
  finish (M) and the hook + hashable lead data already exist — provide the pixel token and I'll wire
  the SHA-256-hashed event POST; or build the no-op scaffold now (it's nearly free risk).**
- **G11 — Chatbot / AI assistant → LLM provider.** The chatbot (NL→SQL agent) AND the cross-app AI
  assistant (PLAN2 Group R, already built) run on a multi-provider factory (`SQL_AGENT_PROVIDER` =
  `groq` | `openai` | `claude` | `ollama`). **MY RECOMMENDATION: stay on the default — Groq's FREE
  tier (`llama-3.3-70b-versatile`) — it already works and costs nothing.** The SAME free Groq key
  also powers FG352 (RAG synthesis), FG353 (summarise), FG354 (reply drafts), and the assistant's
  voice (Groq Whisper). `openai`/`claude` are **optional PAID quality upgrades** (better
  reasoning/French, only if Groq's quality/limits prove insufficient); `ollama` is **fully free but
  self-hosted** (needs your own GPU/CPU) if you ever outgrow Groq's free rate limits. FG358 photo QA
  needs a **vision** model — reuse the already-configured Zhipu key, not a new one. **No new paid key
  is required.** FG357 voice and FG361 forecasting aren't LLM-gated at all (free faster-whisper /
  `statsmodels`).
- **💰 AI running costs (verified in code, 2026-06-21).** The whole AI stack runs **FREE by design** —
  no paid key required. Groq's free tier powers the chatbot, the Group R assistant, and assistant
  voice (`whisper-large-v3`, same Groq key); embeddings (`sentence-transformers all-MiniLM-L6-v2`)
  and chat voice memos (`faster-whisper`) are **local/free**; pgvector is free. The **only
  externally-billed AI is OCR via Zhipu GLM** (`ZHIPU_API_KEY`, `glm-4.5v`/`glm-4.7`) — usage-billed
  but very cheap, with free trial credits, and a **pure no-op without a key** (OCR just doesn't run;
  nothing else is affected). `claude`/`openai` are **optional paid quality upgrades**; `ollama` is a
  **free self-hosted fallback** if Groq's free rate limits are ever hit.
- **G12 — Microsoft 365** (Entra ID, Outlook, OneDrive, Teams). Needs an **Entra app registration**
  in your tenant (client id/secret + admin-consented Graph scopes). **MY RECOMMENDATION: confirm
  your team's mail/file stack first — if you live in Google Workspace, skip M365 and prioritise
  Google (calendar/email) instead; only invest here (L) if the team actually runs on M365.**
- **G13 — Import the 619 real Odoo leads.** The idempotent importer is **already built**
  (`apps/crm/management/commands/import_odoo_leads.py` — 3-way match, fills empty fields only, maps
  to `STAGES.py`, `--dry-run`). Needs a **fresh Odoo export of `crm.lead`** (CSV/JSON, holds PII →
  **never committed**, gitignored). **MY RECOMMENDATION: zero dev work — export `crm.lead`, then run
  `manage.py import_odoo_leads <file> --company <slug> --dry-run` first, then for real.** Effort S to
  operate.
- **G14 — DGI e-invoicing readiness (Morocco).** Mandatory ~**Jan 2027** for businesses with CA >
  500k DH — likely Taqinor's wave. **PARTIAL UNGATE 2026-06-18 → BUILD QUEUE (N105):** only the
  **silent, backend-only local capability** was ungated — on-demand **UBL 2.1 / CII** XML export
  (recipient ICE on every line) + a conformity validator, both behind a master toggle that defaults
  OFF and is completely invisible while off. **STILL GATED here** (blocked on the unpublished DGI
  implementing decree — no API spec exists yet, not a decision of Reda's): the **Simpl-TVA portal
  transmission** and the **certified e-signature** (a PDF is explicitly NOT compliant; clearance
  needs the live DGI platform). CONFIRMED — the silent local UBL/XML export already shipped (N105).
  **MY RECOMMENDATION: WAIT for the spec — there is no published Simpl-TVA API to code against, so
  building the portal half now would be guesswork. Mandatory ~Jan 2027 gives runway; the local UBL
  export already positions the data model. Revisit the day DGI publishes the technical spec.** This
  one is blocked by an external SPEC, not by anything you can provide today. Effort L when it lands.
- **G15 — Arabic / Darija UI** (full interface localization, not just message templates). **UNGATED
  2026-06-20 → BUILD QUEUE (N93/N94)** — i18n framework approved. SEQUENCING: run as the FINAL step of
  the UI/UX overhaul (after the component restyle); not prioritized — pull forward only on Reda's
  explicit instruction.
- **G16 — Heavy modularity options.** (a) extract a bounded context into its own deployable service
  (like `fastapi_ia`), (b) per-app pip packaging. **MY RECOMMENDATION: KEEP DEFERRED.** M1–M7
  already delivered the modularity benefit (CI-enforced decoupled boundaries + the `core/events.py`
  bus). Both moves are large, risky migrations against a shared Postgres + the ubiquitous `company`
  FK with ~zero payoff for a single-installer ERP. Defer until a module genuinely needs independent
  scaling/deploy. No founder action needed — this is a "don't burn weeks on it" call. Effort L.

### Strategic calls (ungated, but I recommend holding until you decide)

- **N100 / N101 / N102 — Multi-tenant SaaS platform** (per-tenant billing, tenant admin console,
  self-serve signup). The `company` foundation is already threaded everywhere and isolation is
  enforced, so **no debt accrues by waiting**. **MY RECOMMENDATION: KEEP DEFERRED until there is a
  2nd paying installer.** Self-serve signup is the single biggest auth surface in the app; building
  SaaS billing/console for zero customers adds cost + risk for no return. Decide on a *demand
  signal*, not a date. (The cheap, reversible bit with standalone value — per-tenant white-label of
  client docs via `CompanyProfile` — can be a small separate task if you want it.) These three are
  flipped to `[ ]` in the BUILD QUEUE per your "ungate all", but **do not let a drain build them yet
  unless you've decided to sell TAQINOR as a product** — tell me and I'll re-park them.
- **S21 — Real-time WebSocket chat** (Django Channels + Redis layer + ASGI + nginx WS proxy). Chat
  already works via 3 s polling. **MY RECOMMENDATION: DEFER — polling is plenty for a small internal
  team; the WS stack adds real ops complexity (sticky sessions, connection draining on deploy) for a
  marginal UX gain.** Build only on a concrete need (live dispatch, many concurrent users).

### From the 2026-06-18 refinement audit — gated copies

_All cleared. The former SCHEMA / DEP / DECISION / GALLERY mirrors of the REFINEMENT QUEUE held
nothing pending even before this change, and auto-gating is now OFF entirely (the labels remain on
the tasks for visibility but no run skips them)._

---

## MANUAL — Reda's / Meryem's tasks (NOT code; agent never does these)

Tracked here so they aren't lost:
- Enter the real **ICE / IF / RC** on the server (Paramètres → Identifiants légaux) — live invoices
  currently lack the legally-required seller ICE until then.
- Enter the **11 OSP pump prices** on the server (the agricole pump box stays red; don't send an agri
  quote before).
- Enter **real stock quantities** on the server (the ~283 M DH dashboard value is demo quantities).
- **Article 33 / Loi 82-21 outreach** to the install portfolio (decree in force since 9 June —
  overdue, still sendable; Meryem sends; tag replies « Régularisation 82-21 »).
- **Confirm Sami's GitHub org access is removed.**
- **Confirm the PC cleanup ran** (taqinor-secrets.txt + test-leads JSON deleted).
- **Set the default lead responsable** to Meryem (Paramètres → Leads).
- **Personalize the WhatsApp templates** (Paramètres → Messages WhatsApp).
- Optional: add `PUBLIC_BASE_URL=https://api.taqinor.ma` to the server `.env` for cleaner WhatsApp
  links (they already work via auto-redirect).
- **DEBUG:** turn it off when you decide the OS is ready (your call — the agent will not raise it).


---

## DONE LOG (agent appends one plain-language line per completed task)
- 2026-08-08 — **§E drainé : 76 écrans du « backend que personne ne voit », + les correctifs §C de ventes/RH (8 lanes, 1 merge).** Le lot précédent avait livré la règle de méthode PACT13 ; c'était la porte qui tenait 120 tâches §E fermées — elles se sont ouvertes d'un coup. **Ce qui existait côté serveur et n'avait AUCUN écran est désormais visible** : compta (15 — fiscalité avancée, immobilisations avancées, rapprochements de comptes de bilan, référentiels parallèles + analytique multi-axes, clés de répartition, consolidation groupe, IFRS 15, balance d'ouverture, comparateurs, codes promo, vente guidée, e-catalogue, annexes, échéanciers, approbations de configuration), RH (14 — permis/affectations véhicule, dépôt de bulletins, allocations, demandes RH, modèles d'intégration, éléments variables de paie, entretiens de sortie, grilles salariales, horaires, jours bloqués, parcours employé, fermetures collectives, primes, réglages), installations (7 — sous-traitance, suivi d'import + coût débarqué, prix négociés, documents projet, jalons/modèles, RFQ, paramétrage des kits), GED (7 — coffres, règles de dossier + leur JOURNAL D'EXÉCUTION qui n'avait aucun endpoint, règles d'accès, dispositions, envoi en masse, routage, planifications), AO (8 — bordereau branché sur la vraie ressource, suivi administratif, checklist partenaire, pièces fournies, bibliothèque de pièces, pièces du DCE, économie directeur, plans sources), portail client (6 — première interface ERP de l'app, comptes, acceptations, paiements, documents, jalons, demandes de ticket), BTP chantier (7 — PREMIER frontend de l'app : réserves sur plan cliquable, RFI, visas, journal, avenants, DGD, diffusion de plans), CRM (6 — partenaires, concurrents perdus, points de contact en écriture, revues de compte, tarifs négociés, brouillon IA jamais auto-envoyé). **§C — le passif de ventes/RH** : PACT17 a construit l'agrégat serveur `devis/action-requise/` qui MANQUAIT (l'entrée de menu était publiée aux responsables et admins, l'appel tombait sur la route de détail et rendait 404) avec un exemple de contrat committé que le test backend ET le test frontend lisent — les 5 écrans qui en dépendent ne peuvent plus diverger ; PACT18 le tiret contre le souligné du grand-livre (et le constat que `check_api_contract` ne couvre PAS ce sous-arbre — vérifié en substituant une URL bidon) ; PACT19 les deux actions RH existaient sur d'AUTRES viewsets (un doublon client supprimé, l'export paie déplacé sur l'écran qui l'atteint et qui télécharge vraiment un CSV) ; PACT20 les 4 tuiles de recrutement recalculées sur ce que le serveur dit vraiment (les rejetés sont comptés HORS entonnoir) ; PACT25 le registre des 42 producteurs de la fabrique + **la mort d'un FAUX SUCCÈS : `produire_pack_task` marquait le job « terminé » avec ZÉRO pièce — un pack vide annoncé prêt à déposer ; il échoue désormais par son nom** (3 producteurs sur 10 montés, les 7 autres déclarés avec leur motif — le monteur de contexte AOF111 est une tâche à part) ; PACT180 l'inventaire fermé des 62 fonctions productrices (33 citées / 19 justifiées hors-pack dont les 2 de rentabilité directeur, qui feraient entrer le coût de revient dans un pack CLIENT / 10 à câbler, avec cliquet). **Deux apps neuves côté écran** (`btp_chantier`, `portail`) avec leur premier `module.config.jsx`, glyphe et accent uniques. **Passe orchestrateur au fold** : 36 erreurs eslint invisibles aux lanes (32 × `setState` synchrone dans un effet — corrigées en DÉRIVANT, jamais par un `eslint-disable`) puis la campagne de sélecteurs vitest. Aucune dépendance payante ; aucune migration.
- 2026-08-07 — **Vague PACT §A/§B + §C/§F + AO/QHSE/compta/RH/flotte (48 tâches, 1 merge).** **Les gardes (§A) et la méthode (§B) d'abord, comme le groupe l'exige** : PACT5 garde de contrat élargie à tout `frontend/src` (2 895→3 006 appels ; 1 vrai défaut trouvé — l'export Excel du stock appelait une route inexistante — et 1 faux positif résolu par un MÉCANISME `ternaire_hisse`, jamais une ligne d'exception) ; PACT6 la dérive de l'instantané OpenAPI devient BLOQUANTE (l'instantané était **597 opérations en retard, dont 245 dans `apps/ao`** — le module entré en prod le 02/08 sans que le contrat bouge) ; PACT7 `check_openapi_shapes.py` interdit `response=dict` (0 aujourd'hui, 0 demain) + cliquet sur les 406 « unable to guess serializer », et corrige les DEUX agrégats MENTEURS (flotte, litiges, documentés avec un sérialiseur de détail alors qu'ils renvoient 6 sections) ; PACT8 spec e2e « fumée des écrans » ouvrant les 302 routes lues automatiquement des 43 `module.config.jsx`, câblée dans la ligne playwright qui tourne DÉJÀ à chaque merge ; PACT9 garde écran→endpoint appariée PAR ENDPOINT (5 faux positifs éliminés par leur CAUSE — un paramètre de callback masquant le nom lié — pas par une liste d'exceptions ; retrouve les DEUX plantages AO depuis l'écran) ; PACT10 contrat-d'abord + `apps/ao/contract_samples/` pilote ; PACT11 `plan_lanes.py` refuse une lane frontend dont la source backend est dans le même run sans `@after` (**refuse 28 tâches réelles aujourd'hui** : compta ×17, ventes ×5, stock ×3, qhse, flotte — autant d'ordres front↔back non déclarés) ; PACT12 une tâche mixte non livrée des deux côtés ne peut plus être cochée ; PACT13 fixtures dérivées du contrat au lieu de mocks tapés à la main ; PACT14 les deux règles écrites dans CLAUDE.md. **§C/§F — le passif** : PACT21/187 éco-conduite + amortissement flotte (7 champs fantômes → tirets éternels), PACT22 auto-affectation projet (le toast annonçait le compte SIMULÉ comme appliqué), PACT23 (le vrai défaut était dans les MOCKS, pas le serveur), PACT24 appel fantôme `soumettre` supprimé + `/api/public/v1/openapi.json`, PACT26 **42 ViewSets doublement enregistrés dans `compta/urls.py`** désenregistrés (149→107 ; ressources canoniques intactes sous /ao/, /marketing/, /portail/), PACT149/150/173 la garde d'atteignabilité couvre `pages/**` (431→691 écrans), attrape la classe « route sans entrée de nav » (l'écran Paramètres→Achats, 182 lignes, n'était joignable qu'en tapant l'URL) et ferme la faille du `lazy()` crédité sans route ; PACT152-158 sept faux zéros/faux verts (piste d'audit toujours « intacte », précheck loi 09-08 jamais bloquant, deux compteurs de paie) ; PACT160-165 approbation RIB, séquences de relance enfin abonnées au changement de stage, 3 capacités XACC sans écran, producteurs adsengine orphelins, PDF signé GED, commissions par plan ; PACT166-174 l'atelier toiture, le studio calepinage, les questions terrain, le dossier, les variantes (2 écrans lisaient des champs que le serveur n'envoie pas — dont un badge « reproduit l'ancien calcul ✓ » affiché SANS vérification serveur) et les 5 composants orphelins CÂBLÉS (jamais supprimés : un cycle d'import via le baril expliquait qu'ils n'aient jamais été montés) ; PACT178-186 caution définitive dérivée du CPS + règle bloquante d'expiration, upload de plan, 4 endpoints QHSE. Migrations : veille_ao 0001-0007 uniquement. Aucune dépendance payante. PACT165 a produit `docs/rapport-orphelines.md` (129 fonctions backend à câbler, 64 déjà servies autrement, 42 doublons morts) — 129 lignes de tâches prêtes à coller, à verser au plan par un run futur.
- 2026-08-05 — **N100/N101/N102 — vague multitenant (décision fondateur : l'ERP se VEND, le « KEEP DEFERRED jusqu'au 2ᵉ installeur » est levé).** Run plateforme en 3 lanes worktree parallèles, 1 merge. **N100** : (a) vérification d'isolation = DÉJÀ PRÉSENTE (sweep auto-découvrant `core/tests/test_tenant_isolation_sweep.py` + gardes SCA4/check_tenant_isolation — rien re-construit) ; (b) onboarding tenant = action console « Créer un tenant » (`views_console_create.py` : Company + profil + 1er admin invitation, `init_roles` + hooks signup SCA20, idempotent) ; (c) white-label = TenantTheme/portail + branding CompanyProfile vérifiés — 4 chaînes « TAQINOR » en dur côté client repérées (ged/services, ventes/services OTP+acceptation, public_views LINK_EXPIRED) à queuer en tâche séparée, AUCUNE ne touche le quote engine ; (d) limites par plan = `adminops.PlanLicence` + `CompanyProfile.plan` + `parametres/feature_flags.has_feature` (NTADM7, NULL = accès complet) ; (e) facturation tenant = registre fondateur `FactureLicence` adminops (émise/payée, CSV, jamais côté client, jamais dans ventes). **N101** : console tenants enrichie (plan/sièges/santé/facturation/support) + demandes d'inscription design-partners `DemandeInscription` derrière `TENANT_SIGNUP_ENABLED` (OFF par défaut → 404 ; jamais de création de compte sans approbation fondateur en console) + impersonation support sous consentement explicite du tenant (NTADM22, AUTH — nouveau flag `is_taqinor_support`, noté ici par règle d'autonomie). **N102** : cette ligne + CODEMAP §3/§4/§10 refaits. Nouvelles migrations : authentication 0027, adminops 0002-0007, parametres 0068-0069, notifications 0046, crm 0068, ventes 0092, facturation 0003, stock 0085, roles 0002-0003. AUCUNE nouvelle dépendance payante ; aucune migration destructive.
- 2026-07-16 — **ADSENGINT1/2 (déjà présents)** : chemins API front↔back + endpoints console déjà construits+testés sur main (vérifié : les 42 méthodes de `adsengineApi.js` résolvent). **ADSENGINT3** : e2e Playwright console adsengine (connexion write-only, dashboard cost-per-signature, approve/reject bout-en-bout). **ADSENG19/34 (GATED)** : règle Meta homme-mort (spec `deadman.py` + commande d'installation documentée, OFF) + dossier décision ctwa_clid (docs seulement). **ADSENG50/51/52 (GATED)** : stubs plateformes Google/Snapchat/TikTok (tests de contrat verts, rien d'activé ni de dépense possible ; TikTok prouve que la création sans `PAUSED` forcé est impossible). Aucune campagne créée — règle #3 respectée. **YAPIC6 [BLOCKED]** : le contrôle OpenAPI CI exige l'image WeasyPrint prod (non reproductible hors CI Linux) — reste `[ ]`.
- 2026-07-12 — Lane backend/sav union (fin de run) : `[BLOCKED]` laissés `[ ]` avec annotation `@blocked` — ODX14/17/18/19/20/22 (app-splits state-only apps/facturation+achats, exigent `makemigrations --check` live pour prouver "zéro SQL" ; Django local 6.x incompatible avec le 5.1.4 épinglé, aucun docker/DB dans cette lane — même conclusion que le run du 2026-07-11), XACC12/XPOS19/YCASH5 (chemins argent/GL, même précédent "validation DB + décision fondateur"), XSAL5/XSAL14 (touchent `quote_engine` RULE #4, même précédent que le run du 2026-07-04 "revue dédiée"). Non tentés faute de temps (aucun blocage réel — à reprendre) : YHARD1 (chiffrement au repos, 6 apps), YAPIC5/6 (drf-spectacular, nouvelle dépendance), YAPIC7 (stratégie de versionnement), YAPIC8 (retries webhook Celery).
- 2026-07-12 — ODX7 (nav legacy STOCK/CRM/VENTES/CHANTIERS/SAV/ANALYSE extraite vers le registre UX1) : la vague du 2026-07-11 l'avait laissée `[BLOCKED]` en jugeant le byte-identique impossible dans le périmètre nommé — en réalité, router/moduleRoutes.jsx fusionne TOUTES les sections `nav` triées par `order` juste avant ADMINISTRATION, ce qui aurait réordonné SAV/CRM/VENTES/CHANTIERS. Débloqué en gardant chaque section legacy à sa place historique via un lookup explicite par clé (`navFor(key)` dans Sidebar.jsx, qui lit `moduleConfigs.find(c => c.key === key).nav`) au lieu du merge générique par ordre — zéro changement visuel, ordre préservé au caractère près. A aussi fallu exclure ces 6 clés du merge générique `moduleNavSections` (sinon chaque section apparaissait DEUX fois : une à sa place, une de plus dans le bloc générique) — corrigé dans Sidebar.jsx (`LEGACY_NAV_KEYS`, exportée) ET dans BottomTabBar.jsx (VX12, tiroir mobile « Plus », qui refait le même merge indépendamment). Les 6 module.config.jsx (stock/crm/ventes/installations/sav/reporting) portent désormais leur section `nav` (mêmes routes/libellés/i18n `k`/gardes de rôles/icônes 17px·1.75, à côté de leurs routes ARC48/ARC54 déjà migrées).
- 2026-07-01 — compta COMPTA2/3/4/9/10/11/15/16/29/39 (10 comptables ; COMPTA5/7/8/12-14/17-38 + DC22/24 déjà couverts par FG135-153) : mapping document→compte, comptes auxiliaires tiers (via selectors crm/stock), journal AN + numérotation de pièce, écritures numérotées + pièces justificatives + extourne, auto-écritures facture/paiement fournisseur, ESG/SIG + ETIC, piste d'audit hash-chaînée. COMPTA6/37/40 [BLOCKED : DECISION fiduciaire/format export/séparation des tâches].
- 2026-07-03 — GROS RUN (226 tâches, 1 merge) : drain work-stealing des lanes backend en worktrees parallèles + 2ᵉ tour de continuation par app (chaque lane 2ᵉ tour a `git merge` la branche d'intégration pour chaîner ses migrations). Construit + folié : compta XACC1-11, rh XRH1-21, paie XPAI1-16, qhse XQHS1-15+XFSM14/24, ged XGED1-20, flotte XFLT1-22, gestion_projet XPRJ1-24, kb XKB7-16, sav XSAV3-13, ventes XFAC1-12, stock XPUR1-10, installations XMFG1-14, crm (ODX13[BLOCKED], XMKT21/32/37, YLEAD8/10/11/14), notifications (XMKT25, XKB5/6/33, YEVNT3/4/5/8/9), pos XPOS1-18, contrats XCTR5-13, reporting (XSAV8, XKB1, XPLT6/10/11/22, YSERV13, ZSAV7 ; XFSM16/17 BLOCKED dep XFSM5/15), automation (XKB2/3, XPLT3/4, YEVNT11, ZCTR7/8), customfields XPLT14-17. Collisions de migrations inter-lanes renumérotées, conflits de fichiers-fondation (core/events, celery beat, EventType, ventes/services) résolus keep-both. Test combiné local (1948 tests, image prod) a capturé 106 échecs → 67 vrais bugs corrigés par 8 agents de réparation (company forcé aux créations, gardes de permission, shadowing de routes, double-comptage MRR, validators serializer, coercions Decimal), + PyMuPDF==1.28.0 ajouté (NOUVELLE DÉP — fonctions PDF GED) + nettoyage de médias de test fuités. BLOQUÉS laissés `[ ]` : ODX13 (dépend ODX2 non construit + édition compta hors-lane), XFSM16/17 (dépendent XFSM5/15 non construits).
- 2026-07-12 — N102 laissé `[ ]` (lane backend/stock) : `[BLOCKED : dépend de N100/N101, tous deux KEEP DEFERRED — décision fondateur 2026-07-01 « jusqu'à un 2ᵉ installeur payant » — et n'est de toute façon pas une tâche apps/stock (Files: docs/CODEMAP.md, docs/PLAN.md, docs/DONE.md, « do last »)]`.
