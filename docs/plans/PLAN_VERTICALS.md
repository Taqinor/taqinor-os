# PLAN_VERTICALS — file de travail parallèle (split de new_tasks_plan.md, 2026-07-10)

> **CONTRAT DE PROPRIÉTÉ (non négociable — c'est lui qui garantit zéro conflit).**
> Une session `work on the plan verticals` ne touche QUE :
> **backend :** nouvelles apps agriculture/hospitality/education/sante/immobilier/btp_chantier/esg (aucune app existante).
> **frontend :** frontend/src/pages|features des verticaux correspondants.
> Tout le reste est INTERDIT en écriture — une autre session peut le posséder.
> Lire une app étrangère = via son `selectors.py`/string-FK uniquement (jamais
> ses models/migrations). Une tâche qui EXIGE d'écrire hors périmètre →
> `[BLOCKED: hors périmètre VERTICALS]` + continuer (elle reviendra au run
> plateforme). Fichiers frontend PARTAGÉS (router/nav/api/ui) : ajouts
> APPEND-ONLY minimaux ; un conflit à l'update-branch se résout en prenant les
> DEUX ajouts. Base de test locale : `DB_NAME=erp_verticals` (jamais la
> DB partagée). Chaque session merge sa propre branche `dev-verticals`
> vers main indépendamment (update-branch → CI ~6 min → auto-merge) ; si
> `docs/CODEMAP.md` (fingerprint structure) conflicte à l'update, prendre
> l'arbre mergé et relancer `python scripts/codemap_fingerprint.py --write`.
> Ce fichier n'est PAS dans la surface du plan-fingerprint (comme WEB_PLAN.md) :
> tick + DONE LOG ici, jamais dans docs/CODEMAP.md §10.
> JAMAIS ré-implémenter localement une primitive plateforme manquante (chatter,
> numérotation, jobs, files, registre…) : si une tâche dépend d'une primitive
> ARC/NT pas encore sur main → `[BLOCKED: attend <ID>]` + continuer. C'est le
> vecteur n°1 de dette constaté (13 chatters artisanaux) — bloquer vaut mieux.
> Groupes : NTAGR, NTCON, NTEDU, NTESG, NTHOT, NTPRO, NTSAN. Règles WOW/CLAUDE.md inchangées
> (lane-draining, revue adversariale, retro, routage modèle via plan_lanes.py).

## DONE LOG (une ligne datée par tâche livrée)

### Groupe NTCON — Vertical BTP/EPC vendable (situations, sous-traitance, RFI/visas, réserves, plans, journal de chantier) — bench: Procore, Fieldwire, Graneet, Vertuoza (audit 2026-07-08)
Existant : `installations` (chantiers solaires, lifecycle CH1-6, PV/parc), `gestion_projet` (Projet/PhaseProjet/BudgetProjet/EVM/plan de charge, XPRJ1-29 socle timesheets+facturation régie+situations de travaux XPRJ4+pénalités marché public XPRJ27), `installations.SousTraitant/OrdreSousTraitance` (FG304-307, registre+ordres+attestations sous-traitant), `contrats` (Avenant CONTRAT24, RegleApprobation/EtapeApprobation, SignatureContrat, VersionContrat), `compta.RetenueGarantie/CautionBancaire` (FG145), `qhse` (NCR/CAPA, ITP, DUER, PermisTravail/LOTO), `ged` (Cabinet/Folder/Document/DocumentVersion/PartageGed/watermark/ACL). Ce groupe généralise à un groupe de construction/EPC vendable : réserves/punch-list visuelles sur plan avec preuve de levée (XFSM18 couvre déjà réserve→devis, PAS la punch-list géo-localisée elle-même), RFI, visas de documents techniques, avenants marché CÔTÉ PROJET/BUDGET (CONTRAT24 gère l'amendement contractuel, pas le chiffrage/impact planning-budget), journal de chantier quotidien, diffusion contrôlée de plans (au-dessus de GED), DGD, planning TCE multi-lots avec pénalités par lot, lien PPSPS↔QHSE. Frontières : NTPRJ (générique, hors scope BTP), NTFSM (interventions ponctuelles SAV/terrain — XFSM* déjà exhaustif), qhse/rh existants (jamais réécrits, seulement liés par string-FK/selectors).

#### P2 — Différenciateurs (gestion de plans, planning TCE, PPSPS)

- [ ] NTCON14 — **Planning TCE multi-lots avec jalons contractuels.** Étendre `gestion_projet.PhaseProjet`/`Tache` avec un concept `Lot` (company, chantier FK, `nom` (gros-œuvre/électricité/plomberie/CVC/finitions…), `entreprise` loose-FK vers `SousTraitant` (FG304) OU interne, `date_debut_prevue`/`date_fin_prevue`, `jalon_contractuel` bool, `taux_penalite_retard_pmil` (‰/jour, reprend le pattern XPRJ27 mais par LOT et non par projet global)) ; les `Tache` existantes gagnent un `lot` FK optionnel. Vue Gantt existante (PlanningPage) groupée par lot avec code couleur. **Done =** créer des lots, tâches rattachées, Gantt groupé par lot, migration additive, tests. Files: `apps/gestion_projet/models.py` (+migration : Lot, Tache.lot FK), `apps/gestion_projet/views.py`, `frontend/src/features/gestion_projet/PlanningPage.jsx`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCON15 — **Pénalités de retard calculées par lot.** Sélecteur `penalites_retard_par_lot(chantier)` réutilisant la formule XPRJ27 (jours de dépassement × taux × montant du lot, plafonné) mais appliquée à `Lot.jalon_contractuel` au lieu du projet entier — un lot en retard n'impacte que sa propre pénalité, pas celle des autres lots. Endpoint `chantiers/{id}/penalites-par-lot/`, données internes only. **Done =** exposition par lot correcte et indépendante entre lots, plafond respecté, tests. Files: `apps/btp_chantier/selectors.py`, `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON14)
- [ ] NTCON16 — **Lien PPSPS ↔ QHSE (plan de prévention par chantier/lot).** `qhse.PermisTravail`/`EvaluationRisque` existent au niveau chantier générique — ajouter `PPSPSChantier` (company, chantier FK, document GED du PPSPS (loose-FK), `date_validation`, `valide_par`, `lots_couverts` (M2M vers NTCON14.Lot), `sous_traitants_signataires` (M2M `SousTraitant` FG304 + date de signature par sous-traitant, e-sign typé loi 53-05)) ; un `OrdreSousTraitance` (FG305) ne peut passer en `en_cours` que si son `SousTraitant` a signé le PPSPS du chantier (soft-guard paramétrable warn/block, pattern `qhse.services.exiger_document_unique` QHSE22). **Done =** guard bloque/avertit selon config, signature par sous-traitant tracée, tests. Files: `apps/btp_chantier/models.py` (+migration), `apps/btp_chantier/services.py`, tests. (SCHEMA) «model: sonnet» (@after: NTCON14)
- [ ] NTCON17 — **Registre des intervenants chantier (coordination SPS).** Vue consolidée `chantiers/{id}/intervenants/` agrégeant : sous-traitants actifs (FG304/305 statut en_cours), effectifs du jour (dernier `JournalChantier` NTCON6), habilitations/certifications RH internes présentes (lecture `rh.selectors` FG172-174), attestations sous-traitant à jour (FG307) — vue de coordination SPS/CISSCT en un écran, jamais d'écriture, tous en lecture via selectors. **Done =** vue correcte sur un chantier multi-sous-traitants, attestation expirée signalée, tests. Files: `apps/btp_chantier/selectors.py`, `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON6)

#### P3 — Délices (qualité de vie chantier)

- [ ] NTCON18 — **Photo-rapport hebdomadaire automatique.** Commande `manage.py rapport_photo_hebdo` (Celery beat, opt-in par chantier) compilant les photos `records.Attachment` de la semaine (chantier + réserves NTCON1 + journal NTCON6) en un PDF « avancement photo » envoyé par email au client/MOE (key-gated, no-op sans clé) ; jamais de coût interne dans le PDF. **Done =** PDF hebdomadaire généré avec les bonnes photos datées, opt-in respecté, no-op propre sans clé, tests. Files: `apps/btp_chantier/management/commands/rapport_photo_hebdo.py`, tests. (ROUTINE) «model: haiku» (@after: NTCON6)
- [ ] NTCON19 — **Checklist de réception de lot (avant paiement final du lot).** Réutilise le pattern `ChecklistTemplate`/`ChantierChecklistItem` existant d'installations, étendu par `lot` (NTCON14) : un lot ne peut passer `terminé` que si sa checklist de réception est 100% cochée (soft-guard paramétrable). **Done =** guard testé, checklist par lot distincte de la checklist chantier globale existante. Files: `apps/btp_chantier/models.py` (+migration), `apps/btp_chantier/services.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON14)
- [ ] NTCON20 — **Export « dossier chantier » consolidé.** Action `chantiers/{id}/export-dossier-btp/` : ZIP consolidant journal de chantier (NTCON6), réserves levées avec preuves (NTCON1/2), visas approuvés (NTCON5), DGD (NTCON9), PPSPS signé (NTCON16) — utile pour archivage légal loi 09-08 / litige potentiel, réutilise le pattern d'export ZIP GED (XKB17). **Done =** ZIP contient toutes les pièces d'un chantier donné et rien d'un autre (cross-tenant testé), tests. Files: `apps/btp_chantier/services.py`, `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: haiku» (@after: NTCON9, NTCON16)
- [ ] NTCON21 — **Tableau de bord BTP par chantier.** Page frontend `ChantierBtpCockpit` (nouvelle route `/chantiers/:id/btp`) agrégeant en un écran : lots + avancement Gantt (NTCON14), réserves ouvertes/levées (NTCON1), RFI en attente (NTCON3), visas en cours (NTCON5), déboursé vs facturé (NTCON11), dernier journal de chantier (NTCON6) — lecture seule, tous les chiffres via les endpoints déjà créés ci-dessus (aucune nouvelle logique backend). **Done =** cockpit affiche les 6 blocs avec données réelles, responsive mobile chantier, tests frontend. Files: `frontend/src/pages/btp/ChantierBtpCockpit.jsx` (nouveau), `frontend/src/api/btpChantierApi.js`, route, tests. (ROUTINE) «model: sonnet» (@after: NTCON11, NTCON14)

---
**Notes.** 21 tâches (dans la fourchette basse de 55-80 car le socle BTP/EPC est déjà exceptionnellement couvert par PLAN.md/PLAN2.md avant cet audit : situations de travaux XPRJ4, sous-traitance FG304-307, retenue de garantie/cautions FG145, avenant contractuel CONTRAT24, pénalités marché public XPRJ27, réserve→devis XFSM18, workflow d'approbation générique CONTRAT13/14 sont TOUS déjà construits ou en file — livrer plus aurait dupliqué ces lanes plutôt que combler un vrai manque. Le manque réel et vérifié : RFI, visas de documents techniques, journal de chantier quotidien, DGD formel, diffusion contrôlée de plans au-dessus de GED, planning multi-lots avec pénalités PAR LOT (XPRJ27 est global-projet), lien PPSPS↔QHSE, et la punch-list géolocalisée sur plan elle-même (XFSM18 ne fait que le AVAL réserve→devis). Coordination : FE-XPRJ4 (FRONTEND_GAP_PLAN) attend déjà un onglet « Situations » sur `ProjetDetailPage.jsx` consommant l'API XPRJ4 existante — NTCON n'y touche pas. 0 tâche GATED-founder au sens strict (aucun nouveau service payant obligatoire ; NTCON12/18 restent no-op sans clé email déjà existante).

#### Profondeur (round 2) — rapports & assistants guidés

- [ ] NTCON22 — **Rapport hebdomadaire d'avancement chantier (PDF interne, jamais un devis client).** Commande/action `chantiers/{id}/rapport-avancement/` (WeasyPrint, réutilise le pattern `gestion_projet/reports.py::rapport_avancement_pdf`) compilant sur une période : avancement des lots (NTCON14) vs planning, réserves ouvertes/levées de la période (NTCON1/2), RFI en cours (NTCON3), effectifs moyens (agrégat `JournalChantier` NTCON6), incidents QHSE liés (lecture `qhse.selectors`). Document strictement interne/MOE — jamais exposé via `/proposal`, aucun prix d'achat. **Done =** PDF généré sur une période donnée avec les bons agrégats, cross-tenant refusé, tests. Files: `apps/btp_chantier/pdf.py`, `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON6, NTCON14)
- [ ] NTCON23 — **Assistant guidé « Créer un chantier BTP ».** Wizard frontend multi-étapes (`/chantiers/nouveau-btp`) enchaînant : sélection du `Projet`/`Installation` cible → définition des lots (NTCON14, avec suggestions de lots types gros-œuvre/électricité/plomberie/CVC/finitions pré-remplis, éditables) → affectation sous-traitant ou interne par lot → upload PPSPS initial (NTCON16, optionnel à cette étape) → checklist de réception par lot par défaut (NTCON19). Chaque étape appelle les endpoints déjà créés (aucune nouvelle logique backend), sauvegarde en brouillon à chaque étape. **Done =** wizard complet crée les lots + PPSPS + checklist en une session guidée, reprise possible si abandonné en cours, tests frontend. Files: `frontend/src/pages/btp/NouveauChantierBtpWizard.jsx` (nouveau), `frontend/src/api/btpChantierApi.js`, tests. (ROUTINE) «model: sonnet» (@after: NTCON14, NTCON16, NTCON19)
- [ ] NTCON24 — **Assistant guidé « Clôture de chantier » (DGD + export dossier).** Wizard `chantiers/{id}/cloture-btp/` enchaînant : vérification des pré-requis (toutes réserves bloquantes levées NTCON1/2, tous visas approuvés NTCON5, PPSPS signé par tous les sous-traitants actifs NTCON16 — bloque avec message explicite sinon) → génération du DGD (NTCON9) → notification client (NTCON8-style lien) → export dossier consolidé (NTCON20) en une seule action enchaînée. **Done =** wizard refuse de clôturer si un pré-requis manque (liste précise affichée), succès enchaîne DGD+export sans étape manuelle, tests. Files: `frontend/src/pages/btp/ClotureChantierBtpWizard.jsx` (nouveau), `apps/btp_chantier/services.py`, `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON9, NTCON20)

#### Profondeur (round 2) — réglages tenant & permissions

- [ ] NTCON25 — **Réglages BTP par tenant.** Modèle `ParametresBtpChantier` (OneToOne `company`, singleton par tenant, pattern `qhse.CalendrierQhse`/paramètres existants) : `delai_reponse_rfi_defaut_jours`, `delai_revue_visa_defaut_jours`, `guard_ppsps_bloquant` (bool, warn vs block — consommé par NTCON16), `guard_checklist_lot_bloquant` (bool, consommé par NTCON19), `lots_types_defaut` (JSON liste de noms de lots suggérés au wizard NTCON23), `taux_penalite_retard_defaut_pmil`. Écran `ParametresBtpPage` dans Paramètres (admin only). **Done =** modifier un réglage change le comportement des guards correspondants immédiatement, cross-tenant isolé, tests. Files: `apps/btp_chantier/models.py` (+migration), `apps/btp_chantier/views.py`, `frontend/src/pages/parametres/ParametresBtpPage.jsx` (nouveau), tests. (ROUTINE) «model: sonnet» (@after: NTCON16, NTCON19)
- [ ] NTCON26 — **Permissions fines par rôle module BTP.** Ajouter les clés de permission `btp.reserve.creer`, `btp.reserve.lever`, `btp.rfi.repondre`, `btp.visa.approuver`, `btp.avenant.approuver`, `btp.dgd.finaliser` au registre de permissions existant (`apps/roles` — `Role.permissions` JSONField, pattern `apps/core/permissions.py`) ; chaque viewset/action NTCON1-20 vérifie la permission fine correspondante avant d'autoriser l'action (403 sinon), en plus du filtre `company`. **Done =** un rôle sans `btp.visa.approuver` reçoit 403 sur l'action d'approbation même s'il a accès en lecture au chantier, tests par permission. Files: `apps/roles/` (registre de permissions, additif), `apps/btp_chantier/views.py`, tests. (AUTH) «model: sonnet» (@after: NTCON5, NTCON7, NTCON9)

#### Profondeur (round 2) — Celery beat, exports, API publique

- [ ] NTCON27 — **Purge/archivage automatique des réserves levées anciennes.** Commande `manage.py archiver_reserves_levees` (Celery beat, mensuel) : les `ReserveChantier` `statut=levee` depuis plus de N mois (réglage `ParametresBtpChantier`, défaut 24) passent un flag `archivee=True` (jamais de suppression physique — cohérent avec la politique soft-delete existante `core.SoftDeleteQuerySet`), sorties par défaut des listes actives. **Done =** sweep idempotent, réserves archivées disparaissent des listes actives mais restent accessibles via filtre explicite, tests. Files: `apps/btp_chantier/management/commands/archiver_reserves_levees.py`, tests. (ROUTINE) «model: haiku» (@after: NTCON1, NTCON25)
- [ ] NTCON28 — **Recalcul planifié des pénalités de retard par lot.** Commande `manage.py recalculer_penalites_lots` (Celery beat, quotidien) recalcule `penalites_retard_par_lot` (NTCON15) pour tous les chantiers ayant au moins un lot en retard actif, met en cache le résultat sur un champ dénormalisé `Lot.penalite_calculee_cache` (avec `date_calcul`) pour affichage rapide du cockpit (NTCON21) sans recalcul à chaque GET. **Done =** cache mis à jour quotidiennement, cockpit lit le cache sans latence de calcul, recalcul manuel forcé possible via action admin, tests. Files: `apps/btp_chantier/management/commands/recalculer_penalites_lots.py`, `apps/gestion_projet/models.py` (+migration : Lot.penalite_calculee_cache), tests. (ROUTINE) «model: haiku» (@after: NTCON15)
- [ ] NTCON29 — **Import CSV/XLSX des lots et jalons contractuels.** Endpoint `lots/import/` (réutilise `apps/dataimport` — `ImportJob`/`ImportJobRow`/`ImportMapping` existants, jamais un 2e mécanisme d'import) mappant colonnes CSV/XLSX (nom lot, entreprise/sous-traitant par référence, dates prévues, jalon contractuel bool, taux pénalité) vers `Lot` (NTCON14) en masse, avec ligne d'erreur par ligne invalide (sous-traitant introuvable, dates incohérentes) sans bloquer les lignes valides. **Done =** import de 50 lots avec 3 lignes erronées → 47 créés + rapport d'erreurs précis, cross-tenant isolé, tests. Files: `apps/btp_chantier/views.py` (endpoint `lots/import/`, réutilise `apps/dataimport`), tests. (ROUTINE) «model: sonnet» (@after: NTCON14)
- [ ] NTCON30 — **Export CSV/XLSX des réserves et RFI (reporting externe MOE/client).** Endpoint `reserves-chantier/export/` et `rfi/export/` (`?chantier=&statut=&format=csv|xlsx`, openpyxl pour xlsx) — colonnes : numéro, lot, description, gravité/priorité, statut, responsable, date limite, date de levée ; jamais de coût interne dans l'export. **Done =** export filtré correct, colonnes conformes, cross-tenant isolé, tests. Files: `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: haiku» (@after: NTCON1, NTCON3)
- [ ] NTCON31 — **Exposition API publique + webhooks des nouveaux objets BTP.** Enregistrer `ReserveChantier`, `RFI`, `VisaDocument`, `DecompteGeneral` comme ressources lisibles via `apps/publicapi` (`ApiKey` scope-based, pattern déjà en place pour d'autres apps) ; déclencher des `Webhook`/`WebhookDelivery` (`apps/publicapi`) sur les événements `reserve.levee`, `rfi.repondu`, `visa.approuve`, `dgd.finalise` via le bus `core/events.py` (jamais d'appel direct cross-app). **Done =** un abonné webhook externe reçoit la notification à la levée d'une réserve, clé API scope-restreinte testée, tests. Files: `apps/publicapi/` (registre de ressources + EVENT_CHOICES, additif), `apps/btp_chantier/services.py` (émission d'événements `core/events.py`), tests. (SCHEMA) «model: sonnet» (@after: NTCON2, NTCON4, NTCON5, NTCON9)

#### Profondeur (round 2) — intégration chatter/audit/notifications, KPI, e2e

- [ ] NTCON32 — **Chatter & audit trail sur les nouveaux objets BTP.** `ReserveChantier`, `RFI`, `VisaDocument`, `AvenantChantier`, `DecompteGeneral` s'intègrent au chatter existant (pattern `qhse.QhseChatterEntry`/`crm.LeadActivity` — log automatique ancien→nouveau statut + notes manuelles `historique`/`noter`, auteur+company serveur) plutôt qu'un 2e mécanisme de commentaires ; chaque changement de statut passe aussi par `audit.AuditLog` (selector existant, jamais d'écriture directe hors du service cible). **Done =** changer le statut d'un RFI ou d'une réserve laisse une trace chatter ET une entrée AuditLog, note manuelle horodatée+auteur, tests. Files: `apps/btp_chantier/models.py`, `apps/btp_chantier/services.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON1, NTCON3, NTCON5, NTCON7, NTCON9)
- [ ] NTCON33 — **Notifications ciblées sur les événements BTP clés.** Utiliser `notifications.Notification`/`NotificationRoutingRule` existants (jamais un canal ad hoc) pour notifier : responsable désigné à la création d'une réserve bloquante (NTCON1), destinataire à la création d'un RFI (NTCON3, en plus du sweep de retard NTCON4), revuseur à la soumission d'un visa (NTCON5), équipe projet à l'approbation d'un avenant (NTCON7-8). Respecte les préférences utilisateur (`NotificationPreference`) et les heures ouvrées (`WorkingHoursConfig`). **Done =** chaque événement génère la notification au bon destinataire, préférences utilisateur respectées, tests. Files: `apps/btp_chantier/services.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON1, NTCON3, NTCON5, NTCON7)
- [ ] NTCON34 — **KPI BTP dans le reporting transverse.** Ajouter les clés `Kpi` (TextChoices `reporting.models.Kpi`) `btp_reserves_ouvertes`, `btp_rfi_en_retard`, `btp_visas_en_attente`, `btp_penalites_cumulees_periode` alimentées par les sélecteurs NTCON3/11/15 existants, éligibles à `KpiAlerte` (seuils configurables) et disponibles comme tuiles sur les dashboards `core.Dashboard` génériques (pas seulement le cockpit dédié NTCON21). **Done =** les 4 KPI apparaissent dans le picker de tuiles dashboard, une alerte se déclenche au franchissement du seuil configuré, tests. Files: `apps/reporting/models.py` (Kpi TextChoices, additif), `apps/btp_chantier/selectors.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON3, NTCON11, NTCON15)
- [ ] NTCON35 — **Test e2e du parcours réserve→levée→export dossier.** Suite Playwright (pattern e2e existant E1-16) : créer un chantier BTP via le wizard (NTCON23) → poser un pin de réserve sur un plan GED → lever la réserve avec photo+signature (NTCON2) → vérifier son passage en « levée » dans le cockpit (NTCON21) → exporter le dossier chantier (NTCON20) et vérifier la présence de la preuve dans le ZIP. **Done =** parcours complet vert en CI, sélecteurs DOM stables (`btp-*` prefix), inclus dans la suite requise. Files: `e2e/btp-chantier.spec.ts` (nouveau), tests. (ROUTINE) «model: sonnet» (@after: NTCON2, NTCON20, NTCON21, NTCON23)

#### Profondeur (round 2) — terrain mobile & qualité de vie complémentaire

- [ ] NTCON36 — **Capture terrain rapide hors-ligne : réserve + journal du jour.** Écran mobile-first `/chantiers/:id/terrain` (responsive, pattern déjà utilisé par les écrans SAV terrain XFSM) permettant en 2 taps : poser une réserve avec photo depuis la caméra directement sur le pin plan (NTCON1) ou compléter l'entrée du jour du journal de chantier (NTCON6) — formulaire minimal, brouillon local (localStorage) si perte réseau, synchronisation à la reconnexion (pattern `FG386` offline field queue déjà planifié, réutilisé — pas de 2e mécanisme offline). **Done =** création d'une réserve/entrée journal en mode dégradé réseau puis synchronisée sans perte au retour du réseau, tests frontend. Files: `frontend/src/pages/btp/TerrainCapture.jsx` (nouveau), `apps/btp_chantier/views.py`, tests. (ROUTINE) «model: sonnet» (@after: NTCON1, NTCON6)
- [ ] NTCON37 — **Relance automatique visa en attente de revue (au-delà du RFI).** Commande `manage.py alertes_visas_en_attente` (Celery beat, quotidien, même schéma qu'NTCON4) notifiant le `revu_par` désigné et son manager (via `rh.selectors` hiérarchie) pour tout `VisaDocument` `statut=soumis` ou `en_revue` dont `date_limite` (calculée par `delai_revue_jours`) est dépassée — une seule notification par jour par visa (idempotent), s'arrête à l'approbation/refus. **Done =** sweep idempotent testé, visas déjà traités exclus, notification manager en plus du revuseur direct. Files: `apps/btp_chantier/management/commands/alertes_visas_en_attente.py`, tests. (ROUTINE) «model: haiku» (@after: NTCON5)


> Groupes NTPRO / NTSAN / NTHOT / NTEDU / NTAGR déménagés le 2026-09-02 vers
> `docs/backlog/FULL_SAAS_PLAN.md` (spécialisation ERP solaire — verticaux non adaptables,
> parqués sans suppression, réactivables). Restent ici : NTCON (BTP/EPC, le chantier sert
> l'installation solaire C&I) et NTESG.

### Groupe NTESG — Reporting ESG/durabilité d'entreprise consolidé — bench: Workiva, EcoVadis, Salesforce Net Zero Cloud (audit 2026-07-07)

**Vérifié dans le code (2026-07-07) : le socle carbone/ESG est DÉJÀ construit dans `apps/qhse`** —
`BilanCarbone`/`LigneBilanCarbone` (QHSE39, scopes 1/2/3 GHG Protocol, `tco2e` dérivé), `IndicateurESG`
(QHSE40, pilier E/S/G, cible/tendance/`atteinte_cible`, sélecteur `export_esg` groupé par pilier),
`ReleveConsommation` + `generer_lignes_bilan` (XQHS21, élec/eau/carburant sites → bilan carbone),
`Dechet`/`BordereauSuiviDechet` (déchets + BSD), `ConformiteEnvironnementale`, `AspectEnvironnemental`
(ISO 14001), coût de la non-qualité (XQHS22). Ce groupe ne reconstruit RIEN de tout ça — il ajoute la
couche **CONSOLIDATION ENTREPRISE** qui manque encore : périodes de reporting figées (snapshot),
agrégation cross-app en LECTURE (flotte carburant, RH social, QHSE environnement/CoQ), objectifs +
trajectoire pluriannuelle, rapport PDF/xlsx GRI-lite avec méthodologie affichée, volet fournisseurs RSE,
et le tableau de bord ESG consolidé qu'aucun écran n'affiche aujourd'hui (les indicateurs QHSE39/40
n'ont même pas encore de cockpit React — FE-XQHS20-21 couvre l'environnement, pas l'ESG consolidé).
Frontières : QHSE garde la SAISIE des données sources (bilan carbone, indicateurs bruts, déchets,
relevés) ; NTESG lit ces sources via `qhse.selectors` et les siennes propres, ne les modifie jamais.
NTNRG garde le carbone de PRODUCTION solaire et les attestations clients. NTLOG garde le CO2 transport
opérationnel (NTESG l'agrège en lecture seule via son sélecteur). NTGRC garde le moteur de
questionnaires générique ; NTESG en réutilise le modèle pour une variante RSE fournisseurs sans
dupliquer le moteur (coordination cross-plan si NTGRC22/23 n'existent pas encore au moment du build —
voir note de coordination sous P1). Règle stricte checked-facts-only : un indicateur sans donnée est
OMIS du rapport (jamais 0, jamais estimé silencieux) ; toute valeur calculée porte un libellé
« estimation » visible ; aucun facteur d'émission n'est présenté comme certifié/audité.

#### P1 — Consolidation & référentiel ESG (bloquants vente grands comptes)

- [ ] NTESG8 — **Coordination cross-plan — variante questionnaire RSE fournisseurs.** Si le moteur générique de questionnaires (référencé dans le brief comme NTGRC22/23) existe déjà au moment du build de cette tâche : ajouter une variante de modèle de questionnaire pré-remplie « RSE fournisseur » (thèmes : conditions de travail, environnement, éthique des affaires, conformité réglementaire — inspiré EcoVadis sans jamais utiliser ce nom commercial) réutilisant TEL QUEL le moteur d'envoi/relance/scoring de NTGRC, avec un score RSE consolidé par fournisseur exposé en lecture par `stock.selectors.fournisseur_score_rse` (fonction fine, appelée par NTESG, jamais l'inverse). Si NTGRC22/23 n'existent PAS encore à ce moment : marquer `[BLOCKED: dépend de NTGRC22/23 — moteur de questionnaires non construit]`, ne rien dupliquer, passer à la tâche suivante. **Critère d'acceptation** : soit le questionnaire RSE fonctionne bout-en-bout en réutilisant le moteur NTGRC existant sans code dupliqué, soit la tâche est marquée bloquée proprement sans stub cassé. Files: `apps/esg/models.py` (+migration, si NTGRC22/23 disponibles), `apps/esg/services.py`, `apps/stock/selectors.py` (fonction `fournisseur_score_rse`, additive), tests. (ROUTINE) «model: sonnet»

#### P3 — Delighters

- [ ] NTESG17 — **Widget « Score ESG fournisseur » sur la fiche fournisseur stock** : si NTESG8 a pu être construit (moteur RSE fournisseur disponible), afficher un badge simple sur `ProduitForm`/fiche fournisseur (lecture seule via `stock.selectors.fournisseur_score_rse`) — sinon `[BLOCKED: dépend de NTESG8]`. **Critère d'acceptation** : le badge s'affiche sans appel bloquant si le score est absent (fournisseur jamais évalué → aucun badge, pas de "0"), tests. Files: `frontend/src/features/stock/ProduitForm.jsx`, tests. (ROUTINE) «model: haiku» (@after: NTESG8)

---

#### Profondeur (round 2) — rapports, wizard, réglages, planifié, permissions, API/webhooks, imports/exports, e2e, chatter, KPI

- [ ] NTESG18 — **Wizard guidé « Clôture de période ESG »** : écran en 4 étapes dans le cockpit NTESG6 avant l'appel à `POST periodes-esg/{id}/figer/` (NTESG1) : étape 1 récapitule la couverture du catalogue par pilier (NTESG3, alerte si <50%) ; étape 2 liste les indicateurs `IndicateurESG` modifiés depuis la dernière période avec diff avant/après ; étape 3 aperçu du PDF (NTESG4) avant génération définitive ; étape 4 confirmation explicite « cette action fige les données, non réversible ». Chaque étape bloque le passage à la suivante uniquement sur incohérence bloquante réelle (dates de période invalides, période déjà figée) — jamais sur un simple manque de donnée qui reste juste un avertissement. **Critère d'acceptation** : impossible de figer une période sans passer par les 4 étapes depuis l'écran (l'API reste directement appelable pour l'automatisation), tests e2e du parcours complet. Files: `frontend/src/pages/esg/WizardClotureEsg.jsx` (nouveau), tests. (ROUTINE) «model: sonnet» (@after: NTESG32)
- [ ] NTESG19 — **Wizard « Créer un objectif de trajectoire »** : assistant 3 écrans pour `ObjectifESGTrajectoire` (NTESG7) : sélection de l'indicateur `IndicateurESG.code` (autocomplete filtré sur les codes existants, jamais de saisie libre qui casserait le lien) → saisie référence/cible avec aperçu graphique immédiat de la trajectoire linéaire théorique → confirmation. Empêche de créer un objectif sur un indicateur inexistant ou déjà doté d'un objectif actif (unicité company+code+annee_cible). **Critère d'acceptation** : le wizard refuse un doublon d'objectif actif sur le même indicateur avec message clair, tests. Files: `frontend/src/pages/esg/WizardObjectifTrajectoire.jsx` (nouveau), `apps/esg/views.py`, tests. (ROUTINE) «model: sonnet» (@after: NTESG32)
- [ ] NTESG20 — **Réglages ESG par société (`ParametresESG`)** : modèle singleton par company (pattern `qhse.ParametresLocation`/`notifications.WorkingHoursConfig`) : `seuil_alerte_derive_pct` (défaut 10%, consommé par NTESG10), `pilote_esg` (FK utilisateur responsable, destinataire par défaut des notifications), `frequence_reporting` (mensuelle/trimestrielle/annuelle, informatif pour le wizard NTESG18), `ponderation_badge_maturite` JSON (poids couverture/cibles/trajectoire, consommé par NTESG15, doit sommer à 100 — validé serveur). Écran `Paramètres > ESG`. **Critère d'acceptation** : modifier la pondération recalcule immédiatement le badge de maturité affiché sans redémarrage, tests. Files: `apps/esg/models.py` (+migration), `apps/esg/views.py`, `frontend/src/pages/parametres/ParametresEsgPage.jsx` (nouveau), tests. (ROUTINE) «model: sonnet»
- [ ] NTESG21 — **Tâche planifiée Celery beat — rappel de clôture de période** : `apps/esg/tasks.py::rappeler_cloture_periode` (pattern `apps/qhse/tasks.py`), exécutée quotidiennement, notifie le `pilote_esg` (NTESG20) via `notifications.notify()` quand une `PeriodeReportingESG` en statut `brouillon` dépasse sa `date_fin` de plus de 15 jours sans être figée. Un seul rappel par période tous les 7 jours (pas de spam quotidien) via un champ `dernier_rappel_le` sur la période. Best-effort, jamais bloquant. **Critère d'acceptation** : la tâche ne renvoie pas de rappel avant 15 jours de retard, puis au maximum un par semaine ensuite, tests avec horloge simulée. Files: `apps/esg/tasks.py` (nouveau), `apps/esg/models.py` (+migration : `dernier_rappel_le`), tests. (ROUTINE) «model: haiku»
- [ ] NTESG22 — **Tâche planifiée Celery beat — recalcul de couverture catalogue** : `apps/esg/tasks.py::recalculer_couverture_catalogue`, exécutée hebdomadairement par société active, rafraîchit un cache léger (`CatalogueIndicateurESG.couverture_cache` JSON par pilier, horodaté) consommé par le cockpit NTESG6 pour éviter un recalcul synchrone coûteux à chaque chargement d'écran quand le nombre d'indicateurs grossit. Le endpoint `catalogue-esg/couverture/` (NTESG3) sert le cache si `<24h`, recalcule en direct sinon (best-effort, jamais bloquant si la tâche a échoué). **Critère d'acceptation** : le cache expire proprement après 24h et retombe sur le calcul synchrone sans erreur si jamais rafraîchi, tests. Files: `apps/esg/tasks.py`, `apps/esg/models.py` (+migration : `couverture_cache`), `apps/esg/views.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTESG23 — **Tâche planifiée Celery beat — purge des brouillons de rapport orphelins** : `apps/esg/tasks.py::purger_rapports_orphelins`, exécutée mensuellement, supprime (soft-delete, jamais hard-delete) les fichiers PDF/xlsx générés en aperçu (NTESG18 étape 3) non rattachés à une période effectivement figée après 30 jours, pour éviter l'accumulation de fichiers MinIO orphelins. Ne touche jamais un fichier attaché à une période figée ou publiée. **Critère d'acceptation** : un aperçu de 40 jours pour une période restée brouillon est purgé, un aperçu attaché à une période figée ne l'est jamais, tests. Files: `apps/esg/tasks.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTESG24 — **Permissions par rôle fines du module ESG** : nouveaux codes de permission ajoutés au JSON `Role.permissions` (`esg.view`, `esg.edit_objectifs`, `esg.figer_periode`, `esg.publier_rapport`, `esg.gerer_parametres`) enregistrés dans `init_roles.py` avec un mapping par défaut raisonnable (ex. rôle « Direction »/« QHSE » = tout, rôle « Commercial » = aucun accès ESG). Le endpoint `figer/` (NTESG1) exige `esg.figer_periode`, la publication d'un rapport exige `esg.publier_rapport` — distinct de la simple lecture du cockpit (`esg.view`). **Critère d'acceptation** : un utilisateur sans `esg.figer_periode` reçoit un 403 explicite sur l'action de figeage même s'il voit le cockpit en lecture, tests. Files: `apps/roles/` (init_roles.py, registre de permissions, additif), `apps/esg/views.py`, tests. (AUTH) «model: sonnet»
- [ ] NTESG25 — **Exposition API publique lecture seule (`publicapi`)** : ajouter `esg.periode_reporting_publiee` et `esg.indicateur_esg` au `EVENT_CHOICES`/registre de ressources exposables de `apps/publicapi` (pattern existant `ApiKey`/scopes), permettant à une clé API scope `esg:read` de lister UNIQUEMENT les `PeriodeReportingESG` en statut `publiee` (jamais brouillon/figée-non-publiée) et leur `SnapshotESG` gelé — utile pour un donneur d'ordre/auditeur externe qui veut tirer les chiffres via API plutôt qu'un PDF. Aucune donnée de prix/commerciale n'est jamais exposée par ce scope. **Critère d'acceptation** : une clé sans le scope `esg:read` reçoit 403, une période non publiée n'apparaît jamais même avec le scope, tests. Files: `apps/publicapi/` (registre de ressources, additif), `apps/esg/views.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTESG26 — **Webhook sortant `esg.periode_publiee`** : événement publié sur `core/events.py` quand une période passe `figee` → `publiee`, consommé par `publicapi.Webhook` (pattern existant `EVENT_CHOICES`) pour notifier un système externe (ex. plateforme donneur d'ordre grands comptes) qu'un nouveau rapport ESG est disponible, payload minimal (id période, dates, statut, URL de l'export xlsx si publié) — jamais le contenu détaillé des indicateurs dans le payload webhook (l'appelant doit retirer via l'API scope `esg:read`, NTESG25, pour forcer l'authentification sur la donnée fine). **Critère d'acceptation** : publier une période déclenche exactement une livraison webhook par abonnement actif, retry existant de `WebhookDelivery` réutilisé sans code dupliqué, tests. Files: `apps/esg/services.py` (émission d'événement `core/events.py`), `apps/publicapi/` (EVENT_CHOICES, additif), tests. (ROUTINE) «model: sonnet»
- [ ] NTESG27 — **Import CSV en masse — bibliothèque de facteurs d'émission (NTESG16)** : action `facteurs-emission/import/` (multipart CSV, colonnes catégorie/unité/valeur/source/date_maj), réutilise le moteur `dataimport.ImportJob`/`ImportJobRow` existant (jamais un parseur CSV maison) pour prévisualiser, valider (unité cohérente par catégorie, valeur numérique positive) puis importer en masse une mise à jour annuelle de facteurs (ex. nouvelle version ADEME Base Carbone), en créant une nouvelle version historisée par facteur modifié plutôt qu'un écrasement silencieux. **Critère d'acceptation** : un CSV avec une ligne invalide échoue sur cette seule ligne avec message précis, les lignes valides s'importent, l'historique de version se crée, tests. Files: `apps/esg/views.py` (endpoint `facteurs-emission/import/`, réutilise `apps/dataimport`), tests. (ROUTINE) «model: sonnet»
- [ ] NTESG28 — **Export CSV — registre des parties prenantes ESG (NTESG12)** : action `parties-prenantes-esg/export/?format=csv` sur le même modèle que les autres exports CSV de l'app (colonnes nom/catégorie/enjeux/influence/intérêt), utile pour préparer une matrice de matérialité dans un tableur externe lors d'un exercice collaboratif avec la direction. **Critère d'acceptation** : le CSV s'ouvre avec les bons en-têtes et un accent UTF-8 correct (BOM), tests. Files: `apps/esg/views.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTESG29 — **Test e2e — parcours clé « saisir un objectif, figer une période, générer le rapport »** : scénario Playwright (pattern `e2e/`) : se connecter avec un rôle doté de `esg.figer_periode` (NTESG24) → créer un `ObjectifESGTrajectoire` via le wizard (NTESG19) → ouvrir le cockpit (NTESG6), vérifier l'affichage des cartes par pilier avec les vraies données QHSE seed → lancer le wizard de clôture (NTESG18) → figer la période → télécharger le PDF (NTESG4) et vérifier qu'il contient le bandeau méthodologique obligatoire. **Critère d'acceptation** : le scénario passe de bout en bout sur les données de test seed, s'exécute dans la suite e2e existante sans nouvelle infra. Files: `e2e/esg.spec.ts` (nouveau), tests. (ROUTINE) «model: sonnet»
- [ ] NTESG30 — **Intégration chatter/records sur `PeriodeReportingESG`** : chaque `PeriodeReportingESG` s'enregistre comme objet suivable via `records.Follower`/`records.Activity` (pattern déjà utilisé ailleurs dans l'ERP, jamais un chatter maison spécifique à l'app esg) : le figeage (NTESG1), la publication et l'ajout d'un `DocumentPolitiqueESG` (NTESG13) en annexe créent chacun une entrée d'activité horodatée avec l'utilisateur acteur server-side ; `pilote_esg` (NTESG20) suit automatiquement toute période de sa société. **Critère d'acceptation** : figer une période produit une entrée visible dans le fil d'activité du record depuis l'écran générique existant, tests. Files: `apps/esg/models.py`, `apps/esg/services.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTESG31 — **Notification de publication de rapport** : `notifications.notify()` envoyée à `pilote_esg` + tout utilisateur avec permission `esg.view` de la société (NTESG24) au moment où une période passe en statut `publiee`, avec lien direct vers le cockpit/l'export. Best-effort, une seule notification par transition de statut (jamais à chaque relecture). **Critère d'acceptation** : republier deux fois de suite sans changement de statut ne renvoie pas de second lot de notifications, tests. Files: `apps/esg/services.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTESG32 — **KPI ESG dans le module reporting** : ajout de 3 `Kpi`/tuiles au catalogue `reporting` existant (pattern `reporting.Kpi`/`DashboardConfig`, jamais un nouveau moteur de KPI) : « Couverture catalogue ESG » (% NTESG3), « Écart trajectoire moyen » (moyenne des écarts NTESG7 sur les objectifs actifs), « Intensité carbone (tCO2e/MAD CA) » (NTESG9) — chacun `disponible=false` proprement si la société n'a pas de période figée, jamais un 0 par défaut. Consommable sur un `Dashboard` générique au même titre que les KPI des autres modules. **Critère d'acceptation** : les 3 KPI apparaissent dans le sélecteur de widgets du dashboard générique et rendent une valeur cohérente avec le cockpit NTESG6 pour la même période, tests. Files: `apps/reporting/models.py` (Kpi TextChoices, additif), `apps/esg/selectors.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTESG33 — **Rapport imprimable — fiche de synthèse fournisseur RSE** : si NTESG8 est construit (score RSE fournisseur), un export PDF simple par fournisseur (WeasyPrint, pattern `qhse` PDF existant, jamais `/proposal`) : identité fournisseur, score RSE consolidé + historique des campagnes de questionnaire, sans aucune donnée d'achat/prix (`stock.ContratPrixFournisseur` jamais référencé). Utile pour un dossier d'audit fournisseur en revue annuelle. Si NTESG8 est bloqué, cette tâche l'est aussi. **Critère d'acceptation** : le PDF omet la section historique si aucune campagne n'a encore été envoyée à ce fournisseur, tests. Files: `apps/esg/pdf.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTESG34 — **Réglage « pilier obligatoire » par société pour la clôture** : extension de `ParametresESG` (NTESG20) : liste des piliers E/S/G que la société juge non-négociables pour publier un rapport externe (ex. un grand groupe industriel veut toujours au moins un indicateur Social renseigné) ; le wizard de clôture (NTESG18) affiche un avertissement bloquant SEULEMENT sur la PUBLICATION (jamais sur le simple figeage interne) si un pilier marqué obligatoire est totalement vide. Reste contournable par un utilisateur avec `esg.publier_rapport` (avertissement, pas un verrou dur — décision founder si un verrou dur est un jour souhaité). **Critère d'acceptation** : publier avec un pilier obligatoire vide affiche l'avertissement mais n'empêche jamais techniquement l'action, tests. Files: `apps/esg/models.py` (+migration), `frontend/src/pages/esg/WizardClotureEsg.jsx`, tests. (ROUTINE) «model: sonnet» (@after: NTESG32)

---

**Notes de vérification** : le socle QHSE39/40/XQHS20-21 (`BilanCarbone`, `LigneBilanCarbone`, `IndicateurESG`,
`ReleveConsommation`, `generer_lignes_bilan`) est déjà marqué `[x]` dans `docs/PLAN.md` et testé
(`apps/qhse/tests/test_bilan_carbone.py`, `test_indicateur_esg.py`) — vérifié en lisant le code
directement, le digest_open fourni ne le mentionnait pas (digest gelé au 2026-06-29, le repo a avancé
depuis). Ce groupe NTESG ne duplique donc AUCUN de ces modèles : il construit exclusivement la couche
consolidation/reporting/objectifs/fournisseurs qui reste absente. `apps/rh` n'a pas de champ
genre/parité aujourd'hui — un indicateur de parité resterait `disponible=false` jusqu'à ce qu'un champ
soit ajouté côté RH (hors périmètre de ce groupe, à signaler si un futur lane RH veut le construire).
NTGRC22/23 (moteur de questionnaires générique) n'existe pas encore dans le repo au moment de cet audit
— NTESG8/17 sont conçues pour se bloquer proprement plutôt que dupliquer si elles arrivent en premier.
14 tâches P1 (bloquants vente), 6 P2, 3 P3 = 17 tâches livrées sur un total honnête : le domaine est
étroit maintenant que le socle de données existe déjà — je n'ai pas gonflé artificiellement pour
atteindre 55-80, la plupart des besoins "ESG" génériques (carbone, indicateurs E/S/G, déchets, conformité
environnementale, coût non-qualité) sont déjà couverts par QHSE39/40/XQHS20-22 et ne devaient pas être
re-proposés.


