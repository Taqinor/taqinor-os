# PLAN_ADSENG — le moteur publicitaire autonome complet (fichier de travail parallèle, 2026-07-16)

> **CONTRAT DE PROPRIÉTÉ (non négociable — c'est lui qui garantit zéro conflit).**
> Une session `work on the plan adseng` ne touche QUE :
> **backend :** `apps/adsengine` (créée par Groupe ENG dans docs/PLAN.md).
> **frontend :** `frontend/src/features/adsengine`.
> **docs :** `docs/engine/*`, ce fichier.
> **Exceptions chirurgicales nommées (trois, exhaustives)** :
> ADSENG1 peut éditer le mapping leadgen dans `apps/crm/webhooks.py` (rien d'autre du fichier) ;
> ADSENG2 peut éditer l'émetteur CAPI dans `apps/ventes/services.py` (version API + event_id + champs de match, rien d'autre) ;
> ADSENG47 peut ajouter 2 constantes APPEND-ONLY dans `apps/roles/models.py` + leur mapping init_roles.
> S'y ajoute la règle CLAUDE.md standard : un AJOUT FIN et additif à un `selectors.py` étranger est permis
> quand la tâche le nomme explicitement (ex. ADSENG6 → `apps/crm/selectors.py`).
> Tout le reste est INTERDIT en écriture. Lire une app étrangère = via son `selectors.py`/string-FK
> uniquement. Une tâche qui EXIGE d'écrire hors périmètre → `[BLOCKED: hors périmètre ADSENG]` + continuer.
> Fichiers frontend PARTAGÉS (router/nav/api/ui) : ajouts APPEND-ONLY minimaux.
> Base de test locale : `DB_NAME=erp_adseng` (jamais la DB partagée). Branche : `dev-adseng` → main
> (update-branch → CI → merge) — **sauf instruction fondateur contraire en cours** (au 16/07 : montrer avant de merger).
> Ce fichier n'est PAS dans la surface du plan-fingerprint (comme WEB_PLAN.md et les autres docs/plans/) :
> tick + DONE LOG ici, jamais dans docs/CODEMAP.md §10. L'empreinte STRUCTURE, elle, s'applique :
> nouveaux modèles/routes ⇒ régénérer CODEMAP §3/§4 + `codemap_fingerprint.py --write` dans le même commit.
> **Prérequis structurel** : ce plan compose sur Groupe ENG (docs/PLAN.md, ENG1-31). BUILD_ORDER.yml porte
> l'arête `ADSENG after {ENG-noyau: 80}` (noyau = ENG1-5,7,19,20). Une dépendance fine à une tâche ENG
> précise se marque en prose `[BLOCKED: attend ENGxx]` — les tags `@after:` inter-fichiers sont silencieusement
> ignorés par plan_lanes.py en invocation mono-fichier (fait vérifié), ne jamais s'y fier.
> **Tags modèle** : `(@model: haiku|sonnet|opus)` sur chaque tâche (la forme `«model: X»` des autres fichiers
> domaine n'est PAS matchée par plan_lanes.py — bug latent connu ; on utilise la forme qui marche).
> Règles CLAUDE.md inchangées (PAUSED forcé, cross-app selectors, STAGES.py jamais en dur, lane-draining,
> revue adversariale, un merge par run).

> **DOCTRINE (issue de 9 dossiers de recherche, 16/07/2026 — les specs complètes sont les dossiers
> `dd-*.md`/`scope-*.md` de la session ; chaque tâche ci-dessous nomme son dossier source).**
> 1. **Déterministe d'abord** : AUCUN LLM dans la boucle de décision. Le moteur roule des mois sur
>    statistiques pures (bandit Thompson) + règles + garde-fous. L'IA fournit le matériel créatif en amont
>    et commente APRÈS coup (gated) — jamais elle ne décide.
> 2. **Ne jamais combattre Meta** : ABO toujours à notre échelle (CBO interdit < 8 ad sets — garde codée) ;
>    le bandit agit là où Meta ne voit pas (métriques CRM) et au niveau créatif INTRA ad set ;
>    pauser un ad existant ne reset pas le learning, en AJOUTER un le reset ⇒ on rationne les challengers.
> 3. **L'échelle des récompenses** : proxy fréquent (conversations WhatsApp démarrées / impression —
>    action_type Meta vérifié `onsite_conversion.messaging_conversation_started_7d`) = autonome ;
>    lead qualifié (CRM) = proposition humaine ; signature = information seulement. Jamais d'inversion.
> 4. **Un chiffre qui ne se réconcilie pas déclenche une alerte** — jamais de divergence silencieuse
>    Meta-vs-ERP ; une règle qui ne peut pas tourner ALERTE (jamais de skip silencieux).
> 5. **Budgets** : flex quotidien Meta = 1,25× (vérifié source primaire — le « 2× » est du folklore) ;
>    plafond mensuel inviolable ; pas de changement de budget > 15%/jour — le seuil de reset « ~20% »
>    est un consensus SECONDAIRE non vérifié (et possiblement resserré post-Andromeda) : on ne code
>    jamais une garde sur le bord d'un seuil non vérifié.

## DONE LOG (une ligne datée par tâche livrée)

## COMMENT LANCER
`python scripts/plan_lanes.py docs/plans/PLAN_ADSENG.md --build-order docs/BUILD_ORDER.yml` puis le
run standard (lane-draining, revue, un merge). Lanes parallèles par construction : P0 séquentielle
(toutes les migrations y vivent), puis P1/P2/P3/P4/P5 = cinq lanes backend sur fichiers disjoints
(zéro migration après P0), P7 frontend en parallèle par écran, P6 orchestrateur après P1-P5, P8 prep
multi-plateforme. Environ 6-8 agents utiles en pointe.

### Groupe ADSENG — Moteur publicitaire autonome (bandit + gardien + trésorerie + attribution + plan de vol) — bench : Smartly budget pilot (bandit bayésien nightly, précédent nommé), Revealbot/Bïrch (vocabulaire de règles), Madgicx (pièges à éviter), Meta natif (ce qu'on ne reconstruit pas)

Compose sur ENG1-31 (connexion, client PAUSED, miroirs, EngineAction, coût-par-signature v1, fabrique
créative, boîte d'approbation). Ce groupe ajoute ce qui fait un VRAI concurrent des gros : le moteur de
décision statistique, le gardien de règles, le pacing budgétaire, l'attribution par VARIANTE réconciliée,
le backlog créatif + plan de vol 6 mois, l'orchestrateur autonome avec harnais de simulation, la console.
Frontières : `apps/marketing` possède campagnes email/SMS (rien ici) ; `apps/crm` possède les leads (lecture
selectors) ; QJ9 (CAPI devis accepté, couche documents) reste un émetteur SÉPARÉ de l'émetteur CRM-stage
ajouté ici (règle CLAUDE.md #2 — les deux couches ne fusionnent jamais).

#### P0 — Corrections vivantes + fondations données (lane backend/adsengine-core, séquentielle — TOUTES les migrations du groupe vivent ici)

- [ ] ADSENG1 — **FIX attribution Lead Ads : le webhook lit des champs que Meta n'envoie jamais.** Vérifié (dd-attribution) : `apps/crm/webhooks.py` lit `campaign_name`/`adset_name` du payload leadgen — Meta n'envoie QUE `ad_id`/`adgroup_id`/`form_id`/`leadgen_id` ⇒ `utm_campaign`/`utm_content` = None en prod aujourd'hui, l'attribution par variante est morte à la source. Capturer `ad_id`+`adgroup_id`+`form_id` sur le lead (champs additifs si besoin), résoudre les NOMS via les miroirs ENG5 (fallback : lookup API paresseux), remplir `utm_content` = ad_id-encodé selon la convention ADSENG23. Commande de backfill idempotente pour les leads meta_lead_ads existants. **Done =** un lead de test du Testing Tool porte ad_id + noms résolus + utm_* remplis ; backfill testé ; rien d'autre du fichier touché. Files: `apps/crm/webhooks.py` (exception contractuelle, mapping seul), `apps/adsengine/` (résolution), migration additive crm si champ requis, tests. (SCHEMA/AUTH) (@lane: backend/adsengine-core) (@model: opus)
- [ ] ADSENG2 — **FIX CAPI v19.0 morte + event_id.** Vérifié (dd-attribution) : l'émetteur QJ9 (`apps/ventes/services.py`) est codé en dur sur Marketing API v19.0 (expirée 02/2025 — 400 garanti dès qu'un pixel sera configuré), sans `event_id` (double-comptage futur) et sans `ip`/`user_agent` pourtant disponibles (EMQ). Passer la version en constante partagée (v25+), ajouter `event_id` déterministe (dedup), threader ip/user_agent. Ne PAS changer le déclencheur (Devis.accepte — couche documents, règle #2). **Done =** test-events verts sur la version courante, event_id présent, EMQ fields threadés, aucun autre comportement modifié. Files: `apps/ventes/services.py` (exception contractuelle, émetteur seul), tests. (AUTH) (@lane: backend/adsengine-core) (@model: opus)
- [ ] ADSENG3 — **Modèles expérimentation** : `Experiment` (company FK, campagne/ad set cible, variable testée, statut, dates), `ExperimentArm` (creative_asset FK, ad_id miroir, hook_id/visual_id lignée), `ArmDailyStat` (arm FK, date, impressions, clicks, conversations, spend — alimenté par la sync ENG6 étendue), `DecisionLog` (inputs snapshot JSON, posteriors, action produite, EngineAction FK nullable). Migrations additives, company scoping partout. **Done =** CRUD company-scoped + upsert quotidien idempotent testés. Files: `apps/adsengine/models.py` (+migration), serializers/views, tests. (SCHEMA) (@lane: backend/adsengine-core) (@model: sonnet)
- [ ] ADSENG4 — **Modèles gardien + trésorerie** : `RulePolicy` (template_key du catalogue FR, conditions JSON AND/OR, cadence, dry_run défaut True, cooldown par entité), `AnomalyEvent`, extension `EngineAlert` (modèle créé par ENG13 — membre du noyau BUILD_ORDER ; sévérité 🔴🟠🔵, cooldown, escalade WARNING→CRITICAL après 3 cycles non résolus), `PacingState` (enveloppe mensuelle, spend-to-date, projection, état 5-valeurs), champs additifs `GuardrailConfig` : `monthly_budget_ceiling_mad`, `pacing_band_pct`, `exploration_floor_mad` (défaut 20), `exploration_floor_pct` (défaut 20). **Done =** migrations additives + endpoints + défauts seedés. Files: `apps/adsengine/models.py` (+migration), tests. (SCHEMA) (@lane: backend/adsengine-core) (@model: sonnet)
- [ ] ADSENG5 — **Modèles créa + vol** : champs composants sur `CreativeAsset` (modèle créé par ENG15 — membre du noyau BUILD_ORDER ; hook_id, hook_text, primary_text, visual_asset_key, cta — décomposition dd-creative-sci), `CreativeGenerationBatch` (approbation par LOT, jamais par variante), `CreativeBacklogItem` (asset FK, campagne cible, date-au-plus-tôt, tag saisonnier, statut file), `FlightPlan` + `FlightPhase` (variable testée, template de lancement, budget, dates, 2-3 bras, 3-4 semaines — le plan 6 mois comme DONNÉES), `ReconciliationSnapshot` (jour, campagne, chiffres Meta vs chiffres ERP, écart, statut). **Done =** migrations + CRUD + validations de base. Files: `apps/adsengine/models.py` (+migration), tests. (SCHEMA) (@lane: backend/adsengine-core) (@model: sonnet)
- [ ] ADSENG6 — **Jointure d'attribution par VARIANTE** (le delta ENG10→ADSENG, dd-attribution §e) : service reliant ad (utm_content/ad_id) → `crm.Lead` (via selectors, jamais models) → devis → signature ; index nécessaires ; sélecteur fin ajouté côté crm si besoin (lecture seule). Cas gérés : lead organique (pas d'utm), utm manquant, lead Instant Form (ad_id direct d'ADSENG1), lead appel/DM saisi à la main (canal seul). Expose coût-par-lead-qualifié et coût-par-signature PAR BRAS pour P1. **Done =** fixtures multi-cas, chiffres par variante exacts, ENG10 (niveau campagne) inchangé. Files: `apps/adsengine/attribution.py`, `apps/crm/selectors.py` (ajout fin), tests. (ARCH) (@lane: backend/adsengine-core) (@model: opus)
- [ ] ADSENG7 — **Générateur de compte synthétique** (le carburant du harnais P6) : commande créant N mois de `ArmDailyStat`/miroirs/leads CRM synthétiques avec vérités terrain paramétrées (bras gagnant connu, bruit, dérive mi-vol, effondrement de delivery) — scénarios dorés nommés, seeds déterministes. **Done =** 4 scénarios reproductibles documentés. Files: `apps/adsengine/management/commands/seed_synthetic_account.py`, tests. (DC) (@lane: backend/adsengine-core) (@model: sonnet)

#### P1 — Le moteur de décision (lane backend/adsengine-stats — fichiers disjoints, après P0)

- [ ] ADSENG8 — **`bandit.py` : Thompson beta-binomiale** (spec dd-science-core §b, ~40 lignes numpy + tests exhaustifs) : prior Beta(1,1), posterior α=1+conversions/β=1+(impressions−conversions) par bras depuis `ArmDailyStat`, allocation = P(meilleur) par 10 000 tirages Monte-Carlo (méthode GrowthBook vérifiée — RÉFÉRENCE, on ne vend pas gbstats). Fonctions pures, zéro I/O. **Done =** cas dorés (2/3/4 bras, égalité, gagnant net), propriétés (allocations somment à 1, monotonie), déterminisme sous seed. Files: `apps/adsengine/bandit.py`, tests. (ARCH) (@lane: backend/adsengine-stats) (@model: opus)
- [ ] ADSENG9 — **Échelle des récompenses + détecteur de divergence** (dd-science-core §c) : proxy autonome = conversations démarrées/impression (`onsite_conversion.messaging_conversation_started_7d`, vérifié) ; hebdo, jointure vérité CRM par bras (ADSENG6) ; si le classement CRM diverge du classement proxy de PLUS d'une position (un écart d'une seule position = bruit, dd-science-core §2.7) avec ≥10 leads qualifiés cumulés ⇒ le moteur N'AGIT PAS : il lève une `EngineAction` propose-only avec les deux classements et la raison FR. **Done =** divergence synthétique déclenche la proposition, jamais d'action auto ; convergence = rien. Files: `apps/adsengine/rewards.py`, tests. (ARCH) (@lane: backend/adsengine-stats) (@model: opus)
- [ ] ADSENG10 — **Politique d'allocation + règles kill/promote** (dd-science-core §b) : plancher d'exploration ABSOLU max(20% budget, 20 MAD/j) par bras vivant (jamais le 1% GrowthBook — famine sous le minimum de delivery Meta) ; aucun kill autonome avant 7 jours ET 40 conversions/bras, puis P(meilleur)<5% trois jours consécutifs ; repondération QUOTIDIENNE (via ADSENG21, pas ±15%/coup), kill/promotion HEBDOMADAIRE (rythme Monday brief). Fonctions pures + property tests. **Done =** chaque règle testée isolément + scénarios composés. Files: `apps/adsengine/allocation.py`, tests. (ARCH) (@lane: backend/adsengine-stats) (@model: opus)
- [ ] ADSENG11 — **Table d'autorité par barreau comme DONNÉES** (dd-science-core §d) : config (CTR/proxy → autonome ; lead qualifié → propose ; signature → informe) chargée par les services, jamais codée dans la logique ; toute action issue d'un barreau non-autonome passe EngineAction propose. **Done =** tests par barreau, config modifiable sans code. Files: `apps/adsengine/authority.py`, tests. (ARCH) (@lane: backend/adsengine-stats) (@model: sonnet)
- [ ] ADSENG12 — **DecisionLog systématique** : chaque sortie bandit/allocation écrit inputs (snapshot stats), posteriors, décision, lien EngineAction — l'audit de « pourquoi le moteur a fait X ». **Done =** aucune décision sans ligne de log (test d'invariant). Files: `apps/adsengine/` (hook dans stats services), tests. (ROUTINE) (@lane: backend/adsengine-stats) (@model: sonnet)
- [ ] ADSENG13 — **Service MDE/puissance** (l'arithmétique dd-science-core §a en code) : volumes réels → effet détectable par barreau à 7/14/28 j + durée estimée pour un effet cible ; consommé par la validation de plan de vol (ADSENG28) et affiché console (P7). Table de référence du dossier reproduite en test doré. **Done =** chiffres du dossier reproduits exactement. Files: `apps/adsengine/mde.py`, tests. (ARCH) (@lane: backend/adsengine-stats) (@model: sonnet)

#### P2 — Le Gardien (lane backend/adsengine-guardian — fichiers disjoints, après P0)

- [ ] ADSENG14 — **Catalogue de règles FR + CRUD RulePolicy** (dd-guardian §a) : `rule_templates.py` = catalogue FIXE style STAGES.py (stop-loss CPL, revive, fatigue fréquence, zéro-delivery, dépassement pacing, bande CPL, backlog bas, divergence réconciliation) — pas de builder libre (leçon Revealbot « expertise non fournie ») ; conditions AND/OR JSON, fenêtres temporelles, `dry_run` défaut True structurellement bloquant. **Done =** catalogue seedé, CRUD scopé, dry-run n'exécute jamais. Files: `apps/adsengine/rule_templates.py`, views, tests. (ARCH) (@lane: backend/adsengine-guardian) (@model: sonnet)
- [ ] ADSENG15 — **Évaluateur de règles Celery** : boucle 6 h garde-fous critiques + crochets quotidien/hebdo (jamais sub-horaire — rate limits scalés au spend pénalisent les petits comptes, dd-guardian) ; cooldown par (company, template, cible) ; exécution ⇒ EngineAction (auto si capability toggle ENG8, sinon propose) ; JAMAIS d'action directe sans EngineAction. **Done =** cadences enregistrées, cooldowns testés, chemin auto vs propose testé. Files: `apps/adsengine/tasks.py`, `rules_engine.py`, `erp_agentique/celery.py`, tests. (ARCH) (@lane: backend/adsengine-guardian) (@model: opus)
- [ ] ADSENG16 — **5 détecteurs d'anomalies SMB-relatifs** (formules dd-guardian §b) : spend vs médiane 7 j (bande 3,0×/0,2× + plancher), CPL vs bande trainante ±2× (ratio simple, PAS d'écart-type — 5-15 leads/sem trop épars), zéro-delivery 2 niveaux (0 impressions / 0 résultats), fréquence runaway (plafond 3,0 + pente), ad désapprouvé. Chaque détecteur a sa branche `insufficient_data` qui ALERTE toujours (jamais de skip silencieux — LE piège Madgicx). **Done =** chaque formule + branche insufficient_data testées. Files: `apps/adsengine/anomaly.py`, tests. (ARCH) (@lane: backend/adsengine-guardian) (@model: sonnet)
- [ ] ADSENG17 — **Watchdog de l'évaluateur** : heartbeat de la boucle règles ; « une règle n'a pas pu tourner » = alerte 🔴 dédiée ; santé exposée à l'endpoint ENG12. **Done =** évaluateur tué en test ⇒ alerte watchdog. Files: `apps/adsengine/watchdog.py`, tests. (ROUTINE) (@lane: backend/adsengine-guardian) (@model: sonnet)
- [ ] ADSENG18 — **8 gabarits d'alerte WhatsApp FR** (dd-guardian §c) : cause racine → recommandation une ligne → deep link ERP ; 3 sévérités, dedup (company, template, cible), cooldowns 6/24/72 h, escalade après 3 cycles. **Done =** rendus testés, dedup/cooldown/escalade testés. Files: `apps/adsengine/alerts.py`, tests. (ROUTINE) (@lane: backend/adsengine-guardian) (@model: haiku)
- [ ] ADSENG19 — **[GATED: décision fondateur] Règle Meta native homme-mort** : UNE règle `adrules_library` (API brute — le CLI ne l'expose pas, vérifié) pausant tout au-delà d'un plafond de spend catastrophe, indépendante de notre infra. OFF par défaut, spec + commande d'installation documentée seulement. **Done =** commande + doc, rien d'activé. Files: `apps/adsengine/deadman.py`, docs. (DECISION) (@lane: backend/adsengine-guardian) (@model: sonnet)

#### P3 — Trésorerie & pacing (lane backend/adsengine-treasury — fichiers disjoints, après P0)

- [ ] ADSENG20 — **Moteur de pacing** (dd-treasury §a) : enveloppe mensuelle, projection linéaire + saisonnalité jour-de-semaine, bandes sur/sous-pacing, table d'états 5 valeurs, invariant « plafond jamais dépassé » calibré sur le flex quotidien Meta réel **1,25×** (vérifié source primaire — pas 2×). **Done =** formules testées, fin-de-mois projetée juste sur fixtures, invariant prouvé au flex max. Files: `apps/adsengine/pacing.py`, tests. (ARCH) (@lane: backend/adsengine-treasury) (@model: sonnet)
- [ ] ADSENG21 — **Applicateur de budgets** : sorties du bandit → budgets ad set ABO via meta_client, par pas ≤15%/jour (le seuil de reset ~20% est NON vérifié — marge de sécurité délibérée), dans les bandes GuardrailConfig ; **garde codée : CBO interdit si < 8 ad sets OU < 2 semaines de spend consistant** (plancher Madgicx complet, dd-treasury §B2) — `enable_cbo` n'existe qu'en EngineAction propose-only. **Done =** pas >15% impossible (test), plafonds inviolables, CBO refusé sous l'un OU l'autre plancher. Files: `apps/adsengine/budget_applier.py`, tests. (ARCH/AUTH) (@lane: backend/adsengine-treasury) (@model: opus)
- [ ] ADSENG22 — **Nouveaux types EngineAction trésorerie** : `pause_for_month`, `increase_pace`, `rebalance_adset_budget`, `enable_cbo` (propose-only) — payloads, raisons FR, application via ADSENG21. **Done =** cycle propose→approve→apply testé par type. Files: `apps/adsengine/services.py`, tests. (ROUTINE) (@lane: backend/adsengine-treasury) (@model: sonnet)
- [ ] ADSENG23 — **`generate_launch_identity()`** (dd-treasury §c) : UNE fonction = nommage (campagne/adset/ad/créatif encodant date-objectif-audience-variante, parsable) + UTM builder (vocabulaire docs/utm-governance.md étendu si besoin, même commit) ; utilisée par tout lancement moteur ; le parseur inverse sert l'attribution. **Done =** aller-retour encode/parse testé, gouvernance UTM respectée. Files: `apps/adsengine/identity.py`, `docs/utm-governance.md` si extension, tests. (ROUTINE) (@lane: backend/adsengine-treasury) (@model: haiku)
- [ ] ADSENG24 — **3 gabarits de lancement comme DONNÉES** (dd-treasury §c) : Résidentiel CTWA / Pompage agricole saisonnier / B2B Lead Form — objectif, structure, budgets défaut sous plafond, placements, questions qualifiantes, exigences de slots créatifs (validées contre la bibliothèque ENG15/27). **Done =** gabarits seedés, validation slots testée, un lancement à blanc (dry-run) produit la structure attendue PAUSED. Files: `apps/adsengine/launch_templates.py`, seeds, tests. (ROUTINE) (@lane: backend/adsengine-treasury) (@model: sonnet)

#### P4 — Science créative & backlog (lane backend/adsengine-creative — fichiers disjoints, après P0)

- [ ] ADSENG25 — **Protocole de rotation** (dd-creative-sci §a + contraintes dd-meta-mechanics) : 1 champion + 2 challengers par ad set ; évaluation au rythme Monday ; entrée depuis le backlog ; sortie selon P1 ; **ration stricte : max 1 nouvel ad par rotation** (ajouter = reset learning, pauser = gratuit — consensus SECONDAIRE non vérifié source primaire, LE fait le plus porteur du design : ADSENG37 le tranche au terrain avant tout vol autonome) ; lignée encodée via ADSENG23. **Done =** cycle complet simulé sur fixtures, jamais 2 ajouts simultanés (test). Files: `apps/adsengine/rotation.py`, tests. (ARCH) (@lane: backend/adsengine-creative) (@model: opus)
- [ ] ADSENG26 — **Recombinaison déterministe** (dd-creative-sci §b, APIs vérifiées) : cross hook×visuel via les adaptateurs ENG17 : Templated `layers` (statiques) et réutilisation `transcriptTaskId` ZapCap (variantes vidéo) — substitution de gabarits, JAMAIS de génération ; lot ≤2/run ; `CreativeGenerationBatch` approuvé par LOT humain puis → backlog (policy stamp hérité + revalidé). **Done =** une recombinaison mockée bout-en-bout, approbation par lot obligatoire (test), politique no-fake-footage structurellement intacte. Files: `apps/adsengine/recombine.py`, tests. (ARCH) (@lane: backend/adsengine-creative) (@model: sonnet)
- [ ] ADSENG27 — **Gestion du backlog** : calcul de runway (semaines restantes au rythme du plan), alerte « backlog bas » < 3 semaines, plancher de diversité ≥4 hooks distincts/3 mois (un backlog d'un seul hook recombiné = fausse diversité), file par campagne cible + date-au-plus-tôt + tag saisonnier. **Done =** runway/diversité/alerte testés. Files: `apps/adsengine/backlog.py`, tests. (ROUTINE) (@lane: backend/adsengine-creative) (@model: sonnet)
- [ ] ADSENG28 — **Plan de vol + préflight** (dd-science-core §d + dd-creative-sci §c) : `FlightPlan` 6 mois = phases séquentielles UNE variable à la fois (hook→format→angle→audience→consolidation), 2-3 bras, 3-4 semaines/phase (le 3-2-2 à 12 cellules est mathématiquement inutilisable à notre volume — MDE ±90%) ; validation préflight : volume backlog (≥12 approuvés/3 mois) ET diversité (≥4 hooks) ET garde-fous configurés ET alertes câblées ET sanité MDE (ADSENG13) par phase. **Done =** un plan invalide est refusé avec raisons FR ; un plan valide se matérialise en phases planifiées. Files: `apps/adsengine/flightplan.py`, tests. (ARCH) (@lane: backend/adsengine-creative) (@model: opus)
- [ ] ADSENG29 — **Arbitrage DCO** (dd-creative-sci §e, caps vérifiés 10 img/vid, 5 textes, 30 assets, UN ad par ad set) : mode bootstrap DCO pour démarrage sans données SEULEMENT ; exclusion mutuelle DCO ↔ rotation multi-ads par ad set (contrainte structurelle vérifiée) ; question ouverte consignée : granularité d'attribution par asset des breakdowns DCO (à trancher par le test terrain ADSENG37). **Done =** l'exclusion mutuelle est une contrainte de modèle testée ; le choix bootstrap/rotation est explicite par ad set. Files: `apps/adsengine/dco.py`, tests. (ROUTINE) (@lane: backend/adsengine-creative) (@model: sonnet)
- [ ] ADSENG30 — **Ads « style post » via object_story_spec** (dd-creative-sci §d, vérifié : dans le scope ads_management existant, PAS de nouvelle App Review) : créatifs page-post-shaped pour les tests type boosted-post ; la publication ORGANIQUE (`pages_manage_posts`) reste GATED (App Review) — hors périmètre. **Done =** un ad object_story_spec créé PAUSED en test mocké. Files: `apps/adsengine/meta_client.py` (extension), tests. (ROUTINE) (@lane: backend/adsengine-creative) (@model: sonnet)

#### P5 — Colonne d'attribution & réconciliation (lane backend/adsengine-recon — fichiers disjoints, après P0)

- [ ] ADSENG31 — **Réconciliation Meta-vs-ERP** (dd-attribution §b) : snapshot quotidien par campagne — leads Meta-comptés vs ERP-comptés (deux dénominateurs explicites : form leads, site leads, saisis-main), règles de dedup réutilisées (téléphone/email normalisés QW10), règle de tolérance, écart au-delà ⇒ alerte 🟠 « divergence silencieuse » (leçon ux-adtools). **Done =** divergences synthétiques détectées, affichage contract JSON stable pour P7. Files: `apps/adsengine/reconciliation.py`, tests. (ARCH) (@lane: backend/adsengine-recon) (@model: opus)
- [ ] ADSENG32 — **Émetteur CAPI CRM-stage (SÉPARÉ de QJ9)** (dd-attribution §c) : événement lead-qualifié sur transition STAGES.py (clé depuis STAGES.py, jamais en dur), event_name libre, event_id déterministe, match via leadgen_id (= `Lead.external_id`) + téléphone haché ; moniteur EMQ ; borne RAPPORTÉE (doc vendeur, NON vérifiée source Meta) : CTWA CAPI = 1 événement/clic/7 j — documentée comme limite structurelle probable (cycle de vente solaire > 7 j), l'événement signature passe par le pixel/dataset standard. **Done =** émission sur transition testée, dedup event_id, QJ9 intact (test de non-régression), EMQ visible. Files: `apps/adsengine/capi_crm.py`, tests. (ARCH/AUTH) (@lane: backend/adsengine-recon) (@model: opus)
- [ ] ADSENG33 — **Endpoints drill-down** (dd-attribution §d) : table par variante (spend/conv/CPL-qualifié/coût-signature + lien leads), entonnoir par campagne, vue cohortes (leads par semaine → lag de signature), export CSV. **Done =** chaque endpoint testé sur fixtures multi-semaines. Files: `apps/adsengine/reporting.py`, tests. (ROUTINE) (@lane: backend/adsengine-recon) (@model: sonnet)
- [ ] ADSENG34 — **[GATED: décision fondateur — coût/architecture] Boucle ctwa_clid complète.** Vérifié : zéro chemin ctwa_clid possible avec wa.me (il faut WhatsApp Cloud API/BSP — le Business App n'a aucun webhook). Spec seulement : ce que la boucle apporterait (attribution clic→conversation exacte), coûts BSP connus (dossiers antérieurs), le déclencheur de décision (≥15-25 leads/sem ou 2e répondeur). **Done =** spec docs/engine/ctwa-decision.md, rien de construit. Files: docs. (DECISION) (@lane: backend/adsengine-recon) (@model: sonnet)

#### P6 — L'orchestrateur autonome (lane backend/adsengine-runner — après P1-P5)

- [ ] ADSENG35 — **FlightRunner — la machine à états** : exécute un FlightPlan validé : matérialise la phase (gabarit ADSENG24 → structures PAUSED → unpause HUMAIN), boucle quotidienne (sync → bandit → repondération ADSENG21 → gardien), boucle hebdo (rotation ADSENG25, kill/promote, brief), transitions de phase selon règles, fin de plan = rapport ; **kill-switch global** : un flag pause tout ce que le moteur a créé (via client, donc PAUSED-safe) ; chaque transition ⇒ DecisionLog + actions-log. **Done =** machine à états testée état par état, kill-switch prouvé, jamais d'unpause programmatique (invariant re-testé ici). Files: `apps/adsengine/flightrunner.py`, tests. (ARCH/AUTH) (@lane: backend/adsengine-runner) (@model: opus)
- [ ] ADSENG36 — **Harnais de simulation** (l'artefact de confiance) : rejoue FlightRunner sur les comptes synthétiques ADSENG7 en accéléré (des mois en secondes, horloge injectée) ; 4 scénarios dorés assertés : gagnant net (le moteur converge), aucun signal (le moteur NE conclut PAS), dérive mi-vol (détectée, proposition levée), effondrement delivery (gardien pause + alerte). Rapport de simulation lisible (P7 le visualise). **Done =** les 4 scénarios passent en CI, déterministes sous seed. Files: `apps/adsengine/simulator.py`, tests. (ARCH) (@lane: backend/adsengine-runner) (@model: opus)
- [ ] ADSENG37 — **Protocole de tests terrain** (dd-meta-mechanics §j) : les 7 inconnues que seul le compte réel tranche (seuils exacts de reset, viabilité split-test à petit budget, défauts des enhancements Advantage+, granularité DCO, coûts BUC réels, rotation intra-ad-set, gating access-tier) — runbook de micro-tests SÛRS (budgets plafonnés, PAUSED d'abord, un facteur à la fois), résultats consignés dans docs/engine/field-tests.md et réinjectés en constantes de config. **Done =** runbook committé, constantes câblées sur config (pas en dur), premier test documenté à blanc. Files: `apps/adsengine/` (constantes config), docs/engine/field-tests.md. (ROUTINE) (@lane: backend/adsengine-runner) (@model: sonnet)
- [ ] ADSENG38 — **Préflight d'autonomie + go-live** : endpoint agrégeant TOUTES les portes (loop signal vert ENG12, garde-fous posés, alertes câblées, backlog volume+diversité, plan validé, simulation verte, tests terrain faits) → tant que tout n'est pas vert, le mode autonome ne peut PAS s'activer (structurellement) ; la liste FR de ce qui manque. **Done =** activation refusée avec raisons tant qu'une porte manque ; verte = activable (mais OFF par défaut). Files: `apps/adsengine/preflight.py`, tests. (ARCH/AUTH) (@lane: backend/adsengine-runner) (@model: sonnet)
- [ ] ADSENG47 — **Permissions fines** (numéro conservé, tâche runner) : `adsengine_flightplan_manage`, `adsengine_autonomy_toggle` (distinctes d'approve ENG19 — activer l'autonomie est un pouvoir séparé), mapping init_roles (admin seul pour autonomy_toggle). **Done =** gating testé rôle par rôle. Files: `apps/roles/models.py` (exception contractuelle n°3 — ajout APPEND-ONLY de 2 constantes uniquement), init_roles, tests. (AUTH) (@lane: backend/adsengine-runner) (@model: haiku)

#### P7 — Console (lane frontend/adsengine — parallèle par écran, après les endpoints respectifs)

- [ ] ADSENG39 — **Écran Expérimentations** : timeline des phases, bras avec posteriors visualisés (bandes de crédibilité, P(meilleur)), DecisionLog filtrable — « pourquoi le moteur a fait X » lisible par un non-statisticien (phrases FR + chiffres). (@after: ADSENG12) **Done =** RTL tests, chiffres = API. Files: `frontend/src/features/adsengine/ExperimentsScreen.jsx`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet)
- [ ] ADSENG40 — **Éditeur de plan de vol + panneau préflight** (l'écran-amiral P7) : composer les phases 6 mois (gabarits, variables, bras depuis le backlog), le préflight ADSENG38 en checklist verte/rouge FR, simulation lançable depuis l'écran. (@after: ADSENG28, ADSENG38) **Done =** un plan se compose, se valide, se refuse avec raisons, RTL. Files: `features/adsengine/FlightPlanScreen.jsx`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: opus)
- [ ] ADSENG41 — **Gestionnaire de backlog** : file par campagne, barre de runway, jauge diversité hooks, approbation par LOT des recombinaisons (CreativeGenerationBatch), drop d'assets. (@after: ADSENG27) **Done =** approbation par lot bout-en-bout RTL. Files: `features/adsengine/BacklogScreen.jsx`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet)
- [ ] ADSENG42 — **Onglet Pacing + vue Réconciliation** sur le dashboard ENG23 (pas de nouvel écran — dd-treasury §e) : enveloppe, burn, projection, états ; réconciliation Meta-vs-ERP avec écart et statut. (@after: ADSENG20, ADSENG31) **Done =** RTL, chiffres cliquables vers le détail. Files: `features/adsengine/DashboardScreen.jsx` (extension), tests. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet)
- [ ] ADSENG43 — **Écran Règles & anomalies** : catalogue de gabarits FR (picker, jamais de builder libre), résultats dry-run, flux d'anomalies avec sévérités, historique d'alertes. (@after: ADSENG14, ADSENG16) **Done =** dry-run visualisé, RTL. Files: `features/adsengine/RulesScreen.jsx`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet)
- [ ] ADSENG44 — **Visionneuse de simulation** : rejouer un run ADSENG36 visuellement (allocations dans le temps, décisions annotées, verdict par scénario) — l'outil de confiance fondateur avant tout dirham réel. (@after: ADSENG36) **Done =** un rapport de sim se charge et se lit, RTL. Files: `features/adsengine/SimulationScreen.jsx`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet)
- [ ] ADSENG45 — **Drill-downs reporting** : table variantes, entonnoir campagne, cohortes/lag, export — consomme ADSENG33. (@after: ADSENG33) **Done =** RTL + export testé. Files: `features/adsengine/ReportsScreen.jsx`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: sonnet)
- [ ] ADSENG46 — **Hooks e2e `ae-*` + a11y** sur les nouveaux écrans (contrat DOM comme ap-/att-/pp-*), specs axe sans violation sur FlightPlan + Experiments. **Done =** hooks documentés, axe vert. Files: `features/adsengine/*`, `frontend/src/test/`, tests. (ROUTINE) (@lane: frontend/adsengine) (@model: haiku)
#### P8 — Multi-plateforme (prep — la promesse « toutes les plateformes » sans en construire aucune à vide)

- [ ] ADSENG48 — **Extraction de l'interface `AdsPlatform`** : le contrat abstrait (auth, inventaire, création PAUSED, budgets, insights normalisés, capacités) extrait de meta_client SANS changement de comportement (tests de contrat golden sur le client Meta existant) ; le FlightRunner/bandit ne parlent plus qu'à l'interface. **Done =** Meta passe les tests de contrat, zéro diff de comportement (snapshots). Files: `apps/adsengine/platforms/{base.py,meta.py}`, tests. (ARCH) (@lane: backend/adsengine-runner) (@model: opus)
- [ ] ADSENG49 — **Matrice de capacités plateforme comme DONNÉES** : paused-par-défaut (Meta oui, TikTok NON confirmé, Google n/a-read-only-MCP…), budgets minimum (TikTok $50/j campagne), granularité insights — la matrice pilote les gardes (une plateforme sans paused-défaut ⇒ le client la force). **Done =** matrice seedée, gardes pilotées testées. Files: `apps/adsengine/platforms/capabilities.py`, tests. (ROUTINE) (@lane: backend/adsengine-runner) (@model: haiku)
- [ ] ADSENG50 — **[GATED: budget fondateur] Adaptateur Google Ads** (Search-only, dev-token + OAuth, mutations via API standard — le MCP Google est read-only vérifié) : stub + tests de contrat, activation à la décision budget (+50-100 MAD/j additifs). **Done =** stub committé + tests de contrat verts, rien d'activé ni de dépense possible. Files: `apps/adsengine/platforms/google.py`. (DEP/DECISION) (@lane: backend/adsengine-runner) (@model: sonnet)
- [ ] ADSENG51 — **[GATED: produit] Adaptateur Snapchat** (API ouverte depuis 2018 — la plus simple ; billing Maroc à vérifier à la main d'abord) : stub + tests de contrat. **Done =** stub committé + tests de contrat verts, rien d'activé. Files: `apps/adsengine/platforms/snapchat.py`. (DEP/DECISION) (@lane: backend/adsengine-runner) (@model: sonnet)
- [ ] ADSENG52 — **[GATED: budget fondateur ≥450 MAD/j] Adaptateur TikTok** (minimum $50/j campagne vérifié ; AUCUN paused-par-défaut confirmé ⇒ la garde ADSENG49 force PAUSED côté client avant toute création) : stub + tests de contrat. **Done =** stub committé + tests de contrat verts (dont le test « création sans PAUSED forcé impossible »), rien d'activé. Files: `apps/adsengine/platforms/tiktok.py`. (DEP/DECISION) (@lane: backend/adsengine-runner) (@model: sonnet)
- [ ] ADSENG53 — **Runbook opérateur du moteur** : docs/engine/operator-runbook.md — démarrer un plan de vol, lire les briefs, répondre aux 8 alertes, le kill-switch, les 7 tests terrain, la checklist préflight — écrit pour Meryem/Reda, FR, zéro jargon stats. **Done =** relu contre les écrans réels P7. Files: docs/engine/operator-runbook.md. (ROUTINE) (@lane: backend/adsengine-runner) (@model: sonnet)

> **GATED ADSENG (ne pas auto-construire)** : ADSENG19 (règle Meta homme-mort — décision fondateur),
> ADSENG34 (boucle ctwa_clid — exige WhatsApp Cloud API/BSP, coût), ADSENG50/51/52 (plateformes —
> budget/produit), publication organique `pages_manage_posts` (App Review — hors périmètre),
> commentaire LLM des briefs (clé + revue anti-hallucination), export d'audiences Meta (XMKT36).
