# PLAN_CRM_VENTES — file de travail parallèle (split de new_tasks_plan.md, 2026-07-10)

> **CONTRAT DE PROPRIÉTÉ (non négociable — c'est lui qui garantit zéro conflit).**
> Une session `work on the plan crm_ventes` ne touche QUE :
> **backend :** apps/crm, apps/ventes (HORS quote_engine — RULE #4 reste opus+review), apps/marketing, nouvelles apps cpq/territoires/contacts.
> **frontend :** frontend/src/pages|features de crm, ventes, marketing.
> Tout le reste est INTERDIT en écriture — une autre session peut le posséder.
> Lire une app étrangère = via son `selectors.py`/string-FK uniquement (jamais
> ses models/migrations). Une tâche qui EXIGE d'écrire hors périmètre →
> `[BLOCKED: hors périmètre CRM_VENTES]` + continuer (elle reviendra au run
> plateforme). Fichiers frontend PARTAGÉS (router/nav/api/ui) : ajouts
> APPEND-ONLY minimaux ; un conflit à l'update-branch se résout en prenant les
> DEUX ajouts. Base de test locale : `DB_NAME=erp_crm_ventes` (jamais la
> DB partagée). Chaque session merge sa propre branche `dev-crm_ventes`
> vers main indépendamment (update-branch → CI ~6 min → auto-merge) ; si
> `docs/CODEMAP.md` (fingerprint structure) conflicte à l'update, prendre
> l'arbre mergé et relancer `python scripts/codemap_fingerprint.py --write`.
> Ce fichier n'est PAS dans la surface du plan-fingerprint (comme WEB_PLAN.md) :
> tick + DONE LOG ici, jamais dans docs/CODEMAP.md §10.
> JAMAIS ré-implémenter localement une primitive plateforme manquante (chatter,
> numérotation, jobs, files, registre…) : si une tâche dépend d'une primitive
> ARC/NT pas encore sur main → `[BLOCKED: attend <ID>]` + continuer. C'est le
> vecteur n°1 de dette constaté (13 chatters artisanaux) — bloquer vaut mieux.
> Groupes : NTCPQ, NTCRM, NTDMO, NTMKT. Règles WOW/CLAUDE.md inchangées
> (lane-draining, revue adversariale, retro, routage modèle via plan_lanes.py).

## DONE LOG (une ligne datée par tâche livrée)

### Groupe NTCPQ — CPQ & moteur de prix enterprise — bench: Salesforce CPQ, DealHub, Tacton (audit 2026-07-07)
Il existe déjà le générateur solaire (3 modes marché, TTC, auto-fill) et `Produit.prix_achat` (marge interne). PLAN.md porte déjà `XSAL1-3` (listes de prix client/paliers de quantité/branchement `prix-applicable`) et `FG209-216` (promotions, bibliothèque de modèles, configurateur guidé, comparateur de versions, routage d'approbation non-standard, e-catalogue, bibliothèque de propositions, simulateur public) — NE PAS refaire ces tâches ; ce groupe les complète en profondeur : moteur de règles de compatibilité produit, bundles à prix cascadé, garde-fous de marge (jamais côté client), matrice d'approbation par profondeur de remise (calquée sur `apps/contrats.RegleApprobation`/`EtapeApprobation`), renouvellements/avenants de devis, prix indexés matières, comparateur de variantes de devis, CGV par type de deal. Frontière stricte : aucun rendu PDF (le PDF reste `/proposal` + `quote_engine`) — ce groupe ne touche que modèles/règles/API/écrans de configuration en amont du PDF. Voisins : NTSUB (récurrent/abonnements), NTCRM (pipeline lead).

#### P1 — Bloqueurs de vendabilité (fondations CPQ)

- [ ] NTCPQ1 — **Modèle `OptionProduit` + `ContrainteCompatibilite`** : nouvelle app `apps/cpq` ; `OptionProduit` (produit FK, groupe_option, obligatoire bool) et `ContrainteCompatibilite` (produit_a FK, produit_b FK, type `INCOMPATIBLE`/`REQUIERT`/`RECOMMANDE`, company FK, message_utilisateur). Endpoint `POST cpq/valider-compatibilite/` prend une liste de `produit_id` et renvoie les violations (bloquantes vs avertissement). Critère d'acceptation : sélectionner deux produits marqués `INCOMPATIBLE` renvoie une violation bloquante avec le message configuré. Files: `apps/cpq/models.py` (+migration), `apps/cpq/views.py`, `apps/cpq/urls.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ2 — **Moteur de règles produit réutilisant `core/rules.py`** : dans `apps/cpq`, `RegleProduitCPQ` (company FK, nom, condition_group JSON évalué par `core.rules.evaluate_condition_group`, actions JSON — ex. "si kWc > 9 alors exiger option triphasé"). Endpoint `cpq/regles/` CRUD + `cpq/regles/evaluer/` (payload = lignes candidates du devis, retourne actions déclenchées). Réutilise le moteur générique existant, n'en crée pas un nouveau. Critère d'acceptation : une règle "kWc>9 → triphasé requis" déclenche une action visible dans la réponse pour un devis à 12 kWc. Files: `apps/cpq/models.py` (+migration), `apps/cpq/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ3 — **Bundle à prix cascadé `OffreGroupee`** : modèle `OffreGroupee` (company FK, nom, lignes `LigneOffreGroupee` avec produit FK, quantite, `mode_prix` `FIXE`/`REMISE_PCT`/`PRIX_COMPOSANT`) ; endpoint `cpq/offres-groupees/{id}/appliquer/?devis_id=` qui insère les `LigneDevis` correspondantes en respectant le mode de prix (le total du bundle prime, réparti au prorata du prix catalogue sur les lignes si `FIXE`). Ne touche jamais au PDF. Critère d'acceptation : appliquer un bundle "Kit résidentiel 3kW" à un devis crée les lignes attendues et le sous-total HT du devis correspond au prix fixe du bundle à la précision du centime. Files: `apps/cpq/models.py` (+migration), `apps/cpq/views.py`, `apps/cpq/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ4 — **Listes de prix multi-devise/multi-segment (étend XSAL1)** : ajoute à `ListePrix` (créée par XSAL1) les champs `devise` (repli `Company.devise` par défaut), `segment_client` (choix libre référentiel, ex. Résidentiel/Industriel/Revendeur), `date_debut_validite`/`date_fin_validite`. Sélection de la liste applicable dans `getPrixApplicable`/`prix-applicable` (créé par XSAL3) : priorité contrat nommé > segment client > liste par défaut, et exclusion des listes hors validité. Critère d'acceptation : une liste expirée n'est jamais retenue par `prix-applicable` même si elle correspond au segment du client. Files: `apps/ventes/models.py` (+migration ListePrix), `apps/ventes/selectors.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ5 — **Liste de prix par contrat nommé `PrixContractuel`** : modèle `PrixContractuel` (client FK, produit FK, prix_ht, date_debut, date_fin, motif texte, company FK) — prix négocié qui prime sur toute liste de prix générique pour ce couple client/produit. Endpoint `cpq/prix-contractuels/` CRUD (Directeur/Commercial responsable uniquement) ; `prix-applicable` (XSAL3) le consulte en priorité 1. Critère d'acceptation : un `PrixContractuel` actif pour un client donné écrase le prix de la liste segment même si celle-ci serait normalement retenue. Files: `apps/cpq/models.py` (+migration), `apps/cpq/views.py`, `apps/ventes/selectors.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ6 — **Garde-fou de marge minimale par famille produit (interne only)** : `SeuilMargeFamille` (company FK, categorie FK vers `stock.Categorie`, marge_min_pct). Au moment de `generer-pdf`/`accepter` sur un devis, un check serveur (jamais affiché au client, jamais dans le PDF) compare `(prix_vente_ht - prix_achat) / prix_vente_ht` par ligne à `marge_min_pct` de sa catégorie ; si en dessous, retourne un flag `marge_sous_seuil: true` dans la réponse API (utilisé par l'écran interne, invisible côté proposition). N'ajoute rien à `quote_engine`. Critère d'acceptation : un devis avec une ligne sous le seuil de marge renvoie `marge_sous_seuil=true` sur son endpoint de détail (staff uniquement) sans que ce champ apparaisse dans le payload `/proposal`. Files: `apps/cpq/models.py` (+migration SeuilMargeFamille), `apps/ventes/views.py`, `apps/ventes/serializers.py`, tests. (SCHEMA)(AUTH) «model: sonnet»
- [ ] NTCPQ7 — **Matrice d'approbation par profondeur de remise, calquée sur `contrats.RegleApprobation`** : `RegleApprobationRemise` (company FK, remise_min_pct, remise_max_pct, niveau_approbation, nombre_approbateurs) + `EtapeApprobationDevis` (devis FK, ordre, statut en_attente/approuve/rejete, approbateur FK) dans `apps/cpq`, réutilisant le même schéma séquentiel que `contrats.EtapeApprobation`. `services.lancer_approbation_devis(devis)` résout la règle par profondeur de remise réelle du devis (remise_globale/sous_total_ht) et instancie les étapes ; bloque `envoyer`/`generer-pdf` tant qu'une étape est `en_attente`. Remplace/étend le seuil unique existant (T17) par une matrice à paliers. Critère d'acceptation : un devis à 25% de remise route vers 2 approbateurs de niveau supérieur alors qu'un devis à 8% n'exige aucune approbation, et l'envoi est bloqué tant que l'étape est en attente. Files: `apps/cpq/models.py` (+migration RegleApprobationRemise/EtapeApprobationDevis), `apps/cpq/services.py`, `apps/ventes/services.py`, tests. (SCHEMA)(AUTH) «model: opus»
- [ ] NTCPQ8 — **Endpoint `devis/{id}/approuver-etape` / `rejeter-etape`** : actions sur `Devis` (permission = `approbateur` de l'étape courante ou rôle supérieur), consignent dans `DevisActivity` (chatter existant), avancent `EtapeApprobationDevis.statut`, débloquent `envoyer` quand toutes les étapes requises sont `approuve`. Écran React "Approbation" (onglet sur `DevisForm`) listant les étapes avec boutons Approuver/Rejeter, réutilisant le pattern déjà en prod pour les contrats. Critère d'acceptation : rejeter une étape renvoie le devis en `brouillon` et logue l'événement dans le chatter avec l'auteur et le motif. Files: `apps/ventes/views.py`, `apps/ventes/models.py`, `apps/cpq/services.py`, `frontend/src/features/ventes/DevisForm.jsx`, tests. (AUTH) «model: sonnet»

#### P1 — Configurateur guidé & catalogue (complète FG211-216 sans les refaire)

- [ ] NTCPQ9 — **Modèle de questions du configurateur guidé, backend pour FG211** : `QuestionConfigurateur` (company FK, ordre, texte, type `CHOIX_UNIQUE`/`CHOIX_MULTIPLE`/`NUMERIQUE`, options JSON) + `ReponseConfigurateur` liée à une session anonyme ou à un devis brouillon. Endpoint `cpq/configurateur/demarrer/`, `cpq/configurateur/{session}/repondre/`, `cpq/configurateur/{session}/resultat/` qui résout les produits/bundles correspondant aux réponses via `NTCPQ2` (moteur de règles). FG211 (déjà planifié) consomme ce backend pour l'écran ; NTCPQ9 fournit le modèle de données/API que FG211 n'a pas encore. Critère d'acceptation : répondre "toiture inclinée" + "9kWc" au configurateur renvoie une composition de produits cohérente avec les règles NTCPQ2 actives. Files: `apps/cpq/models.py` (+migration QuestionConfigurateur/ReponseConfigurateur), `apps/cpq/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ10 — **`generer-devis-depuis-configurateur`** : action `POST cpq/configurateur/{session}/generer-devis/` qui transforme le résultat résolu en `Devis` brouillon (lignes + lead/client si fournis), reprenant le flux `resolve_client_for_lead` existant. Ne génère jamais le PDF directement — le devis reste éditable avant envoi. Critère d'acceptation : la session configurateur aboutit à un devis brouillon éditable dans `/ventes/devis/nouveau`-équivalent avec les lignes pré-remplies. Files: `apps/cpq/views.py`, `apps/cpq/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ11 — **Clauses/CGV dynamiques par type de deal** : `ClauseCGV` (company FK, nom, corps_texte, applicable_si condition_group JSON évalué par `core.rules`, type_deal référentiel libre) ; `Devis.clauses_appliquees` (JSON, snapshot au moment de `envoyer`, jamais recalculé après envoi). Le builder du quote_engine lit `clauses_appliquees` en lecture seule pour l'inclusion dans le rendu existant (pas de nouveau renderer — juste un nouveau bloc de données consommé par `builder.py`). Critère d'acceptation : un devis "Industriel > 500k MAD" reçoit automatiquement la clause "garantie étendue" figée à l'envoi, invisible sur un devis résidentiel standard. Files: `apps/cpq/models.py` (+migration ClauseCGV), `apps/ventes/models.py` (+migration clauses_appliquees), `apps/ventes/quote_engine/builder.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ12 — **Bibliothèque de clauses réutilisables — écran Paramètres** : `parametres/clauses-cgv/` CRUD (React) listant `ClauseCGV`, prévisualisation de la condition en langage clair (ex. "s'applique si type_deal=Industriel ET montant>500000"), test à blanc contre un devis existant. Critère d'acceptation : un admin peut créer/désactiver une clause sans toucher au code, et un devis existant "testé" affiche si la clause s'appliquerait. Files: `frontend/src/features/parametres/ClausesCGV.jsx`, `apps/cpq/views.py`, tests. (SCHEMA) «model: sonnet»

#### P2 — Différenciateurs (renouvellements, indexation, variantes)

- [ ] NTCPQ13 — **Renouvellement de devis** : action `devis/{id}/renouveler/` sur un devis `accepte` lié à un contrat de maintenance ou une échéance métier — crée un nouveau `Devis` en `brouillon` reprenant les lignes actuelles avec les prix courants recalculés (pas une simple copie figée), lien `devis_origine` FK vers le devis source, incrémente un `numero_renouvellement`. Distinct de `reviser` (T10, qui corrige un devis non encore accepté) : `renouveler` s'applique à un devis déjà accepté/clos. Critère d'acceptation : renouveler un devis accepté d'il y a un an produit un nouveau brouillon avec les prix catalogue actuels, tout en gardant une trace `devis_origine` vers l'historique. Files: `apps/ventes/models.py` (+migration devis_origine/numero_renouvellement), `apps/ventes/views.py`, `apps/ventes/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ14 — **Avenant de devis (amendment)** : `AvenantDevis` (devis FK, lignes_ajoutees/lignes_retirees JSON diff, motif, date, auteur) permettant d'ajouter/retirer des lignes à un devis déjà accepté sans réouvrir tout le cycle d'approbation si le delta reste sous le seuil de remise — sinon redéclenche `NTCPQ7`. Endpoint `devis/{id}/avenants/` (liste + création). Critère d'acceptation : ajouter une ligne à un devis accepté crée un avenant tracé, recalcule les totaux, et redéclenche l'approbation seulement si le nouveau taux de remise global dépasse le seuil configuré. Files: `apps/ventes/models.py` (+migration AvenantDevis), `apps/ventes/views.py`, `apps/cpq/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ15 — **Prix indexés matières premières** : `IndexPrixMatiere` (company FK, nom ex. "Cuivre LME", valeur_reference, date_reference, valeur_courante, date_maj) + `Produit.formule_indexation` (FK optionnel vers `IndexPrixMatiere`, coefficient) — au calcul du prix catalogue d'un produit indexé, applique `prix_base × (valeur_courante / valeur_reference) × coefficient`, arrondi au dirham. Mise à jour manuelle de `valeur_courante` par un admin (pas de flux automatique externe — évite tout coût/dépendance non demandée). Critère d'acceptation : modifier `valeur_courante` d'un index recalcule immédiatement le prix catalogue affiché des produits qui y sont liés, sans toucher aux devis déjà émis (snapshot figé à l'émission, comme les autres prix). Files: `apps/cpq/models.py` (+migration IndexPrixMatiere), `apps/stock/models.py` (+migration Produit.formule_indexation), tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ16 — **Comparateur de variantes de devis (généraliste, complète FG212)** : endpoint `cpq/devis/{id}/variantes/` qui génère 2-3 variantes calculées à partir du même besoin (ex. "économique" / "standard" / "premium") en substituant des produits selon des règles de substitution (`ProduitEquivalent`, company FK, produit_source, produit_substitut, tier économique/standard/premium). FG212 (déjà planifié) est l'UI de comparaison ; NTCPQ16 fournit le moteur de génération des variantes que FG212 n'a pas. Distinct du système Sans-batterie/Avec-batterie déjà en prod (celui-là reste un cas spécifique solaire ; NTCPQ16 est générique multi-métiers). Critère d'acceptation : générer les variantes d'un devis produit 3 devis liés (variante_de FK) avec totaux HT différents et cohérents avec les substitutions configurées. Files: `apps/cpq/models.py` (+migration ProduitEquivalent), `apps/cpq/views.py`, `apps/ventes/models.py` (+migration variante_de), tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ17 — **Règles de remise automatique par palier de volume (étend XSAL2, ajoute la cascade)** : `PalierRemiseVolume` (company FK, categorie FK optionnel, quantite_min, remise_pct) créé par XSAL2 — NTCPQ17 ajoute la cascade multi-paliers (ex. 1er palier sur la ligne, 2e palier sur le sous-total global si plusieurs catégories atteignent leur seuil) via un ordre `priorite` et un flag `cumulable`. Endpoint existant `prix-applicable` (XSAL3) étendu pour renvoyer la décomposition (remise ligne + remise cascade) au lieu d'un seul pourcentage. Critère d'acceptation : un devis avec 3 catégories atteignant chacune leur palier applique les remises cumulables dans l'ordre de priorité et affiche la décomposition, pas juste un total. Files: `apps/ventes/models.py` (+migration PalierRemiseVolume), `apps/ventes/selectors.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ18 — **Devis multi-sites/multi-lots avec prix consolidé** : `LotDevis` (devis FK, nom_lot, adresse_site, lignes rattachées à un lot) permettant un devis unique couvrant plusieurs sites/bâtiments avec un sous-total par lot et un total consolidé — chevauche potentiellement MB (multi-building, PLAN2 groupe Quote improvements) : si MB1-3 existe déjà côté PLAN2, ce ticket note l'intersection au lancement (vérifier PLAN2 avant de construire) et se concentre uniquement sur le calcul de prix consolidé multi-lot, pas l'UX site. Critère d'acceptation : un devis à 2 lots affiche un sous-total HT par lot et un total HT consolidé cohérent au centime. Files: `apps/ventes/models.py` (+migration LotDevis), `apps/ventes/views.py`, tests. (SCHEMA) «model: sonnet»

#### P3 — Délighters (bibliothèque, guided selling avancé, e-signature de configuration)

- [ ] NTCPQ19 — **Recommandations de vente croisée/montée en gamme** : au moment de l'ajout d'une ligne dans le générateur, endpoint `cpq/suggestions/?produit_id=` renvoie jusqu'à 3 produits associés (basé sur `ContrainteCompatibilite.RECOMMANDE` de NTCPQ1 + fréquence de co-achat calculée en lecture seule depuis l'historique `LigneDevis`). Purement suggestif, aucune action automatique. Critère d'acceptation : sélectionner un onduleur suggère les 3 accessoires les plus fréquemment associés dans les devis acceptés de la même société. Files: `apps/cpq/views.py`, `apps/cpq/selectors.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ20 — **Historique des changements de configuration sur un devis brouillon** : `ConfigurationDevisSnapshot` (devis FK, contenu JSON, horodatage, auteur) pris automatiquement à chaque modification significative de lignes (ajout/retrait/quantité) tant que le devis est `brouillon`, avec un endpoint `devis/{id}/historique-configuration/` en lecture pour comparer deux snapshots. Distinct de la révision de version (T10) : c'est un historique fin pré-envoi, pas une nouvelle version numérotée. Critère d'acceptation : reconfigurer 5 fois un devis brouillon puis consulter l'historique affiche les 5 états intermédiaires avec diff des lignes. Files: `apps/ventes/models.py` (+migration ConfigurationDevisSnapshot), `apps/ventes/views.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ21 — **Badge "configuration validée" sur devis conforme aux règles** : champ calculé (non stocké) sur le détail devis qui exécute `NTCPQ2` (règles) + `NTCPQ1` (compatibilité) et renvoie `configuration_valide: true/false` + liste des violations, consommé par un badge dans `DevisForm`. Ne bloque rien par défaut (avertissement seul) sauf si une règle est marquée `bloquante=true`. Critère d'acceptation : un devis avec une incompatibilité produit non résolue affiche le badge rouge et liste la violation, sans empêcher l'enregistrement en brouillon. Files: `apps/ventes/views.py`, `apps/cpq/selectors.py`, `frontend/src/features/ventes/DevisForm.jsx`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ22 — **Export "feuille de configuration" interne (PDF technique, distinct du `/proposal` client)** : un export PDF interne (nouveau template, jamais exposé au client, jamais nommé "devis" ni "proposition") listant la configuration technique complète (options choisies, règles déclenchées, marge par ligne visible car interne) pour usage bureau d'études — généré par un service dédié dans `apps/cpq`, PAS par `quote_engine` (qui reste réservé au PDF client via `/proposal`, règle #4). Accessible uniquement aux rôles staff. Critère d'acceptation : le PDF technique affiche `prix_achat`/marge par ligne, alors que `/proposal` du même devis ne les affiche jamais. Files: `apps/cpq/services.py`, `apps/cpq/views.py`, tests. (SCHEMA)(AUTH) «model: sonnet»
- [ ] NTCPQ23 — **Alerte "marge sous seuil" dans le tableau de bord CPQ interne** : petit écran `parametres/cpq/marge/` (staff) listant les devis en cours avec `marge_sous_seuil=true` (NTCPQ6), triable par commercial/famille, sans jamais exposer ces données hors de l'ERP. Critère d'acceptation : le tableau liste uniquement les devis non encore acceptés en dessous du seuil et se vide quand la ligne est corrigée au-dessus du seuil. Files: `frontend/src/features/parametres/CpqMarge.jsx`, `apps/cpq/views.py`, tests. (SCHEMA) «model: haiku»

Notes de couverture (pas de tâche, pour l'orchestrateur) : XSAL1/2/3 et FG209-216 restent la propriété d'autres runs — NTCPQ4/5/17 les étendent explicitement plutôt que de les redéfinir ; NTCPQ9/10 fournissent le backend manquant que FG211 réclamera ; NTCPQ16 fournit le moteur que FG212 (UI comparateur) consommera. Aucune tâche ne touche `quote_engine/` ni `/proposal` (règle #4) — NTCPQ11 ne fait qu'ajouter une source de données lue par `builder.py`, NTCPQ22 crée un export strictement séparé et interne.

#### Profondeur (round 2) — rapports & documents imprimables internes

- [ ] NTCPQ24 — **Rapport "Taux de conformité des configurations"** : endpoint `cpq/rapports/conformite/?date_debut=&date_fin=&commercial_id=` (staff) qui agrège, sur la période, le % de devis créés sans violation `NTCPQ2`/`NTCPQ1` non résolue à l'envoi vs le % envoyés malgré un badge rouge (`NTCPQ21`) — export CSV via le pattern `apps/dataimport` (`export_registry.py`). Jamais un PDF client — usage interne bureau d'études/direction commerciale uniquement. Critère d'acceptation : l'export CSV du rapport contient une ligne par devis envoyé sur la période avec son statut de conformité et le nom du commercial. Files: `apps/cpq/views.py`, `apps/dataimport/export_registry.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ25 — **Rapport "Historique des approbations de remise"** : endpoint `cpq/rapports/approbations/` listant chaque `EtapeApprobationDevis` (NTCPQ7) avec devis, remise demandée, approbateur, délai de traitement (horodatage création → décision), motif de rejet le cas échéant — export XLSX via `apps/dataimport`. Critère d'acceptation : filtrer par approbateur affiche uniquement les étapes qui lui ont été assignées avec leur délai de traitement calculé en heures. Files: `apps/cpq/views.py`, `apps/dataimport/export_registry.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ26 — **Feuille de comparaison de variantes imprimable interne** : export PDF interne (même famille que `NTCPQ22`, généré par `apps/cpq`, jamais `quote_engine`) listant côte à côte les 3 variantes d'un devis (`NTCPQ16`) avec totaux HT, marge par variante et produits substitués — outil de préparation d'entretien commercial, jamais transmis au client. Critère d'acceptation : le PDF affiche 3 colonnes économique/standard/premium avec la marge par colonne, distinct du fichier généré par `/proposal`. Files: `apps/cpq/services.py`, `apps/cpq/views.py`, tests. (SCHEMA) «model: sonnet»

#### Profondeur (round 2) — assistants/wizards guidés

- [ ] NTCPQ27 — **Wizard "Créer une offre groupée" pas-à-pas** : écran React multi-étapes (`parametres/cpq/offres-groupees/nouveau`) guidant la création d'une `OffreGroupee` (NTCPQ3) : étape 1 sélection produits + quantités, étape 2 choix du mode de prix avec aperçu du prorata calculé en direct, étape 3 récapitulatif avant sauvegarde — évite la saisie JSON brute des lignes de bundle. Critère d'acceptation : compléter le wizard crée une `OffreGroupee` identique à une saisie API directe, avec l'aperçu du prorata affiché correspondant au calcul serveur au centime près. Files: `frontend/src/features/parametres/OffresGroupeesWizard.jsx`, `apps/cpq/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ28 — **Wizard "Escalade d'approbation" côté commercial** : quand `envoyer`/`generer-pdf` est bloqué par `NTCPQ7`, un écran guidé affiche à l'auteur du devis l'étape en attente, l'approbateur assigné, un bouton "Relancer" (crée une notification via `apps/notifications`, throttlé à 1 relance/24h) et un lien direct vers l'écran d'approbation (NTCPQ8). Distinct de l'écran d'approbation lui-même : celui-ci est côté demandeur, pas côté approbateur. Critère d'acceptation : cliquer "Relancer" deux fois dans la même journée n'envoie qu'une seule notification. Files: `frontend/src/features/ventes/`, `apps/cpq/services.py`, `apps/notifications/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ29 — **Wizard de résolution de conflit de compatibilité** : quand `NTCPQ1` renvoie une violation bloquante pendant la configuration d'un devis, un modal propose les alternatives valides (produits `RECOMMANDE` ou substituts `ProduitEquivalent` de NTCPQ16 compatibles) au lieu d'un simple message d'erreur bloquant sans issue. Critère d'acceptation : sélectionner deux produits incompatibles ouvre un modal listant au moins une alternative compatible en un clic, sans quitter l'écran de configuration. Files: `frontend/src/features/ventes/DevisForm.jsx`, `apps/cpq/views.py`, tests. (SCHEMA) «model: sonnet»

#### Profondeur (round 2) — réglages par tenant

- [ ] NTCPQ30 — **Écran Paramètres "CPQ" par société** : `parametres/cpq/reglages/` (React, staff) exposant `ParametresCPQ` (modèle 1-1 par company : `marge_min_defaut_pct`, `approbation_active` bool, `variantes_auto_generees` bool, `duree_validite_prix_contractuel_jours`) — un CRUD simple sur un singleton par tenant, pattern identique à `ParametresLocation`/`parametres/*`. Critère d'acceptation : désactiver `approbation_active` pour une société fait passer `envoyer`/`generer-pdf` en direct sans blocage NTCPQ7 pour cette société uniquement, sans affecter les autres. Files: `apps/cpq/models.py` (+migration ParametresCPQ), `apps/cpq/views.py`, `frontend/src/features/parametres/CpqReglages.jsx`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ31 — **Toggle "règles de compatibilité strictes vs avertissement" par société** : champ `ParametresCPQ.compatibilite_mode` (choix `BLOQUANT`/`AVERTISSEMENT`) qui contrôle si une violation `INCOMPATIBLE` (NTCPQ1) bloque réellement `envoyer` ou reste un simple badge (NTCPQ21). Par défaut `AVERTISSEMENT` pour ne rien casser sur les tenants existants. Critère d'acceptation : passer une société en `BLOQUANT` empêche l'envoi d'un devis avec violation non résolue, alors qu'en `AVERTISSEMENT` l'envoi reste possible avec le badge affiché. Files: `apps/cpq/models.py` (+migration compatibilite_mode), `apps/ventes/services.py`, tests. (SCHEMA) «model: sonnet»

#### Profondeur (round 2) — Celery beat (rappels, recalculs, purges)

- [ ] NTCPQ32 — **Job planifié "Expiration des prix contractuels"** : tâche Celery beat quotidienne (dans `apps/cpq/scheduled.py`, même pattern que `ventes/scheduled.py`) qui parcourt les `PrixContractuel` (NTCPQ5) dont `date_fin` est dépassée et pose une entrée `records.Activity` de rappel (type "Prix contractuel expiré") assignée au commercial responsable du client — jamais de suppression, jamais d'email automatique non demandé. Idempotent (une seule activité par prix expiré). Critère d'acceptation : un `PrixContractuel` dont la date de fin est hier génère exactement une activité de rappel au premier run du job, aucune au second run le même jour. Files: `apps/cpq/scheduled.py` (nouveau), tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ33 — **Job planifié "Relance des approbations en attente"** : tâche Celery beat quotidienne qui parcourt les `EtapeApprobationDevis` (NTCPQ7) `en_attente` depuis plus de `N` jours (réglable via `ParametresCPQ`, défaut 2) et envoie une notification `apps/notifications` à l'approbateur assigné — idempotent via un marqueur de dernière relance sur l'étape (jamais plus d'une relance par 24h par étape). Critère d'acceptation : une étape en attente depuis 3 jours avec seuil à 2 jours génère une notification au run du job, aucune seconde notification si le job tourne à nouveau moins de 24h après. Files: `apps/cpq/scheduled.py`, `apps/notifications/services.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ34 — **Job planifié "Purge des sessions configurateur abandonnées"** : tâche Celery beat quotidienne qui supprime les `ReponseConfigurateur`/sessions anonymes (NTCPQ9) inactives depuis plus de 30 jours sans devis généré (`NTCPQ10`) — ne touche jamais une session ayant abouti à un devis (même brouillon). Purge additive-safe (aucune donnée métier engagée n'est perdue). Critère d'acceptation : une session anonyme vieille de 45 jours sans devis lié est supprimée par le job, une session de même âge ayant généré un devis brouillon est conservée. Files: `apps/cpq/scheduled.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ35 — **Job planifié "Recalcul quotidien des prix indexés"** : tâche Celery beat qui, après une mise à jour manuelle de `IndexPrixMatiere.valeur_courante` (NTCPQ15), invalide un cache de prix catalogue calculé (si un cache est introduit) et consigne un `records.Activity` récapitulatif ("Prix catalogue recalculé : N produits impactés") sur chaque `Produit` concerné — purement informatif, ne modifie aucun devis déjà émis. Critère d'acceptation : modifier un index déclenche au run suivant du job une activité listant exactement les produits liés à cet index. Files: `apps/cpq/scheduled.py`, tests. (SCHEMA) «model: haiku»

#### Profondeur (round 2) — permissions fines par rôle

- [ ] NTCPQ36 — **Permissions granulaires CPQ dans `roles.Role`** : ajoute les clés `cpq.regles.gerer`, `cpq.prix_contractuels.gerer`, `cpq.marge.voir`, `cpq.approbation.approuver` au référentiel de permissions existant (même mécanisme que les permissions actuelles de `Role`), vérifiées côté viewsets `apps/cpq` (jamais un simple check de rôle nommé en dur). `cpq.marge.voir` conditionne l'exposition de `marge_sous_seuil` (NTCPQ6) et de l'export technique (NTCPQ22) — un commercial sans cette permission ne voit jamais de donnée de marge, même via l'API. Critère d'acceptation : un utilisateur sans `cpq.marge.voir` reçoit un payload de détail devis sans le champ `marge_sous_seuil`, un staff avec la permission le reçoit. Files: `apps/roles/models.py`, `apps/cpq/views.py`, `apps/ventes/views.py`, tests. (AUTH) «model: sonnet»
- [ ] NTCPQ37 — **Restriction `PrixContractuel` : seul le créateur ou un rôle supérieur peut modifier** : sur `cpq/prix-contractuels/{id}/`, la modification/suppression est limitée à l'auteur original ou à un rôle `cpq.prix_contractuels.gerer` (NTCPQ36) — un commercial ne peut pas modifier un prix négocié par un collègue sans cette permission élevée. Critère d'acceptation : un commercial sans la permission élevée reçoit 403 en tentant de modifier un `PrixContractuel` créé par un autre utilisateur. Files: `apps/cpq/views.py`, tests. (AUTH) «model: haiku»

#### Profondeur (round 2) — API publique & webhooks

- [ ] NTCPQ38 — **Exposition `publicapi` des objets CPQ en lecture** : ajoute `OffreGroupee`, `RegleProduitCPQ` (lecture seule des noms/conditions, jamais la marge) et `PrixContractuel` (scoped strictement au client correspondant à la clé API si applicable) aux sérialiseurs exposés par `apps/publicapi` (`ApiKeyViewSet`/scopes existants) — respecte le même filtrage `company` que le reste de `publicapi`. Critère d'acceptation : une clé API scoped à une société ne peut jamais lister les `OffreGroupee` d'une autre société, même en devinant un ID. Files: `apps/publicapi/views.py`, `apps/publicapi/serializers.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ39 — **Webhook `devis.approbation_requise` / `devis.approbation_decidee`** : nouveaux types d'événements dans `apps/publicapi.Webhook` (réutilise `WebhookDelivery`/`IdempotencyRecord` existants), déclenchés depuis `core/events.py` quand `NTCPQ7` instancie une étape et quand `NTCPQ8` la fait progresser — payload inclut devis id, remise_pct, statut étape, jamais `prix_achat`/marge. Critère d'acceptation : approuver une étape déclenche une livraison webhook vers l'URL configurée avec le statut mis à jour, retryable via le mécanisme `WebhookDelivery` existant en cas d'échec réseau. Files: `apps/cpq/services.py`, `core/events.py`, `apps/publicapi/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ40 — **Webhook `devis.variante_generee`** : événement émis quand `NTCPQ16` génère des variantes, permettant à un système externe (ex. portail partenaire) d'être notifié — même infrastructure `Webhook`/`WebhookDelivery`, payload limité aux totaux HT et noms de variantes. Critère d'acceptation : générer 3 variantes déclenche 1 (ou 3, à documenter dans le payload) livraison(s) webhook contenant les IDs des devis variantes créés. Files: `apps/cpq/services.py`, `core/events.py`, tests. (SCHEMA) «model: haiku»

#### Profondeur (round 2) — imports/exports CSV-XLSX

- [ ] NTCPQ41 — **Import CSV en masse `PrixContractuel`** : commande/écran réutilisant `apps/dataimport` (`ImportJob`/`ImportMapping`) pour charger en masse des prix négociés (colonnes client_ref, produit_ref, prix_ht, date_debut, date_fin, motif) — valide chaque ligne (client/produit existants dans la société, dates cohérentes) et rapporte les erreurs ligne par ligne sans bloquer les lignes valides (comportement additif standard de `dataimport`). Critère d'acceptation : un CSV de 100 lignes avec 5 erreurs importe les 95 lignes valides et renvoie un rapport listant les 5 lignes en échec avec leur motif. Files: `apps/cpq/services.py`, `apps/dataimport/`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ42 — **Export CSV/XLSX du catalogue de règles de compatibilité** : endpoint d'export (`apps/dataimport` export_registry) de `ContrainteCompatibilite` (NTCPQ1) et `RegleProduitCPQ` (NTCPQ2) pour audit/revue hors-ligne par le bureau d'études — lecture seule, jamais un import inverse dans cette tâche (pas de risque d'écrasement accidentel de règles). Critère d'acceptation : l'export XLSX contient une feuille par type de règle avec toutes les colonnes de configuration lisibles sans jargon technique (libellés français). Files: `apps/cpq/views.py`, `apps/dataimport/export_registry.py`, tests. (SCHEMA) «model: haiku»

#### Profondeur (round 2) — tests e2e du parcours clé

- [ ] NTCPQ43 — **Test e2e Playwright : parcours configurateur → devis → approbation → envoi bloqué/débloqué** : nouveau spec (suite existante `E1-16`) simulant le parcours complet : répondre au configurateur guidé (NTCPQ9), générer un devis (NTCPQ10), appliquer une remise déclenchant `NTCPQ7`, vérifier que `envoyer` est bloqué avec le message d'attente, approuver via l'écran NTCPQ8, vérifier que l'envoi devient possible. Critère d'acceptation : le spec passe en CI (suite Playwright existante) et couvre explicitement le cas bloqué ET le cas débloqué dans le même run. Files: `e2e/cpq-configurateur-approbation.spec.ts`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCPQ44 — **Test e2e : incompatibilité produit bloque uniquement en mode `BLOQUANT`** : spec vérifiant le comportement de `NTCPQ31` sur les deux modes de tenant (`AVERTISSEMENT` laisse envoyer avec badge visible, `BLOQUANT` empêche l'envoi) — utilise deux sociétés de test avec réglages différents pour prouver l'isolation multi-tenant du réglage. Critère d'acceptation : le spec échoue si le mode d'une société affecte le comportement d'une autre société dans le même run de test. Files: `e2e/cpq-compatibilite-mode.spec.ts`, tests. (SCHEMA) «model: sonnet»

#### Profondeur (round 2) — chatter, notifications, audit

- [ ] NTCPQ45 — **Entrées chatter automatiques sur `DevisActivity` pour les événements CPQ** : chaque application de bundle (NTCPQ3), résolution de configurateur (NTCPQ10), avenant (NTCPQ14) et renouvellement (NTCPQ13) pose une entrée `DevisActivity` factuelle (auteur, horodatage, résumé) dans le chatter existant du devis — réutilise le mécanisme `historique`/`noter` déjà en prod, aucune nouvelle infrastructure de log. Critère d'acceptation : appliquer un bundle à un devis fait apparaître une entrée chatter "Offre groupée « X » appliquée" horodatée et attribuée à l'utilisateur courant. Files: `apps/cpq/services.py`, `apps/ventes/models.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ46 — **Audit trail des changements de prix contractuel** : toute modification de `PrixContractuel.prix_ht` (NTCPQ5) est consignée dans `audit.AuditLog` existant (ancienne valeur, nouvelle valeur, auteur, société) — réutilise l'infrastructure d'audit générique déjà en prod, pas de nouveau modèle. Critère d'acceptation : modifier deux fois le prix d'un `PrixContractuel` crée deux entrées `AuditLog` distinctes consultables via l'écran d'audit existant. Files: `apps/cpq/models.py`, `apps/audit/`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ47 — **Notification à l'auteur du devis quand une variante est sélectionnée par le client (préparation portail)** : si `apps/portail` permet déjà au client de consulter une proposition, une notification `apps/notifications` informe le commercial quand un événement `devis.variante_generee` (NTCPQ40) atteint un statut "consultée" côté portail — strictement additif, ne crée aucune nouvelle capacité portail si elle n'existe pas déjà (vérifier `apps/portail` avant construction ; sinon marquer bloqué et ne construire que le côté événement interne). Critère d'acceptation : la notification apparaît dans le centre de notifications du commercial dans la minute suivant l'événement, jamais dupliquée pour le même événement. Files: `apps/cpq/services.py`, `apps/notifications/services.py`, `core/events.py`, tests. (SCHEMA) «model: sonnet»

#### Profondeur (round 2) — KPI reporting

- [ ] NTCPQ48 — **KPI "Délai moyen d'approbation de remise" au tableau de bord `reporting`** : nouveau `Kpi` (app `reporting`, réutilise `Kpi`/`DashboardConfig` existants) calculant la moyenne (et p90) du délai entre création d'une `EtapeApprobationDevis` (NTCPQ7) et sa décision, filtrable par période/approbateur — widget disponible dans le générateur de dashboard existant, pas un nouvel écran dédié. Critère d'acceptation : le KPI affiché sur un dashboard correspond au calcul manuel sur les mêmes données pour une période test. Files: `apps/reporting/models.py`, `apps/cpq/selectors.py`, tests. (SCHEMA) «model: haiku»
- [ ] NTCPQ49 — **KPI "Taux d'utilisation des offres groupées" et "Taux de conversion configurateur"** : deux `Kpi` supplémentaires — (a) % des devis envoyés contenant au moins une `OffreGroupee` (NTCPQ3) appliquée, (b) % des sessions configurateur (NTCPQ9) ayant abouti à un devis envoyé (funnel démarrage → devis brouillon → devis envoyé). Consommables comme widgets du dashboard existant. Critère d'acceptation : les deux KPI se recalculent correctement après création d'un nouveau devis avec bundle et n'incluent jamais les devis brouillon non envoyés dans le taux de conversion. Files: `apps/reporting/models.py`, `apps/cpq/selectors.py`, tests. (SCHEMA) «model: haiku»




### Groupe NTCRM — CRM enterprise (territoires, forecast, playbooks, intelligence relationnelle) — bench: Salesforce Sales Cloud, HubSpot Enterprise, Attio, Gong (audit 2026-07-07)

Le CRM leads solaire existant est déjà riche : chatter (LeadActivity), scoring de leads (rule-based, `scoring.py`), attribution multi-touch (PointContact/FG204), hiérarchie de comptes à un niveau (Client.parent, XSAL9), équipes commerciales (EquipeCommerciale, ZSAL3), objectifs/KPI individuels avec réalisé calculé à la demande (ObjectifCommercial, FG39 — nb_leads/nb_contacts/nb_rdv, PAS de commit/best-case/pipeline ni de roll-up hiérarchique ni de snapshot historique), assignation round-robin (XSAL11), concurrents sur deals perdus (ConcurrentPerte/FG242). Ce groupe hisse tout ça au niveau enterprise multi-métiers : moteur de règles de territoires (affectation géo/segment/rotation équitable — AU-DESSUS du round-robin existant), forecast catégorisé (commit/best-case/pipeline/omitted) avec roll-up hiérarchique manager et snapshots hebdomadaires, plans de compte formels + org chart multi-contacts par client (Client n'a aujourd'hui qu'une seule personne de contact), playbooks de vente par étape avec tâches obligatoires (contrainte : lit STAGES.py, n'invente jamais d'étape), détection de comptes dormants, score d'engagement multi-signaux (distinct du lead-scoring existant — porte sur les CLIENTS actifs, pas les leads), salles de vente digitales partageables, apporteurs d'affaires (deal registration), et défis/leaderboards d'équipe. Frontières : NTMKT est en amont (génération/qualification de leads) ; NTCPQ couvre les devis ; le groupe VX (PLAN2) couvre le refonte VISUELLE du CRM — aucune tâche purement cosmétique ici, uniquement modèles/endpoints/logique métier (une UI minimale d'écran est incluse seulement quand elle est le seul moyen d'exposer une fonctionnalité serveur).

#### P1 — Bloqueurs de vendabilité enterprise

- [ ] NTCRM1 — **Moteur de territoires (règles d'affectation)** : nouvelle app `apps/territoires` (ou module dans `crm`) : modèle `Territoire` (company FK, nom, type `GEO`/`SEGMENT`/`SECTEUR`, critères JSON — ex. région/ville/CP, type_installation, tranche CA), `TerritoireRegle` (territoire FK, ordre de priorité, condition JSON réutilisant `core/rules.py` FG367, action = assigner à équipe/utilisateur), `TerritoireMembre` (territoire FK, utilisateur FK, quota de rotation). Endpoint `territoires/` CRUD + `territoires/{id}/resoudre/?lead_id=` (retourne le territoire+assigné résolu sans muter). Le webhook lead entrant et la création manuelle de lead consultent le moteur de territoires AVANT le round-robin existant (XSAL11) si au moins un territoire actif matche ; sinon repli sur le comportement round-robin actuel inchangé. Critère d'acceptation : un lead créé à Marrakech avec territoire "Sud — résidentiel" actif est auto-assigné au bon commercial, testé par au moins 3 cas (match géo, match segment, aucun match → repli round-robin). Files: `apps/territoires/models.py` (+migration), `apps/territoires/views.py`, `apps/crm/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM2 — **Rotation équitable inter-territoires** : dans `apps/territoires/services.py`, `assigner_lead_territoire(lead)` implémente une rotation round-robin PAR territoire (compteur `TerritoireMembre.dernier_assigne_at`, jamais un pur modulo sur count() — réutilise le pattern race-safe de `apps/ventes/utils/references.py`), respecte les quotas si définis (`TerritoireMembre.quota_pct`), et logue l'assignation dans `LeadActivity` (kind MODIFICATION, champ owner). Étend NTCRM1. Critère d'acceptation : 100 leads simulés sur un territoire à 3 membres à quota égal se répartissent à ±2 leads près, vérifié par test. Files: `apps/territoires/services.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM3 — **Écran Paramètres → Territoires** : nouvel onglet dans `parametres` (React) listant les territoires, formulaire créer/éditer critères (région, type_installation, tranche CA), gestion des membres + quotas par territoire, aperçu « simuler cette adresse/ce type » qui appelle `territoires/{id}/resoudre/`. Réservé Administrateur/Directeur (RBAC existant). Critère d'acceptation : un administrateur peut créer un territoire complet et voir sa résolution simulée sans quitter l'écran. Files: `frontend/src/features/parametres/Territoires.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM4 — **Catégories de forecast (commit/best-case/pipeline/omis)** : sur `Lead` (ou nouveau modèle léger `ForecastEntry` lié 1-1 au Lead pour ne pas alourdir le modèle principal — préférer `ForecastEntry` : company FK, lead FK OneToOne, categorie choix COMMIT/BEST_CASE/PIPELINE/OMIS, montant_prevu Decimal (repli sur devis lié si vide), commentaire, mis_a_jour_par FK, mis_a_jour_le). Endpoint `forecast-entries/` CRUD, filtrable `?owner=&categorie=&periode=`. Un lead sans ForecastEntry explicite est classé PIPELINE par défaut (aucune migration de données requise). Critère d'acceptation : un commercial peut catégoriser 3 leads dans des catégories différentes et l'endpoint liste renvoie le total par catégorie. Files: `apps/crm/models.py` (+migration ForecastEntry), `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM5 — **Roll-up hiérarchique du forecast** : `apps/territoires/selectors.py` (ou `crm/selectors.py`) `forecast_rollup(company, periode, manager=None)` — agrège les `ForecastEntry` par commercial puis par équipe (`EquipeCommerciale` existant) puis par territoire (NTCRM1) puis total société ; un manager d'équipe voit le roll-up de son équipe, un Directeur voit tout. Endpoint `forecast/rollup/?periode=&equipe=`. Aucune duplication avec `ObjectifCommercial` (FG39) — le roll-up AGRÈGE les montants forecast, l'objectif reste la cible séparée comparée en sortie (`ecart_vs_objectif`). Critère d'acceptation : le roll-up d'une équipe de 3 commerciaux avec forecasts variés retourne le total exact par catégorie + l'écart vs l'objectif d'équipe s'il existe. Files: `apps/territoires/selectors.py`, `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM6 — **Snapshots hebdomadaires du forecast** : modèle `ForecastSnapshot` (company FK, semaine ISO, categorie, montant_total, nb_leads, owner nullable pour snapshot individuel vs société) créé par une commande `manage.py snapshot_forecast_hebdo` (idempotente : un seul snapshot par semaine+owner, upsert) appelée depuis Celery beat existant. Endpoint `forecast/historique/?owner=&semaines=12` renvoie la série pour un graphe d'évolution (slippage/glissement visible). Critère d'acceptation : exécuter la commande deux fois la même semaine ne crée pas de doublon ; l'historique sur 4 semaines simulées montre l'évolution correcte des montants par catégorie. Files: `apps/crm/models.py` (+migration ForecastSnapshot), `apps/crm/management/commands/snapshot_forecast_hebdo.py`, `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM7 — **Écran Forecast** : nouvelle page `/crm/forecast` (React) — vue manager avec colonnes Commit/Best Case/Pipeline/Total, filtrable par équipe/territoire/période, ligne par commercial + sous-total équipe, graphique sparkline d'évolution hebdo (NTCRM6), bouton « recatégoriser » un lead inline (met à jour `ForecastEntry`). Réservé Commercial responsable+/Directeur. Critère d'acceptation : un manager change la catégorie d'un lead depuis l'écran et voit le total de sa ligne se recalculer sans rechargement de page. Files: `frontend/src/pages/crm/forecast/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM8 — **Modèle Contact multi-rôles par client** : nouvelle app `apps/contacts` (ou modèle dans `crm`) : `ContactClient` (company FK, client FK→crm.Client, nom, prenom, poste, email, telephone, whatsapp, role_achat choix DECIDEUR/INFLUENCEUR/UTILISATEUR/GATEKEEPER/SPONSOR/AUTRE, contact_principal bool unique par client via `clean()`, actif bool). Additif — ne retire rien aux champs contact existants sur `Client` (conservés pour compat, deviennent le contact principal implicite si aucun ContactClient n'existe). Endpoint `contacts-client/` CRUD, filtrable `?client=`. Critère d'acceptation : un client peut avoir 4 contacts avec rôles distincts, exactement un marqué principal, testé par un cas qui tente d'en marquer un second (doit échouer proprement). Files: `apps/contacts/models.py` (+migration ContactClient), `apps/contacts/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM9 — **Org chart visuel du compte** : sur l'écran détail Client (React), nouvel onglet « Organigramme » listant les `ContactClient` groupés par rôle d'achat avec badges couleur, carte "qui influence qui" simple (pas de canvas complexe — une liste hiérarchique CSS suffit), actions rapides (appeler/WhatsApp/email) par contact réutilisant les composants de contact existants. Critère d'acceptation : l'onglet affiche tous les contacts d'un client groupés par rôle, sans regression sur l'onglet existant. Files: `frontend/src/pages/crm/clients/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM10 — **Plan de compte (Account Planning) formel** : modèle `PlanCompte` (company FK, client FK→crm.Client OneToOne, objectifs_strategiques texte, potentiel_estime Decimal, concurrents_presents texte libre — distinct de ConcurrentPerte/FG242 qui ne couvre que les deals perdus, swot_forces/faiblesses/opportunites/menaces JSON, prochaine_revue date, statut BROUILLON/ACTIF/ARCHIVE, created_by/mis_a_jour_par server-side). Endpoint `plans-compte/` CRUD, `plans-compte/{id}/historique/` (chatter réutilisant le pattern LeadActivity). Réservé aux comptes stratégiques (pas tous les clients — création manuelle only). Critère d'acceptation : créer un plan de compte pour un client, l'éditer, voir son historique de modification. Files: `apps/crm/models.py` (+migration PlanCompte), `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM11 — **Écran Plan de compte** : page `/crm/comptes/:id/plan` — formulaire structuré SWOT + objectifs + potentiel, timeline des revues, lien vers l'org chart (NTCRM9) et les devis/factures du client. Critère d'acceptation : un utilisateur remplit et sauvegarde un plan de compte complet en une session, visible depuis la fiche client. Files: `frontend/src/pages/crm/comptes/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM12 — **Playbooks de vente par étape STAGES.py** : modèle `Playbook` (company FK, nom, actif) + `PlaybookEtape` (playbook FK, stage — **importé de STAGES.py, jamais codé en dur**, ordre) + `PlaybookTache` (etape FK, libelle, obligatoire bool, ordre). `LeadPlaybookProgress` (lead FK, tache FK, fait bool, fait_par FK, fait_le) créé automatiquement quand un lead entre dans une étape ayant un playbook actif (signal post_save sur changement de `Lead.stage`, réutilise `core/events.py`). Endpoint `playbooks/` CRUD, `leads/{id}/playbook/` (lecture progression + action cocher tâche). Un changement de stage avec tâches obligatoires non cochées reste POSSIBLE (avertissement seulement, jamais un blocage dur — cohérent avec « never auto-move » existant) sauf configuration stricte optionnelle par playbook (`bloquant` bool, défaut False). Critère d'acceptation : un lead passant à QUOTE_SENT avec un playbook actif génère ses tâches obligatoires ; les cocher les marque faites avec acteur+date. Files: `apps/crm/models.py` (+migration Playbook/PlaybookEtape/PlaybookTache/LeadPlaybookProgress), `apps/crm/views.py`, `apps/crm/receivers.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM13 — **Écran Paramètres → Playbooks** : onglet CRUD des playbooks (sélection des 6 stages STAGES.py, tâches par étape, ordre, obligatoire/bloquant), et widget « Tâches du playbook » sur la fiche lead (checklist par étape courante, cohérente visuellement avec le pattern checklist chantier existant N4). Critère d'acceptation : un administrateur crée un playbook à 3 étapes avec tâches, et un commercial voit et coche les tâches sur un lead réel. Files: `frontend/src/features/parametres/Playbooks.jsx`, `frontend/src/pages/crm/leads/`, tests. (ROUTINE) «model: sonnet»

#### P2 — Différenciateurs

- [ ] NTCRM14 — **Détection de comptes dormants** : `crm/selectors.py` `comptes_dormants(company, seuil_jours=90)` — clients avec au moins un devis/facture passé mais aucune activité (LeadActivity la plus récente sur un lead lié, devis créé, ou PointContact) depuis `seuil_jours`. Réutilise `apps.ventes.selectors` via import function-local (respecte la frontière cross-app). Endpoint `clients/dormants/?seuil=90`. Commande `manage.py detecter_comptes_dormants` (idempotente) qui notifie (`notifications.notify()`) le commercial propriétaire quand un compte franchit le seuil, une fois par compte (flag `derniere_alerte_dormance` sur Client pour éviter le spam). Critère d'acceptation : un client sans activité depuis 100 jours avec seuil 90 apparaît dans la liste, une notification est envoyée une seule fois. Files: `apps/crm/selectors.py`, `apps/crm/views.py`, `apps/crm/management/commands/detecter_comptes_dormants.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM15 — **Écran Comptes dormants** : widget dashboard « Comptes à réactiver » (liste + bouton one-click « créer une activité de relance ») + filtre dédié sur `/crm` liste clients. Critère d'acceptation : le widget liste les comptes dormants réels et le bouton crée bien une activité liée. Files: `frontend/src/pages/crm/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM16 — **Score d'engagement multi-signaux (comptes actifs)** : nouveau module `crm/engagement.py` (pur, stateless, aligné sur le pattern `scoring.py` existant mais portant sur `Client`, pas `Lead`) — combine fréquence de contact (PointContact récents), ouverture de PDF (déjà trackée via `ShareLink`/logs d'accès document si présents, sinon champ additif `DevisActivity` consultation), paiements à temps (`Facture.statut`), activité récente (`Activity`/`LeadActivity`), ratio devis acceptés. Retourne un score 0-100 + label CHAUD/TIEDE/FROID (même taxonomie que le lead scoring pour cohérence UX). Endpoint `clients/{id}/engagement/` + `clients/engagement-bulk/` pour la liste. Distinct du lead-scoring existant (celui-ci reste sur les LEADS en phase de conversion, celui-ci porte sur les CLIENTS en phase de fidélisation/upsell). Critère d'acceptation : deux clients avec historiques différents obtiennent des scores cohérents avec leurs signaux, testé par au moins 4 cas. Files: `apps/crm/engagement.py` (nouveau), `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM17 — **Salle de vente digitale (Digital Sales Room)** : modèle `SalleVente` (company FK, lead FK ou client FK, token unique, titre, `expires_at`, mot de passe optionnel — réutilise le pattern `ShareLink`/`PartageGed` GED20 pour le token+expiry+password_hash). `SalleVenteItem` (salle FK, type DEVIS/DOCUMENT/VIDEO_LIEN/NOTE, reference — string-FK vers devis ou ged.Document, ordre). Endpoint public `GET /api/django/crm/salle-vente/{token}/` (AllowAny, 404/410/403 comme GED20, jamais de prix d'achat, jamais cross-tenant), authentifié `salles-vente/` CRUD + ajout/retrait d'items. Le client peut voir tous les documents partagés (devis multiples, dossier technique, PV) sur une seule page sans compte. Critère d'acceptation : une salle de vente créée avec 2 devis + 1 document GED est consultable via son lien public, expire correctement, et n'expose aucun prix d'achat. Files: `apps/crm/models.py` (+migration SalleVente/SalleVenteItem), `apps/crm/views.py`, `apps/crm/public_views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM18 — **Page publique Salle de vente** : page React publique (pas d'auth, même pattern que `/proposition/<token>`) affichant les items de la salle (devis en preview PDF via le canal `/proposal` existant, documents GED en aperçu via GED14 `apercu`), avec suivi de consultation basique (`SalleVenteVue` : timestamp + IP hashée, pas de PII superflue). Critère d'acceptation : le lien public affiche tous les items et journalise chaque visite sans authentification requise. Files: `frontend/src/pages/crm/salle-vente/`, `apps/crm/models.py` (+migration SalleVenteVue), `apps/crm/public_views.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM19 — **Analytics de la salle de vente** : endpoint `salles-vente/{id}/analytics/` — nombre de vues, dernière vue, items consultés (si consultation par item trackée), durée depuis création vs première vue (signal d'intérêt). Widget sur la fiche devis/lead montrant « le client a consulté 3 fois, dernière fois hier ». Critère d'acceptation : consulter une salle 3 fois fait apparaître le compteur à jour sur la fiche lead liée. Files: `apps/crm/views.py`, `frontend/src/pages/crm/leads/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM20 — **Registre des apporteurs d'affaires (Deal Registration)** : modèle `Apporteur` (company FK, nom, type PARTENAIRE_INSTALLATEUR/COURTIER/APPORTEUR_INDEPENDANT/AUTRE, contact email/tel, taux_commission_pct nullable, actif bool, RIB optionnel pour versement). `DealEnregistre` (apporteur FK, lead FK OneToOne, date_enregistrement, statut EN_ATTENTE/APPROUVE/REJETE/EXPIRE, expire_le = date_enregistrement + fenêtre de protection configurable, montant_commission_estime calculé à l'acceptation du devis lié). Un lead créé référencé par un apporteur enregistré est protégé contre une réassignation par un autre apporteur pendant la fenêtre (`clean()` refuse un second DealEnregistre actif pour le même client tant que le premier n'est pas expiré/rejeté). Endpoint `apporteurs/` CRUD, `deals-enregistres/` CRUD + `{id}/approuver|rejeter`. Distinct du programme `Parrainage` existant (celui-ci est client→client, celui-ci est partenaire B2B→lead avec commission). Critère d'acceptation : un second apporteur tentant d'enregistrer le même client pendant la fenêtre de protection du premier est refusé avec message clair. Files: `apps/crm/models.py` (+migration Apporteur/DealEnregistre), `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM21 — **Portail apporteur en lecture seule** : endpoint scopé `GET /api/django/crm/apporteurs/{id}/mes-deals/` (authentification par token dédié à l'apporteur, distinct des comptes CustomUser — génération d'un token simple à durée longue, pas de session complète) montrant le statut de ses deals enregistrés + commissions estimées, sans exposer les données internes du client au-delà du nom/ville. Critère d'acceptation : l'apporteur voit uniquement ses propres deals et leur statut, jamais ceux d'un autre apporteur (testé cross-apporteur). Files: `apps/crm/views.py`, `apps/crm/public_views.py`, tests. (AUTH) «model: opus»
- [ ] NTCRM22 — **Commission apporteur → paie/compta** : à l'acceptation du devis lié à un `DealEnregistre` APPROUVE, calcul du montant de commission (taux × montant HT accepté) stocké sur `DealEnregistre.montant_commission_du`, statut passe à À_PAYER. Pas d'écriture comptable automatique (respecte la frontière compta) — juste un signal `core/events.py` `deal_commission_due` que `compta` peut consommer plus tard pour créer une facture fournisseur/note de frais si l'app le souhaite. Endpoint `deals-enregistres/a-payer/` liste pour le comptable. Critère d'acceptation : accepter le devis d'un lead avec deal approuvé calcule automatiquement la commission due, visible dans la liste à-payer. Files: `apps/crm/models.py` (+migration montant_commission_du), `apps/crm/services.py`, `apps/crm/receivers.py`, `core/events.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM23 — **Défis et leaderboards d'équipe** : modèle `Defi` (company FK, nom, periode_debut/fin, metrique — réutilise les choix de `ObjectifCommercial.Metric`, cible_equipe nullable, recompense texte libre, actif bool) + selector `classement_defi(defi)` réutilisant les mêmes calculs d'attainment que FG39 mais agrégés PAR commercial en classement trié. Endpoint `defis/` CRUD, `defis/{id}/classement/`. Distinct des objectifs individuels FG39 (le défi est un jeu d'équipe temporaire et public, l'objectif est une cible privée permanente). Critère d'acceptation : un défi « le plus de RDV ce mois » calculé sur 3 commerciaux produit un classement trié correct, vérifié par test avec données connues. Files: `apps/crm/models.py` (+migration Defi), `apps/crm/selectors.py`, `apps/crm/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTCRM24 — **Écran Leaderboard** : page/widget `/crm/defis` — classement en cartes avec avatar+score+rang, animation de podium top-3 (CSS simple, pas de lib supplémentaire), filtrable par défi actif. Visible par toute l'équipe (pas juste le manager — l'aspect gamification suppose la visibilité). Critère d'acceptation : le classement affiché correspond exactement à l'endpoint `classement/` et se met à jour au changement de défi sélectionné. Files: `frontend/src/pages/crm/defis/`, tests. (ROUTINE) «model: haiku»
- [ ] NTCRM25 — **Alertes de territoire non couvert** : sur NTCRM1, endpoint `territoires/couverture/` détectant les leads récents (30 derniers jours) qui n'ont matché AUCUN territoire actif (trou de couverture géo/segment), avec compteur par région/segment non couverte. Widget dashboard Directeur « zones non couvertes ». Critère d'acceptation : un lead créé hors de tout territoire défini apparaît dans le rapport de couverture avec sa région. Files: `apps/territoires/views.py`, `frontend/src/pages/crm/`, tests. (ROUTINE) «model: sonnet»

#### P3 — Délices

- [ ] NTCRM26 — **Recommandation de playbook par similarité** : sur `Playbook`/NTCRM12, quand plusieurs playbooks actifs existent pour le même stage (ex. un par type_installation), auto-sélection du playbook dont les critères (type_installation, canal) matchent le mieux le lead — réutilise la logique de `core/rules.py` (FG367) plutôt qu'un nouveau moteur. Aucune IA/ML — règles pures. Critère d'acceptation : deux playbooks actifs sur le même stage avec critères différents s'appliquent chacun au bon profil de lead, testé par 2 cas. Files: `apps/crm/services.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM27 — **Signal d'intérêt automatique depuis la salle de vente** : quand `SalleVenteVue` (NTCRM18) détecte ≥3 consultations en 48h sur une salle liée à un lead en stage QUOTE_SENT, création automatique d'une `LeadActivity` note « signal d'intérêt fort — 3 consultations en 48h » (jamais un changement de stage automatique — juste un signal informationnel). Réutilise `core/events.py`. Critère d'acceptation : 3 vues simulées en moins de 48h génèrent la note automatiquement, une 4e vue ne duplique pas la note du jour. Files: `apps/crm/services.py`, `core/events.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM28 — **Export CSV du classement de défi** : action `defis/{id}/export-xlsx/` réutilisant le pattern d'export .xlsx déjà présent partout (openpyxl), pour partager le classement en réunion commerciale. Critère d'acceptation : le fichier exporté contient le même classement que l'endpoint JSON, colonnes rang/nom/score/valeur. Files: `apps/crm/views.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTCRM29 — **Widget "portefeuille de comptes" par commercial** : sur le dashboard commercial existant, ajout d'une carte listant ses comptes (clients dont il est owner via leads liés) triés par score d'engagement (NTCRM16) croissant (les plus froids en premier — priorisation d'action), avec lien direct vers chaque plan de compte (NTCRM10) s'il existe. Critère d'acceptation : la carte liste les comptes du commercial connecté triés par score, sans fuite cross-tenant ni cross-commercial. Files: `frontend/src/pages/crm/dashboard/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTCRM30 — **Notes de réunion structurées liées au plan de compte** : sur `PlanCompte` (NTCRM10), sous-modèle `RevueCompte` (plan FK, date, participants texte, decisions texte, prochaine_action + date, created_by) affichée en timeline sur l'écran plan de compte (NTCRM11), même esprit que `ReunionChantier` (FG296) mais côté commercial. Critère d'acceptation : ajouter une revue de compte l'affiche immédiatement dans la timeline du plan, triée par date décroissante. Files: `apps/crm/models.py` (+migration RevueCompte), `frontend/src/pages/crm/comptes/`, tests. (ROUTINE) «model: haiku»


### Groupe NTMKT — Marketing automation : journeys, segmentation, attribution, événements — bench: HubSpot Marketing, Klaviyo, Braze, Brevo (audit 2026-07-08)

**Constat majeur de vérification :** le groupe XMKT (docs/PLAN.md, 33/37 tâches déjà `[x]`) a livré une app `apps/marketing` complète et récente (34 modèles, ~28 endpoints REST : Campagne/EnvoiCampagne/SequenceRelance/EtapeSequence/InscriptionSequence/SegmentMarketing/ListeDiffusion/A-B test/LienTrackee/EvenementMarketing/BilletEvenement/Enquete/CompteFidelite/RegleUpsell/DomaineEnvoi…), avec loi 09-08/CNDP (ConsentRecord + SuppressionMarketing), attribution multi-touch (PointContact FG204), ROI (XMKT17/31). **Mais le frontend n'expose QU'UN SEUL écran** (`/marketing/calendrier`, `frontend/src/features/marketing/module.config.jsx`) — les 27 autres endpoints n'ont AUCUNE UI : c'est un moteur marketing de classe mondiale piloté uniquement par Postman. XMKT10 (canal WhatsApp campagnes), XMKT34 (génération IA), XMKT35 (posts sociaux) et XMKT36 (audience Meta) restent `[ ]` — NE PAS RE-PROPOSER, seulement les citer si le bench les documente ailleurs. Ce groupe NTMKT couvre : (1) la lane frontend manquante (bloqueur de vendabilité — un backend sans écran ne se démontre pas), (2) le vrai chaînon manquant même côté backend face à HubSpot/Braze : un **constructeur de journeys visuel en canevas** (l'existant XMKT1/18/19 est une liste linéaire d'étapes avec une condition simple, pas un graphe drag-and-drop avec embranchements multiples/attentes/déclencheurs composés), le **progressive profiling** sur formulaires versionnés (FormulaireIntake est un JSON de champs statique, pas une page publiée/versionnée à révélation progressive), un **centre de préférences self-service** pour le contact (ConsentRecord existe côté registre, aucune page publique où le contact gère ses abonnements), le **lead scoring de maturité multi-signal** (au-delà du score unique QJ6), et l'**attribution multi-touch paramétrable** (FG204 est un journal brut — étendre avec des modèles configurables premier/dernier/linéaire/pondéré). Frontières : NTCRM (le lead/scoring lui-même, la fiche 360°), NTGRC (le registre légal de consentement CNDP — NTMKT ne fait QUE la page self-service qui écrit dedans via services).

#### P1 — Combler le vide frontend (bloqueur de vendabilité : moteur sans volant)

- [ ] NTMKT1 — **Nouvelle app frontend « Marketing » : shell + navigation + dashboard.** Étendre `frontend/src/features/marketing/module.config.jsx` (existant, une seule route aujourd'hui) avec un sous-menu complet : Campagnes, Séquences, Segments, Listes, Événements, Enquêtes, Fidélité, Domaine d'envoi, Calendrier (existant). Écran `MarketingDashboard.jsx` en page d'accueil du module : KPI cards (campagnes actives, taux d'ouverture moyen 30j, leads MQL du mois, ROI cumulé XMKT17) consommant les endpoints `marketing/campagnes/`, `marketing/envois-campagne/` existants. Gate rôles `responsable/admin` (comme l'existant). **Critère d'acceptation :** le sous-menu Marketing liste 9 écrans, le dashboard affiche 4 KPI réels sans exposer `prix_achat`. Files: `frontend/src/features/marketing/module.config.jsx`, `frontend/src/features/marketing/MarketingDashboard.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT2 — **Écran Campagnes : liste + éditeur + détail avec trace destinataires.** `CampagnesList.jsx` (statut/canal/planifiee_le, filtre) + `CampagneForm.jsx` (objet/corps par canal email/sms/whatsapp, variantes langue XMKT11, variables de fusion XMKT8 avec aperçu) + `CampagneDetail.jsx` consommant `marketing/envois-campagne/?campagne=` (XMKT2, drill-down par destinataire : queued/envoyé/ouvert/cliqué/rebond/désinscrit) + bouton « Envoyer un test » (XMKT13) + pré-check santé affiché avant confirmation d'envoi de masse. **Critère d'acceptation :** créer, prévisualiser fusionné, envoyer un test, voir la trace par destinataire d'une campagne existante, sans toucher au backend. Files: `frontend/src/features/marketing/CampagnesList.jsx`, `frontend/src/features/marketing/CampagneForm.jsx`, `frontend/src/features/marketing/CampagneDetail.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT3 — **Éditeur de test A/B sur CampagneForm.** Section « Test A/B » (XMKT14 backend existant, aucune UI) : deux variantes objet/corps, % d'échantillon, fenêtre de décision en heures, critère ouvertures/clics, et sur `CampagneDetail.jsx` un panneau comparatif A vs B vs reste (métriques temps réel, gagnant affiché une fois décidé). **Critère d'acceptation :** configurer un test A/B et voir le comparatif se peupler après l'envoi de test XMKT13. Files: `frontend/src/features/marketing/CampagneForm.jsx`, `frontend/src/features/marketing/CampagneDetail.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT4 — **Écran Segments marketing avec prévisualisation live.** `SegmentsList.jsx` + `SegmentBuilder.jsx` : constructeur de règles no-code (champs lead/client, activité marketing ouvert/cliqué/jamais-ouvert) au-dessus de `marketing/segments-marketing/` + son action `previsualiser/` (compte + échantillon affiché en direct pendant l'édition des règles, sans sauvegarde). Réutilise le pattern de filtres existant du DataTable engine (`ui/datatable/`) pour la construction de règles simples (pas de nouvelle dépendance). **Critère d'acceptation :** ajouter une règle met à jour le compte en <1s, un segment sauvegardé est sélectionnable dans une campagne. Files: `frontend/src/features/marketing/SegmentsList.jsx`, `frontend/src/features/marketing/SegmentBuilder.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT5 — **Écran Listes de diffusion + import CSV/XLSX.** `ListesDiffusion.jsx` : CRUD listes (XMKT5), import fichier avec mapping de colonnes + rapport ajoutés/doublons/ignorés-supprimés (l'endpoint d'import existe déjà), vue détail d'une liste avec ses abonnés + statut inscrit/désinscrit + historique d'adhésion. **Critère d'acceptation :** importer un CSV de 50 contacts affiche le rapport exact, un contact désinscrit XMKT3 n'apparaît jamais réactivé par un ré-import. Files: `frontend/src/features/marketing/ListesDiffusion.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT6 — **Écran Séquences (trace linéaire d'étapes) avec vue participant.** `SequencesList.jsx` + `SequenceDetail.jsx` affichant les `EtapeSequence` en liste ordonnée (délai J+n, condition XMKT18, action CRM XMKT19) + onglet « Participants » listant `InscriptionSequence` avec statut actif/sorti/terminé et, par participant, la trace `ExecutionEtapeSequence` (quel nœud, quand, quoi envoyé, erreur). Bouton « Activer une recette » pour les séquences seedées XMKT20. **Critère d'acceptation :** une séquence seedée s'active en un clic, un lead inscrit montre sa progression étape par étape. Files: `frontend/src/features/marketing/SequencesList.jsx`, `frontend/src/features/marketing/SequenceDetail.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT7 — **Écran Événements marketing (salons/webinaires) + check-in.** `EvenementsList.jsx` + `EvenementForm.jsx` (type, dates, lieu/lien visio, capacité, billets XMKT via `BilletEvenement`) + `EvenementDetail.jsx` avec liste des `InscriptionEvenement` (statut inscrit/confirmé/présent/absent), bouton de check-in sur place + génération QR par inscrit (réutilise `stock.labels.qr_svg` existant, aucune dépendance), et bouton « Créer le segment présents » (relie à NTMKT4 via `SegmentMarketing`). **Critère d'acceptation :** créer un événement, checker-in un inscrit, créer le segment des présents en un clic. Files: `frontend/src/features/marketing/EvenementsList.jsx`, `frontend/src/features/marketing/EvenementForm.jsx`, `frontend/src/features/marketing/EvenementDetail.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT8 — **Écran Enquêtes (NPS + constructeur générique) + page résultats.** `EnquetesList.jsx` + `EnqueteBuilder.jsx` (questions typées choix/échelle/texte/NPS avec logique conditionnelle « question B si réponse A », XMKT27) + `EnqueteResultats.jsx` (graphiques par question, taux de complétion/abandon, bouton export XLSX déjà présent côté backend) + lien public copiable. **Critère d'acceptation :** créer une enquête à 2 questions conditionnelles, voir les résultats agrégés d'une réponse test. Files: `frontend/src/features/marketing/EnquetesList.jsx`, `frontend/src/features/marketing/EnqueteBuilder.jsx`, `frontend/src/features/marketing/EnqueteResultats.jsx`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT9 — **Écran Fidélité (points, mouvements, règles d'upsell).** `FideliteList.jsx` (comptes `CompteFidelite`, solde de points, historique `MouvementFidelite`) + `ReglesUpsell.jsx` (CRUD `RegleUpsell` — condition d'achat → suggestion produit). Lecture seule depuis la fiche client (lien croisé) sans dupliquer d'écran client. **Critère d'acceptation :** voir le solde de points d'un client et l'historique de ses mouvements. Files: `frontend/src/features/marketing/FideliteList.jsx`, `frontend/src/features/marketing/ReglesUpsell.jsx`, tests. (ROUTINE) «model: haiku»
- [ ] NTMKT10 — **Écran Domaine d'envoi (SPF/DKIM/DMARC) + Support offline (QR flyers).** Page Paramètres → Marketing : statut de vérification DNS par enregistrement (`marketing/domaines-envoi/`, XMKT33 backend), bouton « Revérifier » ; écran `SupportsOffline.jsx` (XMKT29) : créer un support imprimé, télécharger le QR SVG, voir le compteur scans→leads par support. **Critère d'acceptation :** la page affiche l'état SPF/DKIM/DMARC réel, un support offline téléchargé génère un QR scannable qui compte. Files: `frontend/src/features/parametres/DomaineEnvoi.jsx`, `frontend/src/features/marketing/SupportsOffline.jsx`, tests. (ROUTINE) «model: haiku»
- [ ] NTMKT11 — **Touches marketing visibles sur la fiche lead 360°.** `apps/marketing` écrit déjà (XMKT16) dans `LeadActivity` + `PointContact` — le rendu chatter du lead (`LeadForm.jsx`/archive client) doit distinguer visuellement les événements marketing (icône campagne/séquence dédiée, lien vers la campagne source) au lieu de les fondre dans le flux générique. **Critère d'acceptation :** un lead touché par 2 campagnes montre chaque touche avec un lien cliquable vers `CampagneDetail`. Files: `frontend/src/pages/crm/leads/LeadForm.jsx`, tests. (ROUTINE) «model: haiku»

#### P1 — Constructeur de journeys visuel (le vrai différenciateur HubSpot/Braze absent même côté backend)

- [ ] NTMKT12 — **Modèle de journey en graphe (nœuds + arêtes) — extension additive de SequenceRelance.** L'existant `EtapeSequence`/XMKT18 est une liste linéaire avec une seule condition binaire par étape. Ajouter `marketing.NoeudJourney` (company FK, `sequence` FK vers `SequenceRelance` existant — réutilise le moteur d'exécution XMKT1, jamais dupliqué, type déclencheur/attente/action/branche/sortie, position_x/position_y pour le canevas, config JSON) + `marketing.ArcJourney` (nœud source → nœud cible, condition optionnelle : `toujours`/`a_ouvert`/`a_clique`/`score_seuil`/`tag_present` — surclasse le champ `condition` unique actuel avec N embranchements sortants par nœud). Le tick Celery beat existant (XMKT1) parcourt le graphe au lieu de la liste linéaire quand des nœuds existent (fallback linéaire inchangé si aucun nœud défini — comportement actuel préservé à l'octet). **Critère d'acceptation :** une séquence existante sans nœuds graphe s'exécute identiquement à aujourd'hui, une séquence avec 3 nœuds et 2 embranchements route correctement selon la condition évaluée sur les traces XMKT2, tests couvrent le fallback + le multi-embranchement + le scoping. Files: `apps/marketing/models.py` (+migration additive), `apps/marketing/tasks.py`, `apps/marketing/services.py`, tests. (SCHEMA) «model: opus»
- [ ] NTMKT13 — **Canevas visuel drag-and-drop du journey (React Flow léger, sans nouvelle dépendance lourde).** Écran `JourneyBuilder.jsx` : canevas avec nœuds déplaçables (déclencheur/attente/email/sms/whatsapp/action-CRM/branche/sortie), connexions tirées à la souris créant les `ArcJourney` (NTMKT12), palette latérale de types de nœuds, sauvegarde position_x/y. Implémenter un canevas minimal maison (SVG + drag, comme le pattern zéro-dépendance de `Toiture-3D`/DatePicker) plutôt qu'ajouter une lib de flow-chart — évite une dépendance payante/lourde. **Critère d'acceptation :** glisser 3 nœuds, les relier avec 2 conditions différentes, sauvegarder, rouvrir affiche le même graphe, tests vitest sur la sérialisation nœuds/arcs. Files: `frontend/src/features/marketing/JourneyBuilder.jsx`, `frontend/src/api/marketingApi.js`, tests. (ROUTINE) «model: opus» (@after: NTMKT12)
- [ ] NTMKT14 — **Nœud « attente jusqu'à » (date/jour de semaine/heure ouvrée) dans le journey.** Étendre `NoeudJourney` (NTMKT12) avec un type `attente_jusqu_a` (date absolue, ou prochain jour ouvré/heure configurable — réutilise `notifications.WorkingHoursConfig`/`Holiday` via selector, jamais d'import direct) en plus du délai relatif J+n existant (XMKT1). **Critère d'acceptation :** un nœud « attendre jusqu'au prochain lundi 9h » retarde correctement l'exécution, tests couvrent le calcul de la prochaine échéance ouvrée. Files: `apps/marketing/models.py`, `apps/marketing/tasks.py`, `apps/notifications/selectors.py`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT12)
- [ ] NTMKT15 — **Bibliothèque de modèles de journeys (au-delà des 5 recettes seedées XMKT20).** `marketing.ModeleJourney` (company FK, nom, catégorie, graphe pré-construit JSON de nœuds/arcs prêt à instancier) avec une commande `seed_modeles_journey` livrant 4 gabarits supplémentaires orientés B2B solaire industriel/agricole (relance devis étude autoconsommation, cycle appel d'offres, onboarding installateur partenaire, campagne saisonnière post-Ramadan/rentrée agricole) et un bouton « Utiliser ce modèle » qui instancie NoeudJourney/ArcJourney dans une nouvelle SequenceRelance éditable. **Critère d'acceptation :** instancier un modèle crée une séquence éditable avec le bon graphe, re-run de la commande = no-op, tests. Files: `apps/marketing/models.py` (+migration), `apps/marketing/management/commands/`, `frontend/src/features/marketing/JourneyBuilder.jsx`, tests. (SCHEMA) «model: sonnet» (@after: NTMKT13)

#### P2 — Progressive profiling, landing pages, scoring de maturité

- [ ] NTMKT16 — **Landing pages versionnées et publiées (au-delà du JSON de champs `FormulaireIntake`).** `FormulaireIntake` (existant) définit des CHAMPS mais pas une PAGE publiable avec contenu éditorial. Ajouter `marketing.VersionFormulaireIntake` (formulaire FK, contenu HTML/markdown simple du corps de page — titre/pitch/image MinIO, `version` incrémentée serveur, `publie` bool, `date_publication`) : chaque édition crée une nouvelle version, la page publique (`public_views.py` existant) rend toujours la dernière version publiée ; un bouton « Publier cette version » dans l'éditeur. **Critère d'acceptation :** éditer un formulaire crée une v2 en brouillon, publier bascule la page publique, l'historique de versions reste consultable, tests. Files: `apps/marketing/models.py` (+migration additive), `apps/marketing/public_views.py`, `frontend/src/features/marketing/`, tests. (SCHEMA) «model: sonnet»
- [ ] NTMKT17 — **Progressive profiling : champs cachés déjà connus, révélés seulement pour l'inconnu.** Sur la soumission publique de `FormulaireIntake`, si le visiteur revient (cookie/token de session existant réutilisé, aucune nouvelle dépendance) et qu'un lead avec le même email/téléphone normalisé existe déjà (dédup QJ8 existante), ne présenter QUE les champs `champs` non encore renseignés sur ce lead (comparaison au `custom_data`/champs standards existants) au lieu de tout redemander — HubSpot-style. **Critère d'acceptation :** un visiteur connu ne revoit pas les champs déjà connus, un visiteur inconnu voit le formulaire complet, tests couvrent la détection + le filtrage de champs + le no-op sans doublon détecté. Files: `apps/marketing/public_views.py`, `apps/crm/selectors.py`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT16)
- [ ] NTMKT18 — **Score de maturité marketing multi-signal (au-delà du score unique QJ6).** `crm.Lead.score` (QJ6) est un score de QUALITÉ statique recalculé sur les champs du lead ; ajouter un score de MATURITÉ additif `marketing.ScoreMaturite` (lead_id opaque, valeur 0-100, historique des variations horodaté) recalculé à chaque événement marketing significatif (ouverture +2, clic +5, visite page proposition +10, inactivité 30j -10 — pondérations éditables en Paramètres) distinct du score de qualité QJ6 (qui reste inchangé) ; le seuil MQL (XMKT21) peut désormais se déclencher sur le score de qualité OU de maturité (paramètre société additif, défaut = qualité seul = comportement actuel). **Critère d'acceptation :** une ouverture de campagne fait progresser le score de maturité d'un lead sans toucher son score de qualité, l'historique des variations est consultable, tests couvrent le calcul + le seuil + le no-op par défaut. Files: `apps/marketing/models.py` (+migration additive), `apps/marketing/services.py`, `apps/crm/services.py` (lecture seule via selector), tests. (SCHEMA) «model: sonnet» (@after: NTMKT11)
- [ ] NTMKT19 — **Écran Score de maturité sur la fiche lead + widget « chaud/tiède/froid ».** Afficher NTMKT18 sur `LeadForm.jsx`/kanban : badge chaud (≥70) / tiède (30-69) / froid (<30) avec sparkline de l'historique récent, et un filtre kanban additif « maturité » à côté des filtres existants. **Critère d'acceptation :** le badge reflète le score courant, la sparkline montre l'évolution sur 30j, tests vitest. Files: `frontend/src/pages/crm/leads/`, `frontend/src/api/marketingApi.js`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT18)

#### P2 — Attribution multi-touch paramétrable + centre de préférences self-service

- [ ] NTMKT20 — **Modèles d'attribution configurables sur le journal PointContact (FG204 étendu).** `PointContact`/FG204 journalise les touches mais l'attribution actuelle est implicitement dernier-touche (XMKT17 ROI). Ajouter un paramètre société `modele_attribution` (dernier_touche / premier_touche / lineaire / pondere_temporel — formule de dégradation temporelle standard) consommé par `crm.selectors` et par le ROI XMKT17/31 : recalcule la part de revenu attribuée à chaque touche du parcours d'un lead signé selon le modèle choisi, exposé sur un nouvel endpoint `attribution/comparaison/?devis_id=` montrant les 4 modèles côte à côte pour un même deal (aide à la décision, pas de recalcul persistant). **Critère d'acceptation :** changer le modèle société change la répartition affichée sur le ROI, la comparaison affiche les 4 modèles pour un devis signé, défaut = comportement actuel (dernier-touche), tests couvrent chaque modèle + le no-op par défaut. Files: `apps/crm/selectors.py`, `apps/marketing/services.py`, `apps/marketing/views.py`, tests. (SCHEMA) «model: sonnet»
- [ ] NTMKT21 — **Écran Attribution : rapport comparatif multi-modèle.** `AttributionReport.jsx` (nouveau) : sélecteur de modèle (NTMKT20), tableau des campagnes/canaux classés par revenu attribué + CPL/CAC (réutilise XMKT17), et un panneau « comparer les 4 modèles » sur un deal donné. **Critère d'acceptation :** changer de modèle dans le sélecteur recalcule le classement affiché sans rechargement de page, tests. Files: `frontend/src/features/marketing/`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT20)
- [ ] NTMKT22 — **Centre de préférences self-service public (au-delà de la désinscription un clic XMKT3).** XMKT3 ne fait QUE désinscrire totalement. Ajouter une page publique tokenisée `preferences/<token>` (même modèle de confiance que `desinscription/<token>` existant) où le contact choisit PAR CANAL (email/SMS/WhatsApp) et PAR TYPE (promotions/événements/newsletter — catégories `ListeDiffusion` existantes) ce qu'il souhaite recevoir, écrivant dans `core.ConsentRecord` (registre existant NTGRC-adjacent — jamais de second registre créé, ce groupe consomme le service `core` existant) + `AbonnementListe` (existant). Lien `{lien_preferences}` disponible dans `MessageTemplate`/pied de campagne à côté de `{lien_desinscription}` existant. **Critère d'acceptation :** un contact peut garder WhatsApp et couper email sans se désinscrire totalement, le choix est respecté au prochain envoi de campagne, token invalide/expiré rejeté proprement, tests multi-tenant. Files: `apps/marketing/public_views.py`, `apps/marketing/models.py` (+migration additive si champ token manquant), `core/services.py` (fonction fine si besoin), tests. (SCHEMA) «model: sonnet»

#### P3 — Différenciateurs additionnels (delighters)

- [ ] NTMKT23 — **Bibliothèque de blocs de contenu réutilisables dans l'éditeur de campagne.** `marketing.BlocContenu` (company FK, nom, type texte/image/CTA-bouton, contenu HTML fragment) insérable dans `CampagneForm.jsx` (NTMKT2) via un sélecteur — évite de retaper le même bloc « avis clients » ou bandeau promo à chaque campagne. **Critère d'acceptation :** créer un bloc « CTA prendre RDV », l'insérer dans 2 campagnes différentes, modifier le bloc source ne rétro-modifie PAS les campagnes déjà envoyées (snapshot au moment de l'insertion). Files: `apps/marketing/models.py` (+migration additive), `frontend/src/features/marketing/`, tests. (ROUTINE) «model: haiku»
- [ ] NTMKT24 — **Heatmap d'engagement hebdomadaire par heure d'envoi (optimisation du moment d'envoi).** Sélecteur en lecture seule agrégeant les taux d'ouverture historiques (`EnvoiCampagne` existant, XMKT2) par jour-de-semaine × heure, affiché comme heatmap sur `CampagneForm.jsx` pendant la planification (« vos contacts ouvrent le plus mardi 10h ») — suggestion informative, ne bloque jamais l'envoi choisi. **Critère d'acceptation :** la heatmap se peuple depuis l'historique réel de la société, société sans historique affiche un état vide propre, tests. Files: `apps/marketing/selectors.py`, `frontend/src/features/marketing/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT25 — **Tags UTM personnalisés + générateur d'URL de campagne autonome (hors lien tracké XMKT9).** Formulaire « Générateur d'URL » dans le module Marketing : compose une URL avec utm_source/medium/campaign/content/term personnalisés pour un usage HORS campagne ERP (ex. post organique LinkedIn, signature email) sans créer de `Campagne` complète ; l'URL générée reste attribuable côté web existant (le paramétrage utm_campaign alimente déjà `PointContact`/FG34, aucun changement backend nécessaire). **Critère d'acceptation :** générer une URL, la copier, un lead créé via cette URL apparaît attribué au bon utm_campaign dans le rapport existant. Files: `frontend/src/features/marketing/UtmBuilder.jsx`, tests. (ROUTINE) «model: haiku»
- [ ] NTMKT26 — **[GATED: budget ads] Import de coûts publicitaires externes (Meta/Google Ads) dans le ROI par campagne.** Étendre XMKT17 (`cout_reel_mad` saisi manuellement) avec un import CSV (export natif Meta Ads Manager/Google Ads, mapping colonnes réutilisant le moteur dataimport existant) réconciliant le coût externe réel par `utm_campaign` au lieu de la saisie manuelle — aucun appel API externe (pas de jeton requis, juste un fichier), donc pas de coût récurrent. **Critère d'acceptation :** importer un CSV Meta Ads met à jour `cout_reel_mad` des campagnes correspondantes par nom, rapport d'import (matché/non-matché), tests. Files: `apps/marketing/services.py`, `apps/dataimport/` (réutilisation du moteur), tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — rapports imprimables, wizards, réglages tenant

- [ ] NTMKT27 — **Rapport imprimable « Bilan de campagne » (PDF interne, jamais un devis client).** Nouveau endpoint `marketing/campagnes/<id>/rapport-pdf/` générant un PDF interne (moteur WeasyPrint générique déjà utilisé par `reporting`/`compta`, PAS `quote_engine` — rule #4 réservée au devis client) : en-tête campagne, entonnoir envoyés→ouverts→cliqués→convertis, top 10 liens trackés (XMKT9), coût réel vs revenu attribué (XMKT17), watermark « Usage interne ». Bouton « Exporter le bilan PDF » sur `CampagneDetail.jsx` (NTMKT2). **Critère d'acceptation :** le PDF généré contient les 4 sections avec les vrais chiffres de la campagne, jamais de `Produit.prix_achat`, tests de génération + contenu. Files: `apps/marketing/views.py`, `apps/marketing/services.py`, `frontend/src/features/marketing/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT28 — **Rapport imprimable « Registre de consentement » (export CNDP).** Endpoint `marketing/registre-consentement/export-pdf/` : liste horodatée des `ConsentRecord`/`SuppressionMarketing` de la société sur une période, format recevable pour un contrôle CNDP (nom du traitement, base légale, date de collecte/retrait) — lecture seule via `core.selectors`, jamais un second registre. Accessible depuis Paramètres → Marketing → Conformité. **Critère d'acceptation :** l'export contient toutes les entrées de la période choisie, filtrable par contact, tests multi-tenant (aucune fuite entre sociétés). Files: `apps/marketing/views.py`, `core/selectors.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT29 — **Wizard guidé « Créer une campagne » (4 étapes).** `CampagneWizard.jsx` : étape 1 objectif (promo/newsletter/relance/événement — pré-remplit le type), étape 2 audience (choisir liste ou segment existant NTMKT4/5, compte affiché), étape 3 contenu (canal + template + blocs NTMKT23), étape 4 planification + pré-check santé (XMKT existant) + résumé avant confirmation. Remplace l'entrée directe dans `CampagneForm.jsx` pour les nouveaux utilisateurs (le formulaire direct reste accessible en mode « expert »). **Critère d'acceptation :** compléter les 4 étapes crée une `Campagne` identique à celle du formulaire direct, abandon à mi-parcours ne crée rien (aucun brouillon orphelin), tests vitest. Files: `frontend/src/features/marketing/CampagneWizard.jsx`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT2, NTMKT4, NTMKT5)
- [ ] NTMKT30 — **Wizard guidé « Configurer une séquence de relance ».** `SequenceWizard.jsx` : partir d'un modèle seedé (XMKT20) ou vierge, ajouter les étapes en glisser-empiler (délai + canal + contenu), aperçu du calendrier d'envoi type (J+0, J+3, J+7…) avant activation, contrôle bloquant si une étape référence un canal WhatsApp non configuré (XMKT10 gated — message clair au lieu d'une erreur silencieuse). **Critère d'acceptation :** créer une séquence à 3 étapes via le wizard produit les mêmes `EtapeSequence` qu'une création manuelle, tests. Files: `frontend/src/features/marketing/SequenceWizard.jsx`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT6)
- [ ] NTMKT31 — **Réglages tenant « Marketing » dans Paramètres.** Nouvel onglet `MarketingParametres.jsx` (Paramètres → Marketing) : expéditeur par défaut (nom/email/domaine XMKT33), signature de pied de campagne (lien désinscription + préférences NTMKT22 injectés automatiquement, non éditables — conformité), fenêtre silencieuse (aucun envoi avant Xh/après Yh, reconsulte `notifications.WorkingHoursConfig` via selector), plafond d'envois/jour par société (anti-spam auto-imposé), langue par défaut des templates (XMKT11). **Critère d'acceptation :** modifier le plafond d'envois bloque `envoyer_campagnes_planifiees` au-delà du seuil le même jour, tests couvrent le no-op par défaut (plafond désactivé = comportement actuel). Files: `apps/marketing/models.py` (+migration additive `ParametresMarketing`), `apps/marketing/services.py`, `frontend/src/features/parametres/`, tests. (SCHEMA) «model: sonnet»
- [ ] NTMKT32 — **Wizard guidé « Créer une enquête NPS/personnalisée ».** `EnqueteWizard.jsx` : choisir un type prédéfini (NPS pur, satisfaction post-installation, satisfaction SAV) ou personnalisé, questions pré-remplies éditables selon le type, aperçu mobile avant publication, sélection de la cible d'envoi (segment/liste/déclencheur post-événement métier via `core/events.py` — ex. `installation_terminee`). **Critère d'acceptation :** choisir « satisfaction post-installation » pré-remplit 4 questions standards éditables, tests. Files: `frontend/src/features/marketing/EnqueteWizard.jsx`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT8)

#### Profondeur (round 2) — Celery beat, permissions fines, API publique/webhooks

- [ ] NTMKT33 — **Tâche Celery beat « purge des tokens expirés » (désinscription/préférences/formulaires publics).** Nouvelle tâche `marketing.purger_tokens_expires` (nouveau fichier `apps/marketing/tasks.py`, enregistrée dans `erp_agentique/celery.py` selon le pattern existant des autres apps) planifiée quotidienne (03:00) : supprime/anonymise les tokens de désinscription (XMKT3) et de préférences (NTMKT22) expirés depuis plus de 90 jours, jamais les `ConsentRecord` eux-mêmes (rétention légale distincte, RegistreTraitement). **Critère d'acceptation :** un token de +90j est purgé, un token récent ne l'est pas, tests + entrée beat vérifiable. Files: `apps/marketing/tasks.py` (nouveau), `erp_agentique/celery.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT34 — **Tâche Celery beat « recalcul quotidien du score de maturité » (déclencheur d'inactivité).** Étend NTMKT18 : tâche planifiée quotidienne appliquant la pénalité d'inactivité 30j (le calcul événementiel de NTMKT18 ne couvre que les événements entrants, pas le silence) sur tous les `ScoreMaturite` de la société, et déclenche `core/events.py` `lead_maturite_changee` consommable par `crm.receivers` (aucun import direct crm↔marketing). **Critère d'acceptation :** un lead inactif 31 jours voit son score baisser de 10 le lendemain du seuil, tests + no-op si le paramètre société NTMKT18 est resté désactivé. Files: `apps/marketing/tasks.py`, `apps/marketing/services.py`, `core/events.py`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT18, NTMKT33)
- [ ] NTMKT35 — **Tâche Celery beat « rappel d'approbation d'envoi en attente ».** `ApprobationEnvoiCampagne` (existant, XMKT backend) peut rester bloquée sans notification ; tâche planifiée (toutes les 4h, heures ouvrées via `WorkingHoursConfig` selector) qui notifie (réutilise `notifications.Notification` existant, jamais un nouveau canal) l'approbateur désigné si la demande est en attente depuis plus de 24h. **Critère d'acceptation :** une approbation en attente depuis 25h génère une notification unique (pas de doublon au run suivant), tests. Files: `apps/marketing/tasks.py`, `apps/notifications/services.py` (appel), tests. (ROUTINE) «model: sonnet» (@after: NTMKT33)
- [ ] NTMKT36 — **Permissions fines par rôle du module Marketing (`marketing_*`, absent de `ALL_PERMISSIONS`).** `apps/roles/models.py` `ALL_PERMISSIONS` ne contient AUCUN code `marketing_*` aujourd'hui — le module entier est gardé au niveau grossier `responsable/admin` (NTMKT1). Ajouter `marketing_voir`, `marketing_creer`, `marketing_envoyer` (déclencher un envoi réel — distinct de créer/éditer, action irréversible), `marketing_approuver` (agir sur `ApprobationEnvoiCampagne`), `marketing_export`, appliqués sur les viewsets marketing existants (`IsAuthenticated` + permission check, pattern `ventes_valider`/`compta_saisir`). **Critère d'acceptation :** un rôle sans `marketing_envoyer` peut créer/éditer une campagne mais reçoit 403 sur l'action d'envoi, migration de données donnant ces permissions aux rôles `admin`/`responsable` existants (pas de régression), tests. Files: `apps/roles/models.py`, `apps/marketing/views.py`, migration `roles`, tests. (AUTH) «model: sonnet»
- [ ] NTMKT37 — **Webhooks sortants publicapi pour les événements marketing (`WebhookSubscription`).** Étendre le catalogue d'événements de `core.WebhookSubscription`/`apps.publicapi` (existant, pattern déjà utilisé par d'autres apps) avec 4 nouveaux types : `campagne.envoyee`, `campagne.ouverte`, `sequence.terminee`, `enquete.repondue` — émis via `core/events.py` depuis `apps/marketing/services.py` (jamais d'appel HTTP direct dans le code marketing, toujours par le bus + le dispatcher webhook existant). **Critère d'acceptation :** un abonnement webhook configuré sur `campagne.envoyee` reçoit un POST à l'envoi réel d'une campagne, retry/backoff réutilise le mécanisme `WebhookDelivery` existant, tests. Files: `apps/marketing/services.py`, `core/events.py`, `apps/publicapi/services.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTMKT38 — **Endpoints API publique en lecture seule pour les objets marketing (clé API `ApiKey` existante).** Exposer `SegmentMarketing` (comptage seul, jamais la liste nominative), `Campagne` (statut/statistiques agrégées), `EnqueteNPS` (résultats agrégés) en lecture seule via le gateway `apps.publicapi` existant (scope de clé API dédié `marketing:read`, pattern des scopes déjà en place), pour un futur BI externe ou intégration comptable sans donner un accès direct à la base de contacts. **Critère d'acceptation :** une clé API avec le scope `marketing:read` peut lire les stats agrégées d'une campagne mais pas la liste des contacts d'un segment, tests d'isolation par société + par scope. Files: `apps/publicapi/views.py`, `apps/marketing/selectors.py`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — imports/exports, e2e, chatter/notifications, KPI reporting

- [ ] NTMKT39 — **Export CSV/XLSX des campagnes et de leur trace d'envoi.** Bouton « Exporter » sur `CampagnesList.jsx`/`CampagneDetail.jsx` (NTMKT2) : export XLSX des campagnes filtrées (mêmes colonnes que la liste) et export CSV de la trace `EnvoiCampagne` d'une campagne (destinataire/statut/date_envoi/date_ouverture) pour analyse externe — réutilise le moteur d'export XLSX déjà en place ailleurs dans l'ERP (`reporting`/`stock`), jamais une nouvelle lib. **Critère d'acceptation :** l'export XLSX contient exactement les lignes filtrées à l'écran, l'export CSV d'une campagne de 200 envois est correctement échappé (virgules/accents), tests. Files: `apps/marketing/views.py`, `frontend/src/features/marketing/`, tests. (ROUTINE) «model: haiku» (@after: NTMKT2)
- [ ] NTMKT40 — **Export XLSX des segments et de leurs membres (audit RGPD/CNDP).** Bouton « Exporter les membres » sur `SegmentBuilder.jsx` (NTMKT4) : XLSX des contacts résolus par les règles du segment au moment de l'export (snapshot horodaté, pas une vue live), utile pour justifier une base d'envoi lors d'un contrôle. **Critère d'acceptation :** exporter un segment de test donne un fichier avec le nombre de lignes = compte affiché, tests. Files: `apps/marketing/views.py`, `frontend/src/features/marketing/`, tests. (ROUTINE) «model: haiku» (@after: NTMKT4)
- [ ] NTMKT41 — **Import CSV de contacts d'événement (inscriptions en masse hors formulaire public).** Sur `EvenementDetail.jsx` (NTMKT7), bouton « Importer des inscrits » (ex. liste de participants d'un salon partenaire) réutilisant le moteur `dataimport` existant (mapping colonnes, rapport ajoutés/doublons) pour créer des `InscriptionEvenement` en masse sans passer par le formulaire public. **Critère d'acceptation :** importer un CSV de 30 noms crée 30 `InscriptionEvenement` statut « inscrit », doublons (email déjà inscrit à cet événement) ignorés proprement avec rapport, tests. Files: `apps/marketing/services.py`, `apps/dataimport/` (réutilisation), `frontend/src/features/marketing/`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT7)
- [ ] NTMKT42 — **Test e2e Playwright : parcours complet campagne → trace → conversion.** Nouveau spec `e2e/marketing-campaign.spec.ts` (pattern E1-16 existant) : se connecter en responsable marketing, créer un segment (NTMKT4), créer et envoyer une campagne de test (NTMKT2), vérifier la trace `EnvoiCampagne` s'affiche, vérifier le KPI dashboard (NTMKT1) reflète le nouvel envoi. **Critère d'acceptation :** le spec passe en CI Linux (`web-build-test`/e2e gate existant), aucune dépendance à un vrai envoi SMTP (mock/backend console). Files: `e2e/marketing-campaign.spec.ts`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT1, NTMKT2, NTMKT4)
- [ ] NTMKT43 — **Test e2e Playwright : parcours centre de préférences self-service.** Nouveau spec couvrant NTMKT22 : ouvrir un lien `preferences/<token>` valide, décocher un canal, vérifier persistance ; ouvrir un token expiré/invalide, vérifier le message d'erreur propre (pas de 500). **Critère d'acceptation :** le spec passe en CI, couvre le cas nominal + le cas token invalide. Files: `e2e/marketing-preferences.spec.ts`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT22)
- [ ] NTMKT44 — **Notifications internes sur événements marketing clés (au-delà de l'approbation NTMKT35).** Réutilise `notifications.Notification` existant pour notifier le propriétaire d'un lead (commercial assigné) quand : son lead franchit le seuil MQL (XMKT21), son lead répond « détracteur » à une enquête NPS (score ≤6, XMKT27/NTMKT8), son lead s'inscrit à un événement (NTMKT7) — chaque notification lie vers la fiche lead. **Critère d'acceptation :** un lead franchissant le seuil MQL génère une notification unique pour son commercial assigné, pas de doublon si le seuil est déjà franchi, tests. Files: `apps/marketing/services.py`, `apps/notifications/services.py` (appel), `core/events.py`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT18)
- [ ] NTMKT45 — **Audit log des actions marketing sensibles (`audit.AuditLog` existant).** Journaliser via `apps.audit` existant (jamais un second journal) : envoi réel d'une campagne (qui/quand/combien de destinataires), export de segment/campagne (NTMKT39/40 — traçabilité RGPD des extractions), modification du modèle d'attribution société (NTMKT20). **Critère d'acceptation :** ces 3 actions apparaissent dans `AuditLog` avec l'utilisateur acteur et la société, filtrable dans l'écran d'audit existant, tests. Files: `apps/marketing/services.py`, `apps/audit/selectors.py` (appel), tests. (ROUTINE) «model: haiku»
- [ ] NTMKT46 — **KPI marketing dans le reporting central (`reporting.KpiAlerte`).** Ajouter au `TextChoices Kpi` existant de `reporting/models.py` : `TAUX_OUVERTURE_MOYEN_30J`, `TAUX_CONVERSION_MQL_SQL`, `COUT_PAR_LEAD_MOYEN` (réutilise XMKT17), `SCORE_MATURITE_MOYEN_PORTEFEUILLE` (NTMKT18) — permet à un responsable de créer une `KpiAlerte` (ex. « alerte si taux d'ouverture <15% ») sur ces valeurs marketing exactement comme sur les KPI stock/compta existants, sans nouveau moteur d'alerte. **Critère d'acceptation :** créer une alerte sur `TAUX_OUVERTURE_MOYEN_30J` avec un seuil déclenche la notification existante quand franchi, tests couvrent le calcul de chaque nouveau KPI. Files: `apps/reporting/models.py` (+migration additive choices), `apps/marketing/selectors.py`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT18)
- [ ] NTMKT47 — **Widget dashboard « Marketing » réutilisable sur `Dashboard`/`DashboardConfig` existant.** Nouveau type de widget `marketing_funnel` sélectionnable dans le constructeur de dashboard existant (`core.Dashboard`/`DashboardConfig`, pattern des widgets déjà en place) affichant l'entonnoir envoyés→ouverts→cliqués→MQL→signés sur une période configurable, insérable sur n'importe quel dashboard partagé (direction, commercial) sans dupliquer l'écran NTMKT1. **Critère d'acceptation :** ajouter le widget à un dashboard existant affiche l'entonnoir réel de la société, tests. Files: `core/models.py` (registre de widgets si applicable, additif), `apps/marketing/selectors.py`, `frontend/src/features/reporting/`, tests. (ROUTINE) «model: sonnet» (@after: NTMKT46)

#### Notes de portée (ne pas reconstruire)

- XMKT10 (canal WhatsApp dans les campagnes), XMKT34 (génération IA de contenu), XMKT35 (posts sociaux planifiés), XMKT36 (audience Meta) restent `[ ]` dans PLAN.md — NE PAS les re-proposer ici ; ce sont déjà des tâches en file.
- QJ23-25, R AG1-9, S1-8 (PLAN2) restent hors périmètre NTMKT (WhatsApp Business API générale, agent IA d'actions, messagerie interne) — frontières déjà posées par le brief.
- FG204 (attribution simple), FG34 (ROI par canal/UTM), PointContact, ConsentRecord, SuppressionMarketing, WhatsAppTemplate/MessageLog : tous existants, réutilisés (jamais dupliqués) par les tâches ci-dessus.


### Groupe NTDMO — Excellence démo/essai & onboarding produit — bench: Odoo demo data, HubSpot onboarding, Pendo/Appcues, Salesforce Trailhead-lite (audit 2026-07-08)
Déjà construit : `authentication/management/commands/seed_demo.py` (idempotent-partiel, refusé hors DEBUG sans `--force`, crée 1 société + 2 users + 8 produits + 5 clients + 5 devis un par statut + 2 BC + 2 factures + 3 leads — figé, sans historique daté, sans chantiers/tickets/SAV, sans reset). `YHARD10` (PLAN.md, non construit) est un export ANONYMISÉ d'un tenant RÉEL pour l'UAT — objectif différent (clone de prod, pas société synthétique). Aucun mode présentation (masquage), aucun moteur de checklist "time-to-value", aucun tour contextuel, aucun bouton de reset démo n'existent.
Ce groupe ajoute : une commande `seed_demo_company` best-in-class (12 mois d'historique daté relativement à `today`, cohérent sur TOUTES les stages STAGES.py + les 3 modes marché + le cycle complet devis→chantier→facture→ticket), un reset démo, un mode présentation (masquage PII), un parcours first-run par rôle persisté, et des tours contextuels dismissibles sur les 6 écrans money-path. Frontières : NTADM10 (hors lane) possède le clone-sandbox d'un tenant RÉEL ; kb/XKB22 possède la formation contenu (articles, Parcours) ; VX possède le polish visuel des composants que ce groupe réutilise sans les redessiner ; NTPLT a tagué la dev-dep Faker (déjà présente en digest_built comme dépendance taguée, réutilisée ici, jamais re-proposée comme nouvelle dépendance).

#### P1 — Société de démonstration crédible (sellability blocker : le tout premier écran vu par un prospect)
- [ ] NTDMO1 — **Command `seed_demo_company` (remplace/étend seed_demo pour un tenant riche)** : nouvelle management command `backend/django_core/authentication/management/commands/seed_demo_company.py` (app authentication, réutilise `Company`/`CustomUser` comme `seed_demo.py`) acceptant `--slug` (défaut `taqinor-demo-full`) et `--force` (même garde ERR88 : refuse hors `settings.DEBUG` sans `--force`, mots de passe connus documentés dans le help). Crée une Company distincte de `taqinor-demo` (jamais de collision avec `seed_demo` existant), avec CompanyProfile complet (identité légale ICE/IF/RC placeholder réalistes, logo par défaut). Idempotent par `get_or_create` sur le slug ; si déjà peuplée, no-op avec message. Critère d'acceptation : `python manage.py seed_demo_company` exécuté deux fois de suite ne crée aucun doublon (vérifié par count stable). Files: `backend/django_core/authentication/management/commands/seed_demo_company.py` (nouveau), tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO2 — **Leads répartis sur les 6 stages STAGES.py, datés relativement à `timezone.now()`** : dans `seed_demo_company`, générer ~40 `crm.Lead` important les clés depuis `STAGES.py` (jamais de liste en dur) avec une distribution réaliste (plus de NEW/CONTACTED récents que de SIGNED anciens, quelques COLD avec `lost=True`+`lost_reason` peuplé depuis `MotifPerte`), `date_creation` étalées sur les 12 derniers mois via `timedelta(days=random.randint(...))` ancré sur `timezone.now()` (jamais de date en dur — le seed reste vivant si rejoué dans 6 mois). Canal/priorité/tags/type_installation variés ; quelques leads avec `relance` future proche (aujourd'hui+1 à +5j) pour peupler le calendrier et les alertes. Critère d'acceptation : `Lead.objects.filter(company=demo_co).values('stage').annotate(...)` retourne un lead sur chacune des 6 clés STAGES + au moins 3 `lost=True`. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, `STAGES.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO3 — **Devis couvrant les 3 modes marché avec historique de statuts** : générer ~25 `ventes.Devis` répartis Résidentiel / Industriel-Commercial (avec `etude_params` d'autoconsommation peuplé) / Agricole (pompage, `courbe_pompe` cohérente avec les produits OSP déjà seedés par `seed_catalogue` — réutilise le catalogue existant, n'invente pas de nouveaux produits), aux statuts brouillon/envoyé/accepté/refusé/expiré dans des proportions réalistes (~40% signés pour un pipeline convaincant), `date_creation` étalée sur 12 mois, options Sans-batterie/Avec-batterie sur au moins 3 devis Résidentiel signés pour illustrer A1-A3. Réutilise `apps/ventes/utils/references.py` pour la numérotation (jamais `count()+1`). Critère d'acceptation : le dashboard "pipeline par stage" et "devis→signé" affichent des barres non-vides sur au moins 8 des 12 mois. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, `apps/ventes/utils/references.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO4 — **Chaîne chantier→facture→paiement avec balance âgée vivante** : pour chaque devis accepté de NTDMO3, créer le chantier (réutilise `installations.services` existant, jamais de duplication de la logique `creer-depuis-devis`), avec dates de pose/réception étalées sur l'historique, `Installation` liée en aval quand `Réceptionné`. Créer les `Facture`/`Paiement`/échéancier correspondants avec un mélange volontaire : factures payées à temps, factures en retard (`date_echeance` dépassée, non payées, pour peupler la balance âgée), factures partiellement réglées. Critère d'acceptation : `/reporting/balance-agee` affiche au moins 3 tranches d'ancienneté non-vides pour la société démo. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, `apps/installations/services.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO5 — **Tickets SAV, contrats de maintenance et mouvements de stock sur 12 mois** : compléter `seed_demo_company` avec ~15 `sav.Ticket` (statuts variés, quelques-uns avec `PieceConsommee` décrémentant le stock), 3-4 `ContratMaintenance` actifs avec échéances de visite à venir, et des `MouvementStock` d'entrée/sortie/transfert étalés sur 12 mois pour que le rapport stock et la valorisation ne soient pas plats. Réutilise les sélecteurs/services existants de chaque app cible (jamais d'écriture directe cross-app). Critère d'acceptation : `/sav` liste des tickets sur au moins 4 statuts différents ; `/stock/mouvements` affiche des mouvements sur au moins 6 mois distincts. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO6 — **Commande de reset démo (`reset_demo_company`)** : nouvelle command `reset_demo_company` (app authentication) qui supprime intégralement UNE société démo par `--slug` (garde-fou : refuse si le slug ne contient pas `demo` ou si `--slug` n'est pas explicitement fourni, pour ne jamais effacer une société réelle par erreur) puis ré-invoque `seed_demo_company` avec les mêmes paramètres. Utilise une transaction + suppression en cascade via les FK existantes (jamais de SQL brut). Critère d'acceptation : `python manage.py reset_demo_company --slug taqinor-demo-full` exécuté sur une société déjà peuplée la vide puis la re-peuple à l'identique en nombre d'enregistrements (±variation aléatoire documentée). Files: `backend/django_core/authentication/management/commands/reset_demo_company.py` (nouveau), tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO7 — **Endpoint + bouton admin "Réinitialiser les données de démonstration"** : `POST /api/django/authentication/companies/{id}/reset-demo/` (app authentication, restreint : 403 si la société n'a pas de flag `est_demo` — ajouter ce booléen additif sur `Company`, jamais exposé en écriture publique) qui invoque `reset_demo_company` en tâche Celery best-effort (ou synchrone si Celery absent) et notifie l'utilisateur via `notifications.notify()` à la fin. Bouton dans `/admin/users` ou nouvel onglet Paramètres, visible uniquement quand `company.est_demo` est vrai. Critère d'acceptation : cliquer le bouton sur une société démo déclenche le reset et une notification in-app de confirmation ; le bouton est absent sur une société non-démo. Files: `backend/django_core/authentication/views.py`, `backend/django_core/authentication/models.py` (+migration est_demo), `frontend/src/features/parametres/`, tests. (ROUTINE) «model: sonnet»

#### P1 — Mode présentation (masquage sécurisé pour démos live devant prospect)
- [ ] NTDMO8 — **`Company.est_demo` + `CompanyProfile.mode_presentation_actif`** : deux booléens additifs (`Company.est_demo` par défaut False, posé par `seed_demo_company` ; `CompanyProfile.mode_presentation_actif` par défaut False, togglable par un admin de la société démo uniquement). Migration additive. Critère d'acceptation : toggler `mode_presentation_actif` en base ne change le comportement d'AUCUNE société où il reste False (non-régression totale). Files: `backend/django_core/authentication/models.py` (+migration), tests. (SCHEMA) «model: haiku»
- [ ] NTDMO9 — **Middleware/mixin de masquage des champs sensibles en mode présentation** : `core/serializer_mixins.py` (déjà porteur de `SerializerMaskMixin` selon les tests existants — étendre, ne pas dupliquer) : quand `request.user.company.profil.mode_presentation_actif` est vrai, masquer email/téléphone/adresse exacte des `Client`/`Lead` (garder ville, masquer le reste par `***@***`/`06 ** ** ** **`) dans les serializers CRM/ventes en LECTURE seule — jamais en écriture, jamais sur les factures officielles (rule #4 : ne touche pas au PDF `/proposal`). Critère d'acceptation : avec le mode actif, `GET /api/django/crm/clients/` renvoie des champs masqués pour la société concernée uniquement ; test couvrant qu'une société non-démo n'est jamais affectée même si le flag est manipulé. Files: `backend/django_core/core/serializer_mixins.py`, tests. (AUTH) «model: opus»
- [ ] NTDMO10 — **Bannière "Mode présentation actif" + toggle Paramètres** : bandeau discret en haut de l'app (composant réutilisant le design system existant, pas de nouveau composant lourd) visible uniquement quand `mode_presentation_actif` est vrai pour la société courante, avec toggle accessible depuis Paramètres → Société (visible seulement si `company.est_demo`). Critère d'acceptation : le toggle persiste après refresh et n'apparaît jamais pour une société non-démo. Files: `frontend/src/features/parametres/`, `frontend/src/components/`, tests. (ROUTINE) «model: sonnet»

#### P1 — Parcours first-run par rôle (time-to-value)
- [ ] NTDMO11 — **Modèle `OnboardingChecklistItem` + `OnboardingProgress`** : nouvelle app légère `onboarding` (ou modèles dans `authentication` si plus simple — décision au build) avec `OnboardingChecklistItem` (clé stable, rôle cible NULL=tous ou liste de rôles, ordre, libellé FR, lien vers l'écran cible) et `OnboardingProgress` (company FK + user FK + item FK + `complete_le` nullable, unique_together user+item). Seed initial via migration data ou command idempotente : "Importer vos clients", "Créer votre 1er devis", "Encaisser votre 1er paiement", "Inviter un coéquipier", "Configurer votre société" (5-6 items différenciés par rôle Commercial vs Administrateur vs Technicien via `roles.Role`). Critère d'acceptation : deux users de rôles différents dans la même société voient des listes d'items potentiellement différentes, toujours filtrées company-scoped. Files: `backend/django_core/apps/onboarding/models.py` (nouveau, +migration), tests. (SCHEMA) «model: sonnet»
- [ ] NTDMO12 — **Auto-complétion des items via le bus d'événements `core/events.py`** : abonner l'auto-complétion de chaque item à un événement domain existant plutôt qu'à un polling (ex. "créer 1er devis" s'auto-coche sur le signal déjà émis à la création d'un `Devis` — même mécanisme que `ventes` émettant `devis_accepted` consommé par `crm`). Ajout d'un souscripteur léger dans l'app onboarding (`apps.py::ready()`), jamais d'import direct des modèles ventes/crm/stock (respecte la frontière cross-app). Critère d'acceptation : créer un premier devis dans l'UI coche automatiquement l'item correspondant sans action manuelle, visible au prochain fetch du endpoint. Files: `backend/django_core/apps/onboarding/apps.py`, `backend/django_core/apps/onboarding/receivers.py` (nouveau), `core/events.py`, tests. (ARCH) «model: opus»
- [ ] NTDMO13 — **Endpoint `GET/PATCH onboarding/progress/` + widget "Premiers pas" sur le Dashboard** : ViewSet retournant la checklist résolue pour l'utilisateur courant (items + statut fait/à faire + lien), avec action `{id}/ignorer/` pour masquer manuellement un item sans le marquer fait (persistant, jamais de simple state local perdu au refresh). Widget compact sur `/dashboard` (carte "Premiers pas — 2/6" avec barre de progression, se masque automatiquement une fois 100% ou après `ignorer-tout`). Critère d'acceptation : la progression persiste après déconnexion/reconnexion et diffère par utilisateur au sein d'une même société. Files: `backend/django_core/apps/onboarding/views.py`, `frontend/src/pages/dashboard/`, tests. (ROUTINE) «model: sonnet»

#### P2 — Tours contextuels dismissibles (différenciateur produit-led)
- [ ] NTDMO14 — **Modèle `ProductTourStep` + tracking de complétion par utilisateur** : catalogue statique (fixture JSON ou seed idempotent, pas de CRUD admin en V1) de 6 tours ciblant les écrans money-path déjà construits : `/ventes/devis/nouveau` (création devis), `/crm/leads` (kanban), `/ventes/factures` (facturation), `/chantiers` (suivi chantier), `/stock` (catalogue), `/dashboard` (lecture des KPI). Chaque tour = liste ordonnée d'étapes {sélecteur CSS ou id d'ancre, titre, texte FR court}. `TourProgress` (user FK, tour_key, vu_le nullable) pour ne jamais re-montrer un tour terminé/fermé. Critère d'acceptation : le catalogue des 6 tours est chargé sans requête réseau bloquante (bundlé côté frontend ou fetch léger unique au login). Files: `backend/django_core/apps/onboarding/models.py` (+migration ProductTourStep/TourProgress), `frontend/src/`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO15 — **Composant `<ProductTour>` réutilisant les primitives Popover/Tooltip existantes** : overlay séquentiel positionné sur l'élément ciblé (réutilise `ui/Popover` du design system, pas de nouvelle lib de tour — aucune dépendance payante type Pendo/Appcues), boutons Suivant/Passer/Terminer, `Échap` ferme, jamais bloquant (pas de fond opaque plein écran empêchant l'usage réel de l'écran). Se déclenche automatiquement à la première visite de chaque écran cible pour un nouvel utilisateur (moins de 30 jours depuis `date_joined`), jamais si déjà vu ou passé. Critère d'acceptation : test e2e couvrant qu'un tour apparaît une fois puis ne réapparaît plus après fermeture, sur au moins un des 6 écrans. Files: `frontend/src/components/ProductTour.jsx` (nouveau), tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO16 — **Endpoint `POST tours/{key}/vu/` + bouton "Revoir les visites guidées" dans Paramètres** : marque `TourProgress.vu_le`, idempotent. Ajout d'un contrôle dans Paramètres (section Équipe & rôles ou nouvel onglet léger) listant les 6 tours avec statut vu/non-vu et bouton "Revoir" par tour (reset manuel du `vu_le` pour ce tour uniquement). Critère d'acceptation : cliquer "Revoir" sur un tour déjà vu le refait apparaître à la prochaine visite de l'écran cible. Files: `backend/django_core/apps/onboarding/views.py`, `frontend/src/features/parametres/`, tests. (ROUTINE) «model: haiku»

#### P2 — Qualité et réalisme des données synthétiques
- [ ] NTDMO17 — **Utilise Faker (dev-dep déjà taguée) pour varier noms/adresses/entreprises au lieu de listes en dur** : refactorer `seed_demo_company` (NTDMO1-5) pour générer les identités (noms clients/leads, raisons sociales, adresses villes marocaines) via `Faker('fr_FR')` avec une seed fixe (`Faker.seed(42)`) pour reproductibilité déterministe entre deux exécutions — pas de nouvelle dépendance de production (Faker reste dev-only, déjà taguée ailleurs dans le repo). Critère d'acceptation : deux exécutions de `reset_demo_company` produisent des jeux de données identiques (mêmes noms), preuve de déterminisme testée. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO18 — **Notifications de démonstration pré-peuplées (bell non-vide au premier login)** : `seed_demo_company` crée 5-8 `notifications.Notification` réalistes via le service `notify()` existant (retard facture, devis expirant, relance due, garantie proche échéance) pour que la cloche de notification du premier utilisateur connecté ne soit jamais vide. Critère d'acceptation : `demo_admin` connecté pour la première fois voit un badge non-nul sur la cloche. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTDMO19 — **Champs personnalisés d'exemple pré-configurés** : `seed_demo_company` crée 2-3 `customfields.CustomFieldDef` d'exemple (ex. "Type de toiture" choice sur LEAD, "N° de série chantier" text sur CLIENT) pour que Paramètres → Avancé → Champs personnalisés ne soit jamais un écran vide en démo, illustrant la fonctionnalité sans configuration manuelle. Critère d'acceptation : un nouvel évaluateur ouvrant Paramètres sur le tenant démo voit au moins 2 champs personnalisés déjà configurés et utilisés sur au moins un enregistrement. Files: `backend/django_core/authentication/management/commands/seed_demo_company.py`, tests. (ROUTINE) «model: haiku»

#### P3 — Délices (polish différenciant, non bloquant pour la vente)
- [ ] NTDMO20 — **Bannière d'expiration d'essai configurable (fondation pour un futur mode "essai limité dans le temps")** : `CompanyProfile.essai_expire_le` (date nullable, additive, jamais peuplé automatiquement — founder-only via Paramètres/admin) ; quand renseignée et dépassée, bannière non-bloquante "Votre essai a expiré — contactez-nous" sur toutes les pages (jamais un blocage dur qui casserait une société de production existante par erreur de saisie). Pas de logique de facturation/paiement liée (hors-scope, gated future). Critère d'acceptation : le champ vide (comportement actuel) ne change rien pour aucune société existante ; renseigné et dépassé, la bannière apparaît sans bloquer aucune action. Files: `backend/django_core/authentication/models.py` (+migration essai_expire_le), `frontend/src/components/`, tests. (DECISION) «model: sonnet»
- [ ] NTDMO21 — **Résumé "Ce que vous avez accompli ce mois-ci" sur le Dashboard démo** : widget optionnel (visible uniquement `company.est_demo`) résumant en une phrase les données générées (ex. "12 devis envoyés, 5 chantiers réceptionnés, 45 200 MAD facturés ce mois — données de démonstration") pour aider un commercial à raconter une histoire cohérente en rendez-vous, réutilisant les sélecteurs `reporting` existants (jamais de nouvelle agrégation dupliquée). Critère d'acceptation : le widget n'apparaît jamais sur une société non-démo. Files: `frontend/src/pages/dashboard/`, `apps/reporting/selectors.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTDMO22 — **Export "kit de démonstration" (guide PDF one-pager du scénario de démo)** : action admin générant un PDF léger (réutilise le moteur WeasyPrint déjà présent pour les PDFs internes — jamais le moteur premium `/proposal` réservé aux devis clients, rule #4) résumant le scénario de démo (quels comptes, quels écrans visiter dans quel ordre, quelles données illustrent quoi) pour aider un commercial TAQINOR à préparer une démo prospect sans improviser. Critère d'acceptation : le PDF se génère en moins de 3s et liste au moins les 6 écrans money-path des tours (NTDMO14). Files: `backend/django_core/authentication/views.py`, tests. (ROUTINE) «model: haiku»

#### Profondeur (round 2) — rapports & documents imprimables
- [ ] NTDMO23 — **Rapport "Scénario de démo" imprimable par écran (pas le PDF client)** : étendre l'action admin de NTDMO22 avec une variante HTML imprimable (`GET /api/django/authentication/companies/{id}/demo-kit/?format=html`) rendue par le moteur de templates Django interne existant (jamais WeasyPrint réservé au PDF officiel, jamais `/proposal`) listant pour chacun des 6 écrans money-path : le compte à utiliser, les 2-3 enregistrements démo les plus parlants (ex. le devis Agricole signé le plus récent), et le chiffre clé à mentionner à l'oral. Réservé `company.est_demo`. Critère d'acceptation : la page s'ouvre dans un nouvel onglet et s'imprime sur une seule page A4 via `window.print()`. Files: `backend/django_core/authentication/views.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTDMO24 — **Export XLSX "Journal des 12 mois de démo"** : action `GET /api/django/authentication/companies/{id}/demo-kit/export.xlsx` (réutilise `openpyxl`, déjà dépendance existante ailleurs dans le repo pour d'autres exports XLSX — jamais de nouvelle lib) listant en 4 onglets (Leads, Devis, Chantiers, Factures) les enregistrements générés par `seed_demo_company` avec leurs dates relatives, pour qu'un commercial TAQINOR puisse vérifier hors-ligne la cohérence du scénario avant un rendez-vous. Réservé `company.est_demo`. Critère d'acceptation : le fichier généré s'ouvre sans erreur dans Excel/LibreOffice et contient au moins les 4 onglets non-vides. Files: `backend/django_core/authentication/views.py`, tests. (ROUTINE) «model: haiku»

#### Profondeur (round 2) — assistants/wizards guidés
- [ ] NTDMO25 — **Wizard "Créer ma société de démonstration" (3 étapes, UI)** : écran `/admin/demo/nouveau` (visible seulement aux super-admins TAQINOR — même garde que le reste de l'admin technique) enveloppant NTDMO1 en 3 étapes guidées : (1) choix du secteur/scénario (Résidentiel-only / Industriel-only / mix complet — passe un paramètre `--profil` additif à `seed_demo_company`), (2) choix de la densité d'historique (léger ~15 leads / complet ~40 leads — paramètre `--densite`), (3) récapitulatif + bouton "Générer" déclenchant la command en tâche Celery avec barre de progression (poll sur un `AgentActionLog`-like statut, réutilise le pattern de suivi de tâche déjà présent pour d'autres imports longs). Critère d'acceptation : lancer le wizard avec le profil "léger" crée une société avec sensiblement moins d'enregistrements que le profil "complet", vérifiable par comptage. Files: `frontend/src/features/admin/DemoWizard.jsx` (nouveau), `backend/django_core/authentication/management/commands/seed_demo_company.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO26 — **Wizard first-run "Configurez votre société en 5 minutes" (société réelle, pas démo)** : écran `/onboarding/demarrage` déclenché automatiquement à la toute première connexion d'un administrateur sur une société NON-démo fraîchement créée (détecté via `Company.date_creation` récente + aucun `OnboardingProgress` existant), guidant logo/coordonnées légales (CompanyProfile), premier produit du catalogue, premier utilisateur invité, en 4 étapes avec possibilité de "Passer, je configurerai plus tard" à tout moment (jamais bloquant). Consomme/complète directement les items pertinents de `OnboardingChecklistItem` (NTDMO11) au fur et à mesure. Critère d'acceptation : "Passer" ferme le wizard définitivement pour cet utilisateur (jamais de réapparition automatique), et les items complétés via le wizard apparaissent cochés dans le widget "Premiers pas" (NTDMO13). Files: `frontend/src/pages/onboarding/DemarrageWizard.jsx` (nouveau), `backend/django_core/apps/onboarding/views.py`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — réglages/paramètres par tenant
- [ ] NTDMO27 — **Onglet Paramètres → "Démo & Onboarding" (regroupement des toggles du groupe)** : nouvel onglet dans Paramètres (visible selon `company.est_demo` pour la section démo, visible pour tous pour la section onboarding) regroupant : toggle mode présentation (NTDMO10), bouton reset démo (NTDMO7), bouton "Revoir les visites guidées" par tour (NTDMO16), et un toggle global "Activer les tours contextuels pour les nouveaux utilisateurs" (`CompanyProfile.tours_actifs`, additif, défaut True) permettant à une société qui ne veut plus jamais voir de tours de le désactiver une fois pour toutes. Critère d'acceptation : désactiver `tours_actifs` empêche l'apparition de tout `<ProductTour>` pour tout nouvel utilisateur de la société, vérifié par test. Files: `frontend/src/features/parametres/`, `backend/django_core/authentication/models.py` (+migration tours_actifs), tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO28 — **Paramètre par tenant "Items de checklist onboarding actifs"** : dans le même onglet, une liste à cocher (admin uniquement) des `OnboardingChecklistItem` globaux permettant à une société de masquer un item non pertinent pour son activité (ex. masquer "Configurer le pompage agricole" pour une société 100% résidentielle) via une table de jonction additive `OnboardingChecklistItem` ↔ `Company` (masqués), jamais de suppression du catalogue global. Critère d'acceptation : un item masqué par une société n'apparaît plus dans son widget "Premiers pas" mais reste visible pour les autres sociétés. Files: `backend/django_core/apps/onboarding/models.py` (+migration), `frontend/src/features/parametres/`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — Celery beat (rappels, recalculs, purges)
- [ ] NTDMO29 — **Tâche planifiée quotidienne : rappel des relances leads démo "vivantes"** : tâche Celery beat (ajout à `CELERY_BEAT_SCHEDULE`, pattern déjà présent — voir `apps/ventes/scheduled.py`) exécutée une fois par jour qui, pour chaque société `est_demo=True`, ré-ancre les `date_relance` des leads passées à `timezone.now()+timedelta(days=random.randint(1,5))` (jamais sur une société réelle — filtre strict `est_demo=True`) afin qu'une démo lancée un mois après sa création montre toujours des relances "à venir" crédibles plutôt que toutes en retard. Critère d'acceptation : une société démo dont toutes les relances sont passées voit, après exécution de la tâche, au moins 3 relances futures ; une société non-démo n'est jamais touchée (test dédié). Files: `apps/crm/tasks.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO30 — **Tâche planifiée : purge automatique des sociétés démo TAQINOR expirées (staging/marketing uniquement)** : tâche Celery beat hebdomadaire qui supprime (via le même mécanisme cascade que `reset_demo_company`, jamais de SQL brut) toute société `est_demo=True` dont `CompanyProfile.essai_expire_le` (NTDMO20) est dépassé de plus de 90 jours, avec un log dans `AuditLog` avant suppression. Garde-fou : ne s'exécute que si `settings.DEMO_AUTO_PURGE_ENABLED` (nouveau flag env, défaut False — désactivé en production tant que le founder n'active pas explicitement). Critère d'acceptation : le flag à False (comportement actuel) ne supprime jamais aucune société, même expirée depuis longtemps ; activé en environnement de test, seules les sociétés démo expirées de +90j sont supprimées. Files: `backend/django_core/authentication/management/commands/reset_demo_company.py`, `backend/django_core/authentication/tasks.py` (nouveau), tests. (COST) «model: sonnet»
- [ ] NTDMO31 — **Tâche planifiée : recalcul nocturne du widget "Ce que vous avez accompli ce mois-ci"** : tâche Celery beat quotidienne (nuit, faible charge) qui pré-calcule et met en cache (réutilise le cache Redis déjà en place pour d'autres agrégats reporting) le résumé mensuel de NTDMO21 pour chaque société démo, pour que le widget dashboard s'affiche instantanément au lieu de recalculer à chaque chargement de page. Critère d'acceptation : le endpoint dashboard sert le résumé depuis le cache (mesuré : pas de requête d'agrégation lourde synchrone dans le path de la requête HTTP). Files: `backend/django_core/authentication/tasks.py`, `apps/reporting/selectors.py`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — permissions par rôle fines
- [ ] NTDMO32 — **Permission dédiée `peut_reinitialiser_demo` (rôle) distincte de "Administrateur société"** : nouvelle permission fine dans le registre `roles.Role`/`core/permissions.py` (pattern déjà existant pour les permissions granulaires ailleurs dans le repo) contrôlant qui peut déclencher `reset-demo/` (NTDMO7) — par défaut accordée uniquement au rôle Administrateur, mais configurable pour permettre à un rôle "Commercial démo" de reset sans avoir tous les droits admin (utile quand un commercial TAQINOR fait des démos en autonomie sans compte admin complet). Critère d'acceptation : un utilisateur du rôle Commercial sans la permission reçoit 403 sur `reset-demo/` ; accordée explicitement, l'action réussit. Files: `apps/roles/models.py`, `backend/django_core/authentication/views.py`, tests. (AUTH) «model: sonnet»
- [ ] NTDMO33 — **Le mode présentation et le masquage PII restent invisibles aux rôles Technicien/Terrain** : vérifier + verrouiller que le toggle `mode_presentation_actif` (NTDMO10) et l'onglet Paramètres associé (NTDMO27) ne sont exposés qu'aux rôles ayant la permission `gerer_parametres_societe` déjà utilisée ailleurs dans Paramètres (jamais une nouvelle permission redondante) — un technicien connecté sur une société démo ne doit jamais pouvoir désactiver le masquage PII par erreur depuis son écran mobile. Critère d'acceptation : test d'accès confirmant 403 sur le PATCH du toggle pour un rôle Technicien. Files: `backend/django_core/authentication/views.py`, tests. (AUTH) «model: sonnet»

#### Profondeur (round 2) — API publique & webhooks
- [ ] NTDMO34 — **Webhook `onboarding.item_complete` sur le bus `publicapi`** : ajouter le type d'événement `onboarding.item_complete` au registre des `Webhook`/`WebhookDelivery` existant (app `publicapi`, réutilise `WebhookSubscription`/`WebhookDelivery` déjà modélisés dans `core`/`publicapi` — jamais un nouveau système de webhook parallèle) émis quand un `OnboardingProgress` passe à complet, pour permettre à un futur outil externe (ex. un CRM marketing du founder) de suivre l'activation des nouveaux tenants. Payload minimal : `company_id`, `user_id`, `item_key`, `complete_le` — jamais de PII au-delà de l'ID utilisateur. Critère d'acceptation : une souscription webhook active sur cet événement reçoit un `WebhookDelivery` à la complétion d'un item, testé en local avec un endpoint mock. Files: `backend/django_core/apps/onboarding/services.py`, `apps/publicapi/services.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO35 — **Endpoint public en lecture seule `GET onboarding/progress/resume/` pour badge externe** : endpoint additif retournant uniquement un pourcentage agrégé (`{"pourcentage": 66}`, jamais le détail des items ni de PII) protégé par `ApiKey` scope restreint (réutilise `publicapi.ApiKey` existant, jamais un nouveau mécanisme d'auth), pensé pour un futur badge d'activation affiché dans un outil externe founder (ex. tableau de bord commercial). Critère d'acceptation : appelé avec une clé API valide scope "lecture", retourne le pourcentage ; sans clé ou scope insuffisant, 401/403. Files: `backend/django_core/apps/onboarding/views.py`, `apps/publicapi/`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — imports/exports CSV-XLSX
- [ ] NTDMO36 — **Export CSV du catalogue `ProductTourStep` + `OnboardingChecklistItem` pour traduction/relecture externe** : action admin `GET .../onboarding/export-textes.csv` exportant tous les libellés FR des tours (NTDMO14) et des items de checklist (NTDMO11) en CSV à 3 colonnes (clé stable, écran cible, texte FR) pour que le founder ou un rédacteur externe puisse relire/corriger le texte produit hors-ERP dans un tableur, sans toucher au code. Réutilise `dataimport.ImportMapping` en lecture pour la ré-importation symétrique (colonne "clé stable" comme identifiant de correspondance). Critère d'acceptation : le CSV exporté peut être modifié (texte FR) puis réimporté via `POST .../onboarding/import-textes.csv` qui met à jour les libellés existants sans créer de doublon (matché par clé stable). Files: `backend/django_core/apps/onboarding/views.py`, `apps/dataimport/`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — tests e2e
- [ ] NTDMO37 — **Test e2e : parcours complet "premier login sur société démo fraîche"** : scénario Playwright (suit le pattern e2e E1-E16 déjà en place) qui exécute `seed_demo_company` en fixture, se connecte avec l'utilisateur admin démo, vérifie que (1) le widget "Premiers pas" affiche une progression non-nulle, (2) au moins un `<ProductTour>` se déclenche sur `/dashboard`, (3) la bannière mode présentation N'apparaît PAS par défaut (flag False à la création), (4) le bouton "Réinitialiser les données de démonstration" est visible dans Paramètres. Critère d'acceptation : le test passe en CI sur une base fraîchement seedée, échoue si l'un des 4 points régresse. Files: `e2e/demo-first-login.spec.ts`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO38 — **Test e2e : le reset démo ne casse jamais une société réelle voisine** : scénario Playwright/pytest créant une société réelle ET une société démo dans le même run, déclenchant `reset-demo/` sur la société démo, puis vérifiant qu'aucun `Client`/`Devis`/`Facture` de la société réelle n'a été touché (comptage avant/après identique). Ce test constitue le garde-fou non-régression le plus critique du groupe (une purge cross-tenant serait un incident de données grave). Critère d'acceptation : le test échoue immédiatement si `reset_demo_company` est un jour modifié pour accepter un slug ambigu ou omettre le filtre `company=`. Files: `e2e/demo-reset-isolation.spec.ts`, tests. (ROUTINE) «model: sonnet»

#### Profondeur (round 2) — chatter/records, notifications, audit
- [ ] NTDMO39 — **Entrées `records.Activity` automatiques sur les actions démo (reset, mode présentation)** : chaque déclenchement de `reset-demo/` (NTDMO7) et chaque bascule de `mode_presentation_actif` (NTDMO10) crée une `records.Activity` (réutilise le chatter générique existant, jamais un journal parallèle) rattachée à la `Company`, avec l'utilisateur acteur et l'horodatage, visible dans l'historique déjà affiché ailleurs dans l'ERP pour tout objet suivi. Critère d'acceptation : après un reset démo, l'onglet historique de la fiche société affiche une entrée "Données de démonstration réinitialisées par {user}" horodatée. Files: `backend/django_core/authentication/views.py`, tests. (ROUTINE) «model: haiku»
- [ ] NTDMO40 — **Toutes les actions démo/onboarding passent par `AuditLog`** : `reset-demo/`, la purge automatique (NTDMO30), et le masquage/démasquage du mode présentation écrivent systématiquement dans `audit.AuditLog` (réutilise le modèle existant, jamais de nouveau journal), avec l'IP/user-agent déjà capturés par le middleware d'audit en place ailleurs. Critère d'acceptation : `AuditLog.objects.filter(action__in=[...]).count()` augmente de 1 exactement à chaque déclenchement, vérifiable en test. Files: `backend/django_core/authentication/views.py`, `apps/audit/`, tests. (ROUTINE) «model: haiku»

#### Profondeur (round 2) — KPI reporting
- [ ] NTDMO41 — **KPI "Temps moyen jusqu'à la 1ère valeur" (time-to-value) dans le module reporting** : nouveau `Kpi`/entrée dans `reporting` (réutilise le modèle `Kpi`/`KpiAlerte` existant du digest, jamais une nouvelle table parallèle) calculant, pour les sociétés NON-démo, le délai médian entre `Company.date_creation` et la complétion du dernier item obligatoire de `OnboardingChecklistItem` (NTDMO11), segmenté par secteur d'activité si connu. Visible uniquement à l'équipe TAQINOR (super-admin), jamais exposé aux tenants clients (donnée interne d'exploitation produit). Critère d'acceptation : le KPI se recalcule via la tâche planifiée reporting existante et affiche une valeur cohérente (médiane en jours) sur au moins 2 sociétés de test ayant complété leur onboarding à des rythmes différents. Files: `apps/reporting/models.py`, `backend/django_core/apps/onboarding/selectors.py`, tests. (ROUTINE) «model: sonnet»
- [ ] NTDMO42 — **KPI "Taux d'adoption des tours contextuels" par écran** : agrégat reporting (interne TAQINOR uniquement, même restriction que NTDMO41) comptant, pour chacun des 6 tours (NTDMO14), combien d'utilisateurs l'ont vu jusqu'au bout vs. passé/fermé prématurément (`TourProgress` déjà modélisé en NTDMO14), pour permettre au founder d'identifier quel tour est mal calibré et doit être retravaillé. Critère d'acceptation : le rapport liste les 6 tours avec un pourcentage de complétion distinct pour chacun, recalculé quotidiennement par la tâche NTDMO31 (regroupée dans le même job de recalcul nocturne pour éviter une tâche planifiée redondante). Files: `apps/reporting/models.py`, `backend/django_core/authentication/tasks.py`, tests. (ROUTINE) «model: haiku»

---
**Notes de couverture.** 42 tâches au total (P1: 13, P2: 6, P3: 3, Profondeur round 2 : 20) — domaine volontairement resserré : le brief définit un périmètre précis (démo + 30 premiers jours) déjà partiellement construit (`seed_demo.py`), donc moins de surface neuve que les lanes 40+ écrans/API. Le round 2 couvre les 10 thèmes demandés : rapports imprimables non-PDF-client (NTDMO23-24), wizards guidés démo + onboarding réel (NTDMO25-26), réglages par tenant (NTDMO27-28), Celery beat rappels/purge/recalcul (NTDMO29-31), permissions fines par rôle (NTDMO32-33), API publique + webhooks (NTDMO34-35), import/export CSV (NTDMO36), tests e2e critiques (NTDMO37-38), chatter/notifications/audit (NTDMO39-40), KPI reporting interne (NTDMO41-42). Une seule tâche gated parmi les ajouts (NTDMO30, COST — purge automatique désactivée par défaut, le founder doit l'activer explicitement). 2 tâches AUTH ajoutées (NTDMO32-33) suivent la même prudence que NTDMO9/12 du round 1. Aucune tâche du round 2 ne duplique digest_open.md (aucun groupe onboarding/démo/tour n'y figure) ni digest_built_v2.md (aucun modèle `OnboardingChecklistItem`/`ProductTourStep`/`TourProgress` n'existe encore en base).


