RUN: field-execution & outillage module (F1–F24)
Started: 2026-06-17
Work branch: dev-field-exec  (do ALL work here; main untouched until the whole module is built)

PACED RUN — resume model (per Reda's instruction, overrides the generic "a batch is already
running, stop" rule for THIS run only):
- This is one logical "work on the plan" run that spans MULTIPLE fires because it paces against
  the usage window. Re-firing "work on the plan" RESUMES on dev-field-exec from the first
  unchecked F-task — it does NOT start a second batch and must NOT refuse.
- Each fire: checkout dev-field-exec, read docs/PLAN.md (the ticks on dev-field-exec are the
  source of truth for progress), build the next [ ] F-task(s) with tests, commit + tick each on
  dev-field-exec.
- Do NOT merge dev-field-exec → main until ALL buildable F-tasks (F1–F23) are integrated and CI
  is fully green (with MinIO). Only then open one PR and self-merge (the founder's "one session,
  one merge, never split into review PRs" constraint).
- Module constraints: STAGES.py untouched & never runtime-editable; Intervention.statut is its own
  state machine (never touches STAGES.py or chantier status); no buy prices on any client-facing
  doc; voice/OCR/AI-QA add no credential/cost by default and no-op until configured (transcription
  left unconfigured); all photos/voice/GPS = real data kept off the repo; DEBUG unchanged.

PROGRESS (this file is a convenience pointer; the [x] ticks in docs/PLAN.md on dev-field-exec are
authoritative):
- F1 Outillage catalogue — built + committed + pushed on dev-field-exec; frontend builds +
  lints green locally, backend py_compile-clean, backend tests written but UNVALIDATED.
- F2 Kits d'outillage — built + committed on dev-field-exec; frontend builds+lints green,
  backend py_compile-clean, backend tests written but UNVALIDATED.
- F3 Intervention spine — built + committed on dev-field-exec (extended existing model: statut
  state machine, équipe M2M, camionnette, +SAV/Visite types, InterventionActivity chatter,
  historique/noter endpoints). Frontend none yet. Backend py_compile-clean, tests UNVALIDATED.
- F4 Intervention list + kanban (frontend) — next.

*** RUN PAUSED — EXTERNAL BLOCKER (2026-06-17): GitHub Actions CI is disabled org-wide. ***
Every CI job fails in seconds with "The job was not started because recent account payments
have failed or your spending limit needs to be increased" (run 27660225930). PRs #129/#130
merged earlier today before the cap was hit; the repeated 5-min backend-test runs exhausted
the org's Actions minutes/spend.
CONSEQUENCE: F1's backend tests cannot run (no local Postgres either), and the module cannot
be merged to main on green CI (the completion gate). This is a FOUNDER DASHBOARD TASK:
GitHub org → Settings → Billing & plans → fix payment / raise the Actions spending limit.
RESUME after billing is fixed: re-fire "work on the plan" → it checks out dev-field-exec,
lets CI validate F1, then continues building F2 → F23, and merges once the whole module is
green. Do NOT merge dev-field-exec to main until CI is restored and green.
