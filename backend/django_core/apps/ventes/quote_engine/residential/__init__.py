# flake8: noqa
"""Redesigned 3-page RESIDENTIAL proposal renderer (part of the single engine).

Selected by `renderer.is_residential` for `mode_installation == residentiel`;
the engine falls back to the legacy renderer for everything else.
"""
