# Core
Django==5.1.15
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.6.0

# Base de fuseaux horaires (IANA) — ÉPINGLÉE, et elle FAIT FOI.
# Le Maroc est repassé DÉFINITIVEMENT à l'heure GMT (UTC+0) dans la nuit du
# 19 au 20 septembre 2026 (02:00 -> 01:00) : décret n° 2.26.530 relatif à
# l'heure légale, Bulletin officiel n° 7521 du 29/06/2026, qui abroge le
# décret 2.18.855 de 2018 (UTC+1 permanent avec retour à UTC+0 pendant le
# Ramadan). Plus aucune bascule saisonnière ni Ramadan.
# IANA l'encode depuis tzdata 2026c (08/07/2026) ; ce paquet PyPI en est la
# copie — 2026.4 (12/09/2026) rend bien +01:00 le 19/09/2026 et +00:00 le
# 21/09/2026. L'image Debian, elle, peut être EN RETARD (bookworm servait
# encore tzdata 2026b le 21/09/2026, donc SANS le décret) : c'est pourquoi
# les Dockerfiles posent PYTHONTZPATH="" — zoneinfo ignore alors
# /usr/share/zoneinfo et ne lit QUE ce paquet-ci. Ne pas le désépingler.
tzdata==2026.4

# Database
psycopg2-binary==2.9.10
pgvector==0.3.6

# Cache / Queue
redis==5.2.1
django-redis==5.4.0
celery==5.4.0

# PDF generation
weasyprint==62.3
# PyMuPDF (fitz) — PDF split/merge, signature-field zones, annotation/stamp
# overlays and scan-batch separation (GED module). Lazy-imported with graceful
# degradation, but shipped so those features work in prod/CI.
PyMuPDF==1.28.0
pydyf==0.11.0
Jinja2==3.1.6

# Premium quote-PDF engine (apps.ventes.quote_engine) — ROI/savings charts
numpy==1.26.4
matplotlib==3.9.2

# FG361 — Prévision de ventes / demande (séries temporelles). Holt-Winters /
# lissage exponentiel. Importé défensivement dans core/forecast.py (repli pur
# Python : tendance linéaire / moyenne glissante), donc le code reste import-safe
# et les tests passent même sans cette dépendance installée.
statsmodels==0.14.4

# Email
django-anymail[sendgrid]==10.3

# XMKT33 — vérification DNS (SPF/DKIM/DMARC) du domaine d'envoi marketing.
# Dépendance libre (MIT), aucun coût.
dnspython==2.7.0

# File storage MinIO/S3
django-storages[s3]==1.14.4
boto3==1.35.99

# Excel (.xlsx) export — exports CRM/listes (pré-approuvé, voir docs/PLAN.md)
openpyxl==3.1.5

# FG352 — RAG / DocQA : découpage de texte en fragments (free/open-source).
# Importé défensivement dans apps/ged/services.chunk_text (repli pur Python si
# absent), donc le code reste import-safe sans cette dépendance.
langchain-text-splitters==0.3.9

# Image handling
Pillow==12.3.0

# QR codes for the quote PDF (premium scan-to-sign: rounded navy + centre logo).
# Pure Python (Pillow already pinned above); imported defensively, so a missing
# wheel degrades to the textual sign link rather than breaking a PDF.
qrcode==7.4.2

# HTTP client
httpx==0.28.1

# VAO17 — NOUVELLE DÉPENDANCE (à signaler au fondateur) : analyse HTML du
# portail des marchés publics (apps/veille_ao/portail/parser.py). Licence MIT,
# gratuite, pure Python, aucun service tiers, aucun appel réseau — elle ne fait
# que lire du texte. Constat qui la motive : AUCUN parseur HTML n'était installé
# (ni beautifulsoup4, ni lxml, ni feedparser) ; seul httpx l'était. On reste sur
# l'analyseur `html.parser` de la bibliothèque standard : pas de lxml, donc
# aucune dépendance binaire à compiler en production.
beautifulsoup4==4.14.2

# Server
gunicorn==22.0.0

# Web push (PWA) — N92/N109. Diffusion web-push + génération de paire VAPID
# (py_vapid arrive en dépendance transitive). Le sdist http-ece se build
# proprement avec des setuptools récents (la CI et le build Docker mettent pip/
# setuptools à jour avant l'install). Toujours importé paresseusement côté code :
# sans clés VAPID, le push reste un NO-OP.
pywebpush==2.0.3
# YHARD1 — chiffrement au repos des champs sensibles (core/crypto_fields.py,
# Fernet). `cryptography` était déjà tiré TRANSITIVEMENT (via pywebpush) ; on le
# rend EXPLICITE puisque core en dépend directement désormais. Plage permissive
# pour ne pas sur-contraindre le résolveur vs la version tirée par pywebpush.
# Key-gated : sans FIELD_ENCRYPTION_KEY, le module est chargé mais inerte.
cryptography>=42,<47
# TOTP two-factor auth (2FA opt-in, N96)
pyotp==2.10.0
# Architectural import contracts (M3) — enforces the import rules in
# backend/django_core/.importlinter via the `lint-imports` CI step (no cycles
# between core domain models; the `core` app stays a base foundation layer).
import-linter==2.11

# XGED5 — PAdES cryptographic sealing of signed GED PDFs (open-source, free).
# Imported function-locally + guarded in apps/ged/services.py: without the
# lib installed the sealing step degrades cleanly to a no-op (XGED4's flow
# stays intact). RFC 3161 timestamp support (pyhanko also ships the TSA
# client) is key-gated via settings.GED_TSA_URL — no-op without it.
pyHanko==0.28.0

# YHARD6 — Prometheus metrics (open-source, free). OPTIONAL: core/metrics.py
# renders the /metrics exposition text in pure stdlib regardless of whether
# this package is installed, so the endpoint (and its custom Celery/beat
# collectors) never depends on it being present. When installed, it also
# provides standard Django HTTP/DB instrumentation (not wired by default —
# add its middleware in settings if that richer surface is wanted later).
django-prometheus==2.3.1

# YAPIC5 — schéma OpenAPI 3 auto-généré + docs interactives Swagger/ReDoc
# (NOUVELLE DÉPENDANCE, libre/MIT). Remplace le AutoSchema DRF nu : ~97
# viewsets étaient documentés nulle part de façon machine-lisible ; FG105
# (apps/publicapi/docs.py) reste une page FR statique pour l'API PUBLIQUE
# uniquement et n'est pas remplacée.
drf-spectacular==0.28.0

# PVG1 — import DXF réel du contour de toiture (apps/ao/dxf.py), NOUVELLE
# DÉPENDANCE approuvée fondateur 2026-08-14. Parse en mémoire (LWPOLYLINE/
# POLYLINE/LINE groupées par calque) — jamais d'écriture disque, aucun fichier
# persisté (l'atelier ne fait que PROPOSER un mapping de calques à l'écran).
ezdxf==1.3.5

# VT9 — assemblage (panorama) des photos du toit relevées en visite technique.
# NOUVELLE DÉPENDANCE, GRATUITE et libre (Apache-2.0), sans service externe ni
# appel réseau. Variante « headless » à dessein : pas de GTK/Qt, donc rien à
# installer en plus dans l'image serveur. Importée PARESSEUSEMENT dans
# apps/crm/tasks.py — son absence dégrade l'assemblage en échec ANNONCÉ, elle
# ne casse jamais le démarrage de Django. numpy est déjà épinglé plus haut.
opencv-python-headless==4.10.0.84

# NTPLT46 — Sentry (supervision d'erreurs), dépendance OPTIONNELLE gated-founder.
# core/monitoring.py importe sentry-sdk PARESSEUSEMENT et UNIQUEMENT si SENTRY_DSN
# est configuré : sans DSN, ce paquet n'est jamais chargé (aucun coût, aucun appel
# réseau). Décommenter + poser SENTRY_DSN pour activer (service payant à volume).
# sentry-sdk==2.20.0
# CALX198 — NOUVELLE DÉPENDANCE, TRANCHÉE PAR LE FONDATEUR LE 21/09/2026
# (décision n° 1 de l'en-tête du groupe CALX : « pvlib ADOPTÉ »). Libre,
# BSD-3-Clause, gratuite, sans clé d'API, sans donnée payante et sans appel
# réseau dans les chemins que nous utilisons (le sous-module `iotools`, qui
# lui en fait, n'est PAS importé). Ce qu'elle apporte et que le dépôt n'a
# pas : le modèle de cellule à une diode (De Soto / CEC), la transposition
# d'irradiance GHI→plan (Perez, Hay-Davies), l'IAM (Fresnel, ASHRAE,
# Martin-Ruiz) et une position solaire de référence (NREL SPA).
# Transitif : numpy (déjà épinglé ci-dessus, 1.26.4 satisfait `numpy>=1.21.2`),
# pandas et scipy (déjà tirés par statsmodels), requests (déjà tiré), plus
# DEUX paquets réellement neufs dans l'image : `pytz` et `h5py` (roue binaire
# manylinux à HDF5 embarqué — aucun paquet système à ajouter au Dockerfile).
# FRONTIÈRE (contrat import-linter `calepinage-est-un-noyau-pur` +
# `core/tests/test_calepinage_purete.py`) : `core/calepinage/` et
# `core/electrique/` restent stdlib + numpy — pvlib s'importe dans
# `apps/calepinage/services/` et sert d'ORACLE de validation aux tests du
# noyau. Voir docs/decisions/calx198-pvlib.md.
pvlib==0.15.2
