# Core
Django==5.1.4
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0

# Database
psycopg2-binary==2.9.10
pgvector==0.3.6

# Cache / Queue
redis==5.2.1
django-redis==5.4.0
celery==5.4.0

# PDF generation
weasyprint==62.3
pydyf==0.11.0
Jinja2==3.1.5

# Premium quote-PDF engine (apps.ventes.quote_engine) — ROI/savings charts
numpy==1.26.4
matplotlib==3.9.2

# Email
django-anymail[sendgrid]==10.3

# File storage MinIO/S3
django-storages[s3]==1.14.4
boto3==1.35.99

# Excel (.xlsx) export — exports CRM/listes (pré-approuvé, voir docs/PLAN.md)
openpyxl==3.1.5

# Image handling
Pillow==10.4.0

# HTTP client
httpx==0.28.1

# Server
gunicorn==22.0.0

# Web push (PWA) — N92. NO-OP tant que les clés VAPID ne sont pas configurées.
pywebpush==2.0.0
# TOTP two-factor auth (2FA opt-in, N96)
pyotp==2.10.0
