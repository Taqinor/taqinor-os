# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.31
python-dotenv==1.2.2
PyJWT==2.13.0

# Base de fuseaux horaires (IANA) — EPINGLEE, et elle FAIT FOI (meme pin que
# backend/django_core/requirements.txt : les deux services datent les memes
# faits). Le Maroc est repasse DEFINITIVEMENT a l'heure GMT (UTC+0) la nuit du
# 19 au 20 septembre 2026 : decret 2.26.530, Bulletin officiel 7521 du
# 29/06/2026, qui abroge le decret 2.18.855 de 2018. IANA l'encode depuis
# tzdata 2026c ; l'image Debian peut etre EN RETARD (bookworm servait encore
# 2026b le 21/09/2026), d'ou PYTHONTZPATH="" dans le Dockerfile.
tzdata==2026.4

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pgvector==0.3.6
# Parser SQL — validation single-SELECT + isolation tenant de l'agent NL->SQL
# (ERR1/ERR2). Deja tire transitivement par langchain ; epingle ici car
# importe directement par app/services/sql_agent_service.py.
sqlparse>=0.5,<0.6

# Cache
redis==5.2.1

# AI - Agent SQL
langchain==0.3.30
langchain-community==0.3.27
langchain-groq==0.2.3
langchain-openai==0.2.14
langchain-anthropic==0.3.3
openai==1.59.6
sentence-transformers>=2.0,<4.0

# OCR — utilitaires
pypdf>=4.0,<6.0
Pillow>=10.0,<12.0
pymupdf>=1.23,<2.0

# HTTP client
httpx==0.28.1

# Transcription audio — Whisper auto-heberge (S10, founder-approved).
# Modele charge/telecharge PARESSEUSEMENT au premier appel uniquement, derriere
# le flag CHAT_TRANSCRIPTION_ENABLED ; aucun poids tire au build/import.
faster-whisper>=1.0,<2.0
