"""apps.credit.receivers — abonnements au bus d'événements ``core.events``.

NTCRD1 : squelette vide, câblé depuis ``apps.py::CreditConfig.ready()``. Les
abonnements réels arrivent tâche par tâche (best-effort, jamais bloquant).
"""
