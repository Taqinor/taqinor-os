{
  "model": "opus",
  "env": {
    "ENABLE_PROMPT_CACHING_1H": "1"
  },
  "permissions": {
    "allow": [
      "Bash(flake8 *)",
      "Bash(npm run lint)",
      "Bash(npm run check)",
      "Bash(npm test)",
      "Bash(node --test *)",
      "Bash(python manage.py test *)",
      "Bash(python scripts/check_stages.py)",
      "Bash(python scripts/codemap_fingerprint.py --check)",
      "Bash(git fetch *)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git fetch origin main --quiet 2>/dev/null && git branch -f main FETCH_HEAD >/dev/null 2>&1 && echo \"[worktree-base] local main synced to origin/main ($(git rev-parse --short main)) so isolation:worktree lanes start from the current tree\" || true"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "in=$(cat); l=''; if printf '%s' \"$in\" | grep -iqE 'go +extrem|extremely +deep|extremly +deep|ultra +deep'; then l=L3; elif printf '%s' \"$in\" | grep -iqE 'go +very +deep'; then l=L2; elif printf '%s' \"$in\" | grep -iqE 'go +deep|deep +dive'; then l=L1; fi; [ -n \"$l\" ] && printf '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"[go-deep %s] Reda asked for a %s investigation. Invoke the go-deep skill NOW and run this request at level %s per its doctrine: adaptive rounds R1 understand -> R2 dig -> R3 validate -> R4 final review, looping until dry within the level caps; lanes = one per genuinely independent surface; explicit model + effort on EVERY subagent (never inherit the session model); Fable only where the level authorizes it.\"}}' \"$l\" \"$l\" \"$l\" || true"
          }
        ]
      }
    ]
  }
}
