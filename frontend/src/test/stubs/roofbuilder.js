// Stub de l'alias `@roofbuilder` (plugin roofbuilder-ts-transpile de
// vite.config.js, absent de la config Vitest). `pages/ventes/ToitureDesign.jsx`
// fait `await import('@roofbuilder')` : ce stub satisfait la résolution au
// transform sans embarquer le builder réel (aucun test ne monte le builder).
export default {}
