/* ============================================================================
   AOF190 — Constantes du mode MOBILE, isolées du composant.
   ----------------------------------------------------------------------------
   Extrait de `ModeMobile.jsx` (react-refresh/only-export-components) : un
   fichier de composant ne doit exporter QUE des composants pour que le Fast
   Refresh de Vite reste fiable en dev — cette table de raisons de refus n'en
   est pas un et vit donc ici, ré-importée par le composant.
   ========================================================================== */

export const RAISONS_MOBILE = {
  tracer:
    "tracer un contour de toiture au doigt sur un écran de 375 px produirait une géométrie imprécise — utilisez une tablette ou un ordinateur.",
  obstacle:
    "poser un obstacle exige de voir la toiture entière pour juger sa position réelle — indisponible sur un écran de cette taille.",
  calepinage:
    "régler les allées et l'implantation exige de voir le toit en entier ; aucun atelier de calepinage crédible ne tient sur un téléphone.",
  variantes:
    "comparer des variantes affiche plusieurs plans côte à côte — indisponible sur un écran de cette taille.",
}
