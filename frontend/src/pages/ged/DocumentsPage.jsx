// GED5 — page « Documents (GED) » : navigateur arborescent des dossiers + leurs
// documents. La logique vit dans la feature `features/ged/GedNavigator`.
import GedNavigator from '../../features/ged/GedNavigator'

export default function DocumentsPage() {
  return <GedNavigator />
}
