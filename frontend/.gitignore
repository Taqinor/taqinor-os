# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# SOL6 — builds par édition (`vite build --outDir dist-<edition>`), produits par
# la garde `scripts/check_dist_edition.mjs` en local comme en CI.
dist-solar
dist-full
*.local

# Coverage (généré par `npm run test:coverage` / node --test --coverage)
/coverage/

# Playwright / E2E
/test-results/
/playwright-report/
/playwright/.cache/
/e2e/.auth/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
